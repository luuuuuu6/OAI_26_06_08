# 2D-MMSE (true2d) 增强计划

| 字段 | 值 |
|------|----|
| 日期 | 2026-06-04 |
| 范围 | 在已交付的 true2d（频域 Wiener + 时域 PKF + 高 SNR legacy 门控）基础上，列出可增强点、证据、改法、预期收益、工作量/风险与优先级 |
| 现状基线 | 静态 vs LS −9~−12dB / vs legacy −5~−7dB；动态 0~300km/h vs LS ~4~8.6dB / vs legacy ~0.7~5.1dB；高 SNR 门控使全 SNR ≥ legacy；速度稳健、无高速崩溃 |
| 总原则 | 头条结论（频阶主导、速度稳健）已成立；以下多为"把已知丢掉的增量找回来"或"研究性更大 leverage"，不改变已交付结论 |

---

## Tier 1：已实测确认、ROI 最高

### E1. Stage 2 时阶：单 ω → per-band ω（或 ω₀ + slope·k 线性相位）

> **状态（0604 更新）：已实装 + 已验证 + 根因查清 → 本测试床无杠杆，保留为 opt-in，默认关。**
> - **实装**：env `SRS_2D_PKF_PHASE`（0=scalar 默认/1=per-band/2=linear）+ `OMEGA_BANDS`/`BAND_MIN`/`SLOPE_MAX`/`RAMP_GATE`；slope/intercept 用 **double**（float32 桁落会把 slope 抹掉，实机 1248 SC 尤甚）；ramp 门控防 benign 回归。`launch_native.sh` 转发。
> - **验证**：golden mode0 逐位一致；离线 `_e1_eval.py` 的 STO-ramp 场景 mode1 **+0.40dB**、mode2 **+0.43dB**（并修复单 ω 在 ramp 下反劣于裸 LS），benign 无回归。
> - **根因（关键）**：实机时阶 `K` 恒 = `kmax=0.95` 是 **Riccati 物理最优**，因 period-10（5ms）下相邻 SRS 复相干仅 0.815(30km/h)/0.670(60km/h)（每发去相关 1/3~1/2），时间维无可利用相关性。`-p 2` 加密 SRS 被 **TDD（8DL/3UL）卡死**（请求 2→实选 10）。故动态增益全来自频域 Wiener，E1 杠杆≈0。**不为压低 K 改 PKF**。详见 `0531...日志.md` §12.21。
> - **何时才有用**：需改 TDD pattern 增 UL/SRS 机会（SRS < 5ms）或极低速 s3，使相邻 SRS 高相干、K<0.95、时间阶激活。

- **证据（已实测）**：CDL-C 60km/h 开 `SRS_2D_DEBUG=1`，handoff 后诊断稳定报
  `band_ramp ≈ 1~4 rad (>1 rad 阈值) → STO-ramp dominant (need per-SC phase)`。
  即动态频选下各抽头多普勒不同 → 频域出现随帧变化的线性相位，**单 ω 表示不了** → 进创新 → K 顶高 → 时阶退化成"直传频阶"，时间维那层去噪基本没拿到。
- **现状**：`nr_srs_2d_filter.c::update_pkf_core` 用一个标量 `omega`（`pst->omega`，`g_pkf_kw` EMA）。但**已有 `PKF_N_BANDS=8` 的逐带创新统计**（`band_innov`/`band_vals`，用于 Q 自适应）。
- **改法（两步，先易后难）**：
  1. **per-band ω**（低工作量，复用现成 band 结构）：每个频带一个 `omega_b`，预测时该带 SC 用 `e^{j*omega_b}`。把 Pass1 的 `dphi` 改成逐带估、逐带 EMA。
  2. **线性相位 ω(k)=ω₀+slope·k**（更准）：诊断里**已经在算** `omega_common` 和 `slope`，直接把它们做成状态、predict 用 `e^{j(ω₀+slope·k)}`。
- **预期收益**：中等速度 + 中低 SNR（时阶占比大）下，预计动态 vs LS / vs legacy 各 **+1~2 dB**；高 SNR/极高速帮助有限。
- **工作量/风险**：中/低。改 `update_pkf_core` 局部；风险=过拟合相位噪声→需 clip slope、warmup。先做 per-band（最稳），不够再上线性。
- **验证**：CDL-C/E 的 `-nd -6 / -9` × {10,30,60} km/h，对比单 ω vs per-band ω 的 delta；并看 `band_ramp` 是否被吸收。

---

## Tier 2：中等价值，工程改进

### E2. Stage 1 频阶：时延自适应窗口（当前固定）
- **现状**：`nr_srs_get_mmse_window_pilots()` 是**固定窗**（`SRS_MMSE_WINDOW`），不看时延扩展。
- **问题**：长时延（CDL-E 2064ns，频域去相关快）窗太宽会引偏、太窄又欠平滑；短时延（CDL-A）可以更宽多榨增益。固定窗对全模型折中。
- **改法**：用 Stage 1 已经算出的 PDP（EMA）估有效时延扩展（如 −10dB 时延宽度 / RMS 时延），据此自适应选 `window_pilots`（短时延→宽、长时延→窄），或直接据 R(Δk) 的衰减速度定窗。
- **预期收益**：边际（各模型几 dB 内的优化），尤其改善 CDL-E（当前最弱）。
- **工作量/风险**：中/低。纯 Stage 1，不动时阶；风险=自适应估计噪声→需平滑/clip。

### E3. 高 SNR 门控标定（找回 nd=−9 的小胜）
- **证据**：门控后 ≈SNR 23dB（`-nd -9`）的小胜（曾 −1~−2.5dB）被吸收——在线 SNR 代理（pilot/residual_noise）读数偏高、斜坡略提前介入。
- **改法**：把门控的在线 SNR 估计**标定**到与"有效 SNR ≈ −passthru NMSE"一致（一次性拟合），使斜坡正好落在实测交叉点 ~27dB；或提高 `SRS_2D_LEGACY_GATE_DB` 并**跨 seed 验证泛化**（避免过拟合）。
- **预期收益**：在交叉点边缘（≈SNR 23~27dB）找回 ~1~2dB。
- **工作量/风险**：低/中。**注意过拟合**：必须用未参与调参的 seed 验证（见前期讨论）。

### E4. 进取版门控：高 SNR 也"小赢"legacy（而非仅追平）
- **现状**：高 SNR 门控让 true2d **回退到 legacy（追平）**；但 legacy 的固定 FIR 在高 SNR 仍比裸 LS 好 ~3.6dB，说明高 SNR 仍有"免费平滑增益"可拿。
- **改法**：高 SNR 不退回 legacy，而是给 Stage 1 Wiener 加一个**平滑下限**（`noise_norm` floor 抬高 / R 不塌到 identity），使其在高 SNR 也保留 legacy 级的频域平滑 + 时阶时间平均 → 可能**小赢** legacy。
- **预期收益**：高 SNR 区从"追平"变"+1~2dB 小赢"。
- **工作量/风险**：中/中。风险=高 SNR 引偏；需在 CDL 全模型高 SNR 验证不劣化。

---

## Tier 3：研究性、更大 leverage

### E5. 联合 2D-MMSE（非可分）
- **现状**：当前是"先频 Wiener、再时 Kalman"的**可分近似**。
- **改法**：联合频-时 Wiener（用频×时联合相关阵），理论上界更高，尤其频/时相关不可分时。
- **预期收益**：理论更优，但实测增量可能有限（可分近似在多数工况已接近）。
- **工作量/风险**：高/高（复杂度、定点、实时性都是问题）。**研究优先级，不建议近期做**。

### E6. 空间维 MMSE（2×2 MIMO，最大 leverage）
- **现状**：受限于 native 2 端口 SSB（§4.1 #2），目前只能 1T1R SISO；true2d 是 per-link，MIMO 下只是平移、不吃空间增益。
- **改法**：先修 native 2 端口 SSB（infra 前置）→ 再加**天线间联合 MMSE**（空间相关性去噪）。
- **预期收益**：**这是 legacy 完全没有的维度**，潜在几 dB 的额外增益——比继续抠频/时维更值。
- **工作量/风险**：高/高（先解 infra，再做空间算法）。**中长期最值得的方向**。

---

## Tier 4：方法学 / 可信度（非算法，但影响结论强度）

### E7. 相干天花板根因（绝对 NMSE 是否被 GT 管线封顶）
- **现状**：早期 CDL replay 即便高 SNR 裸 LS 也只到 ~−34~−38dB（STO 对齐守门后相干 ~1.0，但绝对 NMSE 仍有上限）。
- **查**：CIR replay 的 per-slot 量化 / STO 残差 / active-mask 是否系统性压低绝对 NMSE。**相对 Δ 不受影响**，但"true2d 能到多少 dB"会被低估。
- **工作量**：中（诊断为主）。

### E8. 多 seed / 误差棒 + 动态 × 多 SNR 网格
- **现状**：主结果单 seed；动态只测了 ≈17dB 一档。
- **改法**：多 seed 出误差棒（交叉点附近尤需）；动态扫"速度 × SNR"二维（低 SNR 时阶占比大，行为可能不同，且正好检验 E1 的收益）。
- **工作量**：中（机时为主，脚本已就绪）。

### E9. 复杂度优化（部署成本论证）
- **现状**：Stage 1 每子载波 O(window³) Toeplitz 解；legacy 是廉价 FIR。
- **改法**：Toeplitz 用 Levinson 递推 O(window²)、或跨 SC 复用/预计算；量化 true2d vs legacy 的算力比。
- **价值**：让"值不值得部署"的论证更完整（性能 vs 算力）。
- **工作量**：中。

---

## 推荐执行顺序

1. ~~**E1（per-band ω）**~~ —— ✅ 已实装+验证，但实测查清：本 TDD 测试床(SRS 最密 5ms)下时间阶 K 恒饱和、E1 无杠杆；保留为 opt-in、默认关。要发挥需先有更密 SRS(改 TDD)或极低速。
2. **E3（门控标定）+ E8（多 seed）** —— 便宜地把结论做扎实、找回交叉点小胜。
3. **E2（自适应窗）** —— 边际优化，顺手。
4. **E7（相干天花板）** —— 搞清"能到多少 dB"。
5. **E4（进取门控）/ E5（联合 2D）** —— 锦上添花，按需。
6. **E6（空间维 MMSE / MIMO）** —— 中长期最大 leverage，需先修 infra。

> 重要：以上均为**增强**，当前交付结论（静/动 × vs LS/legacy 四象限）不依赖它们。E1 之外的多数项是"可信度/边际/研究"，按时间与目标取舍。
