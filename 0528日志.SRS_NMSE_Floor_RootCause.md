# 0528 日志：SRS NMSE Floor 根因分析

> 日期：2026-05-28  
> 条件：SNR=15 dB, UL Pre-Gain=1.0 (g=1), UE speed=3 m/s, fc=3.5 GHz, SCS=30 kHz  
> 场景：P1B RX98, 2x2 MIMO, comb-2, SRS period=10 slots (5 ms)  
> GT_SAVE_EVERY=10 (今日修正，与 SRS 周期 1:1 对齐)

---

## 1. 实验矩阵与结果汇总

### 1.1 五组对照实验

| 模式 | run timestamp | estimator 配置 | per-ant NMSE | p50 | Q4-Q1 drift | 配对率 | median gap |
|------|--------------|----------------|-------------|-----|-------------|--------|-----------|
| Legacy | `150304` | `legacy + 3pt smooth` | **-10.87 dB** | -11.32 dB | +0.4 dB | 401/401 | 0 |
| Kalman | `151732` | `mmse2d + kalman` | **-11.16 dB** | -11.58 dB | +0.1 dB | 359/359 | 0 |
| MMSE1D | `154103` | `mmse1d (PDP-Wiener)` | **-9.66 dB** | -10.04 dB | -0.1 dB | 309/309 | 0 |
| OptFilt #1 | `154325` | `legacy + exact pilot recon` | **-10.17 dB** | -10.63 dB | +0.3 dB | 320/320 | 0 |
| OptFilt #2 | `154850` | `legacy + exact pilot recon` | **-10.15 dB** | -10.60 dB | +0.1 dB | 348/348 | 0 |

### 1.2 排名（违反通常预期）

```
Kalman (-11.16) > Legacy (-10.87) > OptFilt (-10.15) > MMSE1D (-9.66)
```

理论上 MMSE1D 应最优，但实验排名相反。

### 1.3 STO 不是原因

对 legacy 数据做 `--sto-correct global` 和 `--sto-correct per-frame`，NMSE 不变（-10.87 dB → -10.87 / -10.74 dB）。排除 timing offset 影响。

---

## 2. 根因定位

### 2.1 信道场景特性：近似 flat-fading

P1B RX98 的 ray-tracing 延迟参数：

| 参数 | 值 |
|------|-----|
| 有效路径数 | 380 |
| RMS delay spread | **1.04 ns** |
| 相干带宽 (1/5σ_τ) | **193 MHz** |
| NR 有效带宽 | 38.16 MHz |
| 比值 | coherence BW / NR BW = **5.1x** |

结论：整个 SRS 带宽内，真实信道频率响应几乎不变。

### 2.2 GT vs SRS 频域平滑度对比

`smoothness_ratio = mean(|H[k+1]-H[k]|²) / mean(|H[k]|²)`

| 对象 | smoothness_ratio | 含义 |
|------|-----------------|------|
| **GT 真值** | 0.00000004 | 完美平坦 |
| Kalman | 0.0086 | 最平滑估计 |
| Legacy | 0.0097 | 次平滑 |
| OptFilt | 0.0184 | 较粗糙 |
| MMSE1D | 0.0216 | 最粗糙 |

GT 比最好的 SRS 估计平滑约 **20 万倍**。SRS 输出中的频域"细节"几乎全是噪声。

### 2.3 离线频域平滑验证

对 legacy SRS 输出做 moving average（不改 OAI，纯后处理）：

| 平滑窗口 (SC) | per-ant NMSE | 相比 raw 改善 |
|---------------|-------------|-------------|
| raw (无) | -10.87 dB | baseline |
| mavg3 | -11.25 dB | +0.38 dB |
| mavg5 | -11.80 dB | +0.93 dB |
| mavg9 | -12.90 dB | +2.03 dB |
| mavg17 | -14.45 dB | +3.58 dB |
| mavg33 | -16.16 dB | +5.29 dB |
| mavg65 | -17.18 dB | +6.31 dB |
| mavg129 | -18.06 dB | +7.19 dB |
| mavg257 | -19.13 dB | +8.26 dB |
| mavg513 | -21.80 dB | +10.93 dB |
| mavg1248 (全带平均) | -24.90 dB | +14.03 dB |

**越宽平滑 NMSE 越好**，没有拐点 → 在当前 flat-fading 场景下，频域所有"结构"都是噪声。

### 2.4 根本结论

```
NMSE floor 的主因：Stage1 残留 per-SC 噪声
不是：Kalman 无效 / MMSE1D 理论错误 / STO / int16 量化 / GT 对齐
```

---

## 3. 为什么各方法排名违反直觉

### 3.1 Legacy > OptFilt

| 差异 | Legacy | OptFilt |
|------|--------|---------|
| 导频处权重 | [0.25, 0.5, 0.25] (3pt smooth) | [0, 1.0, 0] (exact pilot) |
| 中点插值 | 线性 [0.5, 0.5] | 线性 [0.5, 0.5] |

唯一区别在导频处：Legacy 对导频做了 3 点邻近平均，OptFilt 保留导频原值。
由于每个导频 LS 估计都含噪声，3 点平均有效降噪 ~4.8 dB。在 flat-fading 下这不会丢失频域信息（因为真值相邻导频几乎一样），所以 Legacy 胜。

### 3.2 Legacy > MMSE1D

MMSE1D 理论上是最优 Wiener 滤波。但当前实现有两个限制：

1. **局部窗口太小**：`SRS_MMSE_DEFAULT_WINDOW = 4`，最大 `SRS_MMSE_MAX_WINDOW = 8`。
   对于 coherence BW=193 MHz（>>NR BW）的信道，需要全带 1248 个导频参与平均，而实现只用 4 个。

2. **PDP 噪声污染**：MMSE1D 从 noisy LS 估计 PDP，再推导 R(Δk)。在 flat-fading 下 PDP 应是单尖峰，但 LS 噪声制造假 taps → R(Δk) 估计偏短 → 平滑不够。

代码位置：`nr_srs_mmse.c` L49, L260-L271, L580-L582

### 3.3 Kalman 只好 0.29 dB

Kalman 运行参数（from gnb.log）：

```
K=0.9660  P=3.999e+05  Q=1.133e+07  R=4.139e+05
Q/R = 27.4
```

- Q >> R → 信道变化远大于观测噪声 → K → 1 → 几乎直通当前观测
- Kalman 的微小贡献来自 0.04 权重对历史状态的时域平均
- 这和 mavg3 的频域效果类似（+0.38 dB vs +0.29 dB）

---

## 4. g=2 (UL Pre-Gain=2x) 失败分析（附录）

### 4.1 失败因果链

```
UL Pre-Gain=2x
  → int16 削波 (subcarrier peak overflow)
  → 宽带非线性失真
  → noise_power: 44 → 65 (+21 dB, 线性增益只应 +6 dB)
  → ULSCH BLER = 80% (MCS0 最低也无法解码)
  → NAS Authentication Response 无法送达 core
  → Core 发送 Authentication Reject + Registration Reject
  → RRCReconfiguration 不会下发
  → Attach 超时 120s (状态卡在 WAIT_RRC_RECFG)
```

### 4.2 关键证据

- gNB 报告 UL SNR=30 dB（虚高，含削波失真功率）但 BLER=80%
- UE 日志收到 `FGS_AUTHENTICATION_REJECT` + `FGS_REGISTRATION_REJECT`
- g=2 无 SRS bin（从未完成 attach → 从未调度 SRS）
- PBCH errors: 80（g=1 仅 4）

---

## 5. 下一步方案：自适应频域去噪

### 5.1 核心思路

不是硬编码"全带平均"（flat-only），而是做 **delay-spread-aware adaptive smoothing**：

```
roughness = mean(|H[k+1]-H[k]|²) / mean(|H[k]|²)   ← 频域粗糙度
noise_est = mean(|LS_pilot - legacy_interp|²) / mean(|LS_pilot|²)   ← 噪声估计
```

- 如果 `roughness ≈ noise_est` → 频域变化全是噪声 → 加宽平滑窗口
- 如果 `roughness >> noise_est` → 存在真实频率选择性 → 保守/窄窗口
- 窗口可从 [3, 5, 9, 17, 33, 65, 129] 中自适应选择

### 5.2 通用性保证

| 场景 | delay spread | coherence BW | 自适应行为 |
|------|-------------|-------------|-----------|
| RX98 近 flat | 1 ns | 193 MHz | 选 mavg65~129 |
| 中等多径 | 100 ns | 2 MHz | 选 mavg5~9 |
| 强频选 (NLOS) | 1 μs | 200 kHz | 选 mavg3 或不平滑 |

### 5.3 实施路线

```
Phase 1: 离线验证 (Python, 不改 OAI)
  - 对已有 SRS bin 做 adaptive freq smooth 后处理
  - 用多个 RX (flat / medium / selective) 验证通用性
  - 评估指标：sync NMSE + prediction NMSE (delta=10)

Phase 2: 嵌入 OAI Stage1
  - 新增 SRS_ESTIMATOR=freqsmooth 或 SRS_FREQ_SMOOTH=adaptive
  - 注入点：nr_ul_channel_estimation.c L1035-L1039 (legacy memcpy 之前)
  - 在 filt8/16 dense 输出上做 in-place adaptive MA
  - 可叠加 Kalman (mmse2d + freqsmooth)

Phase 3: 生产化
  - SIMD 优化 MA kernel (AVX2)
  - 窗口选择每 N 帧更新一次（不需每帧重算）
  - 参数通过环境变量 SRS_FREQ_SMOOTH_MAX_WIN 配置
```

### 5.4 预期收益

基于离线 mavg 实验的保守估计：

| 场景 | 当前 NMSE | 预期（自适应窗口） | 改善 |
|------|----------|-----------------|------|
| RX98 flat, SNR=15 | -10.87 dB | -16 ~ -18 dB | **+5~7 dB** |
| 中等多径, SNR=15 | ~-10 dB | -12 ~ -14 dB | +2~4 dB |
| 强频选, SNR=15 | ~-10 dB | -11 ~ -12 dB | +1~2 dB |

---

## 6. 代码位置速查

| 模块 | 文件 | 行号 | 说明 |
|------|------|------|------|
| Stage1 filt8/16 | `nr_ul_channel_estimation.c` | L894-L957 | comb-2: filt8, comb-4: filt16 |
| MMSE1D 入口 | `nr_ul_channel_estimation.c` | L967-L992 | 调用 `nr_srs_mmse_freq_filter` |
| MMSE1D 窗口 | `nr_srs_mmse.c` | L49 | `SRS_MMSE_DEFAULT_WINDOW 4` |
| MMSE1D PDP→R | `nr_srs_mmse.c` | L441-L506 | IFFT + DFT of clean PDP |
| Kalman Stage2 | `nr_ul_channel_estimation.c` | L993-L1004 | `nr_srs_2d_filter_update` |
| Kalman 核心 | `nr_srs_2d_filter.c` | L688-L960 | `update_scalarkalman_core` |
| SRS bin dump | `nr_ul_channel_estimation.c` | L1167-L1194 | `srs_estimated_channel_freq` 后保存 |
| GT saver | `v8.py` | L909-L1350 | `GTBatchSaver` class |
| 新代码注入点 | `nr_ul_channel_estimation.c` | L1035-L1039 | legacy `else` memcpy 处 |

---

## 7. 数据归档

| 用途 | 路径 |
|------|------|
| Legacy baseline | `logs/q4_sweep_20260528_150304/snr_15dB/` |
| Kalman | `logs/q4_sweep_20260528_151732/snr_15dB/` |
| MMSE1D | `logs/q4_sweep_20260528_154103/snr_15dB/` |
| OptFilt | `logs/q4_sweep_20260528_154850/snr_15dB/` |
| g=2 失败 | `logs/q4_sweep_20260528_133536/snr_15dB/` |
| P1B ray data | `DevChannelProxyJIN/vRAN_Socket/P1B_Valid_Results/Area1_7.5GHz_Rays_Valid_RXs.npz` |
| eval 脚本 | `DevChannelProxyJIN/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy/eval_nmse_clean.py` |
