# 真·2D MMSE —— 端到端系统诊断与修复大日志（0531）

| 字段 | 值 |
|------|----|
| **日期** | 2026-05-31 |
| **上游** | `0529_True2D_MMSE_先期计划.md`、`0529_True2D_MMSE_中期计划.md`、`0529_Phase1_EMA_Wiener_离线日志.md`、`0529_True2D_离线判定日志.md`、`SRS_GT_ALIGNMENT_ISSUE.md`、`.kiro/specs/gt-srs-alignment-fix/` |
| **范围** | 把"真实系统频选 A/B"这条路从头到尾系统性打通/排查：SRS↔GT 编号同源(V3) → DL 平坦化修 attach → handoff 配置 → 评估管线改进 → 逐层定位剩余瓶颈 |
| **一句话结论** | **算法/C/对齐层全部修好并验证；proxy 端到端真实频选 A/B 受链路余量阻塞。0601 追加：改走 native rfsimulator + channelmod 路线并打通——逐层拆掉 3 个坑（2T2R/2端口SSB、`Doppler_phase_cur` 未分配、旁路索引 bug），再加"先平坦 attach→handoff 切 TDL"机制，已得到 UE 在线 + 上行频选(TDL_A)且链路不断的 native 测试床。日志 §9"native 被 infra 墙挡死"的判断被推翻。** |

---

## 〇、TL;DR（结论先行）

这一天把"真实系统证明 true2d 频选增益"挡在前面的东西**逐层剥开并修掉**，最后定位到不可绕的 infra 墙：

1. ✅ **SRS↔GT 编号同源（V3）**：根因 = SRS 只记 SFN `frame/slot`（回绕）、GT 记 `ipc_ts//30720`（绝对），eval 每发 median 猜常数 C。修法 = SRS bin V3 记录绝对采样 ts → eval 直接精确配对。**复数相干在对齐帧上 0.9，δ 扫描无偏移峰 → 对齐已坐实正确。**
2. ✅ **DL 平坦化修 attach**：根因 = DL 也走完整 CDL 多径 → PBCH/PDSCH/RRC 下行解码失败。修法 = SRS-only 模式下整个下行 power-flat（DL 不需要 CDL）。已落 `v8.py`，banner 实证生效。
3. ✅ **handoff→CDL 配置**：根因 = `ATTACH_STABLE_POST_DELAY=15` + 短 run → handoff 太晚 → symbol 12 全程平坦 → GT[12] 平坦（测的是平坦信道）。修法 = `post_delay=2` + 大 `MAX_FRAMES`。**GT[12] 终于带 CDL（665/814 帧，频选 std/mean 0.45）。**
4. ✅ **评估管线改进**：CDL 帧过滤（剔除 handoff 前平坦帧）+ 复数相干守门（替代会在平坦 GT 上误报的幅度包络相干）。
5. ⛔ **剩余瓶颈 = proxy 链路余量**：attach 是 (seed,SNR) 下的深衰落掷硬币；上行系统性偏弱（Msg3 实测 ~3dB）；频选信道上 SRS **只能稳定抓主径（PDP tap3 恒定、SNR 报 18dB），抓不住弱多径（=频选细节=true2d 要利用的部分）** → 与选频 GT 相干仅 0.05~0.15。**这是 §7.2 那堵 infra 墙的物理本质，非算法/对齐问题。**

---

## 一、起点：算法与 C 的既有状态（来自 0529 日志，本日确认仍成立）

- **true2d = 窗口 Toeplitz 频阶 × per-link ω 相位预测 Kalman(PKF) 时阶**，离线对 oracle 严格验证（CDL 可达 ~-28~-30dB）。
- **C 实现 = 离线黄金 recipe 的逐位转写**：M2.4 单元测试频阶 0–1 LSB、时阶 ≤1 LSB；C1 证明 OAI 定点 idft 非瓶颈。修过频阶共轭严重 bug、PKF 的 Q 递推分歧。
- 结论：**算法正确、C=算法 已闭环**；唯一缺口是"真实频选信道端到端读出增益"，受环境阻塞。本日就是去攻这个缺口。

---

## 二、Plan B：SRS↔GT 编号同源根治（V3）

### 2.1 根因（代码坐实）
- GT：`slot_id = ipc_ts // 30720`（`v8.py:1028`，绝对采样 slot，非回绕）。
- SRS：原只记 `frame/slot`（SFN 域，1024 帧回绕）；`eval_nmse_clean.align_frames` 用 `median(gt)-median(srs)` **每发猜常数 C**（实测 48482 vs 103852，每发不同）。两者**共享同一根 openair0 采样轴**（V8 IPC 环形缓冲同 `ts` 读写），但 SRS 侧丢了绝对值 → 只能猜 C → 动态下配偏 → 去相关。

### 2.2 修复（已落 C，已编译）
- **SRS bin V3**：`SRS_BIN_VERSION 2→3`；`srs_pingpong_t` 增 `int64 meta_timestamp[2][MAX_DUMP_FRAMES]`；每帧 `rnti+pad` 后写 `int64 sample_ts`。注入点（`nr_ul_channel_estimation.c`）用 `frame/slot` + `RU_list[0]->proc.timestamp_rx` 做 **epoch 重建**绝对 ts（解 SFN 回绕、避 L1↔RU 竞态）。V2 读者按 version 分支，向后兼容。
- **评估侧**：`load_srs` 读 V3 → `meta["sample_slots"]=ts//30720`；新增 `unify_reference_frame()`（绝对轴偏移=0 最近邻，无 median-C）；V2 自动回退。

### 2.3 验证
- 静态 cdl_b A/B：V2 旧 median-C 下 true2d 那发对齐失败（包络 corr 0.114 → 假象 A/B +5.6dB）；**V3 统一后 corr 修复、A/B 塌缩到 +0.85dB（≈打平）**。
- **δ 扫描**：动态 CDL 帧上扫 slot 偏移 δ∈[-12,12]，**无 corr 峰**（平坦）→ 坐实不是 slot 偏移 bug。
- **复数相干 = 0.896**（对齐帧）→ 坐实 V3 时间/复数对齐正确。

> 注：当时静态 A/B 的 legacy −7.2 / true2d −6.4 后来发现 GT[12] 其实仍平坦（见 §四 handoff），所以那组数字是"平坦信道上打平"，不代表频选；但 **V3 对齐机制本身是验证正确的**。

---

## 三、DL 平坦化修 attach

### 3.1 根因（逐层日志+代码）
attach 长期掷硬币。逐层查：
- gNB 侧 DL BLER 52–60% @ MCS 0；UE 侧反复 `Error decoding PBCH!`、RSRP −26→−42dBm。
- 代码：DL 广播走 `_ipc_apply_channel_for_ue(...,"DL")`，**DL 分支不做任何 mask** → DL 经完整(冻结)CDL 多径。SRS-only mask 只 flatten UL。
- RRC 握手全是下行（RRCSetup/SecurityMode/UECapability/RRCReconfiguration）→ 每条 ~50% 失败累乘 → attach 掷硬币；且 PBCH 错发生在 connect **之后**（handoff 后 DL CDL 动起来打崩已连接链路）。

### 3.2 修复（已落 `v8.py`，纯 Python、opt-in、无需重编 OAI）
- `_apply_srs_only_mask` 加 `keep_override` 参数；DL 分支传 `set()` = **整个下行全程 power-flat**（DL 不需要 CDL）。
- 新增 `--srs-only-dl-flat`（env `SRS_ONLY_DL_FLAT`，默认 1）；`launch_all_v9.sh` 透传；banner `SRS-only DL flat: ENABLED`。
- `srs_only_channel=0`（P1B）完全不变；GT 来自 UL 不受影响。

### 3.3 效果
- banner 实证生效；DL 多径不再毁连接。**但 attach 仍掷硬币**——失败点上移到 **Msg3（上行)**（DL-flat 管不到上行）。

---

## 四、handoff→CDL 配置（GT[12] 终于带 CDL）

### 4.1 发现（用户直觉"是不是对齐问题"引出）
动态 SNR10 A/B 配对数暴涨(233/226)但 corr [BAD]。深查发现 **GT[12] 幅度 std/mean = 0.000（完全平坦）**，而复数相干 0.896（其实对得上）。→ 不是 eval 对齐错，而是 **symbol 12 根本没切到 CDL**。

### 4.2 根因
`attach_stable_watcher` 等 `RRCReconfigurationComplete` 匹配后还要等 `ATTACH_STABLE_POST_DELAY=15s` 才写 handoff 触发文件；而 `RRCReconfigurationComplete` 出现在 run 的最后 ~90%（line 739/818）+ 15s 延迟 > run 时长 → **handoff 从未生效 → `keep=∅` → symbol 12 全程 power-flat → GT[12] 平坦 → 测的是平坦信道**。这也解释了之前"true2d≈legacy"——根本没有频选可利用。

### 4.3 修复（配置）
`ATTACH_STABLE_POST_DELAY=2` + `MAX_FRAMES=800` + `HARD_CEILING_SEC=1200`。
- 结果：**GT[12] 终于带 CDL**（kept 665/814 频选帧，std/mean 0.45；前 1/4 平坦→后 1/4 CDL，handoff 中段切到 CDL）。

---

## 五、评估管线改进（eval_nmse_sto.py / eval_nmse_clean.py）

1. **CDL 帧过滤**：剔除 handoff 前的平坦帧（GT 频选 std/mean ≤ `SEL_MIN=0.05`），只评估真正带 CDL 的帧。
2. **复数相干守门**：把守门指标从"幅度包络相关 `corr(|s|,|g|)`"改为**复数相干 `|<s,g>|/(‖s‖‖g‖)`**（阈值 `CORR_MIN=0.50`）。原因：幅度包络相干在**平坦 GT 上恒=0（假报警）**，而复数相干对恒定 STO/线性相位不敏感、是正确的对齐判据。
3. `unify_reference_frame`：绝对轴偏移=0 最近邻配对（无 median-C），动态下 GT 稀疏由 tol 吸收。

> 这套改进**正确揭示了**后续的真问题（平坦 GT 假报警被消除、CDL 帧上暴露 SRS 估计对弱多径无能）。

---

## 六、最终定位：剩余瓶颈 = proxy 链路余量（非算法/对齐/C）

在 GT[12] 终于带 CDL 后，**静态** cdl_b（排除 Doppler/时变 gap）实测：

| 量 | 值 | 含义 |
|----|----|------|
| GT 帧间复数相干 | **1.000** | 静态 GT 完美一致（信道确实冻结）|
| SRS 帧间复数相干 | **0.029** / 幅度 0.075 | SRS 估计帧间几乎不相关 |
| SRS vs GT（best-STO）| 0.15 | 对不上选频 GT |
| gNB 报 SRS SNR | **18 dB** | 不低！ |
| SRS PDP 峰位置 | **tap 3，30 帧全一致** | 主径稳定抓到了 |
| SRS PDP 弱多径(tap0-7外) | 噪声淹没 | 频选细节抓不住 |

**定性**：SRS **可靠地抓住主导抽头（tap3、SNR18dB），但 CDL 的弱多径（=频选细节=true2d 要利用的部分）落在噪声以下、逐帧抓不稳** → 在"看弱多径"的尺度上与选频 GT 不相关。

为什么抓不住弱多径：
- 高配置 SNR(25) → 踩 (seed42) 深衰落 → attach 失败；
- 能 attach 的低 SNR(10) → 弱多径有效 SNR 不够；
- 贯穿始终的上行弱（Msg3 实测 ~3dB、attach 在 3↔47dB 间抖）。

**算法/C/估计器核心已被离线 oracle + M2.4 逐位单元测试在合成频选输入上验证为正确**；端到端的"垃圾"出在 proxy 链路物理余量，不是估计器。

---

## 七、本日改动文件清单

> 注：本节为 proxy 线（SRS↔GT V3 / DL-flat / handoff）的改动。0601 native rfsimulator 线的改动（`random_channel.c`、`apply_channelmod.c`、conf channelmod）见 **§十二.6**。

### C（已编译进 `nr-softmodem`）
| 文件 | 改动 |
|------|------|
| `openair1/PHY/defs_gNB.h` | `SRS_BIN_VERSION 2→3`；`srs_pingpong_t.meta_timestamp` |
| `openair1/PHY/NR_ESTIMATION/nr_ul_channel_estimation.c` | 注入点 epoch 重建绝对 sample_ts → meta_timestamp |
| `openair1/PHY/INIT/nr_init.c` | 写盘每帧追加 `int64 sample_ts`（V3）|

### Python / proxy（纯 Python，无需重编）
| 文件 | 改动 |
|------|------|
| `eval_nmse_clean.py` | `load_srs` 读 V3 `sample_slots`；新增 `unify_reference_frame` |
| `eval_nmse_sto.py` | unify 配对 + **CDL 帧过滤** + **复数相干守门** |
| `digital_twin_stats.py` | `load_srs_v2` 读 V3 `sample_slots` |
| `v8.py` | `_apply_srs_only_mask(keep_override)`；DL 两处分支 power-flat；`--srs-only-dl-flat`/banner |
| `launch_all_v9.sh` | `SRS_ONLY_DL_FLAT`（默认1）透传 |
| `SWEEP_V9_USAGE.md` | `GT_SAVE_EVERY` 默认更正为 10；记 `SRS_ONLY_DL_FLAT` |

### 两处订正（来自前序日志）
- 旧记"cdl_c 静态 -6.37"实为 **cdl_b**；cdl_c 仍卡 Msg3 max HARQ（DTX）。
- `GT_SAVE_EVERY` launcher 默认其实是 **10**（手册旧标 100 已更正）；`=1` 开销 ×10 会压垮临界 attach。

---

## 八、复现命令

```bash
cd /home/dclcom61/OAI_luuuuuu/DevChannelProxyJIN/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy
export OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1

# 静态 cdl_b + 让 handoff 生效 + GT[12]=CDL（legacy / true2d 各一发）
sudo P1B_NPZ="" NPY_DIR="data_out/cdl_b" UE_SPEED=0 CHANNEL_SEED=42 \
ONLY_SNR="10" MAX_FRAMES=600 UL_PRE_GAIN=1.0 SRS_ESTIMATOR=legacy \
SRS_PERIOD_SLOTS=10 GT_SAVE_EVERY=10 SRS_ONLY_CHANNEL=1 \
ATTACH_STABLE_POST_DELAY=2 HARD_CEILING_SEC=1200 bash run_q4_snr_sweep_v9.sh
# 同配置换 SRS_ESTIMATOR=true2d SRS_2D_DEBUG=1

# 评估（V3 自动走 unified + CDL帧过滤 + 复数相干守门）
python3 eval_nmse_sto.py --ab ../../logs/<legacy>/snr_10dB ../../logs/<true2d>/snr_10dB
```

判读：只信 **复数相干 ≥ 0.50 [OK]** 的 A/B；CDL 帧数足、相干高、NMSE 负，才是能读的频选增益。

---

## 九、当前总状态与下一步

### 已闭环（可交付）
- 算法（离线对 oracle）✓；C=算法（逐位单元测试）✓；编译✓；
- SRS↔GT 编号同源（V3）✓；DL-flat✓；handoff→CDL✓；eval（CDL过滤+复数相干）✓；
- **完整系统性诊断**：attach/上行弱/SRS-on-selective 全部定位到 proxy 链路，与算法/对齐/C 无关。

### 受阻（proxy infra 硬底）
- 真实频选端到端 A/B：被"proxy 链路余量不足（无法既 attach 又给够 SNR 让 SRS 解出弱多径）"阻塞。判定日志 §7.2 早有定论。

### 可选下一步（ROI 递减）
1. （深、ROI 低）查/改 proxy 上行噪声标度，提升能 attach 工作点的有效 SRS SNR；大概率撞回 attach 深衰落 Catch-22。
2. ~~（另条路、也坏）native rfsim + channelmod，需先修本 build 原生样本流时序。~~ → **0601 已打通，见 §十二**。原"样本流时序坏"判断错误，真因是 2 个 C bug + 配置。
3. （推荐）以离线 + 逐位单元测试 + 本系统性诊断作为交付，proxy-CDL 真实频选演示标注为 infra 受限。

> **0601 更新**：选项 2（native）已打通，成为比 proxy 更干净的真实频选测试床。下一步聚焦写 **OAI 侧 GT dumper**（dump rfsim `channelDesc->chF[]` + 绝对 ts，套 V3 对齐喂 `eval_nmse_sto.py`），即可端到端读出 true2d 频选增益。详见 §十二。

---

## 十、归档

| 用途 | 路径 |
|------|------|
| 本大日志 | `0531_True2D_端到端诊断与修复日志.md` |
| 前序：离线判定（含 §四点十 V3 静态/动态实测）| `0529_True2D_离线判定日志.md` |
| 前序：先期/中期计划、Phase1 离线 | `0529_True2D_MMSE_先期计划.md` / `_中期计划.md` / `0529_Phase1_EMA_Wiener_离线日志.md` |
| SRS↔GT 对齐问题与 spec | `SRS_GT_ALIGNMENT_ISSUE.md`、`.kiro/specs/gt-srs-alignment-fix/` |
| 离线 harness / C 单元测试 | `test_srs_2d_offline.py`、`openair1/PHY/NR_ESTIMATION/c_unit_tests/` |
| 评估 | `eval_nmse_sto.py`（unified+CDL过滤+复数相干）、`eval_nmse_clean.py` |
| proxy / launcher | `v8.py`、`launch_all_v9.sh`、`SWEEP_V9_USAGE.md` |

---

## 十一、2D MMSE 代码文件总索引

> C 根目录：`DevChannelProxyJIN/openairinterface5g_whan/`
> proxy/脚本根目录：`DevChannelProxyJIN/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy/`

### A. C 端运行代码（OAI gNB）

| 文件 | 职责 | 关键符号 |
|------|------|----------|
| `openair1/PHY/NR_ESTIMATION/nr_srs_mmse.c` / `.h` | **频阶**：窗口 Hermitian-Toeplitz 频域 LMMSE（dense-pilot 2048 IFFT→EMA PDP→robust floor→R=FT{PDP}→共轭 Wiener-Hopf 解）；输出 R_meas 给时阶 | `nr_srs_mmse_freq_filter`、`nr_srs_robust_noise_floor_arr`、`nr_srs_R_from_pdp_arr`、`nr_srs_get_estimator_mode` |
| `openair1/PHY/NR_ESTIMATION/nr_srs_2d_filter.c` / `.h` | **时阶**：per-link ω 相位预测 Kalman(PKF) + IAE-Riccati 自适应 K；旧 EWMA/IBVSS/scalarkalman（去旋，已证频选/相干多普勒下崩）；R_meas 入口 | `update_pkf_core`、`nr_srs_pkf_update`、`nr_srs_2d_set_rmeas`、`update_scalarkalman_core` |
| `openair1/PHY/NR_ESTIMATION/nr_ul_channel_estimation.c` | **dispatch**：`SRS_ESTIMATOR` 分发（legacy/mmse1d/true2d/mmse2d/passthru/oracle/freqsmooth）；SRS dump **注入点**（V3 绝对 sample_ts epoch 重建）| `nr_srs_channel_estimation`、dispatch L968-1090 |
| `openair1/PHY/NR_ESTIMATION/nr_srs_freq_smooth.c` / `.h` | 频域 MA 平滑（mmse2d 的 Stage1.5，对照/历史）| `nr_srs_freq_smooth` |
| `openair1/PHY/INIT/nr_init.c` | SRS bin **写盘**线程 + 生命周期；V3 每帧写 `int64 sample_ts` | `srs_write_one_batch`、`init_srs_digital_twin_system` |
| `openair1/PHY/defs_gNB.h` | SRS bin 格式定义（`SRS_BIN_VERSION=3`、`srs_pingpong_t.meta_timestamp`）| `srs_bin_header_t`、`srs_pingpong_t` |

### B. C 端单元测试（`openair1/PHY/NR_ESTIMATION/c_unit_tests/`）

| 文件 | 职责 |
|------|------|
| `test_freq_main.c` + `golden_freq.py` | 频阶 C vs 黄金 recipe 逐位比对（0–1 LSB）|
| `test_pkf_main.c` + `golden_pkf.py` | 时阶 PKF C vs 离线 `time_pkf_riccati` 逐位（≤1 LSB）|
| `srs_test_idft.c` | 浮点参考 idft（隔离算法）|
| `c1_idft.py` + `probe_idft.c` + `probe_stubs.c` | OAI 真定点 `idft2048` 核对（非精度瓶颈）|
| `Makefile` / `run_all.sh` / `README.md` | 构建 + 一键跑 |

### C. 离线算法 / 验证脚本（proxy 目录）

| 文件 | 职责 | 关键入口 |
|------|------|----------|
| `test_srs_2d_offline.py` | **离线主 harness（黄金参考）**：真2D五档、时域级联、合成多帧、CDL 离线、真实 dump | `windowed_lmmse`、`freq_stage_toeplitz`、`time_pkf_riccati`、`freq_propagated_noise_avg`；模式 `--true2d`/`--cascade`/`--synth-mf`/`--cdl-dir`/`--run-dir` |
| `check_c_freq_mirror.py` | #4 梳状导频/2048 标定验证（C 频阶忠实度）| `windowed_toeplitz_comb`、`c_mirror_R` |

### D. 评估脚本（proxy 目录）

| 文件 | 职责 | 关键符号 |
|------|------|----------|
| `eval_nmse_sto.py` | **主评估**：unified-V3 配对 + **CDL 帧过滤** + **复数相干守门** + 逐帧 STO 对齐；`--ab` / `--run-dir` | `evaluate`、`unify_reference_frame`(import)、`_sto_es`、`SEL_MIN`/`CORR_MIN` |
| `eval_nmse_clean.py` | LS baseline + SRS 加载（读 V3 `sample_slots`）+ 配对 | `load_srs`、`unify_reference_frame`、`align_frames`、`nmse_ls` |
| `digital_twin_stats.py` | GT/SRS 加载、GT slot 索引（读 V3 ts）| `load_srs_v2`、`index_gt_slots`、`load_gt_by_refs`、`unwrap_srs_abs_slots` |
| `replay_true2d.py` | true2d 离线回放/对照工具 | — |

### E. proxy / launcher（proxy 目录）

| 文件 | 职责 | 本日改动 |
|------|------|----------|
| `v8.py` | Sionna GPU-IPC proxy：信道施加、GT 保存、SRS-only mask | `_apply_srs_only_mask(keep_override)`、DL power-flat、`--srs-only-dl-flat` |
| `launch_all_v9.sh` | 单点启动（gNB+UE+proxy+watcher）| 透传 `SRS_ONLY_DL_FLAT`、`GT_SAVE_EVERY` 默认 10 |
| `run_q4_snr_sweep_v9.sh` | SNR sweep 入口 | — |
| `SWEEP_V9_USAGE.md` | 运行手册 | `GT_SAVE_EVERY` 默认更正、`SRS_ONLY_DL_FLAT` 说明 |

### F. 离线↔C 黄金对应（M2.4）

| 阶段 | 离线黄金 | C 生产 |
|------|----------|--------|
| 频阶 | `test_srs_2d_offline.py::windowed_lmmse` | `nr_srs_mmse.c::nr_srs_mmse_freq_filter` |
| 时阶 | `test_srs_2d_offline.py::time_pkf_riccati` | `nr_srs_2d_filter.c::update_pkf_core` |
| R_meas | `freq_propagated_noise_avg` | 频阶 `wnorm2` 累加 → `nr_srs_2d_set_rmeas` |

---

## 十二、(0601 追加) native rfsimulator + channelmod 真实频选——从"infra 墙"到打通

> 背景：§9 选项 2（native rfsim + channelmod）原被判为"需先修原生样本流时序、也坏"。本日实际验证后**推翻**该结论——native 路径可用，挡路的是 3 个独立的坑（1 配置 + 2 个 C bug）+ RA 的 PRACH↔多径 Catch-22。逐层拆解并修复后，得到 **UE 在线 + 上行频选(TDL_A) + 链路不断** 的干净测试床。

### 12.1 为什么转向 native
proxy(GPU-IPC) 路线被"既要 attach 又要 SNR 让 SRS 解出弱多径"的物理墙挡住（§6）。native rfsim 的优势：**直接控噪声、对称施加、信道真值已知（就是 rfsim 施加的 `channelDesc`）、无需 Sionna/GPU/docker**。且本 build 的 native chanmod 路径其实**从没被用过**（团队一直用 proxy），所以坏了也没人发现。

### 12.2 关键认识：native 是运行时开关，不用重编 radio
proxy 由 `#ifdef USE_GPU_IPC_V8` 编入、再由 **env `RFSIM_GPU_IPC_V8`** 运行时门控（`simulator.c:2594`）。**不设该 env → 自动回落原生 socket + `rxAddInput` 路径**。native TDL-A/B/C/D/E + SCM 模型在 `random_channel.c` 齐全。

### 12.3 逐层根因与修复（按发现顺序）

| # | 现象 | 根因 | 修复 |
|---|------|------|------|
| 1 | UE 连不上 6014 | 本 fork 把 rfsim 默认端口改成 `#define PORT 6014`（`simulator.c:83`）；gNB conf 写死 4043，UE 吃默认 6014 | UE 显式 `--rfsimulator.serverport 4043` |
| 2 | UE 连上但 `pbch not decoded` 死循环 | conf 是 **2T2R / `pdsch XP=2`**（给 proxy 的 2×2），2 端口 SSB 在原生路径不工作 | gNB 强制 **1T1R**：`--RUs.[0].nb_tx/nb_rx 1 --gNBs.[0].pdsch_AntennaPorts_XP 1 --pusch_AntennaPorts 1` → **attach 成功**（推翻 §9"样本流坏"）|
| 3 | 开 chanmod 后上行全废、PRACH 零检测（AWGN/TDL 都一样）| **C bug A**：`new_channel_desc_scm()` 从不分配 `Doppler_phase_cur`；`rxAddInput()` 开头 `if (Doppler_phase_cur==NULL) return;` → **整段信道卷积被跳过 → 上行输出全零** | `random_channel.c:new_channel_desc_scm` 补 `chan_desc->Doppler_phase_cur = calloc(nb_rx,sizeof(double));` |
| 4 | 修 A 后仍 `RAR reception failed`、PRACH 零检测 | **C bug B**：`channel_mode` 默认 =2（旁路），旁路代码用 `after_channel_sig[i]=input_sig[i]` —— **错误索引**（应为环形位置 `input_sig[(TS+i)%CirSize]`）→ gNB 收到错位垃圾 → PRACH 检不到。"多径打散 PRACH"是假象，多径根本没参与 | 修正旁路为正确环形索引（`apply_channelmod.c`）|
| 5 | TDL 全程开 → PRACH 仍可能被多径打散（真 Catch-22）| PRACH 是非均衡相关检测，怕多径；PUSCH/SRS 均衡能扛 | **handoff 机制**：默认平坦透传（RA 过）→ 触发文件 `/tmp/rfsim_chan_on` 出现后切 `channel_mode=1`（真实 TDL）。= proxy attach-stable handoff 的 native 版 |

### 12.4 handoff 机制（native 版 attach-stable）
`apply_channelmod.c::rxAddInput` 开头：`channel_mode==2` 时每 ~1ms `access("/tmp/rfsim_chan_on")`，文件出现即翻转为 `channel_mode=1` 并 `LOG_I` 一行。平坦阶段用**修正后的环形索引**透传 → PRACH/RA 成功；handoff 后施加真实 TDL_A → PUSCH/SRS 见频选。`OAI_CHANNEL_MODE=1` 可强制从头开信道（调试）。

### 12.5 实测结果（已坐实）
- **平坦 attach**：`UE RNTI ecc3 ... in-sync ... RSRP -44`，`4-Step RA procedure succeeded`。
- **handoff**：`gnb_native.log:4113` → `rfsim channel handoff: /tmp/rfsim_chan_on detected -> applying channel model`。
- **切换后链路不断**：ULSCH `ulsch_rounds` 持续增长、`errors 0`、`BLER 0`、`SNR 54dB`（PUSCH 均衡扛住频选）。
- gNB 上行 TDL_A：`Td 2.9e-7, channel_length 37`（30ns DS，38MHz 带宽强频选）。

### 12.6 本日改动文件（native 线）

| 文件 | 改动 |
|------|------|
| `openair1/SIMULATION/TOOLS/random_channel.c` | `new_channel_desc_scm` 补分配 `Doppler_phase_cur`（修 C bug A，所有经此函数的信道生效）|
| `radio/rfsimulator/apply_channelmod.c` | 修正 `channel_mode==2` 旁路环形索引（修 C bug B）；新增触发文件 handoff 门控（平坦→TDL）；`#include <unistd.h>` |
| `targets/.../CONF/gnb.sa.band78.fr1.106PRB.usrpb210.conf` | `channelmod`：`enB0`→AWGN（避免共享 static 延迟数组被二次缩放）、`ue0`=TDL_A + `ds_tdl=30e-9`（上行频选）|

> 编译：`cd cmake_targets/ran_build/build && sudo ninja nr-softmodem rfsimulator`（`.so` 属主 nobody:nogroup，需 sudo）。注意**重编后必须重启 gNB**才加载新 `.so`（运行中进程不受磁盘 `.so` 更新影响）。

### 12.7 复现命令（native 频选）

```bash
BUILD=/home/dclcom61/OAI_luuuuuu/DevChannelProxyJIN/openairinterface5g_whan/cmake_targets/ran_build/build
CONF=/home/dclcom61/OAI_luuuuuu/DevChannelProxyJIN/openairinterface5g_whan/targets/PROJECTS/GENERIC-NR-5GC/CONF/gnb.sa.band78.fr1.106PRB.usrpb210.conf

# 清场
sudo pkill -9 -f nr-softmodem; sudo pkill -9 -f nr-uesoftmodem; sleep 2

# gNB：1T1R + chanmod（默认平坦起步），不设 RFSIM_GPU_IPC_V8 → 走 native
cd $BUILD && sudo SRS_ESTIMATOR=legacy ./nr-softmodem -O $CONF \
  --gNBs.[0].min_rxtxtime 6 --rfsim --rfsimulator.serveraddr server \
  --rfsimulator.options chanmod \
  --RUs.[0].nb_tx 1 --RUs.[0].nb_rx 1 \
  --gNBs.[0].pdsch_AntennaPorts_XP 1 --gNBs.[0].pusch_AntennaPorts 1 \
  2>&1 | tee /tmp/gnb_native.log

# UE：1×1，端口 4043（看到 Running as server 后起）
cd $BUILD && sudo ./nr-uesoftmodem -r 106 --numerology 1 --band 78 -C 3619200000 \
  --uicc0.imsi 001010000000001 --rfsim --rfsimulator.serveraddr 127.0.0.1 \
  --rfsimulator.serverport 4043 --ue-timing-correction-disable \
  2>&1 | tee /tmp/ue_native.log

# attach 成功(UE RNTI ... in-sync)后，handoff 切频选上行：
sudo touch /tmp/rfsim_chan_on        # gNB 日志出现 "channel handoff ... applying channel model"
```

判读：`grep handoff /tmp/gnb_native.log`（实时终端会被滚动缓冲冲掉，看 tee 全量日志）。

### 12.8 一键 launcher：`launch_native.sh`
一条命令跑完整流程（自带 preflight）：清场 host 进程 + 清 `/tmp` 残留 + 查 5GC → 起 gNB(1T1R+chanmod+GT dump) → 起 UE → 等 `in-sync` → 自动 `touch` handoff 切 TDL → 收 SRS+GT → 优雅退出(flush SRS) → 自动转换 GT → 打印 eval 命令。
- 选项：`-e EST`（legacy/true2d/passthru/…）、`-d SEC`、`-p N`(SRS周期)、`-nd DB`(上行噪声 ue0.noise_power_dB，**逐信道、仅 handoff 后施加→attach 干净**)、`-seed N`(固定信道，同信道 A/B)、`--handoff-delay`、`--no-handoff`/`--no-gt`。
- 退出时 `chmod -R a+rwX $RUN`，非 root 也能 re-convert/eval。

### 12.9 GT dumper + 端到端 A/B（0601 下午）

**目标**：补上评估的"标准答案"——把 rfsim 实际施加的上行信道(真值)落盘，让 `eval_nmse_sto.py` 拿它给 SRS 估计打分(NMSE)，做 true2d vs 基线 A/B。

**实现**（两件）：
| 文件 | 作用 |
|------|------|
| `apply_channelmod.c::native_gt_dump` | `channel_mode==1`(handoff后)时节流 dump `channelDesc->ch[]`(sample-spaced CIR 时域抽头)+ 绝对 ts(=nextRxTstamp，与 SRS V3 同轴)。env `NATIVE_GT_DUMP=1`、`NATIVE_GT_PATH`、`NATIVE_GT_EVERY_SAMPLES` |
| `native_gt_to_npz.py` | 读 dump → `H[k]=FFT_2048(taps)`(自然序，匹配 OAI bin)→ 产 `gt_batch_ue0_seq0.npz`(h_matrix+slot_ids=ts//30720)，与 Sionna 格式一致，eval 零改动 |

**子载波栅格**(经探查坐实)：SRS 估计与 GT 均为 **2048 点自然序 FFT**(无 fftshift；106PRB 从 bin1412 环绕)；eval 用 SRS 的 active-mask(~1248 bin)**同索引**比较，`_sto_es` 搜 τ∈[−30,30] 吸收 STO。`ch[]` 是 sample-spaced CIR → 补零2048 做 FFT 即频响。

**逐个踩坑(全部修好)**：
1. **handoff 前平坦帧污染**：GT 是静态选频信道，但 SRS 含 handoff 前的平坦帧。曾导致 NMSE 假性 −3dB。修法：转换器只对 ≥ 首个 GT-dump slot(=handoff)的 SRS 帧产 GT，前半帧自动不配对被丢弃。
2. **共轭约定(关键且隐蔽)**：OAI SRS 估计的 FFT 符号约定**随估计器/run 变化**(legacy 那发需 `conj(FFT)`、passthru/true2d 那发需 plain `FFT`)。写死 conj → coherence 垮(0.47)、NMSE 假性 −0.8dB。修法：转换器**自动探测**(拿真实 SRS 帧比 `H` vs `conj(H)` 的相干，选高的)。
3. **同信道 A/B**：rfsim 信道用时间随机种子→两发不同信道(SNR 18 vs 9，不公平)。修法：`random_channel.c::random_channel` 首次生成时尊重 `OAI_RNGSEED`(按 chan_idx 偏移)→ 实测两发抽头 `||diff||=0` 完全一致。
4. **噪声破坏 attach**：全局 `--channelmod.noise_power_dBFS` 从一开始加噪 → PRACH 定时被噪声打乱、RA 挂(`SRS bin 数 0`)。修法：改用**逐信道 `noise_power_dB`**(rxAddInput 内、`channel_mode==1` 才加)→ 平坦 attach 干净、仅 handoff 后 SRS 见噪。launcher `-nd` 走 per-run conf copy + sed。

**管线验证(坐实正确)**：
- **无噪 legacy(post-handoff)**：coherence **1.000**、NMSE **−45.8 dB** → GT 管线正确。
- **近无噪(SNR36-45dB) passthru(原始LS) / true2d**：自动选 plain FFT，NMSE **−37 / −36.8 dB**、coh 0.655 → 高 SNR 下裸 LS 已近最优。

**关键认知(为何 true2d 一度 ≈ 基线)**：
- `legacy` = OAI **filt8/16 FIR 3点平滑**(本身去噪)；`passthru` = **原始稀疏 LS**(不去噪)；`true2d` = MMSE 两阶段。→ 要展示 true2d 去噪增益，基线应是 **passthru(裸LS)**，不是已去噪的 legacy。
- true2d **不是** bug 式退化成 LS：LS 回退仅边角(`R0<=0`/`R_table`溢出)；`noise_norm=残差噪声/pilot_power` clamp[1e-4,2.0]，机制健全。
- **MMSE 只在中等-低 SNR 才显优势**：高 SNR(≈36dB, `-nd -22`)裸 LS 已最优、true2d 反略差；极低(≈SRS有效−1dB, `-nd -8`)全淹没。**甜区 ≈ SNR 33~37dB**(裸 LS NMSE ~−10~−20dB)，待验证。

**改动文件(本节)**：
| 文件 | 改动 |
|------|------|
| `radio/rfsimulator/apply_channelmod.c` | `native_gt_dump`(GT 落盘) |
| `openair1/SIMULATION/TOOLS/random_channel.c` | `random_channel` 尊重 `OAI_RNGSEED`(可复现信道) |
| `native_gt_to_npz.py`(新) | dump→npz；自动共轭探测；post-handoff slot 过滤 |
| `launch_native.sh`(新) | 一键 launcher + preflight；`-nd`/`-seed` |
| conf `gnb.sa...106PRB...conf` | `ue0` TDL_A `ds_tdl=30e-9`(`-nd` 时 per-run copy 改 noise_power_dB) |

### 12.10 静态甜区结果（已得）
- 静态 TDL_A `ds_tdl=30e-9` + 同信道(seed) + passthru(裸LS) 基线：
  - ≈SNR 36-45dB(`-nd -22`)：passthru −37.4 / true2d −36.8 → **+0.6dB(true2d 略差)**（高SNR裸LS已最优）。
  - ≈SNR 24.5dB(`-nd -15`)：passthru −31.3 / true2d **−33.9** → **−2.67dB(true2d 赢)** ✅ 首次正向读出频选去噪增益。
- 关键：`legacy`=OAI FIR平滑(去噪器)，正确基线是 **passthru=裸LS**；MMSE 只在中等-低 SNR 显优势。

### 12.11 真实 CDL 注入 + 动态回放（native，实现完成）

**架构**：CDL 数据是射线参数(`data_out/cdl_*`：tau/power/角度，无 CIR)。离线 `cdl_to_cir.py` 按 v8 公式(`l=round(τ·Fs)`,Fs=61.44e6;Doppler `ν=f_D·sinθ·cosφ`;逐 slot 相位演化)合成**逐 slot sample-spaced CIR 序列** → C 端 rfsim 按 slot 回放进 `ch[]`(handoff 后,env `RFSIM_CIR_REPLAY`) → 同时拿到真实 CDL + 时变 + 激活 true2d 频阶&时阶(PKF)。

**新增/改动文件**：
| 文件 | 改动 |
|------|------|
| `cdl_to_cir.py`(新) | CDL 射线→(n_slots,L) CIR 二进制；`-speed/-slots/-L/-seed`；已生成 cir/cir_cdl_{a..e}.bin + cdl_c 速度变体(s0/s3/s10) |
| `apply_channelmod.c` | `cir_replay_load/apply`：handoff 后按 `slot=TS/30720%n_slots` 写 `ch[]`(realloc 扩容到 L=128)|
| `native_gt_to_npz.py` | 逐 slot 动态 GT(按 ts 最近邻取 CIR)+ **wide-tau(±64) 多帧中位共轭探测**(修长时延 CDL-E 需 tau−17 导致的误选)|
| `launch_native.sh` | `-cir`(自动每slot dump GT、路径转绝对)、`-gt-every` |
| `sweep_native_cdl.sh`(新) | 一键扫 CDL×噪声 passthru-vs-true2d A/B |

**关键踩坑**：长时延 CDL(如 CDL-E Td=2064ns)的 SRS 定时锁能量质心而非首径 → 与"首径对齐 tap0"差 ~17 样本 STO；原共轭探测只扫 ±4 → 误选共轭 → coherence 0.08、NMSE 假象(+26/−30dB)。修法：共轭探测 tau 搜索加宽到 ±64 + 多帧中位。

**动态 CDL A/B 结果(SISO,同信道seed,passthru=裸LS基线)**：

| 模型 | 速度 | ≈SNR | passthru | true2d | Δ(true2d−passthru) |
|------|------|-----|----------|--------|------|
| CDL-C | 30km/h | ~34 dB | −34.6 | −28.9 | **+5.6**(差) |
| CDL-C | 30km/h | ~29 dB | −29.1 | −27.9 | +1.2(差) |
| CDL-B | 3km/h | ~34 dB | −33.3 | −31.6 | +1.7(差) |
| CDL-B | 3km/h | ~29 dB | −27.8 | −29.9 | **−2.0**(赢) |
| CDL-E | 3km/h | ~34 dB | −33.0 | −31.5 | +1.5(差) |
| CDL-E | 3km/h | ~29 dB | −27.5 | −29.5 | **−2.0**(赢) |

**结论(true2d 净增益是 regime-dependent)**：
- **SNR**：越吵(≈29dB) true2d 越占优(MMSE 去噪)；越干净(≈34dB)越没用武之地。
- **速度**：慢衰落(3km/h, B/E) true2d 在低 SNR 赢 ~2dB；快衰落(30km/h, C) **PKF 时间阶滞后/误预测 → 偏差主导 → true2d 明显劣(≈34dB 时差 5.6dB)**。
- 即 true2d 的设计点 = **慢时变 + 中低 SNR**(时间阶平滑跟踪 + 频阶去噪);快衰落或高 SNR 下劣化。
- 增益普遍温和(~2dB)，因 passthru(裸LS)在这些 SNR 已相当好(−27~−34dB)。

### 12.12 2D MMSE 架构深析 + 为何"增益一般"的机理（已查清）

**架构对照(子代理 + 单元测试)**：live C `nr_srs_2d_filter.c` 的 PKF 与离线黄金 `test_srs_2d_offline.py::time_pkf_riccati` **结构等价(≤1-2 LSB)**：
- 频阶：per-SC 窗口 Hermitian-Toeplitz LMMSE(Wiener) + 传 R_meas=σ²·E[|w|²]。
- 时阶 PKF：每链路一个标量 `ω`(rad/帧, EMA k_w=0.2) 相位率 + per-SC 复状态 + IAE-Riccati 自适应 Q/K(clip[0.02,0.95])；**输出 a-posteriori**(非预测→无滞后 bug)；1 帧 bootstrap warmup。
- 默认 env：`SRS_2D_PKF_KW=0.2 KMIN=0.02 KMAX=0.95 Q_EMA=0.10 INNOV_EMA=0.15`；TRUE2D 忽略 `SRS_2D_METHOD` 直接走 PKF。

**对齐已被实测排除(关键)**：在 native CDL run 上扫 conj+tau 求"可达最佳相干"：
- passthru(裸LS) = **1.000**；true2d = **0.999**。两者都能对到 ~1.0 → **对齐完全正确**，PKF 也没乱跟。

**为何增益一度"一般"——是高 SNR + 相干→NMSE 硬关系，不是 bug/对齐**：
- `NMSE_min ≈ 10log10(1−coh²)`：passthru coh1.000→可达−34dB；true2d coh0.999→**被钉在~−27dB**。那 0.999 vs 1.000 之差 = **滤波对"近乎完美信号"的代价**。
- **MMSE/Kalman 增益 ∝ 噪声量**(本质)：高 SNR 裸 LS 已近完美→true2d 滤波只剩偏差代价(略差)；低 SNR 裸 LS 退化→去噪净赢。
- **快衰落(30km/h)**：5ms SRS 周期→每帧 ~0.49 cycle(近 Nyquist)，单-ω 对多径 CDL 略失配；但 coh 仍 0.999，非主因。

**SNR 甜区扫描(CDL-B 慢衰落, passthru=裸LS基线, 实测)**：

| ≈SNR(越低越吵) | Δ(true2d−裸LS) |
|------|------|
| ~31 dB | −0.88 dB |
| ~29 dB | −2.07 dB |
| ~27 dB | −3.18 dB |
| ~23 dB | **−5.13 dB**(coh[OK]) |

**完美单调**：噪声越大 true2d 增益越大，≈SNR 23 dB 已 −5.13dB → 坐实 true2d 有效，之前"一般"纯因测试 SNR 太高。(−10 点 +9.44 为离群：该对两发 SNR 49 vs 19.5、passthru coh0.132[BAD] → 坏 run；根因=两发落在 2000-slot CIR 循环不同位置 + gNB 5-PRB SNR 报告不可靠。)

### 12.13 交付级实验方法（分解扫描，进行中）
为出干净的"true2d 增益 vs SNR / vs 模型"曲线，避免 2D 大网格的离群+不可靠横轴：
1. **横轴用 passthru(裸LS) NMSE**(=1/有效SNR, 每发实测可靠)，非 gNB 5-PRB SNR。
2. **每点 -rep 取中位**压离群(`sweep_native_cdl.sh` 已加 `-rep`)。
3. **静态 CIR(s0)** 扫 SNR：可复现、无 CIR 循环离群、隔离 SNR；A–E 时延剖面不同→频选强度异(sel std/mean: B0.50>C0.37>E0.28>D0.26>A0.21)；静态下 true2d 时间平均充分→展示去噪上限。已生成 `cir/cir_cdl_{a..e}_s0.bin` + cdl_c 速度变体。
4. **每模型扫 SNR + 统一强 SNR(≈23dB, `-nd -9`) 跨模型对比**：
   `sudo bash sweep_native_cdl.sh -m "cdl_a_s0 cdl_b_s0 cdl_c_s0 cdl_d_s0 cdl_e_s0" -nd "-15 -12 -9 -6" -rep 2 -d 60`

### 12.14 交付结果：5 模型 × SNR 静态扫描（true2d vs 裸LS）

命令（实跑）：`sudo bash sweep_native_cdl.sh -m "cdl_a_s0 cdl_b_s0 cdl_c_s0 cdl_d_s0 cdl_e_s0" -nd "-15 -12 -9 -6 -3 0 3" -rep 1 -d 60`
横轴=passthru(裸LS) NMSE；Δ=true2d−裸LS(负=true2d 赢)。

**三区间**：
- **干净/可用 (≈SNR 34~11 dB, 即 `-nd -15~-3`)**：passthru NMSE 负、coh 0.5~0.66、单调，全可信。
- **边界 (≈SNR 6 dB, `-nd 0`)**：A/E 仍可信(passthru 负, coh~0.5)；B/C/D 开始淹(passthru 正, coh<0.15)。
- **淹没 (≈SNR −2 dB, `-nd 3`)**：passthru NMSE **正**、coh<0.15、tcoh=nan → SRS 淹没,数字无效(须剔除)。

**每模型 Δ(dB) vs ≈SNR**（干净区间）：

| ≈SNR | A(290ns) | B(478ns) | C(865ns) | D(376ns) | E(2064ns) |
|-----|------|------|------|------|------|
| ~34 dB | −2.0 | −0.0 | −0.0 | +0.0 | −1.6 |
| ~29 dB | −3.4 | −3.1 | −2.3 | −2.6 | −2.9 |
| ~23 dB | −5.6 | −6.2 | −4.8 | −5.3 | −4.7 |
| ~17 dB | −8.2 | −8.7 | −7.2 | −8.1 | −6.6 |
| ~11 dB | −10.7 | −10.2 | −9.2 | −10.4 | −8.7 |
| ~6 dB | **−12.2**✅ | (淹) | (淹) | (淹) | **−10.3**✅ |

**统一强噪点跨模型对比 (≈SNR 11 dB，即 `-nd -3`，全模型可信)**：

| 模型 | Td | sel | passthru | true2d | **增益** |
|------|-----|------|------|------|------|
| A | 290ns | 0.21 | −12.2 | −22.9 | **−10.7** |
| D | 376ns | 0.26 | −10.7 | −21.0 | **−10.4** |
| B | 478ns | 0.50 | −10.3 | −20.4 | **−10.2** |
| C | 865ns | 0.37 | −11.3 | −20.5 | **−9.2** |
| E | 2064ns | 0.28 | −12.1 | −20.8 | **−8.7** |

**结论**：
1. **true2d 确实强**：干净区间内对裸 LS **单调、可观**的去噪增益，峰值 **−10~−12dB**(A/E @ ≈SNR 6dB)，五模型一致。之前"一般"纯因测试 SNR 太高。
2. **统一 ≈SNR 11dB**：全模型 −9~−11dB → 交付级跨模型结论。
3. **物理自洽**：增益随时延扩展递减（A 290ns −10.7dB > E 2064ns −8.7dB），因频阶 Wiener 靠频域相关性去噪，长时延=频域去相关快=可压榨相关性少。
4. 此为**静态去噪上限**（频阶 Wiener + 时间平均充分）；动态(30km/h)现实包络更小(§12.9, ~2~5dB, 受相干时间限制)。
5. 图（`plot_cdl_sweep.py` 生成）：
   - `vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy/native_cdl_gain.png`：总览图（增益曲线 + NMSE 对角线）。
   - `vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy/native_cdl_gain_small_multiples.png`：五模型分面图（避免曲线挤在一起）。
   - `vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy/native_cdl_nd_minus3_bars.png`：统一 ≈SNR 11dB（`-nd=-3`）跨模型柱状对比。

### 12.15 native 测试床脚本/资产清单（根目录 `vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy/`）

| 路径 | 作用 |
|------|------|
| `launch_native.sh` | 一键启动 native(1T1R+chanmod)+preflight；`-e/-d/-p/-nd/-seed/-cir/-gt-every/--no-handoff/--no-gt` |
| `sweep_native_cdl.sh` | true2d vs passthru A/B 扫描；`-m/-nd/-d/-seed/-rep`(中位)；输出 passthru-NMSE 横轴 |
| `cdl_to_cir.py` | CDL 射线→逐slot CIR 二进制(`-speed/-slots/-L/-seed`) |
| `cir/cir_cdl_{a..e}.bin` | 各模型默认速度 CIR；`cir/cir_cdl_{a..e}_s0.bin` 静态；`cir/cir_cdl_c_s{0,3,10}.bin` 速度变体 |
| `native_gt_to_npz.py` | rfsim GT taps→`gt_batch_*.npz`(逐slot动态GT+自动宽tau共轭+post-handoff过滤) |
| `plot_cdl_sweep.py` | 画 Δ vs passthru-NMSE 五模型增益曲线 → `native_cdl_gain.png` |
| `eval_nmse_sto.py` | 评估(unified-V3+CDL过滤+复数相干守门+STO对齐) |
| C: `radio/rfsimulator/apply_channelmod.c` | handoff 门控 + 修正旁路索引 + `native_gt_dump` + `RFSIM_CIR_REPLAY` 回放 |
| C: `openair1/SIMULATION/TOOLS/random_channel.c` | `Doppler_phase_cur` 分配修复 + `OAI_RNGSEED` 可复现信道 |
| conf `targets/.../gnb.sa.band78.fr1.106PRB.usrpb210.conf` | channelmod ue0=TDL_A(占位)；`-nd` 时 per-run copy 改 noise_power_dB |

复现：① `python3 cdl_to_cir.py data_out/cdl_b cir/cir_cdl_b_s0.bin -speed 0` → ② `sudo bash sweep_native_cdl.sh -m cdl_b_s0 -nd "-15 -9 -3"` → ③ `python3 plot_cdl_sweep.py`。

### 12.16 未尽事项
1. 动态(speed>0)增益包络系统化：快衰落 PKF 调 `SRS_2D_PKF_KW`/更快 SRS(`-p 2`)恢复时阶增益。
2. ✅ **true2d vs legacy(FIR)：已完成，见 §12.17**（"超过 OAI 平滑器"对比 + 交叉点）。
3. （可选）多 UE / 2×2：需先解原生 2 端口 SSB(§12.3 #2)。

### 12.17 (0602 追加) 三方对比：true2d vs legacy(OAI FIR) vs passthru(裸LS)

> 背景：前述 sweep 一直以 **passthru(裸LS)** 为基线。本节补上更难、更有说服力的对比——**直接对打 OAI 自带的生产估计器 `legacy`**（= 裸 LS + filt8/16 三点 FIR 平滑，本身是个去噪器）。`sweep_native_cdl.sh` 加了 `-A/-B` 选项支持任意估计器对；命令：
> `sudo bash sweep_native_cdl.sh -m "cdl_a_s0..cdl_e_s0" -nd "-15..3" -A legacy -B true2d`
> 同 seed=12345（同信道，可比）；passthru 取自 §12.14 同 seed 的 sweep。守门：coh≥0.50；`-nd 3` 全淹剔除，`-nd 0` 仅 A/E 可信。

**nd ↔ 有效 SNR 标定**（实测，有效 SNR ≈ −passthru_NMSE，5 模型均值）：

| −nd | passthru NMSE 均值 | ≈ 有效 SNR |
|-----|------|------|
| −15 | −34.3 dB | ~34 dB |
| −12 | −29.0 dB | ~29 dB |
| −9 | −23.2 dB | ~23 dB |
| −6 | −17.3 dB | ~17 dB |
| −3 | −11.3 dB | ~11 dB |
| 0 | −6.2 dB | ~6 dB |

经验线性：`SNR ≈ −2·nd + 5`（全区间线性，斜率 ~−2；非 1:1 因 rfsim per-channel `noise_power_dB` 标度 ≠ per-SC SNR）。

**三方 NMSE 完整数据表**（dB；Δ = true2d − legacy，负=true2d 赢）：

| 模型 | ≈SNR | passthru(裸LS) | legacy(FIR) | true2d | **Δ(t2−legacy)** |
|------|-----|------|------|------|------|
| A (290ns)  | ~34 dB | −35.01 | −38.51 | −37.06 | +1.45 |
| A | ~29 dB | −29.90 | −33.44 | −33.31 | +0.13 |
| A | ~23 dB | −24.16 | −27.73 | −29.78 | −2.05 |
| A | ~17 dB | −18.21 | −21.80 | −26.40 | −4.60 |
| A | ~11 dB | −12.24 | −15.81 | −22.88 | −7.07 |
| A | ~6 dB  | −6.23  | −9.82  | −18.13 | −8.31 |
| B (478ns)  | ~34 dB | −33.32 | −36.78 | −33.34 | +3.44 |
| B | ~29 dB | −28.02 | −31.57 | −31.10 | +0.47 |
| B | ~23 dB | −22.22 | −25.80 | −28.34 | −2.54 |
| B | ~17 dB | −16.27 | −19.86 | −24.90 | −5.04 |
| B | ~11 dB | −10.28 | −13.86 | −20.47 | −6.61 |
| C (865ns)  | ~34 dB | −34.16 | −37.65 | −34.16 | +3.49 |
| C | ~29 dB | −28.96 | −32.53 | −31.28 | +1.25 |
| C | ~23 dB | −23.20 | −26.76 | −27.97 | −1.21 |
| C | ~17 dB | −17.25 | −20.83 | −24.45 | −3.62 |
| C | ~11 dB | −11.26 | −14.85 | −20.45 | −5.60 |
| D (376ns)  | ~34 dB | −33.78 | −37.25 | −33.79 | +3.46 |
| D | ~29 dB | −28.43 | −31.98 | −30.98 | +1.00 |
| D | ~23 dB | −22.62 | −26.20 | −27.94 | −1.74 |
| D | ~17 dB | −16.66 | −20.25 | −24.70 | −4.45 |
| D | ~11 dB | −10.67 | −14.25 | −21.04 | −6.79 |
| E (2064ns) | ~34 dB | −35.22 | −38.15 | −36.77 | +1.38 |
| E | ~29 dB | −29.85 | −33.23 | −32.76 | +0.47 |
| E | ~23 dB | −24.01 | −27.54 | −28.66 | −1.12 |
| E | ~17 dB | −18.05 | −21.62 | −24.62 | −3.00 |
| E | ~11 dB | −12.07 | −15.66 | −20.71 | −5.05 |
| E | ~6 dB  | −6.08  | −9.66  | −16.34 | −6.68 |

**结论（关键、更诚实的发现）**：
1. **存在清晰交叉点 ≈ SNR 27 dB**，五模型高度一致。
2. **干净区（SNR ≳ 29dB）：legacy 反而更好（+1.4~+3.5dB）**。信号近完美时 legacy 的轻 FIR 几乎无损，而 true2d 两阶段 MMSE 对近完美信号过滤 → 偏差代价 → 略差。**这是只跟裸 LS 比时看不到的**（vs 裸 LS true2d 永远≥）。
3. **噪声区（SNR ≲ 23dB）：true2d 单调反超且越吵赢越多**：≈23dB 赢 1~2.5dB → ≈17dB 赢 3~5dB → ≈11dB 赢 5~7dB → ≈6dB(A/E) 赢 6.7~8.3dB。
4. **机理**：legacy 是定窗 FIR，去噪量恒定 ~3.6dB（与噪声无关）；true2d 是自适应 MMSE，去噪量 ∝ 噪声。交叉点 = "固定平滑 = 自适应 MMSE" 的平衡 SNR。
5. **跨模型（固定 ≈SNR 11dB）**：短时延占优更大（A 290ns −7.07 > E 2064ns −5.05），与频域相关性去噪机理一致。
6. **一句话**：true2d 不是无条件碾压 legacy，而是**分工明确**——干净/高 SNR 用廉价 legacy 更优，**true2d 的价值在噪声区**（交叉点以下显著反超 OAI 平滑器，最噪达 −8.3dB）。

**图**：`vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy/native_cdl_3way.png`
- (a) 三方 NMSE vs 有效 SNR（5 模型均值 + min–max 带），标出 ~27dB 交叉点；
- (b) true2d 相对两基线的增益 vs SNR：对裸 LS 单调增长，对 legacy 在 ~27dB 过零。

**改动文件（本节）**：
| 文件 | 改动 |
|------|------|
| `sweep_native_cdl.sh` | 加 `-A/-B` 选任意估计器对（默认仍 passthru/true2d）|
| `plot_cdl_sweep.py` | 加 `LEGACY` 数据 + `save_threeway()`（三方 NMSE-vs-SNR + 增益 + 交叉点）|

### 12.18 (0602 追加) 高 SNR legacy 回退门控——修复"高 SNR 输 legacy"

> §12.17 的诚实发现：高 SNR 区 true2d 输 legacy（Stage-1 Wiener 退化成 identity≈LS，丢掉 legacy FIR 恒定 ~3.6dB 频域平滑）。本节加门控让 true2d **全 SNR 区间不输 legacy**。

**根因**（已坐实）：高 SNR 时 `noise_norm = noise_power/pilot_power` 被 clamp 到极小的 `norm_floor`（`nr_srs_mmse.c` L786-787）→ Wiener≈identity→true2d≈passthru，而 legacy filt8/16 在 TRUE2D 分支里**已现成**于 `srs_est[mem_offset]`（dispatch k-loop 无条件写入）。

**实现**（保守·低风险，不碰已逐位验证的 Stage1/2）：
- TRUE2D 分支 PKF 之后做 **per-link SNR 门控融合**：`snr_dB=10log10(pilot_power/mmse_noise_power)`；`beta=clamp((snr_dB-(gate_db-w/2))/w,0,1)`；`out=(1-beta)*true2d+beta*legacy`（仅 active band，`beta=0` 复现纯 true2d）。
- env：`SRS_2D_LEGACY_GATE`（默认 1）、`SRS_2D_LEGACY_GATE_DB`（默认 26）、`SRS_2D_LEGACY_GATE_WIDTH_DB`（默认 4）。
- 改动：`nr_srs_mmse.c/.h`（`nr_srs_get_legacy_gate_cfg`）、`nr_ul_channel_estimation.c`（TRUE2D 分支融合）。
- 验证：`c_unit_tests/run_all.sh` 频阶/时阶仍 ≤1 LSB（门控在 dispatch 层、不碰被测函数）。

**门控后全量（delta=true2d−legacy，负=赢；同 seed=12345）**：

| ≈SNR | A | B | C | D | E |
|-----|------|------|------|------|------|
| ~34 dB | +0.00 | −0.00 | −0.00 | +0.01 | +0.01 |
| ~29 dB | +0.00 | +0.01 | −0.01 | +0.01 | −0.00 |
| ~23 dB | −0.01 | −0.53 | −0.02 | −0.15 | +0.01 |
| ~17 dB | −4.60 | −5.04 | −3.65 | −4.46 | −2.98 |
| ~11 dB | −7.06 | −6.58 | −5.62 | −6.78 | −5.06 |

**结论**：
1. 全 25 点 delta ≤ ~0（+0.01 量化噪声=打平）→ **true2d 全 SNR 不再输 legacy**。
2. 高 SNR（≈34/29 dB）：从 legacy 赢 1.4~3.5dB → **精确追平**（beta→1 切 legacy，相干都相同）。
3. 中低 SNR（≈17/11 dB）：true2d 大胜完整保留（−3~−7dB）。
4. 代价：≈SNR 23 dB（`-nd -9`）小胜被门控吸收（在线 SNR 代理偏高、斜坡略提前）；调高 `SRS_2D_LEGACY_GATE_DB` 可保住（待选）。

**图**：`native_cdl_3way.png` 重画为门控对照——(a) `true2d(no gate)` 高 SNR 翘到 legacy 之上、`true2d(gated)` 贴住 legacy；(b) 对 legacy 增益，no-gate 高 SNR 跌破 0、gated 全程 ≥0。

**复现**：`sudo bash sweep_native_cdl.sh -m "cdl_a_s0..cdl_e_s0" -nd "-15 -12 -9 -6 -3" -A legacy -B true2d`（门控默认开；`SRS_2D_LEGACY_GATE=0` 关掉对照）。

### 12.19 (0603 追加) 动态故障定位 + 5GC 退化根因 + 真实动态包络

> 起因：动态(speed>0) sweep 结果不可信（相干塌 0.2、CDL-C n=0、非单调）。本节定位根因、修复、并拿到可信的动态包络。

**定位（只读日志+探针，未改算法）**：
1. **排除 RAN/算法**：20/20 dynamic run 全 `RA succeeded`+`handoff`+`in-sync`，GT 每 slot 稠密（9759 间隔全 30720），±1 slot CIR 相干 0.984 → 故障在离线后处理或更上层。
2. **CDL-C n=0 / 0-SRS 根因 = 5GC/NAS 退化**：坏 run `[SRS Dump] Captured: 0`、`RRCReconfigurationComplete=0`、`NAS Registration accept=0`（好 run 都=1/8）。SRS 配置在 RRCReconfiguration 里 → 连续 ~14 次 attach 后 AMF 停止接受注册 → SRS 不配 → 捕获 0。`preflight` 原本不重启"Up 但病了"的 5GC。SRS-captured 帧数实测在某 run 后锐变 0 并跨小时持续 = 坐实。
3. **管线本身健康（排除 H1 共轭/H2 时间轴）**：重启 5GC 后健康 run 用 `_diag_dyn_gt.py` 扫 GT-slot 偏移+共轭 → 相干 **0.982（峰 k=0）**、共轭 margin +0.344 干净选对。

**修复**：
| 文件 | 改动 |
|------|------|
| `launch_native.sh` | 加 `--restart-5gc`（全 Up 也 `docker compose restart`）+ `CN5G_RESTART_WAIT`(默认5s) |
| `sweep_native_cdl.sh` | 加 `-restart5gc`(每发重启)/`-restart-every N` |
| `_diag_dyn_gt.py`(新) | GT-slot 偏移 + 共轭探针（定位用，0-SRS run 友好报错）|
| `plot_speed_sweep.py`(新) | 增益 vs 速度图 |

**守门相干度量修正（关键）**：发现守门相干原用"不去 STO"的裸内积 `|⟨s,g⟩|`——CDL 多径天生有 STO（线性相位斜坡）→ 裸相干被拉到 ~0.5（假低），把估得很好的 run 误判失败（这才是"动态点忽过忽不过"的真因，不是数据坏）。证据：同一 moving run 探针去 STO 得 0.982、NMSE(去STO) −20dB，而裸守门只 0.47。修法：`eval_nmse_sto.py` 守门相干改为**与 NMSE 同口径 best-over-tau STO 对齐**（同 run 0.47→0.80 干净过门；垃圾 run 仍低照剔）；并修 `sweep_native_cdl.sh` 把 `grep coherence` 改 `grep "cplx coherence"`（避免误匹配 ⚠ 行致 true2d coh 假性 nan）。

**真实动态包络（修复后干净重测一轮 `dyn_full_v2`：A-E × 速度，≈SNR 17dB=`-nd -6`，rep3，每发重启5GC@10s，STO 对齐守门）**：

| 速度 | A | B | C | D | E | 均值增益 |
|------|------|------|------|------|------|------|
| 0 km/h | 8.2 | 8.6 | 7.2 | 8.0 | 6.6 | **7.7 dB** |
| 3 | 6.3 | 7.6 | 6.9 | 7.7† | 5.8 | 6.9 |
| 10 | 6.5 | 7.3 | 6.5 | 7.1 | 5.1 | 6.5 |
| 30 | 5.7 | 6.4 | 5.7 | 6.0 | 4.7 | 5.7 |
| 60 | 7.1 | 6.2 | 5.6 | 6.7 | 4.2 | 6.0 |

†d_s3 本轮 true2d 单发退化(coh0.87→假性 +8.2)，取干净 rep 7.7(与 d_s0=8.0/d_s10=7.1 一致)。其余 24 点 coh 0.98~1.00、rep 近零抖动。

**结论**：
1. **true2d 增益基本与速度无关**：0~60 km/h 全程 ~6~8 dB（true2d 全赢），静态→移动仅掉 ~1~2dB 后持平，**无高速崩溃**。
2. **推翻旧 §12.11 "30km/h true2d 输 +5.6dB"**——5GC 退化假象。
3. 仅 CDL-E(2064ns) 增益略低且随速度温和降(6.6→4.2)，与长时延频域相关性少一致。
4. 机理：≈17dB 增益主要来自逐帧频域 Wiener（与速度无关），PKF 时间阶为辅 → 高速稳健。
5. **方法学闭环**：修复(5GC 每发重启 + STO 对齐守门 + 解析 bug)后重测，25 点 coh 普遍 ~1.0、rep 一致——动态结论现为干净实测，非推断。

**图**：`native_cdl_gain_vs_speed.png`（五模型 + 均值黑线）。

### 12.20 (0603 追加) 极高速 + 动态 vs legacy（最终交付对比）

扫速度 {0,30,60,120,300} km/h，**对比 legacy（OAI FIR，工程门槛）**（≈SNR 17dB，rep3，STO 对齐守门）。增益=legacy_NMSE−true2d_NMSE（正=true2d 赢）：

| 速度 | A | B | C | D | E | 均值 |
|------|------|------|------|------|------|------|
| 0 | 4.6 | 5.1 | 3.6 | 4.5 | 3.0 | **4.2** |
| 30 | 2.2 | –† | 2.1 | 2.4 | 1.2 | 2.0 |
| 60 | 3.5 | –† | 2.3 | 3.3 | 0.7 | 2.5 |
| 120 | 3.2 | 2.4 | 2.0 | 3.0 | 1.1 | 2.3 |
| 300 | 3.4 | 2.3 | 2.1 | 2.7 | 0.8 | 2.3 |

†b_s30/b_s60 各一端单发退化(coh<0.9)剔除；其余 23 点 coh~1.0。

**结论**：
1. **true2d 在所有速度（含 300 km/h）都赢 legacy**——PKF 时间阶在 300km/h 完全失效(5 cycle/SRS)仍稳赢 ~2~3.4dB(A/C/D)。
2. 静态 ~4dB（频域 Wiener + 时间阶叠加）→ 动态收窄到 ~2~2.5dB 后**持平到 300km/h**：高速优势来自**频域阶比 legacy 固定 FIR 强**（与速度无关），PKF 失效不致输。
3. CDL-E(2064ns) vs legacy 最弱(高速 ~0.7~1.2dB)，长时延频域相关性少。
4. **极高速优雅退化坐实**：IAE-Riccati 在 PKF 失配时压低权重、回退频域阶，true2d 不被错误预测带崩。

**图**：`native_cdl_gain_vs_speed_legacy.png`。**至此 true2d 静态(vs LS/legacy) + 动态(vs LS/legacy, 0~300km/h) 全部干净实测、可交付。**

---

### 12.21 (0604 追加) E1：时间段 PKF 相位升级（单 ω → per-band / 线性相位）+ K 饱和根因坐实

承增强计划（`0604_2DMMSE_增强计划.md`）的 Tier-1 E1。把时间段 PKF 的相位预测从**单标量 ω** 升级为可选 **per-band ω**（mode1）与 **线性相位 ω₀+slope·k**（mode2），并最终查清"动态下时间段为何拿不到增益"的真正根因。

#### 12.21.1 实装（`nr_srs_2d_filter.c::update_pkf_core`）
- 新增 env `SRS_2D_PKF_PHASE`：`0`=scalar（默认，与改前**逐位一致**）/`1`=per-band ω（复用现成 `PKF_N_BANDS=8` 分带）/`2`=线性相位。配套 `SRS_2D_PKF_OMEGA_BANDS`(默认8)、`SRS_2D_PKF_BAND_MIN`(默认8，带内 active SC 不足则回退全局 ω)、`SRS_2D_PKF_SLOPE_MAX`(默认0.2)、`SRS_2D_PKF_RAMP_GATE`(默认1.5)。
- mode2：slope=相邻 active-SC 相位增量（`angle(Σ p_sc·conj(p_prev))`）的 EMA；截距用**去斜率后再求和**（普通 `angle(Σp)` 在 band_ramp>1 圈时会抵消失效）；ramp 门控只在去斜率能相干提升幅度时才施加线性项 → benign 信道不回归。
- **double 精度修复（关键）**：slope/intercept 累加器是 `|H|⁴~1e17` 量级、slope 藏在微小虚部交叉项，**float32 桁落会把它抹掉**。实机 active SC=1248 时尤甚（曾导致 `slope≈0.0001` 而诊断 double 报 `0.0033`，相差 30×）。改 double 后 slope 正确跟踪。
- `launch_native.sh` 转发 `SRS_2D_PKF_*` 到 gNB。

#### 12.21.2 验证
- **离线 golden**（`c_unit_tests/golden_pkf.py`）：mode0 与改前**逐位一致**（worst 1 LSB）。
- **离线增益自测**（`c_unit_tests/_e1_eval.py`）：构造 STO-ramp（移动时延）场景，单 ω 的 PKF 反而劣于裸 LS（−16.10 vs −16.35dB），per-band/linear 修复后 mode1 +0.40dB、mode2 +0.43dB；benign(高 SNR、无 ramp)场景三 mode 无回归。
- **实机**（CDL-C，seed12345，−nd −9）：mode2 已正确传到 gNB，handoff 后诊断稳定报 `band_ramp ≈ 1~4 rad → STO-ramp dominant`，double 修复后 `slope≈−0.002~−0.003` 被吃上。

#### 12.21.3 根因坐实：K 恒=0.95 不是 bug，是物理最优
实机所有 mode/速度下时间段 `K` 恒等 `kmax=0.95`、`innov≫R`。从同 seed/CIR 的 GT 实测**每次 SRS 之间的信道变化**（最佳复标量对齐后残差 / 复相干）：

| 速度 | 逐 SRS 残差 | 逐 SRS 相干 | ≈SNR17dB 下 innov/R | 最优 K |
|------|------|------|------|------|
| 30 km/h | 0.335 | 0.815 | ≈17 | 0.944 |
| 60 km/h | 0.551 | 0.670 | ≈26 | 0.963 |

`K = P_pred/(P_pred+R)` 在这些数字下本就该 ≈0.95。即 **period-10（5ms）下相邻 SRS 已去相关 1/3~1/2，时间维平均"去相关样本"只会增误差 → Riccati 最优就是几乎全信当前测量（K→1，截到 kmax）**。喂入的 `R_meas = noise_power·mean(wnorm2)`（`nr_srs_mmse.c` L961-963）正常，K/Q/IAE 逻辑正常，**无代码 bug**。

三方 NMSE（单 rep，K 全 0.95，差异在单发噪声内）：30km/h mode0/1/2 = −26.56/−26.38/−26.87dB；60km/h = −25.80/−25.52/−25.82dB —— 与"K 饱和→输出≈直传频段→相位模型杠杆≈0"一致。

#### 12.21.4 密集 SRS 存在性验证（−p 2）：被 TDD 卡死
试图用 `-p 2`（5ms→1ms）提高相邻 SRS 相干以激活时间段。结果 MAC 日志：
```
SRS_PERIOD_SLOTS override: requested ideal period 2 slots
SRS period adjusted for uid 0: requested=2 selected=10 offset=8
TDD: 8 DL slots, 3 UL slots, 10 slots per period (5ms)
```
即 **TDD（8DL/3UL，每 10 slot 一周期）下能放 SRS 的上行 slot 每 10 个才一个，请求 period 2 被顶回 10**。该发 GT 实测 slot 间隔仍=10、逐 SRS 相干仍 0.815、K 仍 0.95。**结论：在当前 TDD pattern 下 SRS 物理上无法 < 5ms，时间段在本测试床没有可激活工况**（要么改 TDD pattern 增 UL/SRS 机会，要么极低速 s3）。

#### 12.21.5 结论与处置
- **动态下 true2d 的增益全部来自频域 Wiener（Stage 1）；时间段 PKF（Stage 2）在 period-10 被 Riccati 正确"关闭"（K≈kmax，输出≈直传频段）。E1 把相位模型修对了，但时间维在本测试床没有可利用相关性 → 杠杆≈0。**
- **不为压低 K 改 PKF 的 K/Q/Riccati/kmax**（与物理对抗）。E1 代码（mode0/1/2 + double + 门控）保留为 **opt-in**；**默认 `SRS_2D_PKF_PHASE=0`，生产行为不变**。
- 改动文件：`nr_srs_2d_filter.c`（相位三 mode + double + 门控）、`launch_native.sh`（env 转发）；新增 `c_unit_tests/_e1_eval.py`（离线增益回归）。
