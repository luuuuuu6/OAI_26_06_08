# SRS 信道预测(Channel Prediction)— 方向 A 实装日志

| 字段 | 值 |
|------|----|
| 日期 | 2026-06-05 |
| 主题 | 把 2D-MMSE(PKF)时间级从“估计当前 H(t)”扩展为“预测未来 H(t+kΔ)”,服务 Digital Twin 的前瞻能力 |
| 范围 | Phase 0 离线验证 + Phase 1 C 端只读预测 API + Phase 2 DT 接入与评估;全经典方法,不含 AI |
| 测试床 | **仅 native rfsimulator + channelmod**(GPU-IPC proxy 已废弃:链路余量墙致 CDL 弱多径不可信,见 `2D_MMSE_项目报告.md` §1.1/§4)。捕获 `launch_native.sh`,GT=rfsim 施加的 CIR,评估 `eval_nmse_sto.py`(STO 对齐) |
| 关联 | 录音 26-06-04(教授指导:SRS 稀疏 + 移动性、复杂度、上下行)、`0604_2DMMSE_增强计划.md`、`2D_MMSE_项目报告.md` |
| 状态 | 时间维 Wiener 预测器 ≫ Kalman 相位(mode0)。**在线 EMA(去 genie)+ 正则化后仍胜 mode0 ~4 dB(可部署)**;定阶 p=4。**已港到 OAI C(`nr_srs_wiener_update/predict`,env 默认关、on-time 路径不变),golden C≡离线 ≤1 LSB 全 PASS**。native 实跑待用户重编。详见 §4.4–4.6 |
| 复杂度优化(教授 6-04 要求 A,收官) | **freq_wiener 单条 5042→190µs(26.5×,三步叠乘:求逆一次5.1×+twiddle+权重缓存,均 bit-exact 始终启用)**;维度自适应解除 4×4 上限;多核(默认关,访存 bound 无益)+ Option B 跨天线共享 R(默认关,near-lossless,复用链路~69µs)。真实 NMSE −22~−24dB(−44 是无噪天花板)。详见 §4.11.1–4.11.9 |

---

## 1. 背景与动机

- **现状**:时间级 `update_pkf_core`(`nr_srs_2d_filter.c`)每帧只输出“当前帧”的滤波估计。SRS 周期越稀疏、UE 速度越高,两次 SRS 之间信道变化越大,中间的“空窗期”没有任何信道信息。
- **教授 motivation(录音 26-06-04)**:SRS 对终端负担大 → 发得稀疏;终端有移动性 → 信道变得快。如何在稀疏 SRS 下仍拿到“当前/未来最优 CSI”。
- **我们的回答(方向 A)**:**只用 SRS**,把 PKF 的预测步迭代 k 次、不做观测更新,外推 `H(t+kΔ)`。
  - 不需要 준수的 PMI(Type 2)融合;正面回应教授的稀疏+移动性问题。
  - 预测正是 **Digital Twin 最缺的“前瞻/镜像”能力**(DT 要能往前推演信道,而不只是复现当前)。
- **与 AI 的关系**:Phase 0/1/2 全为经典方法。预测“地平线 vs NMSE”曲线即后续 AI 预测器的 **baseline 与立项判据**——经典在哪崩,就从哪上 AI。

---

## 2. 方法与数学

PKF 每帧两步:predict `s_pred = s · e^{jφ}`,update `s = s_pred + K·(obs − s_pred)`。
**预测 = 把 predict 迭代 k 次、跳过 update**,幅度保持不变(纯相位外推):

- mode0 标量:`H_pred(t+k) = s · e^{jωk}`
- mode1 per-band:每带 `e^{jω_b·k}`
- mode2 linear-phase:逐 SC `e^{j(ω0 + slope·(m−m_c))·k}`

幅度去相关(多径多普勒展宽 / 长地平线)是纯相位外推的**固有极限**——这是预期行为,正是 Phase 3(AI)的判据,不是 bug。

**关键认知(实测得出)**:标量 PKF(mode0)只能跟踪**净公共相位**(CFO / 主径多普勒)。若到达角对称、净多普勒≈0,标量预测退化为 hold-last(无增益)。真实移动 UE 有有限角度扩展 → 存在净多普勒 → 标量预测有效;各径多普勒展宽则随地平线累积去相关。

---

## 3. 实现清单(逐文件)

### Phase 0 — 离线验证(不重编 OAI)

- **`test_srs_2d_offline.py`**
  - 新增 `time_pkf_predict(Hf, r_meas, horizons)`:与 `time_pkf_riccati` 同一 PKF+IAE-Riccati 递推,每帧 update 后快照 `(s, ω)`,按地平线 k 外推 `s·e^{jωk}`;同时返回 **zero-order-hold(hold-last)基线**。
  - 新增 `run_predict_sweep()` + `synth_coherent_expdp()`:自带相干多普勒频选信道(按 RMS 时延展宽近似 CDL-C/E,有限到达角扩展 → 净多普勒),把 **(速度, SRS 周期)→ 每帧多普勒** `ω = 2π·f_d·(period_slots·slot_sec)`,扫 `速度 × 周期 × 地平线`,多 trial 平均出 NMSE,自动判“经典极限拐点”,写 CSV + 画 PNG。
  - 参数:`--predict-sweep --predict-horizons --predict-speeds --predict-periods --predict-snr --predict-trials --predict-angle-spread --predict-csv --predict-plot`。

- **`replay_true2d.py`**
  - 新增 `--predict-horizon K`:对真实 captured LS 跑完整频级(windowed Toeplitz),再对每个 SRS 帧 n 用 PKF 状态外推 n+1..n+K,与**未来 GT 帧**比,对比 hold-last,报告拐点。
  - 预测/时间级需 **连续帧**:`--predict-horizon>0` 时自动取连续子集。
  - **native 适配 + 度量修正(2026-06-05,见 §4.4)**:配对改用 V3 `unify_reference_frame`(sample-slot 同轴);预测打分改为**固定全局标定 + 逐帧仅 STO**(phase-preserving),不再用吞相位的逐帧复标量。

### Phase 1 — C 端只读预测 API

- **`nr_srs_2d_filter.c` / `.h`**
  - 抽出共享内联 `rotate_state(sr,si,c,s,...)`,供 update 的 predict 步与预测器复用(update 路径行为不变)。
  - `pkf_state_t` 增 `apply_ramp`(mode2 是否启用线性 ramp,供预测器忠实外推)。
  - 新增 `nr_srs_pkf_predict(ant, port, out, n_sc, k_ahead)`:**只读** per-SC 状态与 `ω/ω_band/slope`,按 mode0/1/2 把相位×k 写入 out,**不改任何全局状态**(可重入、对估计路径零副作用);无收敛状态时退化为 hold-last。
  - 新增 env `SRS_PKF_PREDICT_AHEAD`(默认 0=关) + getter `nr_srs_pkf_predict_ahead()`。

### Phase 1 golden — C↔Python 对拍

- **`c_unit_tests/test_pkf_predict_main.c`**:每帧 update 后调用 `nr_srs_pkf_predict`,输出 k 步预测。
- **`c_unit_tests/golden_pkf_predict.py`**:`time_pkf_predict_launch` 金标准逐 SC 对比。
- **`Makefile` / `run_all.sh` / `README.md`**:加 `test_pkf_predict` 目标与说明。

### Phase 2 — DT 接入与评估

- **`nr_ul_channel_estimation.c`**:`nr_srs_channel_estimation` 的 SRS dump 处,env 门控(`SRS_PKF_PREDICT_AHEAD>0` 且 true2d 路径)时,**复用现有双缓冲**改 dump `H_pred@k`;`srs_estimated_channel_freq` 下游不受影响;多符号尾部回退到估计。
- **`launch_native.sh`**(native 测试床捕获脚本):env 转发白名单加入 `SRS_PKF_PREDICT_AHEAD`,使 `sudo SRS_PKF_PREDICT_AHEAD=k bash launch_native.sh -e true2d ...` 能让 gNB 真正走预测 dump。
- **`eval_nmse_sto.py`**(native STO 对齐评估,**测试床主用**):`--predict-horizon K`,把 SRS 槽轴(V3 `sample_slots` 或 legacy `abs_slots`)前移 `k×SRS周期` 再与未来 GT 配对、STO 对齐打分。
- **`eval_nmse_clean.py`**(proxy 时代非 STO 版,保留):同样新增 `--predict-horizon K`。
- **`digital_twin_stats.py`**:新增 `compute_prediction_accuracy` + `plot_prediction_accuracy` + JSON `prediction_lookahead`。用**固定全局复标定**(振幅+常量偏置,避免逐帧复 α 吞掉预测相位)度量 DT 前瞻保真度;`--predict-horizons`。

---

## 4. 验证结果

### 4.1 C↔Python golden(`c_unit_tests/run_all.sh`)
- `golden_pkf.py`(估计路径):**PASS,≤1 LSB** → 预测是只读、加预测后估计路径 **bit-identical**。
- `golden_pkf_predict.py`(预测器,k=1/4/16 × 多 seed × n_sc):**PASS,≤1 LSB**。
- `golden_freq.py`(频级):**PASS,≤1 LSB**。

### 4.2 离线 sweep(`predict_sweep.csv` / `.png`,trials=12, snr=20dB)
- **低速/密 SRS(10km/h,周期10,ω≈1.0 rad/帧)**:
  - k=1:pred **−13.1 dB** vs zoh −1.7 dB(**优 ~11 dB**);k=2:−8.6 vs +3.4;k=4:−3.9 vs +5.6;拐点 k≈32。
- **高速/稀疏 SRS(ω≫1)**:增益快速收缩、拐点前移到 k≈2–4 → 经典极限暴露。
- 结论:**短地平线、净多普勒可观时预测显著优于 hold-last;长地平线/高展宽则失效** → AI 立项判据成立。

### 4.3 DT 前瞻指标(`digital_twin_stats.py`)
- 合成验证:有净 CFO 时 pred 比 zoh **优 +1.5~+3.2 dB**(k=1..8);净多普勒≈0 时 pred≈zoh(符合 mode0 理论)。

### 4.4 ⭐ native 真实 capture 结果(2026-06-05,CDL-C,nd=-6,seed 12345)

**先修了 replay 的两个度量 bug(否则结论是错的):**
1. **V3 GT 配对**:`replay_true2d.py` 原用老的 SFN median-C `align_frames` 给 native V3 数据配对 → 全错配,连 genie oracle 都塌到 −1.4 dB。改用 native 同轴 `unify_reference_frame`(sample-slot,offset 0)后,raw LS 立刻对上 `eval_nmse_sto.py`(−16.0 dB)。
2. **预测度量(两版迭代,最终用自参考)**:
   - 先试 `sto_nmse`(逐帧复标量)→ 把预测提供的"时变公共相位"整个抵消 → 假象 `predict≡zoh`。
   - 再试"固定全局标定 + GT"→ 仍被 SRS **逐帧相位参考漂移**污染,绝对 NMSE 跨 capture 不可比(3km 反而最差,违反物理)。
   - **最终采用自参考(self-referenced)**:预测目标改为**未来真实测到的 SRS 估计 `est_t2d[f]`**(与预测输入同参考域,不碰 GT/标定/STO)。这正是"填空窗 = 预测下一个 CSI 样本"的运维任务,绝对值干净、可跨速度比。

### 4.4.1 STO(采样定时偏移)根因 —— 为何 eval 必须 STO 对齐(非估计误差)

**现象**:每发都见 `scalar-only NMSE ≈ +4 dB`(比啥都不做还差),但 `STO+scalar 对齐`后 → −22~−45 dB、复相干 ~1.0。差距全在一个**跨子载波的线性相位斜坡**。

**根因(本质是"时间原点不一致",不是估得不准)**:
- SRS 信道估计 `H_srs` 和真值 `H_gt` 由**两条独立管线**产生、各自的**时间原点不同**:
  - `H_srs` = gNB 在自己的 **FFT 接收窗**位置上对收到信号做估计;
  - `H_gt` = channelmod 直接 dump 的 **CIR(从 sample 0 起的抽头)** 做 FFT。
- 两者相差一个**采样级时移 τ**(FFT 窗起点 vs CIR 原点),频域即 `H_srs[k] = H_gt[k]·e^{−j2π·k·τ/N}` —— **一条线性相位斜坡**。再叠加一个整体复标量(公共相位/增益)。
- τ 的来源:残余**定时提前(TA)/FFT 窗摆放**、信道**主径时延(第一个 CIR 抽头不在 0)**、分数采样时延、以及逐 slot 取最近 CIR 配对的微小抖动 → τ 近似常数 + 小逐帧抖动。

**为何"永远"要后处理对齐**:
- 这是**评估口径的记账问题**,不是接收机问题。真实接收机里 `H_srs` 是**在同一个 FFT 窗内被使用**(均衡/预编码),信道估计与数据符号经历**同一个 τ**,会被一致吸收 → 对解调/预编码**无害**(均衡对"公共时移+标量"不变)。
- 但 eval 要把 `H_srs` 和**另一条管线的 `H_gt`** 比,两者时间原点不同,τ 不抵消 → 不对齐的话 NMSE 被这条斜坡主导(scalar-only +4 dB),量到的是"两套时间基的差",**不是估计质量**。
- 所以 `eval_nmse_sto.py` 先估最优 `τ`(+复标量)消掉斜坡,余下才是**真实估计误差** → −24 dB(频域)/−45 dB(静态)等才是有意义的数。

**一句话**:STO 是"SRS 估计窗"与"GT 的 CIR 时间原点"之间的固定采样偏移在频域的线性相位表现;接收机内部自洽吸收、无害,仅在与外部 GT 对比时需后处理扣除。要从根上少依赖它,只能让 GT 与 SRS 共享同一时间基(本项目 native 已用 `unify_reference_frame` 同轴 sample-slot 把"配对"对齐,但 FFT 窗 vs CIR 原点的 τ 仍需 eval 扣)。

**频域级(三速度一致,印证项目报告 §6):** raw LS ≈ −16 dB,true2d-freq **≈ −24 dB(+8 dB vs raw)**,oracle ≈ −25 dB。

**预测 vs hold-last(自参考 NMSE,真实数据;PKF-predict 的绝对 NMSE,越负越好):**

| 地平线 k(×5ms) | 3 km/h | 10 km/h | 60 km/h |
|----|----|----|----|
| 1 | **−10.0 dB** | **−4.3 dB** | **−1.8 dB** |
| 2 | −7.4 | −3.2 | −0.9 |
| 4 | −4.1 | −1.8 | −0.3 |
| 8 | −0.8 | −1.4 | +1.2 |
| 过 0(误差=信号)≈ | k≈10(~50ms) | k≈11(~55ms) | k≈5–6(~25ms) |
| vs ZOH 平均 | 大幅胜 | 大幅胜 | 胜 |

**关键物理图像(自参考后才看清):**
- **ZOH 的 NMSE 随 k 振荡**(如 10km/h:k1 −0.4 → k3 +4.5 → k6 −2.6 → k9 +4.2 …)——因为它不转相位,误差按**多普勒拍频周期**(≈2π/ω,10km/h ω≈0.8rad/帧 → 周期~8帧)起伏。
- **PKF-predict 的 NMSE 平滑单调**(−10→0 随 k),因为它把公共多普勒相位**外推掉了**。两条线之差 = 预测相对 ZOH 的真实价值;ZOH 偶尔反超只是它恰好转回同相的巧合点。
- **可用预测地平线随速度收缩**:≤10 km/h ~50ms,60 km/h ~25ms(∝ 相干时间 ∝ 1/速度)。

**结论(最终,可信):** native 真实数据上 **SRS 信道预测确实有效**:k=1(5ms)PKF-predict 达 **−10 / −4.3 / −1.8 dB**(3/10/60 km/h),且全程平滑跑赢"硬冻结上一帧"的 ZOH(ZOH 因不补多普勒相位而振荡)。低速可外推 ~50ms、高速 ~25ms。这是方向 A 在真实 rfsim 上的扎实证据,也给 AI(更长地平线 / 高速)划清了 baseline。

> 复现:`python3 replay_true2d.py --run-dir ../../logs/native_passthru_<TS> --win 16 --predict-horizon 16 --max-frames 300`(`<TS>` 换真实目录名)。指标=自参考(vs 未来 SRS 估计)。

### 4.5 预测器升级:相位模型 vs 时间维 Wiener(2026-06-05)

**先测 E1 相位三档(mode0 scalar / mode1 per-band / mode2 linear-STO),用 `time_pkf_predict(phase_mode=)`:**
- 三速度下 **mode0 ≥ mode1/mode2**。mode1(per-band)一致更差(每带 ω 样本少、噪声大);mode2(STO 线性相位)的 ramp 门控在真实信道基本不触发 → 退化回 mode0(10km/h 逐位相同)。
- 结论:per-band/STO 精细化对**预测**也无增益(与 §12.21 对滤波的结论殊途同归)→ 相位旋转类的最优就是 mode0。

**再测时间维 Wiener(线性 MMSE 预测器,`time_wiener_predict`,p=4):多 lag + 预测幅度 → 经典真上限。** mode0 不是上限,Wiener 大幅胜出:

| 指标 | 速度 | ZOH | mode0(相位) | **Wiener(p=4)** |
|------|------|-----|-----|-----|
| k=1 NMSE(dB) | 3 km/h | −8.4 | −10.0 | **−14.4** |
| | 10 km/h | −0.4 | −4.3 | **−13.7** |
| | 60 km/h | −1.0 | −1.8 | **−2.5** |
| k≤5 平均胜 ZOH | 3 km/h | — | +3.4 | **+7.4** |
| | 10 km/h | — | +5.2 | **+10.2** |
| | 60 km/h | — | +1.9 | **+3.0** |

- Wiener 在整个地平线保持负 NMSE(10km/h −13.7@k1 → −3.3@k12),**不像 mode0/ZOH 那样随多普勒拍频振荡转正**。
- 赢在两点(phase-only 碰不到的):① p=4 个过去样本 → **去噪**(k=1 即 −4.3→−13.7);② **预测幅度** + 按实测 R(Δt) 最优加权多普勒动态。
- **结论(修正):mode0 不是上限;时间维 Wiener 把胜 ZOH 增益翻约一倍、地平线大幅拉长。Kalman 增益小的根因 = 它只是 Wiener 的 1-lag/仅相位特例。** → 经典远未到头,**暂不需 AI**;真正该用/该港到 C 的"经典预测器"是时间维 Wiener。
- caveat:R(Δt) 用整窗估(≈已知统计),在线 EMA 估会略差但接近,是合理的经典 baseline/上限。

> 复现:`python3 replay_true2d.py --run-dir ../../logs/native_passthru_<TS> --win 16 --predict-horizon 12 --max-frames 300 --predict-phases 0 --wiener-order 4`

### 4.6 时间维 Wiener:在线化(去 genie)+ 定阶 + C 端口(2026-06-05)

**0.1 去 genie(在线 EMA 估 R(Δt)):** 朴素在线 EMA **崩溃**(gain 转负,比 ZOH 还差)——R 估计噪声经 Wiener 解被放大。**加正则化(对角加载)即救回**:`diag_load=0.3, r_ema=0.05` 下 10km/h k=1 = **−8.6 dB(胜 ZOH +8.2)**(genie 上界 −13.7,mode0 −4.2)。→ **在线 Wiener 可部署、胜 mode0 ~4 dB**。默认锁 `r_ema=0.05, diag_load=0.2`。

**0.2 定阶:** 10km/h 在线,k≤5 平均胜 ZOH:p=2 +6.99 / **p=4 +8.12** / p=8 +8.46 → **p=4 拐点**(p=8 仅 +0.34)。解 Toeplitz:C 用 **Cholesky/double**(p 小,Levinson 无必要),离线 `np.linalg.solve` 为参考。

**0.3 是否替换 Stage-2 Kalman(on-time):** 不替换。报告 §6.4/§12.21 已证动态下时阶 K 饱和、on-time 贡献≈0(去相关墙);Wiener 滤波(k=0)同墙、收益小且替换需重验四象限。**结论:on-time 维持 Kalman,Wiener 只做预测(k>0)。**

**Phase 1 C 端口(scope=validate,已完成,native 实跑待用户):**
- `nr_srs_2d_filter.c/.h`:新增 `wiener_state_t`(每链路 ring 缓冲 + double EMA R)、`nr_srs_wiener_update/predict`(只读估计路径)、复数 Hermitian Cholesky 解(double)、getter;env `SRS_WIENER_PREDICT_AHEAD/ORDER/KMAX/R_EMA/DIAG_LOAD`(默认全关 → on-time 逐位不变)。
- golden:`c_unit_tests/test_wiener_predict_main.c` + `golden_wiener_predict.py`(镜像在线-EMA Wiener)→ **C(double)vs 离线 ≤1 LSB 全 PASS**(k=1/4/8,p=4,多 seed);`run_all.sh` 四项(pkf/pkf-predict/wiener-predict/freq)全 PASS。
- dump:`nr_ul_channel_estimation.c` predict-dump 处 Wiener 优先于 PKF(`SRS_WIENER_PREDICT_AHEAD>0`),复用双缓冲;true2d 分支每帧调 `nr_srs_wiener_update` 维护历史;`launch_native.sh` 转发 `SRS_WIENER_*`。

**native 实跑(待用户重编 nr-softmodem):**
```bash
cd .../cmake_targets/ran_build/build && sudo ninja nr-softmodem   # 重编(加载新 .c)
cd .../G1C_MultiUE_MIMO_Channel_Proxy
sudo SRS_WIENER_PREDICT_AHEAD=4 bash launch_native.sh -e true2d -cir cir/cir_cdl_c_s10.bin \
     -nd -6 -seed 12345 -p 10 -d 120 --restart-5gc
python3 eval_nmse_sto.py --run-dir ../../logs/native_true2d_<TS> --predict-horizon 4
```
由 golden(C≡离线 ≤1 LSB)保证:native 实测预测 NMSE 应 ≈ 离线 replay 的在线 Wiener。

**native 实跑结果(2026-06-05,`native_true2d_20260605_194257`,SRS_WIENER_PREDICT_AHEAD=4,p=10):**
- dump = C Wiener k=4 预测(20ms 前瞻),1894 帧,coh(plain)=0.875。
- `eval_nmse_sto --predict-horizon 4`(SRS 轴 +40 slot 对未来 GT):**NMSE STO = −3.16 dB,coh 0.809** → C 在 gNB 里预测 20ms 后的信道,和真未来信道吻合 −3.16 dB。
- 不移位(k=0,拿预测和当前 GT 比)= −6.10 dB ≠ on-time −24 dB → 证明 dump 是**真预测**(已向未来外推、非 hold-last)。
- 与离线 replay 在线 Wiener(10km/h k=4 ≈ −3.5 dB)**一致** → **C≡离线 端到端闭环成立**(golden + 实机双重确认)。Phase 1.5 完成。

---

## 4.7 Wiener 预测器:完整原理 + 上限分析 + 改进路线(2026-06-05 讨论结)

### 4.7.1 现在的 Wiener 预测怎么做(逐 rx-tx 链路)
输入:每次 SRS 的频域级(Stage1 freq Wiener)当帧估计 `H_t`(活跃 SC,已跨频去噪 ~−24dB)。

1. **存历史**:环形缓冲最近 `L=p-1+kmax` 帧(p=4, kmax=8 → 11 帧)。
2. **在线估时间自相关 R(Δt)(因果 EMA)**:每帧、每 lag d:
   `inst(d)=mean_sc(H_t·conj(H_{t-d}))`(对 ~624 SC 平均);`r[d]=(1-a)r[d]+a·inst(d)`,a=0.05。
   `r(d)=E[h(t)conj(h(t-d))]` 即多普勒二阶统计(`r(d)≈|r(d)|e^{jωd}`)。
3. **解 Wiener-Hopf(每地平线 k)**:`R[i,j]=r(i-j)`(Hermitian Toeplitz)`+ diag_load·r0·I`(对角加载 0.2,在线 R 噪声大不加载会病态炸);`p_k[i]=r(k+i)`;复 Cholesky/double 解 `R a=p_k`。
4. **预测**:`H_pred(t+k)[sc]=Σ_{j=0}^{p-1} a_j·H_{t-j}[sc]`(最近 p 帧线性组合,**同时预测幅度+相位**)。历史<p → ZOH。

代码:C `nr_srs_wiener_update`+`nr_srs_wiener_predict`;离线 `time_wiener_predict(online=True)`。默认参数 p=4, r_ema=0.05, diag_load=0.2, kmax=8。env 默认关、on-time 不动。

### 4.7.2 性能阶梯与"上限"的诚实定位
```
ZOH(-0.4) < mode0 Kalman(-4.3) < 在线 Wiener(-8.6) < genie Wiener p=4(-13.7) ≤ 真线性MMSE(p大/真R) ≈ 真MMSE(高斯信道)
```
(10km/h, k=1, dB)
- **−13.7 不是理论上限**,是实测值:整窗估 R(genie 统计)、p=4 的线性 Wiener 在本数据上的表现。
- 它是**在线预测器的现实参考天花板**(在线估同一个 R 不可能超过已知 R 版)→ 衡量"R 估计还差多少"的标尺。**修正(见 §4.8):用对正则化后,在线 vs genie 实际差 ~2-3.5 dB(非先前误报的 5dB);且简单参数化 R 无法吃掉这残差。**
- 真上限:CDL≈高斯 → 线性 MMSE ≈ 真 MMSE,故 AI 在"已知统计"下赢不过 genie-Wiener;AI 空间只在**估计/非平稳/非线性**。要钉死真上限:genie-R 扫大 p 看饱和 + Kolmogorov-Szegő 多普勒谱闭式界 + 用干净 GT 去目标噪声底。

### 4.7.3 预测的意义(为什么值得做)
预测 ≠ 替代测量(−3dB ≪ on-time −24dB);价值是把"过时 CSI"变"新鲜 CSI":
1. **补 CSI 时延**:测→算预编码→发下行有几 ms 滞后,高速下 outdated CSI 掉吞吐;预测"发射时刻"信道补上(最硬痛点)。
2. **填 SRS 空窗**:TDD 互易,两次 SRS 间下行用老信道;预测给中间 slot 更新信道。
3. **降 SRS 开销**:能预测就能少测(拉长周期)→ 上行开销小。
4. **Digital Twin**:前瞻能力本身。
衡量基准 = "比硬用旧 CSI(ZOH)好多少"(我们 +8dB),不是绝对值。

### 4.7.4 改进路线(按性价比)
| 优先级 | 改进 | 预期 | 性质 |
|---|---|---|---|
| **1(最值)** | **参数化 R**:估几个多普勒参数(主频移+展宽 / ESPRIT-MUSIC 时间维)由模型生成 R,替裸 per-lag EMA → 自由度暴降、方差降、逼 genie | 吃那 ~5 dB 大头 | 纯经典 |
| 2 | 对齐地平线到应用(补时延 1-2 slot)+ 干净 GT 评 | k 小精度天然高 | 不作弊 |
| 3 | 钉死真上限:genie-R 扫 p=4/8/16/32 + KS 闭式界 | 知经典剩多少肉 | 诊断 |
| 4 | per-tap(时延域)预测:每 tap 单独多普勒外推 | 分辨多径多普勒 | 经典 |
| 5 | 非平稳自适应(自适应 EMA / 测速变化) | 动态稳健 | 经典 |
| 6 | AI(LSTM/Transformer):隐式学少样本统计+非线性/非平稳,打瓶颈① | 以 Wiener-genie 为 baseline | 经典到顶后 |
| 7(大、独立) | 闭环喂预编码/调度看 BLER/吞吐;统一时间 Wiener 替 Stage-2 Kalman | 真部署价值 | 需重验四象限 |

> **注:加密 SRS 不算改进(作弊+本 TDD 做不到);"缩短地平线"指对齐应用真实前瞻需求,非放水。**
> **下一步推荐:先做 #1(参数化多普勒 R 估计),离线对比 裸-EMA-R vs 参数-R vs genie,直接打当前唯一瓶颈,并回答"要不要上 AI"。**

## 4.8 R 估计消融:裸-EMA vs 参数化-Doppler vs genie(2026-06-05,负结果)

测 §4.7.4 #1(参数化多普勒 R)能否吃掉 online↔genie 差距。三方在 10km/h、k=1 / k≤5 平均胜 ZOH:

| R 估计 | 最佳 load | k=1 NMSE | k≤5 平均胜 ZOH | 说明 |
|--------|-----------|----------|----------------|------|
| ZOH | — | −0.4 | — | 基线 |
| mode0 Kalman | — | −4.2 | +5.5 | 相位-only |
| **裸-EMA Wiener** | **0.2(重)** | **−9.7** | **+8.9** | **当前可部署(已在 C)** |
| 参数-R Wiener(单多普勒+指数包络) | 0.01(轻) | −9.0 | +7.5 | **反而更差** |
| genie Wiener | 0.001(轻) | −13.2 | +10.9 | 上限 |

**关键发现(诚实):**
1. **online↔genie 差距比先前以为的小**:用对正则化(裸-EMA 需重 load 0.2)后,裸-EMA = −9.7 / +8.9,距 genie −13.2 / +10.9 约 **2~3.5 dB**(先前误报的 5dB 是初始 `diag_load=1e-3` 太轻致在线崩溃的假象)。
2. **载荷是双刃**:噪声大的在线 R 需重 load 压住(代价~几 dB 偏置);genie R 准、要轻 load。同一 load 比不公平(重 load 也会把 genie 压到 −9.8)。
3. **简单参数-R 是负结果**:单多普勒+指数衰减模型即使在其最佳(轻 load)也只 +7.5,**仍输给裸-EMA(重 load)+8.9** —— 模型太刚(真实 CDL 是多多普勒/非指数),引入偏置 > 降方差收益。
4. **结论**:可部署的 **online 裸-EMA Wiener(load 0.2)已接近其框架最优**;那 ~2-3.5dB 的 genie 残差不是简单参数化能拿的,要靠**更丰富的 R 模型(多多普勒分量/ESPRIT)或 AI**(隐式学统计)——但相对已有 +8.9dB 收益,边际递减。`_param_R`/`--wiener-compare-r` 代码保留(默认关),作为消融记录。

## 4.9 统一口径性能汇总(2026-06-05,可交付)

同一配置(online Wiener p=4 / r_ema=0.05 / diag_load=0.2,horizon10,250 帧),消除前述各 run ±0.5dB 抖动。自参考 NMSE(dB,越负越好;括号=胜 ZOH):

| 速度 | 频域 on-time | ZOH k=1 | mode0 k=1 | **Wiener k=1(5ms)** | Wiener 平均胜ZOH(k≤5) | mode0 平均 |
|------|------|------|------|------|------|------|
| 3 km/h | −23.4 | −8.6 | −10.5 | **−11.6** | +5.1 | +3.9 |
| 10 km/h | −24.1 | −0.4 | −4.2 | **−9.7** | **+8.1** | +5.0 |
| 60 km/h | −23.9 | −1.1 | −1.9 | **−2.9** | +3.0 | +1.8 |

(频域 on-time 三速度全 ~−24 dB / +8 vs 裸LS,稳定)

**解读:**
1. Wiener 全速度/全地平线 ≥ mode0 ≥ ZOH(除 ZOH 多普勒拍频偶发同相点)。
2. 绝对可预测性**越慢越好**:5ms 前瞻 3km −11.6 → 10km −9.7 → 60km −2.9 dB。
3. **胜 ZOH 甜区在 ~10 km/h(+8.1)**:3km 时 ZOH 本身已 −8.6(冻结够好,Wiener 多赢空间小);60km 太快大家都差。
4. 比 mode0(Kalman 相位)稳赢 ~1~5 dB;实机 C(k=4)−3.16 dB 与离线一致。

## 4.10 实机锚定:live C Wiener 落在离线曲线上(2026-06-05)

`anchor_native_wiener.sh`:同信道(`cir_cdl_c_s10`, seed 12345, nd=-6, p=10)采 1 发 passthru(离线曲线)+ 3 发 true2d 实机 Wiener(SRS_WIENER_PREDICT_AHEAD=1/4/8),`eval_nmse_sto --predict-horizon k` 打分。

| k(×5ms) | ZOH | mode0 | 离线 Wiener(Python) | **实机 C Wiener(gNB)** | 差 |
|------|------|------|------|------|------|
| 1(5ms) | −0.4 | −4.1 | −9.0 | **−10.9**(coh0.96) | 实机好 1.9 |
| 4(20ms) | +3.9 | −1.0 | −3.3 | **−3.1**(coh0.81) | ≈0.2 |
| 8(40ms) | +2.3 | −0.8 | −2.8 | **−2.5**(coh0.78) | ≈0.3 |

(频域 on-time −24.2 dB / +8 vs 裸LS)

**结论:**
- **C≡离线 端到端、跨地平线坐实**(不止一个点):k=4/8 实机 vs 离线差 ~0.2-0.3 dB(capture 抖动级)。
- **实机 k=1 略优(−10.9 vs −9.0)**:离线 Wiener 输入=Stage1;实机=Stage1+Stage2 PKF(多一点时域去噪)→ 当帧估计更干净 → 短地平线更准(k 大时去相关主导,二者收敛)。
- 实机 C Wiener 远超 ZOH(+10dB@k1)/ Kalman mode0(+7dB@k1)。
- 这是最强验证:golden 验证过的算法编进 gNB 实跑,**实测点落在离线曲线上(甚至略优)**。方向 A 在真实 OAI 硬件端到端成立。

## 4.11 复杂度实测(教授 6-04 要求 A)— 计时打点已加(待实跑)

教授录音反复强调:加的 block 每 TTI 占多少 CPU、别超 TTI、长跑取平均出"additional time"。已在 `nr_ul_channel_estimation.c` 加 **env 门控 (`SRS_TIMING=1`) 的 per-stage 计时**(`clock_gettime(CLOCK_MONOTONIC)`),包住四个块:
- `freq_wiener`(Stage1 `nr_srs_mmse_freq_filter`)、`pkf_time`(Stage2 `nr_srs_pkf_update`)、`wiener_upd`、`wiener_pred`。
- 每 200 次 SRS 打印各 stage avg/max(µs)+ **`ADDED(freq+pkf+wiener)` per-SRS 平均 vs TTI 预算 500µs(μ=1)**。
- 默认关、零逻辑改动;legacy filt 是公共廉价基线(所有 mode 都算),故"additional over legacy" = 我们这几个块之和。`launch_native.sh` 已转发 `SRS_TIMING`。

**怎么跑(需重编 + gNB):**
```bash
# true2d 估计的增量
sudo SRS_TIMING=1 bash launch_native.sh -e true2d -cir cir/cir_cdl_c_s10.bin -nd -6 -p 10 -d 60 --restart-5gc
# 叠加预测的增量
sudo SRS_TIMING=1 SRS_WIENER_PREDICT_AHEAD=4 bash launch_native.sh -e true2d -cir cir/cir_cdl_c_s10.bin -nd -6 -p 10 -d 60 --restart-5gc
# 看日志
grep "SRS timing" ../../logs/native_*/gnb.log | tail -20
```
判据:`ADDED ... per-SRS avg` ≪ 500µs(且 max 不超 TTI)→ 实时可行。(参考:报告 §5.1 旧测 Kalman ~0.3ms;本次给严格 per-stage µs + vs TTI。)

### 4.11.1 Stage1 频域 Wiener 权重复用加速(瓶颈消除,已实现)

**实测瓶颈**:`SRS_TIMING` 显示 `freq_wiener` avg ~**5042µs**(max 7712µs),压倒性主导(legacy ~4µs、pkf 6.5µs、预测块 ~34µs),且 max 已 **超 SRS 周期(5ms@p=10)** → MIMO/多 UE 必爆。根因:`nr_srs_mmse.c` Stage3 对**每个活跃子载波(span≈1248)**都跑一次 `solve_complex_system`(Gauss-Jordan O(W³),W=window=16)。

**关键观察(代数事实,无损)**:窗内矩阵 `A[row][col]=R_table[|row-col|·k_tc]+noise·δ` **平移不变**——只依赖 `row-col`,与 `start_pilot`/`offset` 无关。所以同一个 A 被解了 ~1248 遍。

**优化**:每次调用先把 A **构建并求逆一次**(新增 `invert_complex_system`:对 `[A|I]` 做带选主元的 Gauss-Jordan,O(W³));target 循环内只构建 per-target RHS `b`(随 `target_dk` 变),`w = A⁻¹·b`(O(W²) 矩阵-向量)。复杂度 `O(span·W³) → O(W³+span·W²)`,W=16 约 **16× 加速(预计 ~5ms→~0.3ms)**。apply/`wnorm2`/边带 clamp/奇异回退**全不变**;A 奇异时回退原逐 target 解(保险)。

**精确性验证(已通过,零硬件)**:
- 离线:`test_srs_2d_offline.py:windowed_lmmse_reuse`(求逆一次+mat-vec)vs `windowed_lmmse`(逐 SC 解),win=8/16/32 多组 CDL 合成 **max|diff|=5.9e-13 ≪ 1e-9**(逐位等价)。
- C golden:`run_all.sh` 的 `golden_freq` 多数 **−300 dB(bit-exact)**、worst **1.0 LSB**(tol 2 LSB);`golden_pkf*`/`golden_wiener_predict` 仍 PASS。→ C 输出与改前数值一致,真实 capture 上 `true2d-freq` NMSE 不变。

**待用户重编实测**:`sudo SRS_TIMING=1 bash complexity_native.sh` 确认 `freq_wiener avg` 从 ~5000µs 降到 ~数百µs(<500µs TTI、远低于 5ms SRS 周期),MIMO 余量恢复。

**实测结果(已回归)**:`freq_wiener` avg **5042→993µs**、max **7712→1580µs(已 < 5ms SRS 周期,改前超了)**;`pkf`/预测块不变。两点反推:Stage3 求解 ~4338µs→~289µs(≈15×,与理论 W=16 吻合 ✓),暴露出**另一块 ~700µs 固定开销**(R_table 构建)现在主导 → 见 §4.11.2。

### 4.11.2 Stage2 R_table 构建加速(twiddle 旋转因子缓存 + 稀疏 PDP,已实现)

**新瓶颈**:`freq_wiener` 降到 993µs 后,~700µs 落在 R_table 构建。根因:`nr_srs_R_from_pdp_arr` 对 `dk=0..max_delta_k`(几十项)各扫 N=2048 个 PDP bin,**每个有效 bin 调一对 `cos`+`sin`(超越函数)**——同一个 `cos(2π·m/2048)` 一帧内被重复现算上万次。

**优化(纯实现层,不碰自适应)**:DFT 旋转因子 `cos/sin(2π·m/N)` 是**与信道无关的数学常数**,预存 1D 表(`g_twiddle_cos/sin[2048]`,init-once 只读);把 PDP 先扫一遍成稀疏 `(idx,val)` 列表(只留 `clean=pdp−floor>0` 的 bin),R(dk) 用 `Σ val·(cos_tab[(dk·n)&(N−1)] − j·sin_tab[…])` 累加,消除全部 cos/sin。`R0` 归一、`max_delta_k`、Stage3 求解全不变。PDP-EMA / noise-floor / R(dk) / Wiener 权重的**逐帧自适应一概不变**——只把"现算 cos/sin"换成"查常数表"。

**精确性验证(已通过,零硬件)**:
- 离线:`test_srs_2d_offline.py:R_from_pdp_table`(查表+稀疏)vs `R_from_pdp_direct`(直算 `Σ clean·exp`),6 组随机 PDP(含噪底)× k_tc=2/4 **max|diff|=3.1e-10、相对 ~1e-15(机器精度)** ≪ 1e-9。
- C golden:`run_all.sh` 全 PASS,`golden_freq` worst **1.0 LSB**(tol 2);`golden_pkf*`/`golden_wiener_predict` 不受影响。→ 数值不变,真实 capture 上 `true2d-freq` NMSE 不变。

**实测结果(已回归)**:`freq_wiener` avg **993→433µs**(max 996µs),`pkf` 6.4µs,预测块(`wiener_upd` 26µs + `wiener_pred` 9µs)不变。`ADDED(freq+pkf+wiener)` per-SRS avg **≈440µs(true2d)/460µs(+预测)< 500µs TTI 预算 → 首次进预算**。R-build ~560µs 基本清零,余下 433µs ≈ Stage3 mat-vec(~289µs)+ IFFT/noise-floor/apply(~144µs)。

**三步累计**:`freq_wiener` **5042 → 993 → 433µs(总 11.6×)**;max **7712 → 1580 → 996µs**(远低于 5ms SRS 周期)。单 UE/SISO 实时达成。

**剩余**:① Stage3 mat-vec(~289µs)现为头号单块;② `freq_wiener` 是 per-(ant,port),4×4 MIMO≈16×433≈6.9ms > 5ms 周期(2×2≈1.7ms 可行)→ 若要满 4×4 仍需再砍(Stage3 缓存 k_tc 个权重向量 / 跨 port 复用 PDP·R)。③ max ~1ms 偶发尖峰(疑首帧 EMA 冷启/调度抖动),稳态无碍。

### 4.11.3 精度复测(两步加速后,确认数值无损)

两步加速(Stage1 权重复用 + Stage2 R_table twiddle)落地后,对"精度是否变化"做最终复测。**关键:`replay_true2d.py` 走的是 Python 离线 true2d-freq,不调用被改的 C 函数,验证不了 C 优化**;验证 C 当前状态最权威的是 `golden_freq`(它真正驱动生产 C `nr_srs_mmse_freq_filter`,逐 SC 比对参考配方)。

**复测 1 — C golden(当前双优化态,`run_all.sh` 全 PASS)**:
- `golden_freq`(C `nr_srs_mmse_freq_filter` vs float64 配方镜像):多数 **−300 dB(bit-exact)**、最差 **1.0 LSB / −101 dB**(tol 2 LSB)。
- `golden_pkf` / `golden_pkf_predict` / `golden_wiener_predict`:均 PASS,≤1.0 LSB。
- → C 频域输出与"规范配方"逐位一致 → 真实 capture 上 on-time NMSE(−24dB)不变。

**复测 2 — 离线 NMSE A/B(老路径 vs 新路径端到端,5 组合成信道 SNR=20dB)**:
- OLD = 直算 R(`R_from_pdp_direct`,per-dk cos/sin)+ 逐 SC 解(`windowed_lmmse`);NEW = twiddle 表 R(`R_from_pdp_table`)+ 求逆一次(`windowed_lmmse_reuse`)。
- 结果:`NMSE_old ≡ NMSE_new`,**ΔNMSE ≤ 1.7e-12 dB**,估计 `est max|diff| 3.4e-13`,`R max|diff| 2.7e-15`。判定 **ACCURACY UNCHANGED (PASS)**。

**当前精度水平(供"是否继续"决策)**:
- on-time 频域:裸 LS −16dB,**true2d-freq −24dB**(距 oracle −25dB 仅 ~1dB → 已贴天花板)。
- vs OAI legacy FIR:true2d 增益 **+0.7~5dB**(`plot_speed_vs_legacy.py`,CDL-A~E × 0~300km/h;短时延展宽/低速增益最大)。
- 预测(k=1=5ms,在线 Wiener):3/10/60 km/h = −11.6 / −9.7 / −2.9 dB(胜 ZOH +5/+8/+3dB),已近经典框架最优。

**结论**:两步加速**数值无损**(golden ≤1 LSB + 离线 ΔNMSE ~1e-12 dB);精度无"待修窟窿"(on-time 贴 oracle、预测近经典最优)。**后续降时延(Stage3 缓存 / 跨 port 复用,为 4×4 MIMO)是纯性能工作,同样 golden 守门、精度零风险**。

### 4.11.4 Stage3 权重向量缓存(Option A,内部 SC 按残差复用,已实现)

**方案搜索结论**:对剩余 `freq_wiener ~433µs`(=Stage3 mat-vec ~289µs + IFFT/noisefloor/apply ~144µs),比对了 5 条路:① Stage3 权重缓存(精确,首选);② 跨 port 复用 PDP/R(更激进但近似,需 NMSE 验证,暂不做);③ 更快 noise-floor / ④ SIMD(微优化,A 后不需);⑤ 缩窗/插值(损精度,否)。选 **A:精确无损,且单独就能把 4×4 MIMO 拉进 5ms**。

**代数依据**:Stage3 内部子载波 `target_dk = (offset − center·k_tc) + (W/2−row)·k_tc`,而 `offset − center·k_tc` **只由 `offset % k_tc` 决定**(共 `k_tc` 种)→ 所有内部 SC 的 `weights = A_inv·conj(b)` 只有 `k_tc` 种。现状重算了 ~`span/k_tc` 遍。

**实现**:Stage3 内部 SC 按 `offset%k_tc` **懒缓存 k_tc 个权重向量**(`w_cache[KTC_CACHE_MAX=8][W]` + `w_cached[]`),命中则跳过 O(W²) mat-vec;边带(center/start 被 clamp 的头尾 ~W·k_tc 个 SC)逐个算;`apply` 改用 `w_active` 指针(无拷贝命中、`memcpy` 存缓存);`wnorm2` 仍逐 SC 在 apply 里累加;`ainv` 奇异回退逐 target 解、dump/env 全不变。复杂度 `span·W² → k_tc·W² + span·W`(W=16,k_tc=4 约 16×),Stage3 289µs→**~20µs**(apply 主导)。

**精确性验证(已通过,零硬件)**:
- 离线:`test_srs_2d_offline.py:windowed_lmmse_comb`(comb 镜像)`use_cache=True` vs `False`,6 组 × k_tc=2/4 **max|diff| = 0.00e+00(逐位相同)** → 残差键 + 边带分流精确。
- C golden:`run_all.sh` 全 PASS,`golden_freq` worst **1.0 LSB**(与改前同,bit-exact 居多);`golden_pkf*`/`golden_wiener_predict` 不受影响。

**实测结果(已回归)**:`freq_wiener` avg **433→190µs**(max 531-580µs),`pkf` 7µs,预测块(`wiener_upd` 29µs + `wiener_pred` 10µs)。`ADDED(freq+pkf+wiener)` per-SRS avg **≈197µs(true2d)/239µs(+预测)≪ 500µs TTI**。Stage3 mat-vec 基本清零,余 190µs ≈ IFFT/noise-floor/R/apply 固定开销(比预估 165µs 略高,差在边带逐算 + 固定开销估计)。

**4×4 MIMO 达成**:`freq_wiener` per-(ant,port) 190µs → 4×4 = 16×190 ≈ **3.0ms < 5ms SRS 周期 ✓**(2×2 ≈ 760µs)。max ~530-580µs 偶发尖峰(>500µs 单 TTI 但 ≪ 5ms 周期,冷启/抖动)。

**四步累计(实测)**:`freq_wiener` avg **5042 → 993 → 433 → 190µs(总 ~26.5×)**;max **7712 → 1580 → 996 → ~550µs**。全程精度逐位不变(golden ≤1 LSB)。剩余 190µs 全是 per-call 固定开销(IFFT+noisefloor+R+apply);要再降只剩 Option B(跨 port 复用,近似,需验 NMSE)——但 4×4 已实时,非必要。

### 4.11.5 精度终核 + MIMO 容量上限

**NMSE 终核(三步加速逐位无损)**:Stage1 求逆一次(离线 5.9e-13)、R_table twiddle(3.1e-10 / rel 1e-15)、Stage3 权重缓存(**离线 max|diff|=0.00 逐位相同**),三者 C golden 均 `freq` 1.0 LSB。→ **当帧估计与未优化版逐位一致,物理 NMSE 不变**:on-time true2d-freq −24dB(距 oracle ~1dB)、vs OAI legacy +0.7~5dB、预测 k=1 = −11.6/−9.7/−2.9 dB(3/10/60km/h)。

**MIMO 容量上限(190µs/port,单核串行,预算=5ms SRS 周期@p=10)**:`freq_wiener` 是 per-(ant,port),一次 SRS 算 `N_rx×N_tx` 遍。单条 freq 190µs+pkf 7µs(+预测 40µs)≈ 200/240µs → 上限 ≈ 5000/单条 ≈ **20~25 条**。

| 配置 | 条数 | 总时间(估计/预测) | vs 5ms |
|---|---|---|---|
| 2×2 | 4 | 0.8 / 0.96 ms | ✓ 充裕 |
| **4×4** | **16** | **3.2 / 3.84 ms** | ✓ 达标(~30% 余量) |
| 5×5 | 25 | 5.0 / 6.0 ms | 估计擦边/预测超 |
| 8×8 | 64 | 12.8 ms | ✗ 超 |

→ **190µs 下约 25 条 (ant,port) 是上限,4×4 是达标且有余量点(非硬顶,可到 ~5×5)**;massive MIMO(如 64rx×4=256 条≈48ms)放不下。两个突破杠杆:① 多核并行(per-(ant,port) 分核,上限×核数,零精度风险,耗 CPU);② Option B 跨 port 复用 PDP/R(结构性、近似)。

**Option B 离线验证(已做,跨 port 复用近乎无损)**:模型 = 同 UE 的 N_rx port 共享传播时延谱(tap 时延+均功率),各 port 独立衰落+噪声;对比"per-port R" vs "一个 shared R(跨 port 平均 PDP)"在 EMA 收敛后的 NMSE。

| N_rx | per-port dB | shared dB | Δ |
|---|---|---|---|
| 2 | −26.99 | −26.99 | +0.009 |
| 4 | −25.62 | −25.58 | +0.035 |
| 8 | −27.13 | −27.13 | +0.003 |

→ 共享 R 的 NMSE 损失 **≤0.035 dB(可忽略)**;port 越多损失越小(平均更多 port → PDP 估计方差更低,抵消"非 port 专属")。**结论:跨 port 共享 PDP/R 近乎无损**,物理上成立(共址 gNB 天线看同一 UE,时延谱本就基本相同)。

**B 不是上 MIMO 的唯一路**:① **多核并行**(把 N_rx×N_tx 条 freq_wiener 分到 L1 多线程)是正交、零精度风险的工程路,上限直接 ×核数,实际部署最常用;② **B(共享 R)**是单线程下唯一结构性杠杆——它让复用 port 跳过 IFFT/noisefloor/R-build/求逆,只留 ~20µs 的 apply,256 条从 ~48ms 降到 ~0.5ms 量级。massive MIMO 通常 **B + 多核并用**。其余(缩 IFFT、SRS 端口子采样、GPU 卸载)是次要或降密度方案。

### 4.11.6 维度自适应(解除 4×4 上限,已实现+验证)

**动机**:per-link 状态写死 `[4][4]`,>4×4(massive MIMO/64rx)直接越界。

**实现(RX 维运行时动态,TX 保持协议上限 4)**:
- `nr_srs_2d_filter.c`:`g_pkf_state/g_wiener/g_freq_rmeas/g_adapt/g_ibvss/g_kalman` 由静态 `[MAX_RX][MAX_TX]` 改 **pointer-to-array `(*)[MAX_TX]`**(访问 `arr[ant][port]` 语法不变),`g_state` 按 `g_n_rx*MAX_TX*MAX_SC` 堆分配;新增 `nr_srs_2d_ensure_capacity(n_rx)`(单线程 realloc+reset);所有 `ant>=SRS_2D_MAX_RX` 边界换 `g_n_rx`;`get_state` 步长不变(TX/SC 仍编译期常量)。
- `nr_srs_mmse.c`:`g_true2d_pdp_ema/init` 同改 pointer-to-array;`nr_srs_mmse_ensure_capacity(n_rx)`(并预热 twiddle 表)。
- dispatcher(`nr_ul_channel_estimation.c`):per-link 循环**之前**单线程调 `ensure_capacity(nb_antennas_rx)`(TRUE2D 调两个,MMSE2D 调一个)→ >4×4 生效、且为并行预留(worker 不触发分配)。
- 直接/golden 调用者有 lazy fallback(默认 RX=4)兜底;生产由 dispatcher 预分配,parallel 区不触发。

**验证(零硬件)**:
- C golden:`run_all.sh` 四套全 PASS,`golden_freq`/pkf/wiener 均 worst **1.0 LSB** → **4×4 数值逐位不变**。
- ASAN+UBSAN 自测(throwaway):容量 2→8 增长、`ant=0` vs `ant=7` 同激励 **max|diff|=0(状态隔离)**、`ant>=cap` 安全 passthrough、wiener@ant7 无崩溃 → **无越界/泄漏**。
- 内存随实配:n_rx=8 状态 ~3.1MB(报告打印);64rx 约几十 MB。

→ **4×4 天花板已解除,64rx 功能上可跑(串行)**。

### 4.11.7 多核并行 SRS 估计(per-RX-天线 fork-join,已实现,待用户 build 验证)

**做法**:`nr_ul_channel_estimation.c` 的 `for(ant)for(port)` 串行循环改成**每 RX 天线一个线程池 task**(复用 OAI `gNB->threadPool` + `init_task_ans/pushTpool/join_task_ans`,与同文件 PUSCH CE 同范式)。循环体**原样搬进 worker**(用 VLA 指针从 `void*` 重建输出数组,body 零改写;per-link 缓冲 `srs_ls/srs_est` 移进 worker 栈),聚合段(signal_power/per-RB 噪声/twin dump)留 join 后串行。链路独立、状态 `(ant,port)`-keyed → 决定论、数值逐位不变。
- **安全默认**:`launch_native.sh` 新增 `TPOOL`(默认 `n`=无池→`pushTpool` inline=**串行兜底,与改前完全一致**);设 `TPOOL="0,1,2,3"` 开池→ SRS 按天线并行。`NB_RX/NB_TX` 旋钮暴露(默认 1T1R)。
- **线程安全**(已审计):per-link 状态全 keyed 安全;`g_last_abs_slot`/全量 reset 仅 MMSE2D 路径(TRUE2D worker 不碰);twiddle 表 + ensure_capacity 在并行区前单线程预热;`SRS_TIMING`/debug 计数非原子 → 计时取串行档(`TPOOL=n`)、墙钟取并行档。

**沙箱内验证**:golden 全 PASS(叶子文件数值逐位不变);brace/paren 平衡核对、worker 与外层无符号泄漏。

**native 实测验证(用户重编 `ninja nr-softmodem` 后,已通过)**:
- **inline 路径**(`TPOOL=n`,动态 cir_cdl_c_s60):两发 coh 0.996、NMSE −22dB(decorr 0.4-0.5);低运动发 coh 1.0、−45dB(decorr 0.18)→ worker 抽取 + VLA 重建正确。
- **开池路径**(`TPOOL=0,1`,L1 池 Tpool0@core0/Tpool1@core1 实际创建):coh 1.0、估计正确 → fork-join 的 `init_task_ans/pushTpool/join` 机制走通。
- **串/并 A/B(同 cir_cdl_c_s60、只切 TPOOL)**:`TPOOL=n` −45.05dB vs `TPOOL=0,1` −45.02dB,**Δ0.03dB、coh 均 1.000、scalar 同 +4.28** → **并行路径与串行数值等价,无回归**(非 bit-exact 因 seed=rand 信道实现略不同,但远小于 capture 抖动 + golden 叶子已 bit-exact → 结论确凿)。
- 自动时限(默认 180s / `-d` / `LAUNCH_DURATION`,`-d 0` 禁用)实测生效("到时自动收尾")。

**端到端闭环**:golden(叶子 bit-exact)→ inline → 开池 → 串/并 NMSE 等价。**维度自适应 + 多核功能正确、无回归、并行无损**。

**2-rx MIMO 验证(`NB_RX=2` rfsim 已跑通)**:gNB 收 2 天线 SRS(GT 1×2,attach/handoff 正常);串/并 A/B(同 cir):`TPOOL=n` −45.04dB vs `TPOOL=0,1` −44.97dB,**Δ0.07dB、coh 均 1.000** → 真 2-rx 下并行与串行仍数值等价。`NB_RX` 旋钮 + 维度自适应在 rfsim 实链路上工作。

**墙钟计时(已加 `SRS_TMG_TOTAL`)**:`nr_ul_channel_estimation.c` 新增"整次 fork-join 墙钟"计时(主线程、无竞态,`SRS_TIMING=1` 门控,打印 `total_wall`)。per-block(freq/pkf)在并行下跨线程竞态、只在 `TPOOL=n` 有效;**`total_wall` 两档都有效**——串行≈Σ链路、并行≈max链路。待重编后 `NB_RX=2 SRS_TIMING=1` 串(`TPOOL=n`)/并(`TPOOL=0,1`)各跑一发,看 `total_wall` ~2× 下降。

**坑 1:`nb_antennas_rx = pusch_AntennaPorts`,不是 RU `nb_rx`**(`config.c:685` `num_rx_ant=pusch_AntennaPorts` → `nr_parms.c:314` `fp->nb_antennas_rx=num_rx_ant`)。第一次 `NB_RX=2` 只改了 RU 射频天线(GT/rfsim 看到 2、SRS 收 2 天线),但 SRS 估计循环仍 `ant<1`(`freq_wiener n == total_wall n` → 每次只 1 个 job)。修复:`launch_native.sh` 把 `--gNBs.[0].pusch_AntennaPorts` 也设 `=NB_RX` → 之后 `freq_wiener n = 2×total_wall n`(真 2 job)。

**多核墙钟实测(2-rx,`cir_cdl_c_s60`,Threadripper 7960X 48 逻辑核,单机 gNB+UE+5GC):**

| 档 | per-link freq_wiener | total_wall avg | total_wall max |
|---|---|---|---|
| 串行 `TPOOL=n` | **~185µs** | ~510µs | 1368µs |
| 并行 `TPOOL=0,1`(近核) | ~230µs | ~437µs | 5031µs |
| 并行 `TPOOL=20,22`(远核/独立物理核) | **~280µs** | ~520µs | 3420µs |

**结论:多核 fan-out 本机无墙钟收益**。决定性证据:**per-link 时间随核越远越长(185→230→280µs)**——串行(全在主线程核、缓存热)反而最快;丢远核(20/22 在另一 CCD)每条要跨 Infinity Fabric 取 `srs_received_signal`/PDP-EMA → 单条被压到 280µs,并行省的被"每条变慢 + 池唤醒延迟(max 3-5ms 尖峰)"吃掉。**根因:freq_wiener 是访存/带宽 bound(2048-pt IFFT + 读各天线信号 + 跨 1248 SC apply),不是 CPU bound**;chiplet 机器把这种小访存任务散到远核得不偿失。

**处置:留代码 + 默认关门**。加 `SRS_PARALLEL`(默认 0):默认每天线 inline = **原串行行为(即便真机有默认 8 线程池也不自动 fan-out)**;`SRS_PARALLEL=1` 才走线程池。`launch_native.sh` 已转发该 env。维度自适应(Phase A)恒开(MIMO 前提)。→ 多核休眠可选、零影响,需要时再开(需重编 nr-softmodem)。

**方向校正**:多核**逻辑正确、NMSE 等价、默认关=零风险**(保留),但对本 workload+机器非有效加速杠杆。**MIMO 扩展的正确路 = Option B(跨 port 复用 PDP/R)**:直接砍掉每条的 IFFT/噪底/R(复用 port → ~20µs/条,正好减访存大头),256 条单核 ~5ms 内 → 绕开局部性墙。离线已验证近无损(≤0.035dB)。

**仍待(非阻塞)**:① 若上 64rx → 实现 Option B(C 端,offline 已验);② `NB_RX=8` 跑通 >4×4 扩维冒烟。

### 4.11.8 Option B:跨 RX 天线共享 R(massive-MIMO 降本,已实现,默认关)

**动机**:多核访存 bound 无效;正确杠杆是**减每条的活**。同 UE 各 RX 天线 PDP 基本相同 → R 可共享:参考天线(ant0)算一次,其余复用、跳过 Stage1/2(collect+IFFT+EMA+noisefloor+Rbuild)。

**实现**:
- `nr_srs_mmse.c`:`nr_srs_mmse_freq_filter` 内部重构为 `freq_filter_core(...,R_in,...,R_out,...)`:`R_in==NULL`=全路径(逐位等价原版,golden 守门);`R_in!=NULL`=复用共享 R、只做 Stage3 apply(per-天线 noise_norm/R_meas 仍各算)。新增公开 `_build`(全路径 + 导出 R/noise_norm/max_dk)、`_reuse`(apply-only)。`nr_srs_mmse.h` 加 `NR_SRS_MMSE_R_TABLE_SIZE` + `complex.h`。
- `nr_ul_channel_estimation.c`:`SRS_SHARE_R`(默认 0)。开启 + TRUE2D 时,per-port:ant0 `_build` → 共享 `R[port]`,其余 ant `_reuse`;ant0 build 失败(`max_dk=-1`)则各 ant 回退全路径。**强制串行**(`!_srs_share` gate,确保 ant0 先 build)。
- 加 `nr_srs_mmse.c` 子分计时 `SRS freq-breakdown`(ifft/noise_floor/R_build/stage3,`SRS_TIMING` 门控)量 190µs 构成。`launch_native.sh` 转发 `SRS_SHARE_R`。

**验证**:
- 离线:参考天线 R 变体 N_rx=2/4/8 × SNR10/20,**worst ΔNMSE=0.048dB ≤0.1dB**(near-lossless)。
- golden:默认关 → `freq_filter`=`_build`+`_reuse`(R_in=NULL)逐位等价,`run_all.sh` 全 PASS(worst 1.0 LSB)。
- **非 bit-exact**(共享改估计值)→ 用 NMSE-delta 守门,不用 golden。

**实测(已回归,NB_RX=2,cir_cdl_c_s60)**:

| | SHARE_R=0 | SHARE_R=1 |
|---|---|---|
| total_wall avg | 576µs | **442µs**(−134µs) |
| coh / NMSE | 1.000 / −44.55dB | 1.000 / **−46.05dB** |
| breakdown 计数 | nfloor n = stage3 n | **nfloor n = stage3 n / 2**(ant1 跳 Stage1/2) |

- **190µs breakdown(SHARE_R=0)**:noise_floor **~90µs(#1)**、stage3(invert+apply)**~69µs(#2)**、R_build ~40µs、**ifft 仅 ~3µs**(纠正:IFFT 不是大头,robust noise-floor 排序才是;幸而它在 Stage1/2 可共享)。
- **Option B 工作确认**:`noise_floor n = stage3 n / 2` → ant1 跳过 Stage1/2;total_wall 省 −134µs ≈ 一根天线的 Stage1/2(ifft+nfloor+Rbuild ~135µs),算术吻合。
- **near-lossless 硬件确认**:NMSE 不降(−44.55→−46.05dB,差异是 seed=rand 信道抖动;系统性损失离线 ≤0.048dB),coh 均 1.000。
- **复用链路 205→69µs(3×)**;每条 freq = Stage1/2(~135µs)+stage3(~69µs),复用只剩 stage3。

**真 CDL 离线验证(data_out/cdl_{a..e} 真 3GPP 时延谱,NB_RX=4,SNR20,参考天线 R vs per-天线 R)**:

| 模型 | rms 时延 | per-ant | ref(ant0) | Δ |
|---|---|---|---|---|
| CDL-A | 68ns | −29.35 | −29.38 | −0.031 |
| CDL-B | 150ns | −28.62 | −28.57 | +0.053 |
| CDL-C | 265ns | −29.14 | −29.25 | −0.110 |
| CDL-D | 123ns | −29.30 | −29.41 | −0.106 |
| CDL-E | 556ns | −28.15 | −28.16 | −0.010 |

→ **worst Δ=0.053dB ≤0.1dB**(部分为负=ref 略优,属平均噪声)。**Option B 在全 CDL 时延谱(68→556ns 短到长)上 near-lossless**。

**在线固定-seed 干净对照(native,seed 4242,同 cir_cdl_c_s60,NB_RX=2,只切 SHARE_R)**:

| | SHARE_R=0 (210619) | SHARE_R=1 (211402) |
|---|---|---|
| total_wall avg | 574.28µs | **437.89µs**(−136µs) |
| coh / NMSE | 1.000 / −44.50dB | 1.000 / **−44.49dB** |

→ 同信道实现下 **ΔNMSE = 0.01dB**(消除了 seed=rand 的 1.5dB capture 抖动)、coh 均 1.000、墙钟省 −136µs(≈一根天线 Stage1/2)。

**带噪(-nd -6 ≈17dB SNR)真实工作点 + 多发均值(消抽样抖动)**:

| 档 | 三发 NMSE | 均值 |
|---|---|---|
| SHARE_R=off | −23.19 / −21.29 / −20.82 | **−21.8 dB** |
| SHARE_R=ON | −21.05 / −21.66 / −24.93 | **−22.5 dB** |

→ run-to-run 抖动 ~3-4dB(单发不可信);**均值上 ON ≈ off(略好),Option B 带噪下也无系统性损失**(单发曾见 +0.79dB 是抽样噪声)。**真实工作点 ≈ −22dB**(CDL-C@60km/h+17dB SNR,同 −24dB 基线量级)。

**度量澄清(避免误引用)**:eval 是 STO+scalar 对齐,残差近似 `NMSE ≈ −10·log10(1−|coh|²)`。**−44dB 是 noise-off 天花板**(coh≈0.99998),**不是真实性能**;加 `-nd -6` 后 coh≈0.997 → **真实 ≈ −22~−24dB**。之前若干 −44/−45dB 发均为 noise_dB=off。

**Option B 验证齐全**:① golden 默认关 bit-exact;② 离线真 CDL A–E ΔNMSE ≤0.053dB;③ native 无噪固定-seed 0.01dB + total_wall −136µs;④ native 带噪 ×3 均值 ON 不差于 off。**结论:near-lossless(含低 SNR)、确有降本、默认关零风险,可部署。**

**下一墙 + 可选 B+**:复用后 stage3(~69µs,per-天线 apply)成瓶颈(256 条 ×69 ≈17.6ms)。stage3 的 `A_inv` 只依赖共享 R + per-天线 noise_norm 对角;**再共享 noise_norm → A 全共享 → invert 也只算一次**,复用链路降到纯 apply(~20µs,256×20≈5ms)。代价:再加一点近似(noise_norm 用参考天线)。本期未做,留作 B+。

### 4.11.9 优化总账(收官)

**单条 freq_wiener 延迟(native 实测,三步叠乘 = 26.5×,全程逐位等价)**:

| 步骤 | 位置 | freq_wiener | 单步 | 累计 |
|---|---|---|---|---|
| 原始 true2d | — | 5042µs(max 7712) | — | 1× |
| ① 求逆一次(A 全局不变,O(span·W³)→O(W³+span·W²)) | Stage3 解法 | 993µs | **5.1×** | 5.1× |
| ② R_table twiddle 表 + 稀疏(消 per-bin cos/sin) | Stage2 R 构建 | 433µs | 2.3× | 11.6× |
| ③ Stage3 权重缓存(内部 SC 只 k_tc 种权重) | Stage3 apply | **190µs** | 2.3× | **26.5×** |

→ 注:**①③ 在 Stage3、② 在 Stage2**;26× 是三处叠乘,非单步。① 贡献最大(原版每 SC 重解 16×16,O(W³)×1248)。

**预算**:原始 5042µs **超 5ms SRS 周期**(不可行)→ 现 190µs **≪ 500µs TTI**,4×4≈3ms 进周期。

**两类优化(默认态)**:
- **始终启用 = bit-exact**(golden ≤1 LSB 守门,取代原版,无开关):① 求逆一次 + ② twiddle + ③ 权重缓存 + 维度自适应(RX 维运行时动态,解除 4×4 上限)。
- **默认关 = 近似/并行**(按需开):`SRS_PARALLEL`(多核 fork-join;本机访存 bound 无益)、`SRS_SHARE_R`(Option B 跨天线共享 R;~0.05dB 近似,massive-MIMO 用)。

**Option B 增量(默认关)**:复用天线跳 Stage1/2 → 单条 190→**~69µs(vs 原始 ~73×)**;2-rx total_wall 574→438µs(−136µs)。四重验证 near-lossless(见 §4.11.8)。

**精度口径(防误引用)**:真实工作点 **true2d-freq ≈ −22~−24dB**(-nd -6 ≈17dB SNR);`NMSE≈−10log10(1−coh²)`,**−44dB 是 noise-off 天花板,非真实**。全程优化精度 near-lossless。

> 下行(教授 B):结论=**不单独做**。TDD 互易下 SRS 已服务下行预编码;算法移植 CSI-RS 是机械工作非新贡献。下行只作为 **SRS+PMI 融合(C)** 的入口(PMI 是下行反馈)与 Digital Twin 下行支路——要做就并进融合。

---

## 5. 如何运行

### 5.1 纯离线 sweep(零硬件,马上能跑)
```bash
cd DevChannelProxyJIN/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy
python3 test_srs_2d_offline.py --predict-sweep --predict-trials 12 \
    --predict-csv predict_sweep.csv --predict-plot predict_sweep.png
# 可调:--predict-speeds 10,30,60,120 --predict-periods 10,40,80 \
#        --predict-horizons 1,2,4,8,16,32 --predict-snr 20 --predict-angle-spread 1.0
```

### 5.2 C golden(改完 C 必跑)
```bash
cd DevChannelProxyJIN/openairinterface5g_whan/openair1/PHY/NR_ESTIMATION/c_unit_tests
bash run_all.sh    # golden_pkf / golden_pkf_predict / golden_freq 均应 PASS
```

### 5.3 在真实 dynamic capture 上跑 `replay_true2d.py --predict-horizon`(native rfsim)

> **重要:全部走 native rfsimulator + channelmod,不用 GPU-IPC proxy。** proxy 受链路余量墙挡死(既要 attach 又要给够 SNR 解 CDL 弱多径,二者不可兼得 → 选频 GT 复相干仅 0.05~0.15,结果不可信;见 `2D_MMSE_项目报告.md` §1.1/§4)。native 路径信道真值 = rfsim 实际施加的 `channelDesc->ch[]`,可直接控噪、可复现。
>
> 预测要对“未来 GT”打分,所以 capture 必须 **动态(`-cir` 用 speed>0 的 CDL 回放)+ 逐 slot GT(`-cir` 自动设 `NATIVE_GT_EVERY_SAMPLES=30720`)+ 连续帧**,并用 `-e passthru`(replay 离线套完整频级 Toeplitz + PKF)。

**步骤 1:生成动态 CDL CIR 序列**(speed>0,在 proxy 目录,无需 sudo):
```bash
cd DevChannelProxyJIN/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy
# 60 km/h;cdl_c 频选。输出 cir/cir_cdl_c_s60.bin(逐 slot sample-spaced CIR)
python3 cdl_to_cir.py data_out/cdl_c cir/cir_cdl_c_s60.bin -speed 60
```

**步骤 2:native rfsim 采集 passthru 动态 capture**(需 sudo 起 OAI gNB/UE):
```bash
sudo bash launch_native.sh \
     -e passthru \
     -cir cir/cir_cdl_c_s60.bin \
     -nd -6 \
     -seed 12345 \
     -p 10 \
     -d 120 \
     --restart-5gc
```
关键点:
- `-cir cir_cdl_c_s60.bin`:speed=60 km/h 的动态回放,**必须 speed>0**,否则信道不变、预测无意义;`-cir` 自动把 GT dump 设为**每 slot**(30720 样本)→ 满足“n+k 槽也有 GT”。
- `-e passthru`:dump 原始梳状 LS;replay 离线套 freq Toeplitz + PKF。
- `-nd -6`:逐信道噪声(仅 handoff 后施加,attach 干净);≈ 有效 SNR 17 dB(经验式 `SNR≈−2·nd+5`)。
- `-p 10`:SRS 周期 10 slots(=5ms);TDD(8DL/3UL)下这是最密(`-p` 更小会被 TDD 卡回 10,见报告 §9.5)。
- `-d 120`:采 120 s 拿到足够连续帧;`--restart-5gc`:防多次 attach 后 NAS 退化致 SRS 捕获 0(报告 §4.6)。
- 产物:`DevChannelProxyJIN/logs/native_passthru_<时间戳>/`,内含 `srs_matrix_gNB_*.bin` + `sionna_gt/gt_batch_ue0_seq0.npz`(GT 由 `native_gt_to_npz.py` 退出时自动转换),并有 `logs/native_latest` 软链。

**步骤 3:离线跑预测评估**(无需 sudo;run-dir 即 native 产物,replay 直接吃):
```bash
python3 replay_true2d.py \
    --run-dir ../../logs/native_passthru_<时间戳> \
    --win 16 --time-pkf --predict-horizon 16 --max-frames 200
```
输出会有一节(STO 对齐,与报告同口径):
```
=== Direction A: SRS channel prediction vs FUTURE GT (STO+scalar NMSE) ===
horizon   PKF-predict   zero-order-hold   predict-vs-ZOH
  k=1      ...           ...               ... dB
  ...
[predict] classical-prediction knee at horizon k=...
```

**对照试验(画“拐点 vs 速度”)**:对同一 `data_out/cdl_c` + 同 `-seed`,改 speed 重生成 CIR(`cdl_to_cir.py ... -speed {10,30,60,120,300}`),各采一发 native passthru,分别 replay,汇总拐点 vs 速度。

### 5.4(可选)端到端 C 预测 dump + native eval

让 C 直接 dump “预测信道”(true2d 路径,env `SRS_PKF_PREDICT_AHEAD=k`,launch_native 已转发该 env):
```bash
# 采集:true2d + 预测 dump(dump 写 H_pred@k 而非估计;srs_estimated_channel_freq 下游不变)
sudo SRS_PKF_PREDICT_AHEAD=4 bash launch_native.sh \
     -e true2d -cir cir/cir_cdl_c_s60.bin -nd -6 -seed 12345 -p 10 -d 120 --restart-5gc
# native STO 对齐 eval:把 SRS 轴前移 k×周期,对未来 GT 打分
python3 eval_nmse_sto.py --run-dir ../../logs/native_true2d_<时间戳> --predict-horizon 4
```
> 注:`eval_nmse_clean.py --predict-horizon` 是 proxy 时代的非 STO 版;native 测试床统一用 **`eval_nmse_sto.py --predict-horizon`**(STO 对齐守门,与项目报告同口径)。

DT 前瞻曲线(对**普通** native capture 即可,内部离线套预测器,无需预测 dump):
```bash
python3 digital_twin_stats.py --log-dir ../../logs/native_true2d_<时间戳> --predict-horizons 1,2,4,8,16
```

---

## 6. 关键发现与对教授的回应

1. **稀疏 SRS + 移动性确有“空窗”问题**:周期 10 slots(5ms)在车速下每帧多普勒已达 ~1 rad,经典外推只在短地平线有效。
2. **标量预测的天花板**:mode0 只跟净公共相位;多径展宽 + 长地平线必然去相关 → **这就是上 AI(LSTM/Transformer)或 per-band(mode1)/linear(mode2)的客观理由**。
3. **零风险落地**:C 预测只读、env 默认关,估计路径 bit-identical(golden 证);对现有交付结论无影响。
4. **直接喂 Digital Twin**:`prediction_lookahead` 指标 + 曲线即 DT“可前瞻性”的量化,衔接需求文档阶段四 AI。

---

## 7. 风险与回退
- 纯相位外推在高速/长地平线失效 = 预期极限(非 bug),作为 Phase 3 立项证据。
- C 预测必须只读 + env 默认关 → 对现网零副作用(已 golden 验证)。
- 先离线 sweep + 真实 capture 出曲线,再决定是否投入 Phase 3(AI 预测)。

## 8. 下一步(详见 §4.7.4 改进路线)
1. **【推荐立即做】参数化多普勒 R 估计**:替裸 per-lag EMA,逼 genie −13.7、吃那 ~5 dB 瓶颈(纯经典)。离线对比 裸-EMA-R vs 参数-R vs genie。
2. 对齐地平线到应用(补 CSI 时延 1-2 slot)+ 干净 GT 评估;genie-R 扫大 p + KS 闭式界钉死真上限。
3. per-tap(时延域)多普勒预测;非平稳自适应。
4. 经典到顶后再上 AI(LSTM/Transformer),以本日志 Wiener-genie 曲线为 baseline / 上限参照。
5. (大、独立)闭环:预测喂回预编码/调度看 BLER/吞吐;统一时间 Wiener 替换 Stage-2 Kalman(需重验四象限)。
- 已废弃判断:per-band/linear-phase(mode1/2)对预测无增益(§4.5);加密 SRS 属作弊且本 TDD 做不到。

---

## 9. 收官(2026-06-07):R 估计四方对比 + 经典上限 + "代码 vs 物理"定性

> 本节为 §8.1 的执行结果与最终定性。详细过程见 `0607_预测R估计与时间级虚惊_日志.md`。

### 9.1 R 估计四方离线对比(§8.1 落地)
在 `test_srs_2d_offline.py` 接入时间维 Wiener 四方对比(`--predict-wiener`):EMA / 单多普勒 param(`_param_R`)/ PSD 投影(`_psd_R`,功率谱裁非负→合法正定 Toeplitz)/ 多态多普勒 K-line(`_kline_R`)/ genie。

- **合成(CDL-C/E,SNR20)**:崩溃 EMA 26/64、param 37/64、**PSD 0/64**、genie 0/64;PSD 距 genie 仅 +0.8~1.2dB。
- **真实 capture**:**裸 EMA ≈ genie(差 0.2~0.3dB)**;PSD/param/多态多普勒**均无增益**(param/K-line 输 EMA ~1dB,K=2 即饱和)。合成里的 EMA 暴走在真实数据**不出现**(预测输入是频域 Toeplitz 滤波后的 `est_t2d`,r(d) 本就干净)。

→ **结论:C 端现用 EMA-R Wiener 已是经典线性预测实际最优,无需移植 PSD/param/多态多普勒。**

### 9.2 经典上限曲线(报告核心图)
`plot_predict_ceiling.py` 出 "NMSE vs 地平线"(ZOH / EMA-Wiener / genie × 速度 × 周期),标 knee:`predict_ceiling_cdlc.png` / `predict_ceiling_cdle.png`。

knee(cdl_c,P=10=最密 SRS):10km/h **>16**、30km/h **k=4**、60km/h **k=1**、120km/h **k=1**。

### 9.3 "是代码差还是预测本来就难?" —— 物理定性(关键 insight)
**答:代码已达 linear-MMSE 上限(EMA≈genie);车速下预测物理上接近不可能,非代码问题。**

判据 = 信道相干时间 `T_c≈0.42/f_d`(f_d=v/λ,fc=3.5GHz)对比 SRS 周期 5ms(P=10):

| 速度 | f_d | T_c | vs 5ms | 实测 knee |
|---|---|---|---|---|
| 10 km/h | 32 Hz | **13 ms** | ≫ 周期 | >16(好预测) |
| 30 km/h | 97 Hz | 4.4 ms | ≈ 周期 | k=4 |
| 60 km/h | 194 Hz | 2.2 ms | < 周期 | k=1 |
| 120 km/h | 389 Hz | **1.1 ms** | ≪ 周期 | k=1(几乎没用) |

**相干时间与实测 knee 完全吻合。** 车速下信道在**一个 SRS 周期内就已去相关**(T_c≪周期)→ 过去/未来近乎独立 → 无互信息可外推 → 任何方法(含 AI)都救不回来(信息论极限)。

**本质矛盾(报告/论文核心论点):** 真正杠杆不是更强预测器,而是 **SRS 采样需快过相干时间**(更密 SRS)——这与"SRS 稀疏以降终端负担"直接冲突。这正是教授(录音 26-06-04)点的张力。

**对 AI 的含义:** AI 只在**中速(~30km/h)+ 中地平线(k=2~8)** 有窄窗口(genie 仍有头顶且有非高斯结构可学);低速经典已够,高速物理封死。

### 9.4 顺带澄清的"时间级虚惊"
06-06 批量发现 replay `+PKF(time)` 在 11/12 capture 上"退化 9~43dB"→ 排查确认是 **replay 工具喂错输入的假象**(把已是 C 成品的 dump 当原料二次加工;且 mode2 也不救)。**生产 C 时间级健康**(`eval_nmse_sto` 实测 −16~−45dB),C 移植位级一致(`c_unit_tests/run_all.sh` 全 PASS ≤1 LSB)。replay 的 `+PKF(time)` ladder 已删除。
