# OAI 数字孪生系统 — 项目全链路蓝图

> 最终目标：用自建的 V-RAN 数字孪生引擎生成海量训练数据，训练出能从 SRS 信道估计直接推断终端位置或环境特征的 AI 模型，部署到 RAN Twin 平台，实现"空口信号进 → 感知结论出"的闭环。

---

## 系统总架构

```
  ① 数据引擎          ② 信道估计          ③ 特征提取          ④ AI 建模与部署
 (Channel Proxy     (OAI gNB SRS       (PDP / Cov /        (训练 → Sensing
  + OAI 环回)        + 2D 滤波)          State Sample)        Block → RAN Twin)

  基本完成，持续完善 ←── 正在攻坚 ──────→ 待定义接口 ──────→ 未来主战场
```

全链路由一个人贯穿：从仿真平台、信道估计算法、特征工程到 AI 模型训练与部署。

---

## 阶段一：数据引擎（基本完成，持续完善中）

### 定位

为整个系统提供**可控、可复现、带真值标签**的信道数据。这是所有下游工作的基础——没有可信的数据源，信道估计无从验证，AI 模型无从训练。

### 架构

```
┌──────────────────────────────────────────────────────────────────┐
│                     数据引擎 运行时拓扑                            │
│                                                                  │
│   ┌──────────────┐         GPU IPC SHM          ┌────────────┐  │
│   │  OAI gNB     │◄═══════════════════════════►  │  OAI UE    │  │
│   │  nr-softmodem│     /tmp/oai_gpu_ipc          │            │  │
│   │  (4T4R)      │           ▲                   └────────────┘  │
│   └──────────────┘           │                                   │
│                              │                                   │
│                    ┌─────────┴──────────┐                        │
│                    │  Channel Proxy v8   │                        │
│                    │  (sionna-proxy 容器) │                        │
│                    │                    │                        │
│                    │  信道模型输入:       │                        │
│                    │  • P1B Ray Tracing  │                        │
│                    │  • CDL-A/B/C/D/E   │                        │
│                    │  • Sionna RT 场景   │                        │
│                    │                    │                        │
│                    │  DL: H[k]×X → UE   │                        │
│                    │  UL: H[k]^T×X → gNB│                        │
│                    └────────────────────┘                        │
│                              │                                   │
│                    ┌─────────┴──────────┐                        │
│                    │     双源数据产出      │                        │
│                    │                    │                        │
│                    │  H_GT (.npz)       │ ← Ground Truth 真值    │
│                    │  Ĥ_SRS (.bin V2)   │ ← OAI 估计结果         │
│                    │  场景元数据          │ ← 位置/速度/信道类型    │
│                    └────────────────────┘                        │
└──────────────────────────────────────────────────────────────────┘
```

### 已完成的能力

- **Proxy 版本演进 (v4→v8)**：多 UE MIMO 叠加、CUDA Graph 加速、异步 GT 保存（不阻塞 attach）
- **OAI 修改**：SRS V2 二进制 dump（带 magic/RNTI）、SRS 调度修复、GPU IPC 适配
- **信道模型接入**：P1B ray-tracing 和 CDL 标准模型均可作为输入
- **自动化 sweep 框架**：`launch_all_v8.sh` → SNR sweep → 后处理管线
- **Q3 合成器验证闭环**：GT 与 Ray 输入的 PDP/空间统计一致性已确认

### 持续完善中的部分

- **int16 精度瓶颈**：SRS AMP=512 导致有效 SC-SNR 偏低（~7 dB），是单帧 NMSE 底板的主因之一
- **IPC 延迟**：当前约 28 ms/slot，影响高速场景下的信道时变忠实度
- **GT 对齐精度**：SRS/GT slot 配对的 STO 校正仍在迭代
- **CDL 容器内联调**：`cdl_ray_adapter.py` 已实现，与 v8 的端到端联调待验证
- **4×4 MIMO 常态化**：架构支持但日常 sweep 仍以 2×2 为主

---

## 阶段二：信道估计 + 2D 滤波（正在攻坚）

### 定位

在 OAI gNB 物理层内，以**可忽略的复杂度**（≪ 1 ms/SRS 块）提升 SRS 信道估计精度。这一阶段的产出质量直接决定阶段三提取的特征是信号还是噪声。

### 信号处理链

```
SRS 原始导频 (稀疏子载波)
        │
        ▼
  ┌──────────────┐
  │  LS 信道估计   │   逐 pilot SC 除参考信号
  └──────┬───────┘
         │
         ▼
  ┌──────────────┐
  │  频域处理      │   Stage 1
  │  (插值到全 SC) │   当前: filt8/16 (固定 FIR)
  │              │   目标: 频域 Wiener MMSE
  └──────┬───────┘
         │  dense 1248 SC
         ▼
  ┌──────────────┐
  │  时域处理      │   Stage 2 (跨 SRS slot)
  │  (时域平滑)   │   当前: EWMA / adaptive α
  │              │   目标: 时域 Wiener / 闭环自适应
  └──────┬───────┘
         │
         ▼
    Ĥ(f) per RE
    ├── → PUSCH 解调 (V-RAN 路径)
    └── → dump .bin  (孪生验证 / 特征提取)
```

### 算法路线与决策

| 算法 | 状态 | 结论 |
|------|------|------|
| Legacy filt8/16 | 生产默认 (频域) | 单帧基线，float 约 -22 dB |
| PDP→R 1D MMSE | 已实现后淘汰 | 5-10 ms/帧，违反复杂度约束 |
| EWMA 时域 | OAI C 已集成 | MVP 方案，静态 p50 +0.55 dB |
| Adaptive α (CDL 离线标定) | 已放弃 | CDL 内 8/8 PASS，不可迁移到 ray-tracing |
| Adaptive α (闭环) | 构想中 | IBVSS / Scalar Kalman / 在线梯度 |
| Lattice-RLS (LSL) | 已论证不适用 | 信道平滑 ≠ 均衡，退化为 identity |
| 可分离 2D MMSE | 规划中 | 时域 Wiener + 频域 Wiener 替代 filt8 |

### 验证体系

```
H_GT (.npz)  ←─── 对比 ───→  Ĥ_SRS (.bin)
                    │
        ┌───────────┴───────────┐
        │     评估指标体系        │
        │                       │
        │  • NMSE (per frame)   │
        │  • PDP 形状相关系数     │
        │  • 收敛性曲线 (Q4)     │
        │  • 空间协方差一致性     │
        │  • SNR vs NMSE 全景    │
        └───────────────────────┘
```

---

## 阶段三：特征提取与状态样本定义（待定义）

### 定位

把阶段二输出的 Ĥ(f) 转化为下游 AI 模型可以消费的**标准化特征向量**。这是"信号处理世界"和"机器学习世界"的桥梁。

### 候选特征体系

```
Ĥ(f) ──┬── IFFT ──────────→ PDP(τ)              功率延迟分布
        │                     可提取: 时延扩展、主径功率、路径数
        │
        ├── 空间协方差 ────→ R(Δk) 矩阵           频域相关结构
        │                     可提取: 相关带宽、矩阵特征值
        │
        ├── SVD ───────────→ σ₁, σ₂, σ₃, σ₄      空间秩信息
        │                     可提取: 条件数、有效秩
        │
        ├── 容量估计 ──────→ C (bits/s/Hz)         信息论指标
        │
        ├── SNR 估计 ──────→ SNR_est (dB)          链路质量
        │
        ├── Doppler ───────→ 多普勒扩展             移动性特征
        │
        └── [扩展] ────────→ K-factor / AoA / ...  高阶物理特征
                │
                ▼
        ┌───────────────────┐
        │   State Sample     │
        │   标准化特征向量     │
        │                   │
        │   x = [pdp_feat,  │
        │        cov_feat,  │
        │        capacity,  │
        │        snr, ...]  │
        └───────────────────┘
```

### 关键设计决策（待确定）

| 决策 | 选项 | 影响 |
|------|------|------|
| 特征粒度 | 原始 Ĥ(f) vs 压缩统计量 | 维度 vs 信息保留 |
| 时间窗口 | 单帧 vs 滑动窗口 | 时序信息 vs 延迟 |
| 特征标准化 | per-sample vs global | 训练稳定性 |
| 与민지 EA state 的关系 | 复用 vs 独立定义 | 实验室内一致性 |

---

## 阶段四：AI 模型训练与 RAN Twin 部署（未来主战场）

### 定位

利用阶段一的数据引擎和阶段三定义的特征，训练 AI 模型并部署到实验室 RAN Twin 平台，实现从 SRS 信号到感知结论的端到端推断。

### 训练架构

```
┌──────────────────────────────────────────────────────────────┐
│                       训练管线                                │
│                                                              │
│  数据来源 (阶段一产出)                                         │
│  ┌──────────────────────────────────────────┐                │
│  │  Proxy Sweep 数据                         │                │
│  │                                          │                │
│  │  输入: State Sample 向量 (阶段三定义)       │                │
│  │  标签: 位置坐标 / 场景类型 / 移动速度 / ... │                │
│  │  来源: Sionna RT 元数据 (天然带标签)        │                │
│  └──────────────┬───────────────────────────┘                │
│                 │                                            │
│                 ▼                                            │
│  ┌──────────────────────────┐                                │
│  │  模型训练                  │                                │
│  │                          │                                │
│  │  候选架构:                 │                                │
│  │  • MLP (统计特征输入)      │                                │
│  │  • CNN (PDP 图像化输入)    │                                │
│  │  • Transformer (时序输入)  │                                │
│  │  • GNN (场景图结构)        │                                │
│  │                          │                                │
│  │  候选任务:                 │                                │
│  │  • 终端定位 (回归)         │                                │
│  │  • 场景分类 (LOS/NLOS)    │                                │
│  │  • 信道预测 (下一时刻 Ĥ)   │                                │
│  │  • 多任务联合              │                                │
│  └──────────────┬───────────┘                                │
│                 │                                            │
│                 ▼                                            │
│  ┌──────────────────────────┐                                │
│  │  验证与评估                │                                │
│  │                          │                                │
│  │  • 仿真内交叉验证          │                                │
│  │  • 跨场景泛化测试          │                                │
│  │  • Sim-to-Real Gap 分析   │                                │
│  └──────────────────────────┘                                │
└──────────────────────────────────────────────────────────────┘
```

### 部署架构

```
┌──────────────────────────────────────────────────────────────┐
│                    RAN Twin 平台部署                           │
│                                                              │
│   ┌─────────────────────────────────────────┐                │
│   │          Sensing Block                   │                │
│   │                                         │                │
│   │   实时 SRS IQ                            │                │
│   │       │                                 │                │
│   │       ▼                                 │                │
│   │   信道估计 (阶段二算法)                    │                │
│   │       │                                 │                │
│   │       ▼                                 │                │
│   │   特征提取 (阶段三管线)                    │                │
│   │       │                                 │                │
│   │       ▼                                 │                │
│   │   AI 推断引擎                             │                │
│   │   (训练好的模型)                           │                │
│   │       │                                 │                │
│   │       ▼                                 │                │
│   │   输出: 位置 / 场景 / 预测                 │                │
│   └─────────────────────────────────────────┘                │
│                                                              │
│   部署约束:                                                   │
│   • 推断延迟: 需匹配 SRS 周期                                  │
│   • 模型格式: ONNX / TFLite / OAI C 嵌入                      │
│   • 内存占用: 与 gNB PHY 共存                                  │
└──────────────────────────────────────────────────────────────┘
```

### 核心研究问题

| 问题 | 说明 | 学术价值 |
|------|------|----------|
| **Sim-to-Real Gap** | 仿真训练的模型能迁移到真实部署吗？ | 迁移学习 / 域适应 |
| **特征 vs 端到端** | 手工特征 vs 直接喂原始 Ĥ？ | 可解释性 vs 表达力 |
| **估计精度对 AI 的影响** | 更好的 CE 能让 AI 性能提升多少？ | 量化阶段二的价值 |
| **轻量化部署** | 推断开销能做到多小？ | 嵌入式 AI / 模型压缩 |
| **在线适应** | 模型部署后能自我更新吗？ | 持续学习 |

---

## V-RAN 路径 vs e-MIMO 路径

同一套基础设施（阶段一 + 阶段二），两种不同的消费方式：

```
                 阶段一 + 阶段二
                      │
         ┌────────────┴────────────┐
         ▼                         ▼
  ┌─────────────┐          ┌──────────────┐
  │  V-RAN 路径  │          │  e-MIMO 路径  │
  │             │          │              │
  │  完整 NR 协议栈│          │  不需要解码    │
  │  解调 + 解码  │          │  只提取特征值  │
  │             │          │              │
  │  产出:       │          │  产出:        │
  │  • BLER     │          │  • 容量增益    │
  │  • 吞吐量    │          │  • NMSE 改善  │
  │  • AI 训练数据│          │  • 特征对比    │
  │             │          │              │
  │  → 论文      │          │  → 专利子块    │
  │  → 测试床    │          │    (윈즈主导)  │
  │  → RAN Twin │          │              │
  └─────────────┘          └──────────────┘
```

| 维度 | V-RAN | e-MIMO |
|------|-------|--------|
| 解码 | 必须完整跑通 | 不需要 |
| 从阶段二取什么 | Ĥ → PUSCH → 比特 → BLER | Ĥ → 容量/NMSE → 特征值 |
| 复杂度要求 | 极严格（实时） | 可放宽 |
| 我的角色 | 主导 | 提供数据支撑 |

---

## 我的独特优势：全链路闭环

```
  数据引擎 → 信道估计 → 特征提取 → AI 训练 → RAN Twin 部署
  └───────────────── 全链路一个人贯穿 ────────────────────┘
```

1. **数据自主权**：不依赖外部数据集，Proxy sweep 可按需生成任意 SNR / 速度 / 场景的配对数据
2. **物理可解释**：理解每个特征的信号处理来源，不是黑盒堆特征
3. **端到端可追溯**：AI 效果不好时能精确定位瓶颈在哪个阶段
4. **标签天然存在**：Sionna RT 自带位置坐标、场景几何、信道参数，无需人工标注

---

## 时间线概览

```
2026 Q1-Q2                    2026 Q3                    2026 Q4+
┌──────────────────┐   ┌──────────────────┐   ┌──────────────────────┐
│ 阶段一 + 阶段二    │   │ 阶段二收尾        │   │ 阶段三 + 阶段四        │
│                  │   │ + 阶段三启动       │   │                      │
│ • Proxy v4→v8    │   │                  │   │ • 特征体系固化         │
│ • Q3/Q4 验证     │   │ • 2D MMSE 落地    │   │ • AI 模型训练          │
│ • EWMA/adaptive  │   │ • 闭环 adaptive α │   │ • RAN Twin 集成       │
│ • 验证体系搭建    │   │ • 状态样本定义     │   │ • Sim-to-Real 验证    │
│                  │   │ • A/B 对比结论     │   │ • 论文 / 专利          │
└──────────────────┘   └──────────────────┘   └──────────────────────┘
     我们在这里 ──────►
```
