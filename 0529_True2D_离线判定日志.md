# 真·2D MMSE —— 离线判定日志（复现 + Toeplitz/ESPRIT 天花板）

| 字段 | 值 |
|------|----|
| **日期** | 2026-05-29 |
| **依据** | `0529_True2D_MMSE_先期计划.md`、`0529_True2D_MMSE_中期计划.md`、`0529_Phase1_EMA_Wiener_离线日志.md` |
| **范围** | 纯 Python 离线；全部合并进统一 harness `test_srs_2d_offline.py`（`--true2d` / `--cascade`）；**未改动任何 OAI C 运行代码** |
| **一句话结论** | "真 2D MMSE"（全 Toeplitz 联合 LMMSE）在 CDL 上比当前对角/MA 路线高 **5–7 dB**，真实存在且值钱；但这 5–7 dB 几乎全部来自**精确二阶统计量 R_f**，与求解器结构（对角 vs Toeplitz）无关。任何基于单次周期图的数据 R（`FT{PDP}` ≡ 经验自相关）都因 **DFT 泄漏的结构性偏差**卡在 oracle 下方 4–6 dB，再平均无效；**ESPRIT 参数化 R 在窄延迟谱可逼近 win_genie，但在宽/密延迟谱（cdl_c/e）灾难性退化（比 raw 还差），不满足全场景鲁棒**。→ 可落地的真 2D = **C 已有的窗口 Toeplitz 频域 LMMSE（`nr_srs_mmse_freq_filter`）× 时域 Kalman 级联**；剩余 4–6 dB 属脆弱超分辨研究天花板，工程上不追。 |

---

## 一、先复现：日志三道坎全部属实（以下均为本机实测，非引用）

### 1.1 白噪声+稠密合成（auto-gamma 对角自适应）
`--synth-mf --n-sc 256 --synth-frames 150 --snr-db 15 --ema-auto-gamma`

| case | raw | MA | EMA-Wiener(对角) | oracle |
|------|-----|-----|------|--------|
| flat | -14.97 | -29.89 | **-35.95** | -38.44 |
| 2tap | -14.96 | **-3.84(崩)** | **-34.86** | -36.31 |
| 4tap | -15.02 | **-8.24(崩)** | **-31.89** | -32.98 |

✅ 对角自适应在白噪声+稠密上确实逼近 oracle、MA 在频选崩——与 Phase1 日志 §八一致。

### 1.2 真实 P1B（filt8 输入）→ 坐实"坎#3"
`--run-dir logs/q4_sweep_20260528_150304/snr_15dB --ema-auto-gamma --cport`

- auto-gamma：**K=489、NMSE -10.92（≈raw，彻底崩）**；只有 top1/energy(K=1) 能到 -45.13。
- cport（filt8 输入）：-11.87，同样崩。
- ✅ 印证中期计划坎#3"auto-gamma 在真实有色噪声上过度保留"。

### 1.3 passthru raw-LS（前90帧干净）
`--run-dir logs/q4_sweep_20260529_163146/snr_15dB --max-frames 90 --ema-auto-gamma --cport`

- auto-gamma K=47 → **-14.79**；cport K=47 → -15.65。离 M0 目标(Legacy+6≈-17)仍差。
- ✅ raw-LS 也有 K_TC 混叠伪峰，离线代理已达极限（与日志 §六/§八一致）。

---

## 二、决定性实验：真 2D 到底是什么、瓶颈在哪

`test_srs_2d_offline.py --true2d`，CDL A–E 由 ray (τ,power) 白噪声合成（带宽 1248·30kHz），n_sc=256，含 O(n³) 全 Toeplitz oracle。五档对比：

- `od_diag`：对角延迟域 Wiener（真值 PDP，对角天花板）——当前离线计划路线。
- `win_pdpR`：窗口 Toeplitz LMMSE，R=FT{清洗后EMA-PDP} —— **等价于 C 现有 `nr_srs_mmse_freq_filter`**。
- `win_acfR`：窗口 Toeplitz，R=经验频域自相关（EMA+半汉宁锥）。
- `win_genie`：窗口 Toeplitz，**精确 R**（隔离"窗口截断"损失）。
- `oracle`：全 Toeplitz LMMSE（精确 R，**真 2D 频阶上界**）。

### 2.1 SNR=15，win=64（frames=40）

| model | DS(ns) | raw | od_diag | win_pdpR(C) | win_acfR | win_genie | **oracle** |
|-------|--------|-----|---------|------|------|------|--------|
| cdl_a | 30 | -14.9 | -21.9 | -23.8 | -23.4 | -27.6 | **-29.4** |
| cdl_b | 100 | -15.0 | -21.1 | -22.8 | -22.6 | -25.3 | **-27.1** |
| cdl_c | 100 | -15.0 | -23.9 | -22.6 | -22.1 | -23.3 | **-26.7** |
| cdl_d | 78 | -15.0 | -19.7 | -22.2 | -22.1 | -26.0 | **-28.8** |
| cdl_e | 260 | -15.1 | -22.0 | -22.4 | -21.9 | -23.9 | **-28.6** |

### 2.2 三个硬结论

1. **真 2D 真实且值钱**：oracle 比对角天花板高 **5–7 dB**；win_genie 随窗口持续逼近 oracle（cdl_a：W32 -26.3 / W64 -27.6 / W128 -28.7 / oracle -29.4）。对角近似把 5–7 dB 扔在桌上。
2. **`FT{PDP}` ≡ 经验自相关**（差<0.5 dB，本就是同一周期图的维纳-辛钦傅里叶对）。所以 C 现有 Toeplitz(`win_pdpR`) 与离线对角是**同一信息**，都只到 -22~-24，离 oracle 差 4–7 dB。
3. **瓶颈是 R 质量，且是结构性偏差非方差**：平均从 40 帧×α0.95 加到 **200 帧×α0.98（5×）**，数据 R 几乎不动（cdl_a -23.76→-24.33），仍卡 genie 下方 3–5 dB。CDL 离格延迟 → DFT 泄漏的**确定性偏差**，再平均消不掉，只有精确 R 才拿得到真 2D 增益。

---

## 三、ESPRIT 参数化 R 判定（"一锤定音"实验）

CDL ray 结构：n_rays 260–480，但**唯一延迟（簇）仅 13–24**（同簇共享 τ，频域合成单复指数）→ per-link 真实模型阶 = 13–24，ESPRIT 理论可行。

实现：空间平滑 + 前后向协方差（子阵 M=128，全帧快照）→ 特征分解 → ESPRIT 旋转不变求 e^{-j2πτ/N} → 离格 τ → 每帧 LS 复幅 → 平均功率 → 重建 R → 窗口 Toeplitz。

### 3.1 SNR=15，P=24，frames=80

| model | DS | od_diag | win_pdpR | **win_esprit** | win_genie | oracle |
|-------|-----|------|------|------|------|--------|
| cdl_a | 30 | -22.2 | -24.4 | **-26.8 ✅** | -27.7 | -29.2 |
| cdl_b | 100 | -22.9 | -23.7 | -22.7 | -25.2 | -27.1 |
| cdl_c | 100 | -20.8 | -22.5 | **-10.6 ✗(崩)** | -23.9 | -26.8 |
| cdl_c_30 | 73 | -21.3 | -23.2 | **-17.0 ✗** | -24.8 | -26.8 |
| cdl_d | 78 | -21.1 | -23.8 | -22.8 | -26.3 | -29.3 |
| cdl_e | 260 | -24.0 | -23.2 | **-20.0 ✗** | -23.9 | -28.5 |

P=16/24/32 结果几乎相同（瓶颈非阶数）。

### 3.2 SNR=25，P=24，frames=120（锁定"结构性 vs 噪声"）

| model | od_diag | win_pdpR | **win_esprit** | win_genie | oracle |
|-------|------|------|------|------|--------|
| cdl_a | -27.7 | -31.2 | **-32.4 ✅** | -36.7 | -38.8 |
| cdl_b | -31.0 | -32.9 | **-35.0 ✅** | -35.0 | -37.1 |
| cdl_c | -26.0 | -29.4 | **-21.0 ✗(比raw -25还差)** | -33.1 | -36.7 |
| cdl_c_30 | -26.2 | -29.9 | **-24.5 ✗** | -34.1 | -36.4 |
| cdl_d | -28.6 | -32.2 | **-35.1 ✅** | -36.0 | -39.1 |
| cdl_e | -31.2 | -32.3 | **-24.8 ✗** | -33.8 | -38.2 |

### 3.3 ESPRIT 判定结论
- **窄/紧凑延迟谱（cdl_a/b/d，DS≤100ns 且延迟集中 0–14 bin）**：ESPRIT-R 逼近 win_genie，真 2D 可达。
- **宽/密延迟谱（cdl_c/e、cdl_c_30，延迟铺 24–77 bin，簇密集）**：ESPRIT **灾难性退化（-10~-25，常比 raw 还差）**，高 SNR×多帧也救不回 → **结构性失败**（密集/宽展源 + 子空间病态 + 幅度 LS 病态），非噪声。
- → **ESPRIT 不满足"全场景鲁棒/永不退化"**。NMSE 在 -27~-10 之间剧烈摆动，对宽延迟谱信道不可用，且需要重型保护（root-MUSIC + 阶数选择 + NNLS 功率 + 伪峰剔除 + fallback）才可能稳，风险高、收益不确定。

---

## 四、最终工程判定（对齐"做真的 true 2D MMSE"）

1. **可落地的真 2D MMSE = 可分离级联：窗口 Toeplitz 频域 LMMSE × 时域 Kalman。**
   - 频率阶用 **C 已有的 `nr_srs_mmse_freq_filter`**（窗口 Toeplitz，R=FT{清洗PDP}）：实测**稳定**比对角天花板高 1–3 dB（SNR15 -22~-24、SNR25 -29~-33），**从不崩**。
   - 时域阶接 **C 已有 Kalman**（`mmse2d` 路径已具备）。
   - 这是"在可鲁棒估计的最优 R 下"的真 2D MMSE——理论上 WSSUS 可分离最优，工程上全场景不退化。
2. **当前 C 现状（审 `nr_ul_channel_estimation.c:968-1006`）零件齐但没拼对**：
   - `mmse1d` → Toeplitz 频 LMMSE，**但单独跑、无时域级**。
   - `mmse2d` → filt8 → 频域 **MA**(会崩) → Kalman，**频率级太弱**。
   - → 真 2D 落地 = 把 `mmse1d` 的 Toeplitz 频阶接到 `mmse2d` 的 Kalman 时阶。
3. **剩余 4–6 dB（到 oracle）= 研究天花板，不追**：周期图 R 受泄漏偏差封顶（再平均无效），参数化超分辨脆弱（宽延迟谱崩）。在"全场景鲁棒/永不退化"约束下不值得，列为长期 ceiling 研究。

---

## 四点五、2D 时域级联增益 + 现有 Kalman 去旋缺陷（2026-05-29 增补，`test_srs_2d_offline.py --cascade`）

此前所有对比都是**逐帧、纯频域**；本节首次量化"时域级"在 Toeplitz 频阶之上的级联增益（= 真正的"2D"），并复现现有 C `nr_srs_2d_filter` 的去旋行为。

### 4.5.1 时域级联增益（CDL，n_sc=256，SNR15，frames=60）

| doppler | f_data(频Toeplitz) | +per-SC EWMA | +C式去旋EWMA | **时域增益(EWMA)** |
|---------|------|------|------|------|
| 0.005（≈静/低速） | -23~-25 | **-29~-31** | -27~-30 | **+5.9~+6.7 dB** |
| 0.02（低速3m/s档） | -23~-25 | **-28~-30** | -19~-30 | **+4.1~+5.5 dB** |
| 0.08（高速） | -23~-25 | -22~-27 | **-9~-22(崩)** | ~0 dB |

**结论：时域维有真实、鲁棒的 +4~6 dB（低/中速）**。正确做法（逐 SC 复数 EWMA）把 Toeplitz 频阶 -24 抬到 **-30**，追平/超过频域 oracle(-27~-29)。即：频域单维卡住的 4-6 dB，**时域维用最简单 per-SC EWMA 即可鲁棒拿回**，无需脆弱超分辨。高速时域相关消失、增益归零（符合物理）。

### 4.5.2 现有 C Kalman/2D 滤波的"全局去旋"缺陷（已复现）
`nr_srs_2d_filter.c` 的 adaptive/ibvss/kalman 三种模式都先算**整带单标量相位** `rot=phase(<state,obs>)`、把所有 SC 去旋后更新 state，且**输出 state 未乘回 rot**（`update_scalarkalman_core` L835-851）。

- 低速(0.005)：去旋≈EWMA（近静止，≈no-op）。
- 低速(0.02)：去旋比 per-SC EWMA **差 6-8 dB**（cdl_a -22.3 vs -28.2、cdl_c -19.3 vs -27.8）。
- 高速(0.08)：**灾难性 -9~-22，常比 raw 还差**（cdl_c -8.94）。
- 根因：**单标量全局相位无法表示频选信道各 SC 不同的相位演化；输出停在陈旧参考系（未回旋）**，多普勒越大错得越离谱。仅对超平坦（P1B）低速近似成立，解释了它历史上 P1B -17 能用、频选会退化。

### 4.5.3 修正后的整体判断
- "到 oracle 还差 4-6 dB = 研究天花板"**仅对频域单维成立**。
- 加时域维后：**Toeplitz 频 × per-SC EWMA/Kalman 时 的级联，CDL 可达 ~-30 dB，鲁棒无脆弱性**。
- （注）脚本 `+twien`（genie 时域 Wiener）列因把频域泄漏偏差误当时域白噪声 + R_t 估计偏差，劣于 EWMA，非有效天花板，不据其下结论。

### 4.5.4 时域级最终形态：相位预测 Kalman（PKF）> EWMA > 去旋（2026-05-29 增补）

**关键建模前提**：上节随机游走多普勒（`a·e^{jθ·randn}`）相位不可预测，预测型滤波理论上赢不了 EWMA。改用**相干多普勒**（匀速 UE，每 ray 固定相位率 `ω=ω_max·cos(phi_r)`，用 CDL 到达角合成）才是检验"预测能否要回 dB"的正确场景（`--doppler-mode coherent`）。

PKF：`pred=s·e^{jω}`（按估计 ω 把上帧推到当前帧）→ `s=pred+K(obs−pred)` → 输出 `s`（**当前帧、已回旋**）；ω 由 `ω+=Kω(angle⟨obs,s⟩−ω)` 在线估计（per-SC 或 per-link 公共）。

CDL，n_sc=256，SNR15，frames=60：

| omega_max | f_data(频) | +ewma | +C去旋 | +pkf(per-link) | **PKF赢EWMA** |
|-----------|------|-------|--------|------|------|
| 0.05（低中速） | -23~-25 | -24.5~-25.2 | **+1~+2(全毁)** | -25.8~-28.4 | **1~3.7 dB** |
| 0.15（中高速） | -23~-25 | **-17.5~-19.5(EWMA伤)** | +3(毁) | -23~-26.7 | **5~9 dB** |
| 0.40（高速） | -23~-25 | **-10~-12(EWMA崩)** | +3(毁) | -17.7~-24.3 | **7~14 dB** |

**结论：**
1. **预测型 Kalman 决定性优于 EWMA，差距随速度拉大**（低 +1~4、中 +5~9、高 +7~14 dB）。
2. **EWMA 中/高速主动变差**（盲平均旋转相量→相干相消），仅低速安全。
3. **C 现有去旋在相干多普勒下彻底崩**（输出 +1~+3 dB，比 raw 差），必须替换（实锤）。
4. **PKF 高速守住频阶、低/中速再加几 dB**；per-SC≈per-link，**per-link 公共 ω（每链路一个多普勒 + 逐 SC 复数状态）更简单鲁棒，进 C 首选**。

### 4.5.5 C 端时域级定稿（替代 `nr_srs_2d_filter` 去旋）
- **每 (ant,port) 一个 ω**（功率加权聚合相位率，EMA 更新）；**每 SC 一个复数状态 s**。
- 每帧：`pred=s·e^{jω}` → `s=pred+K(obs−pred)` → **输出 s（当前帧、已回旋）**。K 自适应（IAE-Riccati，见 §4.5.6），R_meas 由频阶传播白噪声 `σ²‖bᵀA⁻¹‖²` 喂入（§4.5.7），作用在 per-SC 复数残差上，**不做全局去旋**。
- warmup/塌缩 fallback；gap-reset 保留。彻底删除"单标量全局去旋 + 输出未回旋"。

### 4.5.6 自适应 K 验证：IAE-Riccati 追平/超过最优固定 K（2026-05-29）
- **朴素自适应失败**：`ratio=innov_smooth/分位地板` 对纯噪声恒 >1 → 静止信道也给大 K → 比固定 K 差 5–18 dB（负结果，已弃）。
- **正确结构 = IAE-Riccati**：`P_pred=P+Q; K=P_pred/(P_pred+R); Q=EMA(innov−P−R)`，**需真实测量噪声 R**。喂 genie R 验证结构（CDL，SNR15）：

| omega_max | ricc−fix（自适应 vs 最优固定，负=更好） |
|-----------|------|
| 0.05（低速） | +0.2 ~ +1.1（≈固定） |
| 0.15（中速） | −0.7 ~ +0.6（≈固定） |
| 0.40（高速） | **−5.4 ~ +0.5（cdl_a 比固定好 5.4 dB）** |

- **结论**：自适应 K（**喂 genie R**）自动收敛到≈最优固定 K，高速更优 → 结构正确，**目标采用自适应 K**。

### 4.5.7 ✅ 开口项①已闭合：数据驱动 R_meas = 频阶传播白噪声（2026-05-29）
Riccati 自适应 K 需要真实测量噪声 R_meas（=频阶输出残差噪声）。

**根因（曾踩坑）**：复 Hermitian 下，`est=bᵀA⁻¹y` 的等效权重是 `bᵀA⁻¹`（即 `A⁻¹·conj(b)`），**不是 `A⁻¹b`**。误用 `A⁻¹b` 会对齐 A 的近奇异（噪声子空间）特征向量，把噪声增益高估 ~7000×（实测 ‖A⁻¹b‖²=966 vs 正确 ‖bᵀA⁻¹‖²=0.145，经验实测 0.145 ✓）。

**正解 R_meas = 频阶传播白噪声**：`R_meas = σ²·mean_k ‖bₖᵀA⁻¹‖²`（b=交叉相关行、A=R_ww+σ²I，全部来自频阶 Toeplitz；σ² 即 C 的 noise_power）。这是频阶把输入白噪声传到输出的方差，正是时域级该消的部分（时不变的泄漏 bias 不计入 R，正确）。

修正后验证（CDL，SNR15，扫多普勒）：

| omega_max | Rd/Rg | ricc_dataR vs ricc_genieR | dataR vs 最优固定K |
|-----------|-------|------|------|
| 0.05（低速） | 0.70~0.94 | 差 ≤0.25 dB | ≤+1.0 dB（≈固定）|
| 0.15（中速） | 0.78~1.01 | 差 ≤0.25 dB | −0.9~+0.4 dB（≈/略优）|
| 0.40（高速） | 0.81~0.99 | 差 ≤0.06 dB | **cdl_a −5.44 dB（自适应大胜固定）** |

- **结论**：**全数据驱动 Riccati 追平 genie 版、匹配/超过最优固定 K，高速大幅领先**。Rd/Rg≈1（略<1 = 排除泄漏 bias，符合预期）。
- **Kalman 改法至此完整、全部可移植 C**：① 频阶 窗口 Toeplitz（已有）；② 时阶 per-link ω 相位预测 + per-SC 复数状态 + IAE-Riccati 自适应 K；③ R_meas = `σ²‖bᵀA⁻¹‖²` 由频阶 A,b,σ² 现成算出。**进 C 改 Kalman 的前置条件已满足。**

## 四点六、C 端逐项核对（编译前，2026-05-29）—— 发现频阶共轭严重 bug

进 C 前对照离线逐项核对 `nr_srs_mmse_freq_filter`（频）与 `update_pkf_core`（时）：

### 频阶 Wiener（vs 离线 `windowed_lmmse`/`freq_stage_toeplitz`）
| # | 项 | 离线（正确） | C 原状 | 处置 |
|---|----|------|------|------|
| 1 | **权重共轭** | `out=bᵀA⁻¹y`（`b@solve(A,y)`）| `weights=A⁻¹b`、`out=Σwᵢyᵢ=bᵀA⁻ᵀy` | **🔴BUG，已修** |
| 2 | PDP 时域 EMA | 跨帧 EMA α=0.95 | 每帧现算（无 EMA）| 🟡待集成 |
| 3 | 噪声地板 | robust 迭代裁剪 | 远尾 25% 分位 | 🟡待集成 |
| 4 | 变换/输入 | n_sc 点 IFFT 全观测 | 2048 零填充梳状导频 | ✅已验证OK（见下）|
| 5 | R_meas 输出 | `σ²‖bᵀA⁻¹‖²` | 无 | ✅已集成（精确）|

**bug #1 影响（离线实测，n_sc=256 win=64）**：
| 信道 | 正确 LMMSE | C 原做法 | 差 |
|------|-----|------|-----|
| flat 在格 | -27.4 | -25.1 | 2.2（≈对）|
| flat 离格(0.3) | -23.3 | **-8.9** | 14.3（崩）|
| 频选 3抽头 | -25.2 | **+48.6** | 73.8（灾难）|

→ 复 Hermitian 下 `A⁻ᵀ=conj(A⁻¹)≠A⁻¹`。**现有 mmse1d 只在平坦+整数延迟对**，任何真实频选必崩 → 解释了 C 端诚实稠密永远摸不到离线 -42。
**修法（已落 C + 离线验证 diff≈2e-14）**：RHS 用 `conj(b)`（`system[row][W]=conj(R)`）、应用 `out=Σ conj(wᵢ)·yᵢ`。同时修正 mmse1d（本就错）。

### 时阶 Kalman/PKF（vs 离线 `time_pkf_riccati`）—— 基本忠实，已对齐 2 处瞬态
- 递推逐行一致 ✅。
- Q/P 初值：已改为 `P=R_meas, Q=0.01·R_meas`（对齐离线；R_meas 由频阶当帧先置）。
- warmup：已改为只 bootstrap 帧 raw、其余滤波（对齐离线无 warmup）。

### #4 验证（`check_c_freq_mirror.py`，忠实复刻 C 梳状/2048 recipe，2026-05-29）
对照"梳状导频 Toeplitz"，扫 NFFT∈{n_pil, n_pil·K_TC, 2048}：

| 信道 | cmirror_npil | cmirror_2npil | **cmirror_2048(C)** | genie 上限 |
|------|------|------|------|------|
| cdl_a(K2) | -24.9 | -23.5 | -23.6 | -29.9 |
| cdl_c(K2) | -24.4 | -24.2 | -24.2 | -27.4 |
| cdl_e(K4) | **-12.2(崩)** | -16.1 | **-16.1** | -24.3 |

- **`2048 ≈ n_pil·K_TC`，处处等效** → 2048 零填充无标定问题，**不需 KISS-FFT/精确 n_pil 变换**。
- **2048 更鲁棒**：K_TC=4+宽延迟(cdl_e)下 `NFFT=n_pil`(不补零)崩到 -12，2048 稳 -16 → C 选 2048 正确。
- cmirror 全程优于 raw 导频、不崩；距 genie 2–6 dB = 数据 R 周期图天花板（非标定 bug）。→ **#4 闭环：保留 2048。**

### 状态：全部清单 + Kalman 两处已完成（待编译）
- **已落 C（lint 干净）**：频阶 #1 共轭、#2 EMA PDP、#3 robust floor、#5 R_meas；时阶 PKF + Q/P 初值 + warmup；dispatch `SRS_ESTIMATOR=true2d`（Toeplitz频→PKF时）。
- **#4 离线验证 OK**（2048 保留）。
- **唯一剩余**：编译 + 端到端（用户执行；P1B 平坦只能验不崩/不回归，频选端到端待 CDL attach 打通）。
- 改动文件：`nr_srs_mmse.{c,h}`、`nr_srs_2d_filter.{c,h}`、`nr_ul_channel_estimation.c`；新增 `check_c_freq_mirror.py`。

## 四点七、算法脚本函数标注索引（2026-05-30）

主脚本 `DevChannelProxyJIN/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy/test_srs_2d_offline.py`（行号以当前版本为准）。

### A. 真 2D 主链（黄金参考 → 对应已落 C）
| 函数 (行) | 作用 | 状态 / 对应 C |
|-----------|------|----------------|
| `windowed_lmmse` (524) | **频阶**：窗口 Hermitian-Toeplitz LMMSE，`out=bᵀA⁻¹y`（Wiener-Hopf 正确共轭） | 黄金参考 → C `nr_srs_mmse_freq_filter` |
| `freq_stage_toeplitz` (716) | 逐帧频阶封装：EMA-PDP→robust floor→R=FT{PDP}→windowed_lmmse；支持 data R / genie R | 黄金参考（cascade 用） |
| `robust_noise_floor` (321) | 迭代裁剪鲁棒噪声地板（中位数起，trim k=3） | → C `nr_srs_robust_noise_floor_arr` |
| `correlation_from_pdp` (73) | R(Δk)=Σp·e^{-j2πΔk d/N}（维纳-辛钦/genie R） | 频阶 R 的理论形式 |
| `time_pkf_riccati` (652) | **时阶**：per-link ω 相位预测 + per-SC 复数 + IAE-Riccati 自适应 K | 黄金参考 → C `update_pkf_core` |
| `time_pkf_link` (636) | PKF 固定 (kv,kw) 版（对照/best-fixed） | 对照基准 |
| `freq_propagated_noise_avg` (681) | **R_meas** = `σ²·mean‖A⁻¹·conj(b)‖²`（频阶传播白噪声，喂时阶） | → C 频阶 `wnorm2` 累加 + `nr_srs_2d_set_rmeas` |
| `_band_median_power` (602) | 创新功率 8-带中位数（鲁棒） | → C `update_pkf_core` 内同名逻辑 |
| `lmmse_fullband` (81) | 全带 Toeplitz LMMSE（O(n³)，真 2D 频阶 **oracle 上界**） | 仅离线上界，不进 C |

### B. 自适应门限（Phase1.5，白噪声稠密下的无参数选择器）
| 函数 (行) | 作用 | 状态 |
|-----------|------|------|
| `adaptive_floor_threshold` (278) | robust floor + 噪声 DOF → Wilson-Hilferty chi² 分位 → auto-gamma（`p_fa=1/N`） | 白噪声稠密 OK；真实 filt8/passthru 崩（坎#3） |
| `_norm_ppf` (249) | 标准正态分位（Acklam 有理逼近，可移植 C） | 工具 |

### C. 时域对照 / 证伪
| 函数 (行) | 作用 | 状态 |
|-----------|------|------|
| `time_ewma_persc` (610) | per-SC 复数 EWMA（无相位预测） | 对照：中/高速会伤 |
| `time_derot_ewma` (621) | **复刻 C 旧全局去旋 + 输出未回旋** | 证伪用：频选/相干多普勒下崩 |

### D. 频域基线 / 天花板 / 参数化（多为历史或对照，未进主线）
| 函数 (行) | 作用 | 状态 |
|-----------|------|------|
| `oracle_pdp_wiener_active` (98) | 对角延迟域 oracle PDP Wiener（**od_diag 对角天花板**） | 上界对照 |
| `data_pdp_ema_wiener_active` (348) | 对角延迟域 因果 EMA + 秩选择 Wiener（Phase1 主力） | **已被 Toeplitz 取代**（弱 1–3dB） |
| `data_ema_wiener_cport` (421) | C-faithful 梳状导频→2048 IFFT→稠密重建（diagonal 版） | 历史（cport 验证） |
| `esprit_reconstruct_r` (552) | 参数化 ESPRIT 估 τ→重建 R | **判定脆弱**：宽延迟谱崩，不进主线 |
| `estimate_noise_delay` (132) | 尾部噪声地板（tail-mean/p25/median） | 工具 |
| `data_pdp_wiener_active` (145) / `data_topk_delay_active` (190) / `data_energy_delay_active` (215) | 早期 data-PDP / top-k / energy 截断基线 | 历史基线 |

### E. 信道合成 / 通用工具
| 函数 (行) | 作用 |
|-----------|------|
| `synth_cdl_channel` (486) | CDL ray (tau,power[,phi_r]) → 频域信道；`randomwalk`(扩散) / `coherent`(匀速多普勒 ω=ω_max·cosθ) |
| `freq_response_from_pdp` (65) / `moving_average` (36) / `smooth_active_complex` (55) / `active_circular_order` (45) / `nmse_db` (30) / `pct` (736) | 合成/MA/排序/NMSE 工具 |

### F. 运行模式入口（`main` 1163 分发）
| 模式 | 函数 (行) | 触发 |
|------|-----------|------|
| 真实 SRS dump | `run_real_data` (741) | `--run-dir` |
| 合成多帧频选 | `run_synth_multiframe` (880) | `--synth-mf` |
| CDL 离线（raw/MA/EMA/od/oracle） | `run_cdl_offline` (945) | `--cdl-dir` |
| 真 2D 频阶五档（对角/data-Toeplitz/ESPRIT/genie/oracle） | `run_true2d` (1060) | `--cdl-dir --true2d` |
| 2D 时域级联（EWMA/去旋/PKF/Riccati） | `run_cascade` (1114) | `--cdl-dir --cascade` |

### G. C 端口忠实度验证脚本 `check_c_freq_mirror.py`
| 函数 (行) | 作用 |
|-----------|------|
| `windowed_toeplitz_comb` (18) | 梳状导频 → 重建全 SC 的窗口 Toeplitz LMMSE（带正确共轭），复刻 C 的解 |
| `c_mirror_R` (54) | 复刻 C 的"连续导频→NFFT IFFT→EMA PDP→robust floor→R=FT{PDP}"，扫 NFFT 验证 #4 标定 |

> 离线↔C 对应关系（M2.4 逐点比对的两对）：`windowed_lmmse`↔`nr_srs_mmse_freq_filter`；`time_pkf_riccati`↔`update_pkf_core`。R_meas：`freq_propagated_noise_avg`↔C 频阶 `wnorm2` 累加。

## 四点八、M2.4 离线↔C 逐点比对（2026-05-30，路线 A，已闭环）

不碰 attach/信道/GT/rfsim，把两段 C 抽成隔离单元测试，喂固定向量逐 SC 比对黄金参考。harness 在 `openair1/PHY/NR_ESTIMATION/c_unit_tests/`（`make && ./run_all.sh`）：用 shim 头 + 浮点参考 idft `#include`/链接**真实生产 .c**，参考 idft 两端逐位一致（隔离 OAI 定点 idft 标定，那由 #4 已闭合），故纯测**算法**。

| 阶段 | 生产 C | 黄金参考 | 结果（多 seed/配置）|
|------|--------|----------|------|
| 频阶 | `nr_srs_mmse_freq_filter`(true2d) | `golden_freq.py` 忠实复刻 recipe | **0–1 LSB（逐位一致）** |
| 时阶 | `update_pkf_core`/`nr_srs_pkf_update` | `time_pkf_riccati`（离线逐字） | **≤1 LSB（float32 vs float64）** |

### 结论
1. **频阶逐位一致**：坐实共轭修复（`A·w=conj(b)`、`out=Σconj(w)·y`）+ EMA-PDP + robust floor + R=FT{PDP} + 窗口 Toeplitz 解，是黄金 recipe 的**精确转写**。
2. **时阶发现并修复一处真实分歧（实锤，非 float 噪声）**：原 `update_pkf_core` 的 Q（过程噪声）更新被 `frame_count>3` 门控、且**EMA 后**再裁地板；离线黄金 `time_pkf_riccati` 是**每帧**更新、且把地板放进 `max(innov_smooth−P−R, R·1e-3)` **增量内**。早期帧差**最高 ~590 int16 LSB**。对齐两行后 C↔黄金 ≤1 LSB（仅 float32 舍入）。**已改 `nr_srs_2d_filter.c::update_pkf_core`**（删 fc>3 门控 + 地板入 max）。
   - 影响面：仅 true2d PKF 时阶（新路径，端到端尚未投产），低风险；需重编译 `nr-softmodem` 生效。
3. **此前日志称"时阶递推逐行一致"被本次逐点比对修正**：递推主体一致，但 Q 两行此前不一致，现已对齐。

> 改动/新增文件：`nr_srs_2d_filter.c`（PKF Q 对齐）；新增 `c_unit_tests/`（shim 头、`srs_test_idft.c`、`test_pkf_main.c`、`test_freq_main.c`、`golden_pkf.py`、`golden_freq.py`、`Makefile`、`run_all.sh`、`README.md`、`_diag_*`）。

### 4.8.1 C1：OAI 真定点 idft 逐项核对（2026-05-30，堵最后未逐位的环节）
M2.4 频阶测试用浮点参考 idft（隔离算法）；C1 把唯一没逐位核过的环节——OAI **真定点 `idft2048`**（从已编译 `libdfts.so` 链接，探针 `probe_idft.c`+`probe_stubs.c`，`make probe_idft && python3 c1_idft.py`）——喂相同稠密导频，量化它对频阶输出的扰动：

| 量 | 实测（多 seed）|
|----|------|
| idft 缩放(OAI/ref) | ≈√2048（OAI 是 1/√N 归一；经 R(0)=1 归一**抵消**）|
| PDP 形状相对误差 | **<1%**（0.006–0.0085）|
| 频阶输出 NMSE(OAI vs 浮点ref) | **−41 ~ −44 dB**（远低于信号）|

**结论**：OAI 定点 idft **不是精度瓶颈**，M2.4 浮点参考的算法结论**可迁移到运行系统**。至此频阶链路（含定点 idft）全部核完，无遗留未验证数值环节。新增 `c1_idft.py`、`probe_idft.c`、`probe_stubs.c`。

## 四点九、方案三：SRS-only CDL 施加（2026-05-30，已落 proxy，待端到端验证）

### 背景（为什么需要）
proxy-CDL 端到端被两道墙挡住：① attach 临界/随机（多径 → PRACH/TA + RRC 阶段 UL BLER ~43% → `WAIT_RRC_RECFG` 掷硬币，cdl_a 偶尔连上但只采到 3–10 帧）；② SRS↔GT 对应：默认 eval 只做单标量对齐，在 CDL 上 NMSE 爆正（cdl_a SNR25 实测 legacy +12.16 / true2d +13.16），逐帧 STO 对齐后仍有 ~+2 dB 残留；且 cdl_a 近乎平坦（GT roughness 1e-3）无频选可展示。

### 方案三机制（代码调研确认）
proxy 的 GPU 管道**内部本就逐 OFDM 符号**做频域 `H(f)` 乘法（`gpu_H` [14,n_rx,n_tx,FFT]）；且 **GT 与 IQ 共用同一 `channels_ul` 张量**（`stage_for_ue` 拷贝 `channels_ul[12]`，symbol 12=SRS，OAI `startPosition=1`）。→ 在 `process_slot_ipc`/`stage_for_ue` 之前，把**非 SRS 符号**的 `H(f)` 改成**该符号跨子载波频域均值（平坦，零延迟扩展）**、保留符号 12 的完整 CDL：
- PRACH/Msg3/PUSCH 在平坦信道 → attach 与 RRC 稳定（BLER 回落）、attach **与信道选频性解耦**（cdl_c/e 也能连）；
- SRS（符号 12）仍看完整 CDL；**GT[12] 自动等于 SRS 所见 CDL**（同一张量，零额外对齐）。

### 已落代码（`DevChannelProxyJIN/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy/v8.py`，lint/py_compile 净）
- 新增 CLI：`--srs-only-channel`（默认关，opt-in）、`--srs-symbols`（默认 "12"）。
- 新增 `Proxy._apply_srs_only_mask(channels_ul)`：非 SRS 符号置频域均值，返回 **copy**（不污染 ring buffer view）。
- 接线：`_ipc_ul_superposition_slot`（主 UL 路径，mask 在 `process_slot_ipc`+`stage_for_ue` 之前，保证 IQ/GT 一致）；防御性同样加在 `_ipc_apply_channel` / `_ipc_apply_channel_for_ue` 的 UL 分支（DL 不动）。
- 默认关 → P1B/现有回归不受影响。
- 透传：`launch_all_v9.sh` 加 `SRS_ONLY_CHANNEL`(默认0)/`SRS_SYMBOLS`(默认12) env → 拼进 `PROXY_EXTRA_ARGS`；`SRS_ONLY_CHANNEL=1` 经命令行 env 由 run_q4 继承到 launch_all（与 NPY_DIR 同机制）。

### 评估侧：`eval_nmse_sto.py`（新增）
- 在 `eval_nmse_clean` 的基础上加**逐帧、逐天线 STO（真实 FFT bin 线性相位）对齐 + 标量**，给出 scalar-only（与 eval_nmse_clean 逐位一致）与 STO 对齐两列，及 A/B delta。
- 自检（旧 cdl_a 数据）：scalar-only legacy +12.16/true2d +13.16（== eval_nmse_clean ✓），STO 对齐 +1.61/+2.14，A/B delta(STO) +0.53 dB。STO 中位数 ~4 样本。

### 端到端验证命令（用户 sudo 跑；attach 解耦后可上强频选 cdl_c/e）
```bash
cd DevChannelProxyJIN/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy
# 1) attach 冒烟（cdl_c，以前 attach 崩；SRS_ONLY_CHANNEL=1 开启 SRS-only CDL）
sudo P1B_NPZ="" NPY_DIR="data_out/cdl_c" UE_SPEED=3 CHANNEL_SEED=42 \
ONLY_SNR="25" MAX_FRAMES=400 UL_PRE_GAIN=2.0 SRS_ESTIMATOR=legacy \
SRS_PERIOD_SLOTS=10 GT_SAVE_EVERY=100 SRS_ONLY_CHANNEL=1 \
bash run_q4_snr_sweep_v9.sh
# 2) 同种子换 SRS_ESTIMATOR=true2d SRS_2D_DEBUG=1 再跑一发；然后 A/B：
#    python3 eval_nmse_sto.py --ab <legacy_sweep>/snr_25dB <true2d_sweep>/snr_25dB
# gnb.log 应见 "SRS-only CDL: ENABLED ..."；attach 应稳定、UL BLER 大幅回落。
```

### 端到端实测结果（2026-05-30，cdl_c，SRS_ONLY_CHANNEL=1，legacy，SNR25）
- **attach 首次成功**：`25 OK`、5 GT、11 SRS、**178s**（以前 cdl_c 全是 900s 超时崩）。`proxy.log` 实证 `SRS-only CDL: ENABLED (... symbol(s) 12 ...)`。→ **attach 与信道选频性解耦验证成功**（墙①破）。
- **GT[12] 确实是完整 CDL（强频选）**：PDP tap0=0.14 / E[taps1..63]=**1.55**（能量铺在多径上）vs cdl_a tap0=1.0 / spread 0.22（近平坦）。→ 符号 12 保留 CDL、GT 一致，设计正确。
- ⚠️ **UL BLER 仍 67%**：均值平坦对 NLOS（cdl_c）增益偏低（DC/均值抽头小），PUSCH 仍吃力；但比 full-CDL cdl_c 的 92% 已改善、足以 attach。**"mean 平坦"是弱点**，可换"保功率平坦（|H|=sqrt(mean|H|²)）"或最强抽头改善。
- ❌ **SRS↔GT 对应残留未解、且随选频性变大**：legacy STO 对齐 NMSE **+5.04 dB**（cdl_c）> cdl_a 的 +2.10。即 GT 参考与 OAI 估计逐 SC 仍对不上，越选频越差。这个 +5 dB 地板会淹没 1–3 dB 的 true2d 增益。

### 修正：mean 平坦对 NLOS 是错的 → 改默认 power 保功率平坦（2026-05-30）
- 实验：`GT_SAVE_EVERY=10` 那发 attach 失败（仅与成功发差此一参数）→ 加密 GT 增开销压垮临界 attach；且 `gap=0` 同 slot 配对**本就存在**却零相关，**加密 GT 修不了对应**，已撤回（回 100）。
- 根因实证：NLOS（多径铺开）的**频域均值≈0**（散径在均值里相消）。numpy 实测 20 散径信道：mean-flat 功率比 **0.00**、power-flat 功率比 **1.00**。→ `mean` 平坦给非 SRS 符号近零增益 = PUSCH 被毁 = 67% BLER + attach 掷硬币的真正原因。
- 已落代码：`_apply_srs_only_mask` 增 `--srs-flat-mode {power,mean,firsttap}`，**默认 `power`**（`|H_flat|=sqrt(mean|H|²)`+均值相位，保满功率→PUSCH ≈ 平坦信道→attach 应确定）。launcher 加 `SRS_FLAT_MODE`(默认 power) 透传。
- **重跑（用户，回 GT_SAVE_EVERY=100，默认即 power）**：
```bash
sudo P1B_NPZ="" NPY_DIR="data_out/cdl_c" UE_SPEED=3 CHANNEL_SEED=42 \
ONLY_SNR="25" MAX_FRAMES=400 UL_PRE_GAIN=2.0 SRS_ESTIMATOR=legacy \
SRS_PERIOD_SLOTS=10 GT_SAVE_EVERY=100 SRS_ONLY_CHANNEL=1 \
bash run_q4_snr_sweep_v9.sh   # banner 应显示 flat mode=power
```
预期：UL BLER 大幅回落、attach **稳定可复现**（多跑几发都 OK）。

### SRS↔GT 对应是另一条独立的预存 bug（与算法/我们的改动无关）
排查实锤（旧 cdl_a legacy 也有，故非本次改动）：SRS 与 GT 幅度包络**零相关**（cdl_c +0.08）、频域平滑救不回（mavg101 仍 0.08）、`gap=0` 同 slot 仍零相关、GT 帧间自相关 ≈0（`GT_SAVE_EVERY=100`=50ms ≫ 相干时间~10–28ms，每存的 GT 是独立衰落）。→ **SRS dump 的 frame/slot 编号与 GT `slot_id=ts//30720` 不同源**，align 用常数偏移凑中位数但没配到同物理快照。这是 `SRS_GT_ALIGNMENT_ISSUE` 类问题，需单独查编号参考系（独立工作项，attach 修稳后再办）。

### ✅ 突破：静态 cdl_c + gain=1.0 → SRS↔GT 对应坐实（2026-05-31）
两个根因同时定位并验证：
1. **UL_PRE_GAIN=2.0 削顶**：实测 4 发 cdl_c，gain=2.0 三发 attach 失败（int16 饱和→ULSCH BLER 65%→掷硬币）；**gain=1.0**（=各脚本默认；之前被显式传 2.0 覆盖）才稳。已在 `SWEEP_V9_USAGE.md` 加警告。
2. **SRS↔GT 时间对齐**：`UE_SPEED=0`（静态）run（q4_sweep_20260530_235303）采到 **431 SRS 帧 + 6 GT**，`eval_nmse_sto`：
   - STO = **恒定 +4 样本**（p10/p90 都 +4，正是 §10.3 预测的 IPC 管道延迟），不再乱跳；
   - NMSE：scalar-only **+14.91** → **STO 对齐 -6.37 dB（负！rx1tx1 -9.49）**。
   → **SRS 与 GT 在静态 cdl_c 上确实对得上**（提取栅格一致、STO 对齐有效）；speed3 的 +5dB 全是 Doppler 把动态包络去相关 + 未去 +4 STO。
- **legacy cdl_c 静态基线 = -6.37 dB（STO 对齐）**。下一步同配置跑 true2d → 首个干净的真实系统频选 A/B。
- 复现：`UE_SPEED=0 UL_PRE_GAIN=1.0 SRS_ONLY_CHANNEL=1 NPY_DIR=data_out/cdl_c`，`eval_nmse_sto.py --run-dir <sweep>/snr_25dB`。
  （注：manifest 标 FAIL-ATTACH-TIMEOUT 是 attach-stable watcher 没捕到 rrc_reconfig 触发，但 SRS/GT 数据已采到 431/400 帧，可直接评估。）

### 🎯 cdl_c attach 根因定论 + 修复（2026-05-31）
**根因(实锤)**：静态(speed 0)下 PRACH `timing_offset` 仍**散乱 0~30 样本**（成功/失败两发都是）。静态信道恒定,若 PRACH 看平坦则 timing 应恒定;实测**不同 preamble(RAPID 8/13/3/34/52…)给出完全不同 timing(0/6/10/1/27/15/18/23/28…)** → PRACH 仍在经历 cdl_c 多径(不同 preamble 与多径相关→峰值落不同抽头)→ TA 错 → "Scheduling retransmission of Msg3" 无限重传 → RA 永不完成 → attach 失败。cdl_a DS≈1 样本 → 抖动 ±1 可容忍 → 能过。
- **为什么 SRS-only 没救到 PRACH**：`attach_stable` 只把信道在**时间上冻结**,**没变平坦**;且 mask 保留 symbol 12 的 CDL(PRACH 很可能落在那)。→ attach 阶段 PRACH 仍吃完整多径。这就是 §7.2 那堵 OAI-PRACH-多径墙。

**修复(已落 v8.py)**：用已有的 dynamic-handoff 触发文件 `v8_dynamic_enable` 给 mask 加门控 ——
- **handoff 之前(attach 阶段)**：`keep=∅` → **全符号平坦**(含 symbol 12)→ PRACH/Msg3/RRC 全走平坦 → attach 可靠/确定;
- **handoff 之后(已连上)**：`keep={12}` → 只保 SRS 符号 CDL → SRS 测频选。
- 实现:`_apply_srs_only_mask` 检查 `attach_stable_trigger_file` 是否存在(latch `_srs_handoff_done`);attach_stable 关闭时退化为从头 SRS-only。py_compile 通过。

**重跑(配置不变,门控自动生效;静态保证干净对齐)**：
```bash
sudo P1B_NPZ="" NPY_DIR="data_out/cdl_c" UE_SPEED=0 CHANNEL_SEED=42 \
ONLY_SNR="25" MAX_FRAMES=400 UL_PRE_GAIN=1.0 SRS_ESTIMATOR=legacy \
SRS_PERIOD_SLOTS=10 GT_SAVE_EVERY=100 SRS_ONLY_CHANNEL=1 \
bash run_q4_snr_sweep_v9.sh
```
预期:timing_offset 在 attach 阶段变稳(不再 0~30 散)→ attach **确定性成功**;连上后 SRS 仍见 CDL → eval 仍 -6 dB 级。然后 true2d 同跑 → A/B。

### ⚠️ 根因修正：timing 散乱是误判,真凶是 Msg3/RA(2026-05-31,~12 发后定论)
- **推翻"PRACH 多径定时"假设**:对照 **P1B(平坦、稳定 attach)的 timing_offset 也散乱**(11/14/7/25/0/0)——和 cdl_c 一样。→ timing_offset 散 0~30 是这套 rfsim/proxy 的 **PRACH 检测固有方差**,P1B 照样 attach,**不是凶手**。
- **真凶 = Msg3 撞 max HARQ**:cdl_c 失败日志 `RA failed at state WAIT_Msg3 (Reached msg3 max harq rounds)`。Msg3(PUSCH)反复重传解不出 → RA 失败。
- **信道级干预全部无效**:把 attach 阶段信道**全平坦**(handoff 门控,handoff 未触发=全程 flat)后 Msg3 **仍**解不出。→ 不是信道多径问题,是 **OAI Msg3/RA infra**(cdl_c 配置在此 proxy+OAI 下 Msg3 过不去,P1B 能)。SRS-only/gain/静态/平坦都不响应。
- **定论**:cdl_c attach 卡在 OAI Msg3/RA,信道级修不动 = §7.2 那堵墙的硬底。**与算法/C 代码无关**。已落代码(attach-gating、power-flat、SRS-only、eval_nmse_sto)保留,概念验证有效,但可靠端到端被此 infra 阻塞。
- **proxy-CDL 净产出**:cdl_c attach 通过 1 次(GT[12]=CDL 验证)、legacy 静态 SRS↔GT 对齐坐实(-6.37 dB)。可靠 A/B 受阻于 Msg3/RA infra。

### 判读结论（2026-05-30）
- 方案三**达成其 scoped 目标**：attach 可靠 + 拿到干净充足的强频选 CDL SRS 数据 + GT[12]=CDL。
- **真正的长杆 = SRS↔GT 对应残留（+5 dB，选频下更大）**，与 attach 正交（计划已预判）。**A/B 在此地板下无法干净读出 true2d 增益**，需先攻 GT 对应根因。
- 决定性下一步（用户 sudo 跑）：cdl_c + SRS_ONLY_CHANNEL=1 + `SRS_ESTIMATOR=passthru`（raw LS），STO 对齐评估：若 raw LS 也 +5 → 纯 GT 参考问题（查 GT 符号/时延/CP 参考系）；若转负 → 估计器侧。

### 判读 go/no-go（原计划口径）
- cdl_c/e 在 `--srs-only-channel` 下能稳定 attach + 采足帧、且 GT[12] roughness 体现频选 → 墙①破。
- STO 对齐 NMSE 转负且 true2d < legacy（尤其 cdl_c/e）→ 首次坐实真实系统频选增益；若仍 +2 dB 残留地板 → 触发 SRS↔GT 对应根因排查（独立项）。

## 四点十、Plan B：SRS↔GT 编号同源根治（V3）+ 静态/动态实测（2026-05-31）

### 4.10.1 根因精确化（推翻"两个无关时钟"）
代码核对（explore 实证）：GT 与 SRS **共享同一根 openair0 采样轴**——UL IQ 经 V8 IPC 环形缓冲，proxy 写 `ul_rx@ts`、gNB 用同一 `ts`(`nextRxTstamp`) 读回。
- GT：`slot_id = ipc_ts // 30720`（`v8.py:1028`，绝对、非回绕）。
- SRS：原仅记 `frame/slot`（SFN 域，1024 帧回绕），`align_frames` 用 `median(gt)-median(srs)` **每发猜常数 C**（实测 C 每发不同：48482 vs 103852）→ 回绕 + 窗口错位致 median 落偏，动态下 gap=0 仍零相关(+0.08)。
- **真凶 = SRS 侧丢了绝对采样值**，只能靠每发猜 C。

### 4.10.2 修复（已落 C + 评估侧，编译通过）
- **SRS bin V3**（`defs_gNB.h` `SRS_BIN_VERSION 2→3` + `meta_timestamp[2][MAX_DUMP_FRAMES]`）：每帧 `rnti+pad` 后追加 `int64 sample_ts`。注入点（`nr_ul_channel_estimation.c:1253`）用 `frame/slot` + `RU_list[0]->proc.timestamp_rx` 做 **epoch 重建**绝对 ts（解 SFN 回绕，避 L1↔RU 竞态）；写盘 `nr_init.c`。V2 读者按 version 分支，向后兼容。
- **评估侧**：`eval_nmse_clean.load_srs` / `digital_twin_stats` 读 V3 → `meta["sample_slots"]=ts//30720`；新增 `unify_reference_frame()`（**绝对轴偏移=0 最近邻**，无 median-C）；`eval_nmse_sto.py` V3 走统一配对、V2 回退；新增 **gap=0 包络相关性硬校验**（`corr(|SRS|,|GT|)`，阈值 0.30，不达标打 `⚠ ALIGNMENT SUSPECT`）。
- 改动文件：`defs_gNB.h`、`nr_init.c`、`nr_ul_channel_estimation.c`、`eval_nmse_clean.py`、`eval_nmse_sto.py`、`digital_twin_stats.py`。

### 4.10.3 静态 cdl_b A/B（V3 实测，证修复生效）
`UE_SPEED=0, UL_PRE_GAIN=1.0, SRS_ONLY_CHANNEL=1, cdl_b, SNR25`：

| 量 | V2(旧 median-C) | **V3(unified)** |
|----|----|----|
| true2d gap=0 corr | **+0.114（坏，≈零相关）** | **+0.531（OK）** |
| legacy gap=0 corr | +0.695 | +0.652 |
| A/B delta(STO对齐) | **+5.60 dB（假象）** | **+0.85 dB** |

- **结论**：之前"true2d 差 5.6 dB"是**该发 median-C 对齐失败的假象**（corr 0.11）；V3 把 true2d 配回正确物理 slot（corr 0.53），A/B 塌缩到 +0.85 dB ≈ **打平**。
- 静态 NMSE（STO 对齐）：**legacy −7.2 / true2d −6.4 dB**。系统地板 ~−7 dB（SRS↔GT 残留 + STO + 稀疏配对主导）远高于离线频阶 −22 dB → **静态分不出估计器优劣**（都在地板下），需动态。
- 残留：legacy STO −12 / true2d +4（per-estimator 群延迟 + 搜索抖动，~±1dB）。

### 4.10.4 动态 speed3 cdl_b：legacy 首次采到，true2d 0 SRS = 与估计器无关
- **legacy（`q4_sweep_20260531_183732`）**：RRC 全程走完（RRCSetupComplete→SecurityMode→UECapability→**RRCReconfiguration**=配 SRS）→ **245 帧**（首个 speed3 动态采集）。
- **true2d（`_185541`）**：connect(Msg4 ACK)@127 后 **无 RRCSetupComplete、无任何 RRC 进展** → SRS 从未配置 → **0 帧**（estimator banner 都没打=估计器一行没跑过）。
- **根因 = 与 true2d 无关**：`RRCSetupComplete` 是 **UE 上行 PUSCH** 消息，`SRS_ESTIMATOR` 只在 SRS 被调度后（RRC 重配之后）才起作用。true2d 那发连 RRC 重配都没到，估计器代码**结构上不可能执行**。真凶 = **speed3 下 connect 后上行在 RRCSetupComplete 处失败**（同 Msg3/RA infra 家族的脆弱掷硬币），legacy 赢、true2d 输，estimator 标签纯巧合。
- **3 m/s 不是变量**：3.5GHz 下 Doppler ~35Hz、相干 ~28ms，物理上极轻；且 attach 阶段信道被 handoff 门控成**平坦**（rrc_reconfig 未触发=全程 flat），speed3 那时根本没作用到信道。脆弱纯属 proxy/OAI attach/UL infra 非确定性。
- **处置**：true2d 重跑几发（掷硬币，legacy 已证 speed3 可成）catch 一发 n_srs>0 即可与 legacy 245 帧 A/B；或降 speed1 提成功率。

### 4.10.5 两处订正
- 旧记的"cdl_c 静态 -6.37"实为 **cdl_b**（proxy.log `npy_directory=...cdl_b` 实证）；cdl_c 仍卡 Msg3 max HARQ（DTX，零功率，调功率/HARQ 无效）。
- `GT_SAVE_EVERY` launcher 实际默认 **10**（非手册旧标的 100，`SWEEP_V9_USAGE.md` 已更正）；`=1` 每 slot 存 GT 开销 ×10 会**压垮临界 attach**（speed3 一发 connect 后 UL failure timer 到期释放、0 SRS）；干净逐 slot A/B 用 `=1` 但需 attach 撑得住，否则用 `10`（unify 最近邻吸收 ~5slot gap，speed3 相关性~0.99）。

---

## 五、复现命令

```bash
cd DevChannelProxyJIN/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy
export OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1   # 必须，否则 BLAS 过度订阅

# 真 2D 五档对比（含 ESPRIT）—— 已合并进统一 harness 的 --true2d 模式
python3 test_srs_2d_offline.py --cdl-dir data_out --true2d --n-sc 256 --synth-frames 80 --snr-db 15 --win 64 --esprit-p 24
python3 test_srs_2d_offline.py --cdl-dir data_out --true2d --n-sc 256 --synth-frames 120 --snr-db 25 --win 64 --esprit-p 24

# 2D 时域级联 + 去旋缺陷 + PKF/Riccati —— --cascade 模式（相干多普勒升速）
python3 test_srs_2d_offline.py --cdl-dir data_out --cascade --n-sc 256 --synth-frames 60 --snr-db 15 --win 64 --doppler-mode coherent --synth-doppler 0.05
python3 test_srs_2d_offline.py --cdl-dir data_out --cascade --n-sc 256 --synth-frames 60 --snr-db 15 --win 64 --doppler-mode coherent --synth-doppler 0.40

# 复现三道坎
python3 test_srs_2d_offline.py --synth-mf --n-sc 256 --synth-frames 150 --snr-db 15 --ema-auto-gamma
python3 test_srs_2d_offline.py --run-dir ../../logs/q4_sweep_20260528_150304/snr_15dB --ema-auto-gamma --cport
python3 test_srs_2d_offline.py --run-dir ../../logs/q4_sweep_20260529_163146/snr_15dB --max-frames 90 --ema-auto-gamma --cport
```

> ⚠️ 标定提醒：绝对 dB 随 n_sc 变（n_sc 同时是带宽采样与 FFT 尺寸）；定性结论（5–7dB 真2D差、ESPRIT 宽谱崩、Toeplitz>对角）稳健。`win=256` 全窗口 O(n⁴) 极慢，用 W≤128 看趋势即可。

---

## 六、归档

| 用途 | 路径 |
|------|------|
| 统一离线 harness（已合并 true2d/cascade） | `DevChannelProxyJIN/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy/test_srs_2d_offline.py`（`--true2d` / `--cascade`；原 test_wiener_offline.py 改名） |
| （已删冗余） | ~~test_true2d_wiener.py / test_2d_cascade.py~~ → 合并进 `test_wiener_offline.py` |
| #4 comb/2048 验证脚本（新增） | `DevChannelProxyJIN/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy/check_c_freq_mirror.py` |
| C 真2D 落地（已改） | `nr_srs_mmse.{c,h}`（共轭+EMA+robust floor+R_meas+true2d）、`nr_srs_2d_filter.{c,h}`（PKF）、`nr_ul_channel_estimation.c`（true2d dispatch） |
| C 时域 2D 滤波（去旋缺陷，已被 PKF 取代） | `openair1/PHY/NR_ESTIMATION/nr_srs_2d_filter.c:update_scalarkalman_core`（L835-851 输出未回旋） |
| C 频域 Toeplitz LMMSE（已存在） | `openair1/PHY/NR_ESTIMATION/nr_srs_mmse.c:nr_srs_mmse_freq_filter`（R 见 `nr_srs_pdp_to_R_dft:500`） |
| C dispatch | `openair1/PHY/NR_ESTIMATION/nr_ul_channel_estimation.c:968-1006` |
| 本判定日志 | `0529_True2D_离线判定日志.md` |
