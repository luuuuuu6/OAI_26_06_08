# v7 使用说明 — G1C Multi-UE MIMO Channel Proxy

本文档记录 `v7.py` 的用途、auto attach-stable 机制、常用终端命令、启动脚本关系和参数说明。

相关文件：

- `DevChannelProxyJIN/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy/v7.py`
- `DevChannelProxyJIN/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy/launch_all_v7.sh`
- `DevChannelProxyJIN/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy/run_q4_snr_sweep_v7.sh`
- `DevChannelProxyJIN/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy/q4_convergence_sweep.py`
- `DevChannelProxyJIN/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy/v7_changelog.md`

---

## 1. v7 解决什么问题

`v6.py` 修复了很多物理问题后，信道变得更真实：

- `carrier_frequency = 3.5e9`
- `sample_times` 跨 batch 累积
- `UE_SPEED` 真正产生 Doppler
- P1B ray data 参与动态信道生成
- 全局能量归一化保留 fading

但是这样也带来一个问题：OAI 初始接入阶段（RA / RRC / NAS / SRS 配置）对信道扰动很敏感。如果一开始就让信道动态变化，可能出现：

- gNB 多次 `RA failed at state WAIT_Msg3`
- UE 生成 `RRCSetupComplete`，但 gNB 没稳定收到
- 没走到 `RRCReconfigurationComplete`
- periodic SRS 没真正启用
- 最终 `SRS=0`

`v7.py` 的核心改进是：

```text
先让信道准静态，帮助 OAI 完成 attach / RRC / SRS 配置；
等 gNB 确认 RRCReconfigurationComplete 后，再自动切换到动态信道。
```

---

## 2. v7 的 stable / dynamic 是什么意思

这里的“动态信道”不是“切到 P1B”。

P1B 一直都在用。P1B 提供 ray tracing 的路径结构，例如 delay、path gain、角度、RX index 等。

v7 的 stable / dynamic 区别在于 `sample_times` 是否推进。

### 2.1 Stable 阶段

```python
sample_times_now = _build_sample_times(0)
```

每个 batch 都用同一个时间点。即使设置了 `UE_SPEED=3`，时间不往前走，Doppler 相位不展开。可以理解为：

```text
UE 先假装没动，信道看起来接近静态。
```

### 2.2 Dynamic 阶段

```python
sample_times_now = _build_sample_times(dyn_batch_idx)
```

每个 batch 时间往前走。此时：

```text
P1B ray data + UE_SPEED + carrier_frequency + sample_times
```

共同产生随时间变化的 Doppler / 相位演化。可以理解为：

```text
UE 真正按 UE_SPEED 动起来。
```

### 2.3 P1B_DISTANCE_MODE

v7 默认：

```text
P1B_DISTANCE_MODE=default
DEFAULT_DIST_M=100
```

意思不是不用 P1B，而是：

- P1B 的 ray data 仍然使用
- `distance_3d` 不再默认由 P1B 最短 `tau` 推导
- `distance_3d` 使用固定 `100m`

这样可以避免某些 P1B ray dump 的最短 `tau` 导致 `distance_3d` 异常，影响 OAI attach。

---

## 3. 推荐入口

一般不要直接运行 `v7.py`。推荐使用：

```bash
run_q4_snr_sweep_v7.sh
```

它会：

- 重启 / 检查 5GC
- 按 SNR 点循环运行
- 调用 `launch_all_v7.sh`
- 等待 GT 和 SRS 达到阈值
- 归档日志到 `DevChannelProxyJIN/logs/q4_sweep_*`
- 生成 `sweep_manifest.txt`

单点调试时可以直接用：

```bash
launch_all_v7.sh
```

直接运行 `v7.py` 只适合非常底层的 proxy 调试。

---

## 4. 文件关系

```text
run_q4_snr_sweep_v7.sh
  └── 多 SNR sweep 管理器
      ├── 设置 SNR、MAX_FRAMES、MIN_SRS_BINS、P1B、UE_SPEED 等
      ├── 调用 launch_all_v7.sh
      ├── 监控 GT 和 SRS 文件数量
      └── 归档每个 SNR 点日志

launch_all_v7.sh
  └── 单次 OAI 实验启动器
      ├── 启动 v7.py proxy
      ├── 启动 gNB
      ├── 启动 UE
      ├── 启动 attach-stable watcher
      └── 退出时移动 GT / flush SRS

v7.py
  └── Channel Proxy 本体
      ├── 加载 P1B ray data
      ├── 生成 Sionna 信道
      ├── GPU IPC 转发 DL/UL IQ
      ├── 保存 GT H(f)
      └── 执行 attach-stable / dynamic handoff
```

---

## 5. 最常用命令

先进入目录：

```bash
cd /home/dclcom57/OAI_luuuuuu/DevChannelProxyJIN/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy
```

### 5.1 推荐：10 dB 快速验证 v7 auto

```bash
sudo ONLY_SNR="10" \
  UE_SPEED=3 \
  MAX_FRAMES=100 \
  MIN_SRS_BINS=1 \
  HARD_CEILING_SEC=360 \
  bash run_q4_snr_sweep_v7.sh
```

默认会使用：

```text
ATTACH_STABLE_MODE=auto
ATTACH_STABLE_TRIGGER=rrc_reconfig
ATTACH_STABLE_POST_DELAY=15
ATTACH_STABLE_SEC=300
P1B_DISTANCE_MODE=default
DEFAULT_DIST_M=100
```

成功标志：

```text
status=OK
srs>=1
```

并且日志中应出现：

```text
[attach-stable-watcher] trigger matched (rrc_reconfig)
[v7 AttachStable] dynamic channel handoff triggered by file
[SRS Dump] Written srs_matrix_gNB_2x2_seq0.bin
```

### 5.2 更保守：RRC 完成后等 30 秒再切动态

```bash
sudo ONLY_SNR="10" \
  UE_SPEED=3 \
  ATTACH_STABLE_POST_DELAY=30 \
  MAX_FRAMES=100 \
  MIN_SRS_BINS=1 \
  HARD_CEILING_SEC=360 \
  bash run_q4_snr_sweep_v7.sh
```

用途：如果 `15s` 切动态后有较多 `No SRS signal` / `UL Failure`，可以试这个。

### 5.3 固定时间模式：不自动触发，只等满 stable 时间

```bash
sudo ONLY_SNR="10" \
  UE_SPEED=3 \
  ATTACH_STABLE_MODE=time \
  ATTACH_STABLE_SEC=300 \
  MAX_FRAMES=100 \
  MIN_SRS_BINS=1 \
  HARD_CEILING_SEC=360 \
  bash run_q4_snr_sweep_v7.sh
```

用途：复现实验，验证“长稳定窗口”是否能稳定出 SRS。

### 5.4 关闭 stable，复现 v6/v7 动态失败风险

```bash
sudo ONLY_SNR="10" \
  UE_SPEED=3 \
  ATTACH_STABLE_SEC=0 \
  P1B_DISTANCE_MODE=default \
  MAX_FRAMES=100 \
  MIN_SRS_BINS=1 \
  HARD_CEILING_SEC=300 \
  bash run_q4_snr_sweep_v7.sh
```

用途：对照实验。预期更容易出现 `PARTIAL, srs=0`。

### 5.5 全 SNR sweep

```bash
sudo UE_SPEED=3 \
  MAX_FRAMES=300 \
  MIN_SRS_BINS=2 \
  HARD_CEILING_SEC=900 \
  bash run_q4_snr_sweep_v7.sh
```

默认 SNR 点：

```text
-5 0 5 10 15 20 25
```

### 5.6 指定 SNR 点

```bash
sudo ONLY_SNR="0 10 20" \
  UE_SPEED=3 \
  MAX_FRAMES=300 \
  MIN_SRS_BINS=2 \
  HARD_CEILING_SEC=900 \
  bash run_q4_snr_sweep_v7.sh
```

### 5.7 断点续跑

如果已有 sweep 目录：

```bash
sudo SWEEP_ROOT="/home/dclcom57/OAI_luuuuuu/DevChannelProxyJIN/logs/q4_sweep_YYYYMMDD_HHMMSS" \
  ONLY_SNR="15 20 25" \
  UE_SPEED=3 \
  MAX_FRAMES=300 \
  MIN_SRS_BINS=2 \
  HARD_CEILING_SEC=900 \
  bash run_q4_snr_sweep_v7.sh
```

---

## 6. `run_q4_snr_sweep_v7.sh` 参数

这是推荐入口。参数主要通过环境变量传入。

| 环境变量 | 默认值 | 说明 |
|---|---:|---|
| `SNR_POINTS` | `-5 0 5 10 15 20 25` | 要扫的 SNR 列表 |
| `ONLY_SNR` | 空 | 只跑指定 SNR，覆盖 `SNR_POINTS` |
| `SWEEP_ROOT` | 新 timestamp 目录 | 复用已有 sweep 目录 |
| `MAX_FRAMES` | `300` | 每个 SNR 点目标 GT 帧数 |
| `MIN_SRS_BINS` | `2` | 至少需要多少个 SRS bin；每个 bin 通常 100 帧 |
| `HARD_CEILING_SEC` | `900` | 单个 SNR 点最大运行秒数 |
| `GNB_NX`, `GNB_NY` | `2`, `1` | gNB 天线面板布局 |
| `UE_NX`, `UE_NY` | `2`, `1` | UE 天线面板布局 |
| `PROXY_VER` | `v7` | proxy 脚本版本 |
| `NUM_UES` | `1` | UE 数量 |
| `DIGITAL_AGC` | `0` | 是否启用 OAI 数字 AGC |
| `P1B_NPZ` | `../P1B_Valid_Results/Area1_7.5GHz_Rays_Valid_RXs.npz` | P1B ray 文件 |
| `UE_RX_INDICES` | `98` | P1B RX index |
| `CHANNEL_SEED` | 空 | 固定随机种子，便于复现实验 |
| `UE_SPEED` | 空 | 不填时使用 v7 内置默认 3 m/s |
| `DEFAULT_DIST_M` | `100` | P1B default distance 模式下的距离 |
| `P1B_DISTANCE_MODE` | `default` | `default` 或 `tau` |
| `ATTACH_STABLE_SEC` | `300` | stable 最大秒数 / fallback ceiling |
| `ATTACH_STABLE_MODE` | `auto` | `auto` 或 `time` |
| `ATTACH_STABLE_TRIGGER` | `rrc_reconfig` | `rrc_reconfig`、`srs`、`off` |
| `ATTACH_STABLE_POST_DELAY` | `15` | 触发后延迟多少秒切动态 |

输出目录：

```text
/home/dclcom57/OAI_luuuuuu/DevChannelProxyJIN/logs/q4_sweep_YYYYMMDD_HHMMSS/
```

每个 SNR 点目录：

```text
snr_10dB/
  proxy.log
  gnb.log
  ue0.log
  attach_stable_watcher.log
  sionna_gt/
  srs_matrix_gNB_*.bin
```

manifest：

```text
sweep_manifest.txt
```

关键字段：

```text
status  n_gt  n_srs
OK      25    2
```

---

## 7. `launch_all_v7.sh` 参数

这是单次实验启动器，会启动 proxy、gNB、UE 和 attach-stable watcher。

常用形式：

```bash
sudo bash launch_all_v7.sh \
  -v v7 \
  -n 1 \
  -ga 2 1 \
  -ua 2 1 \
  -snr 10 \
  -mf 100 \
  -p1b ../P1B_Valid_Results/Area1_7.5GHz_Rays_Valid_RXs.npz \
  -rx 98 \
  -speed 3 \
  -dist 100 \
  -stable 300 \
  -stable-mode auto \
  -stable-trigger rrc_reconfig \
  -stable-delay 15 \
  -p1bdist default
```

参数表：

| 参数 | 默认值 | 说明 |
|---|---:|---|
| `-v VERSION` | `v7` | proxy 版本，例如 `v7` |
| `-m MODE` | `gpu-ipc` | `gpu-ipc` 或 `socket` |
| `-n NUM_UES` | `1` | UE 数量 |
| `-pl dB` | 空 | 路径损耗，内部以负号传给 proxy |
| `-snr dB` | 空 | 相对 AWGN SNR |
| `-nf dBFS` | 空 | 绝对 noise floor；不能和 `-snr` 同时用 |
| `-d SEC` | 空 | 运行固定秒数后退出 |
| `-mf N` | 空 | 收集 N frames 后退出 |
| `-b` | 关闭 | bypass Sionna channel，IQ 直通 |
| `-ga Nx Ny` | `1 1` | gNB 天线布局 |
| `-ua Nx Ny` | `1 1` | UE 天线布局 |
| `-bs SIZE` | `2100` | channel producer batch symbol size |
| `-bl LEN` | `42000` | channel ring buffer len |
| `-p1b PATH` | 空 | P1B npz 路径 |
| `-rx INDICES` | 空 | P1B RX index，支持逗号分隔或 `random` |
| `-gpu IDX` | `0` | GPU index |
| `--xla` | 关闭 | 启用 XLA JIT |
| `-agc` | 关闭 | 启用 OAI digital AGC |
| `-gse N` | `10` | GT save every N UL slots |
| `-seed N` | 空 | channel seed |
| `-speed M/S` | 空 | UE speed，v7 内置默认 3 m/s |
| `-dist METERS` | `100` | default distance |
| `-p1bdist MODE` | `default` | `default` 或 `tau` |
| `-stable SEC` | `300` | attach stable 最大秒数 |
| `-stable-mode MODE` | `auto` | `auto` 或 `time` |
| `-stable-trigger EVENT` | `rrc_reconfig` | `rrc_reconfig`、`srs`、`off` |
| `-stable-delay SEC` | `15` | trigger 后延迟切动态 |

`launch_all_v7.sh` 会把这些参数转换为 `v7.py` 参数，例如：

```text
-speed 3        -> --speed 3
-dist 100       -> --default-distance-m 100
-stable 300     -> --attach-stable-sec 300
-stable-mode    -> --attach-stable-mode
-p1bdist        -> --p1b-distance-mode
```

---

## 8. `v7.py` 主要参数

一般不直接运行 `v7.py`，但理解这些参数有助于调试。

| 参数 | 默认值 | 说明 |
|---|---:|---|
| `--mode` | `socket` | 直接运行时默认 socket；脚本里使用 `gpu-ipc` |
| `--custom-channel` / `--no-custom-channel` | enabled | 是否使用 Sionna channel |
| `--buffer-len` | `10500` | 直接运行时默认值；脚本传 `42000` |
| `--buffer-symbol-size` | `4200` | 直接运行时默认值；脚本传 `2100` |
| `--path-loss-dB` | `0.0` | 路径损耗 |
| `--snr-dB` | 空 | 相对 AWGN SNR |
| `--noise-dBFS` | 空 | 绝对 AWGN floor |
| `--gnb-ant` / `--ue-ant` | 环境变量 | 总天线数 |
| `--gnb-nx --gnb-ny` | 环境变量 | gNB 面板布局 |
| `--ue-nx --ue-ny` | 环境变量 | UE 面板布局 |
| `--num-ues` | `1` | UE 数量 |
| `--p1b-npz` | 空 | P1B npz |
| `--ue-rx-indices` | 空 | UE RX indices |
| `--gt-dir` | `$SIONNA_GT_DIR` | GT 保存目录 |
| `--gt-save-every` | `1` | 每 N 个 UL slot 保存一次 GT |
| `--gt-symbols` | `12` | 保存的 OFDM symbol，SRS 默认是 12 |
| `--speed` | 空 | UE speed，覆盖内置默认 3 m/s |
| `--default-distance-m` | `100.0` | default distance |
| `--p1b-distance-mode` | `default` | P1B 距离模式 |
| `--attach-stable-sec` | `300.0` | stable 最大秒数 |
| `--attach-stable-mode` | `auto` | auto/time |
| `--attach-stable-trigger-file` | `/tmp/oai_gpu_ipc/v7_dynamic_enable` | auto trigger file |
| `--seed` | 空 | 固定随机种子 |
| `--xla` / `--no-xla` | off | XLA JIT |

---

## 9. Attach-Stable Auto 参数怎么选

### 9.1 推荐默认

```text
ATTACH_STABLE_MODE=auto
ATTACH_STABLE_TRIGGER=rrc_reconfig
ATTACH_STABLE_POST_DELAY=15
ATTACH_STABLE_SEC=300
```

含义：

```text
先稳定；
看到 gNB 收到 RRCReconfigurationComplete；
再等 15 秒；
切动态；
如果一直等不到，最多 300 秒后切动态。
```

### 9.2 更保守

```bash
ATTACH_STABLE_POST_DELAY=30
```

适合动态切换后 `No SRS signal` / `UL Failure` 较多的情况。

### 9.3 更晚触发

```bash
ATTACH_STABLE_TRIGGER=srs
```

含义：等 gNB 侧出现 SRS 相关日志再切动态。更保守，但可能更晚。

### 9.4 固定时间

```bash
ATTACH_STABLE_MODE=time
ATTACH_STABLE_SEC=300
```

含义：不看 gNB 事件，固定稳定 300 秒后切动态。

### 9.5 关闭 stable

```bash
ATTACH_STABLE_SEC=0
```

用于复现动态信道导致 attach 不稳的问题，不推荐作为正常实验配置。

---

## 10. 日志怎么看

每次运行后看最新日志：

```bash
cd /home/dclcom57/OAI_luuuuuu/DevChannelProxyJIN/logs/latest
```

### 10.1 manifest

```bash
cat ../q4_sweep_*/sweep_manifest.txt
```

重点看：

```text
status  n_gt  n_srs
OK      25    2
```

### 10.2 attach watcher

```bash
cat attach_stable_watcher.log
```

成功应看到：

```text
[attach-stable-watcher] waiting for pattern: Received RRCReconfigurationComplete
[attach-stable-watcher] trigger matched (rrc_reconfig); sleeping 15s
[attach-stable-watcher] trigger file written: /tmp/oai_gpu_ipc/v7_dynamic_enable
```

### 10.3 proxy.log

```bash
grep "AttachStable\\|distance_3d\\|velocities" proxy.log
```

成功应看到：

```text
[v7 ChannelProducer] velocities: scalar |v|=3.0 m/s
[v7 ChannelProducer] distance_3d=fixed 100.0 m (P1B distance mode=default)
[v7 AttachStable] enabled: mode=auto, max=300.0s
[v7 AttachStable] dynamic channel handoff triggered by file
```

### 10.4 gnb.log

```bash
grep "RRCReconfigurationComplete\\|SRS Dump\\|SRS 2D-MMSE\\|No SRS\\|Invalid timing" gnb.log
```

成功应看到：

```text
Received RRCReconfigurationComplete
[SRS Dump] Written srs_matrix_gNB_2x2_seq0.bin
[SRS Dump]   Captured : 100 frames
[SRS Dump]   Written  : 100 frames
[SRS Dump]   Files    : 1
```

注意：`No SRS signal`、`Invalid timing advance offset`、`UL Failure` 仍可能出现，说明动态切换后链路还有不稳定因素，但只要 SRS bin 写出，数据仍可进入分析。

---

## 11. NMSE 分析

### 11.1 跑默认分析

```bash
python3 /home/dclcom57/OAI_luuuuuu/DevChannelProxyJIN/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy/q4_convergence_sweep.py \
  --sweep-dir /home/dclcom57/OAI_luuuuuu/DevChannelProxyJIN/logs/q4_sweep_YYYYMMDD_HHMMSS \
  --plot-dir /home/dclcom57/OAI_luuuuuu/DevChannelProxyJIN/vRAN_Socket/data_out
```

默认 `--fixed-n 100`。当前 v7 动态链路下，`N=100` 可能跨过动态/失稳阶段，不一定适合作为代表值。

### 11.2 推荐：短窗口 N=15

```bash
python3 /home/dclcom57/OAI_luuuuuu/DevChannelProxyJIN/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy/q4_convergence_sweep.py \
  --sweep-dir /home/dclcom57/OAI_luuuuuu/DevChannelProxyJIN/logs/q4_sweep_YYYYMMDD_HHMMSS \
  --plot-dir /home/dclcom57/OAI_luuuuuu/DevChannelProxyJIN/vRAN_Socket/data_out \
  --fixed-n 15 \
  --tag-suffix v7_auto_N15
```

当前 10 dB 单点的代表结果：

```text
N=15
NMSE     = -7.25 dB
RSRPerr  = 0.75 dB
PDP corr = 0.9997
```

### 11.3 常用分析参数

| 参数 | 默认值 | 说明 |
|---|---:|---|
| `--sweep-dir` | 空 | q4_sweep 目录，推荐使用 |
| `--run-dir` | 空 | 单个 run 目录，仅做 N-axis |
| `--plot-dir` | data_out | 输出图和 JSON |
| `--fixed-n` | `100` | SNR-axis 使用的帧数 |
| `--tag-suffix` | 空 | 输出文件名后缀，避免覆盖 |
| `--align` | `slot` | SRS/GT 对齐方式 |
| `--align-tol-slots` | `20` | slot 对齐容忍范围 |
| `--agc` | `none` | 可选 `ewma` |
| `--agc-beta` | `0.95` | EWMA AGC 系数 |
| `--denoise` | `sto` | STO 相位校正，默认推荐 |
| `--pfa` | 空 | Neyman-Pearson PDP denoise 参数 |
| `--skip-first` | `0` | 跳过前 N 帧 |

---

## 12. 推荐实验流程

### Step 1：10 dB 快速验证

```bash
sudo ONLY_SNR="10" UE_SPEED=3 MAX_FRAMES=100 MIN_SRS_BINS=1 HARD_CEILING_SEC=360 bash run_q4_snr_sweep_v7.sh
```

确认：

```text
status=OK
n_srs>=1
dynamic channel handoff triggered by file
```

### Step 2：分析 N-axis

```bash
python3 q4_convergence_sweep.py \
  --sweep-dir /home/dclcom57/OAI_luuuuuu/DevChannelProxyJIN/logs/q4_sweep_YYYYMMDD_HHMMSS \
  --plot-dir /home/dclcom57/OAI_luuuuuu/DevChannelProxyJIN/vRAN_Socket/data_out \
  --fixed-n 15 \
  --tag-suffix v7_auto_N15
```

确认 `N=10~30` 是否稳定，`N=100` 是否崩坏。

### Step 3：多 SNR sweep

```bash
sudo UE_SPEED=3 MAX_FRAMES=300 MIN_SRS_BINS=2 HARD_CEILING_SEC=900 bash run_q4_snr_sweep_v7.sh
```

### Step 4：多 SNR 分析

```bash
python3 q4_convergence_sweep.py \
  --sweep-dir /home/dclcom57/OAI_luuuuuu/DevChannelProxyJIN/logs/q4_sweep_YYYYMMDD_HHMMSS \
  --plot-dir /home/dclcom57/OAI_luuuuuu/DevChannelProxyJIN/vRAN_Socket/data_out \
  --fixed-n 15 \
  --tag-suffix v7_auto_N15
```

---

## 13. 常见问题

### Q1：auto 触发是不是切到 P1B？

不是。P1B 一直在用。auto 触发切的是：

```text
sample_times 固定 -> sample_times 递增
```

也就是 UE 从“假装没动”变成“真正按 UE_SPEED 动起来”。

### Q2：为什么不用固定 300 秒？

固定 300 秒能工作，但太保守。auto 模式可以在 gNB 完成 RRC/SRS 配置后提前切动态，减少无意义等待。

### Q3：为什么 `--fixed-n 15`？

当前数据中：

```text
N=15   NMSE=-7.25 dB
N=100  NMSE=+14.99 dB
```

说明短窗口有效，长窗口跨过动态/失稳阶段后崩坏。因此当前阶段先用 `N=15` 代表有效短窗口性能，同时保留 N-axis 曲线观察长窗口稳定性。

### Q4：`status=OK` 是否等于 NMSE 好？

不是。`OK` 只表示：

```text
GT 文件数达标
SRS bin 文件数达标
```

NMSE 好不好要跑 `q4_convergence_sweep.py` 看对齐和指标。

### Q5：看到 `Killed` 是不是失败？

不一定。脚本达到阈值后会发 SIGTERM / cleanup，随后 hard-kill 残余进程。只要 manifest 是 `OK`，并且 SRS summary 有 `Written`，通常是正常退出流程的一部分。

---

## 14. 当前推荐默认配置

```text
PROXY_VER=v7
UE_SPEED=3
P1B_DISTANCE_MODE=default
DEFAULT_DIST_M=100
ATTACH_STABLE_MODE=auto
ATTACH_STABLE_TRIGGER=rrc_reconfig
ATTACH_STABLE_POST_DELAY=15
ATTACH_STABLE_SEC=300
GNB_NX=2
GNB_NY=1
UE_NX=2
UE_NY=1
```

最小推荐命令：

```bash
cd /home/dclcom57/OAI_luuuuuu/DevChannelProxyJIN/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy

sudo ONLY_SNR="10" \
  UE_SPEED=3 \
  MAX_FRAMES=100 \
  MIN_SRS_BINS=1 \
  HARD_CEILING_SEC=360 \
  bash run_q4_snr_sweep_v7.sh
```
