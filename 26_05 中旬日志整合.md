# SRS 信道估计 & 2D MMSE 完整工作日志

| 字段 | 值 |
|------|----|
| **时间跨度** | 2026-05-09 ~ 2026-05-15 |
| **核心目标** | 定位 SRS 信道估计 -7 dB NMSE floor 根因 → 在 2-4 ops/SC 预算内实现 2D MMSE 信道估计器 |
| **最终成果** | Floor 根因定位 (帧间 STO 抖动) 并修复 (+11.6 dB)；2D MMSE 静态/动态验证完成；De-rotation +10 dB |
| **关键文件路径** | `DevChannelProxyJIN/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy/` (下称 `G1C/`) |

---

## 目录

- [第一部分：前序探索 (05-09 ~ 05-13)](#第一部分前序探索-05-09--05-13)
- [第二部分：-7 dB Floor 根因定位 (05-14 上午)](#第二部分-7-db-floor-根因定位-05-14-上午)
- [第三部分：2D MMSE Phase 1 — 静态信道验证 (05-14 下午)](#第三部分2d-mmse-phase-1--静态信道验证-05-14-下午)
- [第四部分：2D MMSE Phase 2 — 动态信道实验 (05-14 ~ 05-15)](#第四部分2d-mmse-phase-2--动态信道实验-05-14--05-15)
- [第五部分：Adaptive α 算法探索 (05-15)](#第五部分adaptive-α-算法探索-05-15)
- [第六部分：架构级发现汇总](#第六部分架构级发现汇总)
- [第七部分：假说验证总表](#第七部分假说验证总表)
- [第八部分：遗留问题与决策](#第八部分遗留问题与决策)
- [第九部分：方法论教训](#第九部分方法论教训)
- [第十部分：产出文件与复现](#第十部分产出文件与复现)
- [第十一部分：下一步方向](#第十一部分下一步方向)

---

# 第一部分：前序探索 (05-09 ~ 05-13)

教授 4/29 提出 2D MMSE filtering 需求，5/7 点名 Lattice-RLS。此阶段的核心任务是在 OAI 实测环境中验证各种信道估计算法，同时诊断长期存在的 -7 dB NMSE floor。

## 1.1 尝试 1：PDP→R 频域 MMSE (05-09 ~ 05-11)

### 做了什么

实现完整的 1D 频域 complex MMSE 信道估计器：
```
LS → IFFT → PDP → noise_floor → R(Δk) = FT{PDP_clean} → per-SC 4×4 complex MMSE solve → output
```

新增/修改 `nr_srs_mmse.c` (633→790 行)，包含：
- `nr_srs_collect_pilots_dense()` — LS pilot 打包
- `nr_srs_pdp_ifft()` — 2048 点 IFFT
- `nr_srs_estimate_noise_floor()` — 时域噪底 (25th percentile of far tail)
- `nr_srs_pdp_to_R_dft()` — Wiener-Khinchin 频域相关
- `solve_complex_system()` — 4×4 复数 Gauss 消元
- `nr_srs_mmse_freq_filter()` — 主入口

### 实测结果 (seed=42 fair comparison)

| SNR | Legacy p50 | PDP-R p50 | 差距 |
|-----|-----------|-----------|------|
| 10 dB | -7.01 dB | -0.88 dB | +6.13 (更差) |
| 15 dB | -7.15 dB | -4.25 dB | +2.90 (更差) |
| 20 dB | -7.12 dB | -2.46 dB | +4.66 (更差) |
| 25 dB | -7.18 dB | -3.01 dB | +4.17 (更差) |

### 失败原因

1. **复杂度爆炸**：5-10 ms/call vs Legacy 0.1 ms，教授要求 "negligible"，直接出局
2. **实现过度**：用了 IFFT → noise floor → R(Δk) → per-SC Gauss 消元，太重
3. **Fair comparison 太晚**：之前被 random seed 误导以为新算法大胜

### 回顾

**方向本身是对的**——后续诊断确认 -7 dB floor 正是频域相关的问题。用频域 MMSE 替代 filt8/16 是正确思路，只是需要轻量版实现。

### 有价值的产出

- `eval_pdpR_nmse.py` — 通用 NMSE 评估器 (LS-aligned + per-frame percentile)
- `preflight.sh` — sweep 前自动清理 zombie/shm/容器
- `run_q4_snr_sweep_v8.sh` — inter-point preflight 集成
- Fair comparison 纪律：`CHANNEL_SEED=42` 必须显式设置

---

## 1.2 尝试 2：EWMA 时域平滑 (05-12)

### 做了什么

新建 `nr_srs_2d_filter.c/h`，实现 EWMA：
```c
H_smooth(t) = α · H_smooth(t-1) + (1-α) · H_new(t)
```
接入 dispatcher：`SRS_ESTIMATOR=2dmmse` → `NR_SRS_EST_MMSE2D` case。

### 实测结果

**α=0.9 (N_eff=10)**：

| 指标 | Legacy | EWMA α=0.9 | 差异 |
|------|--------|------------|------|
| p50 | -7.39 dB | -7.19 dB | +0.20 (略差) |
| p10 | -17.46 dB | -21.32 dB | -3.86 (好) |
| p90 | -2.63 dB | -0.86 dB | +1.77 (差) |

**α=0.5 (N_eff=2)**：

| 指标 | Legacy | EWMA α=0.5 | 差异 |
|------|--------|------------|------|
| p50 | -7.24 dB | -7.20 dB | +0.04 (持平) |
| p10 | -18.97 dB | -18.17 dB | +0.80 |
| p90 | -2.08 dB | -0.23 dB | +1.85 (差) |

### 分析

- 动态 channel (speed=3 m/s) 下 p50 持平，当时以为"无效"
- **后续实验 7 (静态 channel) 证明 EWMA 是有效的**：p50 改善 0.55 dB，p90 改善 3.52 dB
- 动态下看不到改善是因为 GT-SRS 对应错位 (per-frame alpha 波动 0.23~311) 掩盖了真实改善
- EWMA 能压 i.i.d. 随机成分，但不能消除 deterministic 误差

---

## 1.3 尝试 3：LSL M=2 Lattice-RLS (05-11 ~ 05-12，仅 Python 验证)

### 做了什么

写了 `lsl_m2_reference.py`，实现 Haykin Ch.16 的 M=2 LSL。测试两种输出：

1. **Forward prediction error subtraction** (`H = u - f_M`)：gain ≈ +3 dB
2. **Joint-process estimator** (`H = Σ ρ_m · b_m`)：gain = 0 dB (identity filter)

### 数学发现

Joint-process estimator 在 `d(t) = u(t)` (self-prediction) 模式下：
- 解的是 `min |d - Σ ρ_m · b_m|²`
- 当 `d = u = b_0` 时，最优解是 `ρ_0=1, ρ_1=ρ_2=0`（identity）
- **数学定理，不是实现 bug**

Forward prediction error 有效平均长度 = M+1 = 3，理论增益上限 = 10·log10(3) ≈ 4.77 dB，实测 +3 dB (合理)。

### 结论

教授推荐 lattice 的语境是 **adaptive equalization** (`d = known pilot, u = received`)，不是 **channel state smoothing** (`d = u = noisy LS estimate`)。对后者，EWMA 是最优工具。

---

## 1.4 D0 Floor 诊断系列 (05-12)

### 实验 4：三模式对比

跑 3 个模式定位 -7 dB floor 来源：

| 模式 | NMSE LS-aligned p50 | paired frames | active SC |
|------|---------------------|---------------|-----------|
| legacy | -6.64 dB | 95 | 1248 |
| passthru (跳过 filt8) | -6.49 dB | 96 | 624 |
| oracle (注入 GT) | -0.04 dB | 7 | 2048 |

**关键发现**：Legacy ≈ Passthru (差 0.15 dB) → filt8 频域插值几乎无正面贡献。

### 实验 5：GT save_every=1 对齐测试 (05-12 11:00)

| 指标 | save_every=10 | save_every=1 | 变化 |
|------|---------------|--------------|------|
| paired frames | 95 | 159 | +67% |
| NMSE p50 | -6.64 dB | -7.48 dB | -0.84 dB |

GT 更密让配对增加，p50 改善 ~0.8 dB，但 **-7 dB floor 依然存在**。

### 实验 6：Alpha Floor 深度诊断 (05-12 11:30)

| 指标 | 值 | 含义 |
|------|-----|------|
| Per-frame `|alpha|` | median=92, std=78, range=[0.23, 311] | 帧间 scale 差 1000× |
| angle(alpha) std | 0.95 rad (~55°) | GT-SRS 相位帧间剧变 |
| Per-SC CoV | 0.14 | 频率方向 scale 较均匀 |
| Slot gap | 全部 = 0 | 时间对齐完美 |

**根本问题**：`|alpha|` 从 0.23 到 311，GT 和 SRS 的 per-frame 对应关系不可靠。

### 实验 7：静态 channel A/B 对比 (05-12 11:33 ~ 11:52)

UE speed=0（静态），分别跑 legacy 和 EWMA α=0.9：

| 指标 | Legacy (speed=0) | EWMA α=0.9 (speed=0) | 差异 |
|------|------------------|----------------------|------|
| NMSE p50 | -6.53 dB | **-7.08 dB** | **-0.55 dB** |
| p90 | -0.89 dB | **-4.41 dB** | **-3.52 dB** |
| spread | 6.68 dB | 3.55 dB | -3.13 dB |

**结论**：
1. EWMA 在静态 channel 下有效
2. -7 dB floor 是 OAI 内部的真实 floor（静态 channel 无对齐问题，但 NMSE 仍 ~-7 dB）

### 实验 8：SNR=40 dB 静态 channel — Floor 确认 (05-12 14:44)

| SNR | NMSE p50 | p90 |
|-----|----------|-----|
| 20 dB (静态) | -6.53 dB | -0.89 |
| **40 dB (静态)** | **-6.89 dB** | -5.37 |
| 差异 | **-0.36 dB** | -4.48 |

**SNR 提高 20 dB，NMSE p50 只改善 0.36 dB。彻底确认 -7 dB floor 跟 thermal noise / SNR 无关。**

---

## 1.5 TA 实验与 GT-SRS 对齐深度诊断 (05-12 晚)

### TA 禁用实验

在 `gNB_scheduler_dlsch.c` 中添加 `DISABLE_TA_UPDATE=1` 守卫，禁止 MAC 层发送 TA MAC CE：

| 指标 | TA 开启 | TA 关闭 |
|------|---------|---------|
| SRS 帧数 | 101 | 9 |
| STO std | 25.70 samples | 0.67 samples |
| STO range | [-81, +3] | [-1, +1] |
| NMSE p50 | -6.64 dB | -7.13 dB |

- TA 闭环**确实**是 STO 帧间抖动的根源 (std 降低 40 倍)
- 但 NMSE 仅改善 0.7 dB，per-frame STO 校正已补偿大部分
- **完全禁用 TA 导致连接极不稳定 (BLER 20-45%，9 帧/928s)，不可用于生产**
- TA 代码已回退

### N_TA_offset 相位校正

实现 `apply_nta_correction(H_gt, active, n_ta_offset=800, n_fft=2048)`。

**关键发现**：N_TA 校正与 per-frame STO 校正**冗余**。同时使用导致双重校正，NMSE 从 -6.64 恶化到 +13.25 dB。

### TA-OFF vs TA-ON 决定性对比

```
               TA-OFF (9帧)       TA-ON (101帧)
STO std        0.67 samples       25.70 samples
NMSE best      -8.39 dB           -22.48 dB
NMSE p50       -7.41 dB           -6.64 dB
```

**矛盾**：如果 "STO 抖动→STO 校正残差" 是 floor 根因，那 TA-OFF (STO≈0) 应该突破 -7 dB 达到 -20 dB。但 TA-OFF 的 9 帧全部在 [-8.39, -5.71] dB。

**此矛盾在 05-14 被解决**：TA-OFF 只有 9 帧且帧间 STO 仍有 ±1 样本抖动，宽带累积效应在当时被低估。

---

## 1.6 GT Doppler 残余发现与修复 (05-12 23:30)

### 发现

v8.py ChannelProducer 第 2402 行：
```python
_speed_mag = _tf.abs(_tf.random.normal(
    shape=[batch_size, N_UE], mean=Speed_local, stddev=0.1))
```

当 Speed_local=0 时：实际速度 = |N(0, 0.1)| ≈ 0.08 m/s → f_Doppler ≈ 0.93 Hz。
**"静态"信道根本不是静态的！GT RMS 在 28.93~97.60 之间正弦振荡。**

### 修复

```python
if Speed_local == 0 or Speed_local == 0.0:
    velocities = _tf.zeros([batch_size, N_UE, 3])
```

### 修复验证 (20260513_000323)

| 指标 | 修复前 | 修复后 |
|------|--------|--------|
| GT RMS | min=28.94, max=97.60 (3.4x) | **45.254833 (全部相同)** |
| GT RMS CoV | 14-17% | **0.00%** |
| GT inter-frame NMSE | -41 ~ -48 dB | **-300 dB (bit-perfect)** |

---

## 1.7 Float 仿真推翻 filt8 假说 (05-13 凌晨)

用 `test_sinc_interpolation.py` 在纯 float64 下模拟 filt8/sinc/DFT 插值：

### Part 1：float64 仿真 (GT + SNR=20dB 噪声)

| 方法 | STO=0.0 | STO=-1.5 | STO=-2.7 | STO=-4.0 |
|------|---------|----------|----------|----------|
| filt8_legacy | -22.2 dB | -22.4 dB | -22.5 dB | -22.2 dB |
| filt8_opt | -21.2 dB | -21.2 dB | -21.2 dB | -21.2 dB |
| sinc_W4 | -20.5 dB | -20.5 dB | -20.5 dB | -20.5 dB |
| oracle | -20.0 dB | -20.0 dB | -20.0 dB | -20.0 dB |

**结论**：在 float 精度下所有方法 ~-20 dB (noise-limited)，STO 无影响。filt8 系数不是 floor 根因。

### Part 2：无噪声纯插值精度

| 方法 | NMSE |
|------|------|
| filt8_legacy | -28.9 dB |
| filt8_opt | -85.3 dB |

filt8_legacy 的 3 点平滑引入 -28.9 dB 误差，但远小于实测 -6.8 dB。

---

## 1.8 int16 信号幅度根因 (05-13)

### 核心发现

```
SRS 帧间一致性分析 (speed=0, SNR=20dB, 50 帧):
  帧间 correlation = 0.54  (预期 0.99)
  帧间 self-NMSE   = -0.9 dB (预期 -20 dB)
  有效 SNR          = 2.3 dB (损失 17.7 dB!)
```

**直接原因**：UE IFFT (idft2048) 输出 RMS = 200，仅占 int16 (±32767) 的 **0.6%**。

### int16 链路逐级精度

```
级别              信号 RMS    占 int16%    有效位数
─────────────────────────────────────────────────
[0] X_ref 频域      362       1.1%        8.5 bit
[1] UE IFFT 时域    200       0.6%        7.6 bit   ← 精度瓶颈
[2] Proxy→shm      ~564      1.7%        9.1 bit
[3] OAI FFT 频域   14000     42.7%       13.8 bit  ← FFT 放大后才 OK
[4] LS >>9 输出     9900     30.2%       13.3 bit
[5] filt8 输出      9900     30.2%       13.3 bit
```

### STO 分组发现

| STO 范围 | 帧数 | intra-group NMSE p50 |
|----------|------|---------------------|
| [-5.0, -3.8) | 11 | -17.2 dB |
| [-3.8, -3.0) | 22 | -18.4 dB |
| [-3.0, -2.6) | 10 | -18.2 dB |
| [-2.3, -2.0) | 9 | -19.1 dB |
| [-2.0, -1.0) | 31 | -16.4 dB |

**每个 STO 组内 SRS 形状非常一致 (-16 ~ -19 dB)，但不同 STO 组之间差异巨大**。这个发现为 05-14 的最终根因定位奠定了基础。

### 05-13 假说总结

此时假说为"int16 信号幅度过低 (AMP=512) 导致 FFT 量化失真"，STO 分组数据被认为是佐证但非根因。

**05-14 修正**：STO 抖动本身就是根因，不需要 int16 低幅度这个中介假说。

---

## 1.9 第一部分阶段性总结 (05-09 ~ 05-13)

| 日期 | 方法/实验 | 结论 |
|------|----------|------|
| 05-09~11 | PDP→R 频域 MMSE | 方向对但太重，6 dB worse |
| 05-12 | EWMA α=0.9 | 静态有效 (+0.55 dB)，动态被对齐问题掩盖 |
| 05-11~12 | LSL M=2 | self-prediction 退化为 identity，不适用 |
| 05-12 | D0 floor 诊断 | passthru ≈ legacy → filt8 不是瓶颈 |
| 05-12 | 实验 7-8 | 静态 + SNR=40 确认 floor 真实存在 |
| 05-12 晚 | TA 实验 | TA 是 STO 来源之一，但禁用不可用 |
| 05-12 23:30 | GT Doppler | speed=0 有残余 0.93Hz Doppler → 修复 |
| 05-13 凌晨 | Float 仿真 | filt8 在 float 下 -22 dB → 系数不是根因 |
| 05-13 | int16 链路 | RMS=200, 0.6% of int16, 有效 7.6 bit |
| 05-13 | STO 分组 | 同 STO 组 corr 很高 → **STO 线索浮现** |

**5 天的核心矛盾**：-7 dB floor 的根因假说从 "filt8" → "int16 精度" → "GT-SRS 坐标系差异" → "int16 低幅度" 反复变化，没有被实验彻底闭合。

---

# 第二部分：-7 dB Floor 根因定位 (05-14 上午)

| 时间 | 10:13 ~ 13:45 (约 3.5 小时) |
|------|----------------------------|
| 目标 | 彻底定位并修复 -7 dB NMSE floor |
| 结果 | **帧间 STO 抖动 (±0.5 样本)** 是唯一根因；5 行 phase slope 补偿消除 floor |

## 2.1 阶段 1：Proxy AGC 尝试 (10:25 ~ 11:10)

### 假说

UE IFFT 输出 RMS=200 (0.6% of int16)，int16 FFT 量化失真是根因。在 Proxy 端放大信号填满 int16 动态范围可以消除。

### 行动

在 `v8.py` 中实现自适应 AGC (target_peak=30000)，在 int16 量化前放大信号。

### 结果

```
NMSE: -6.5 dB → -4.55 dB (恶化 1.95 dB)
SRS 检测频繁失败 ("No SRS signal")
```

### 失败分析

- target_peak=30000 导致 OAI FFT 内部 butterfly 加法溢出 (`adds_epi16` 饱和)
- AGC 方向错误：精度损失发生在 UE→Proxy 的 int16 **输入端**，Proxy **输出端**放大无法恢复已丢失的 LSB
- **AGC 代码已全部回退**

---

## 2.2 阶段 2：E1 Float FFT 假说 (11:35 ~ 11:46)

### 假说

OAI 的 int16 FFT (dft2048) 在低幅度输入下量化失真严重，用 float FFT (FFTW) 旁路可消除。

### 关键对照实验

用 `oai_dft_wrapper.py` (bit-exact libdfts.so) 在 Python 中模拟完整 int16 链路：

```
Path A (OAI int16 FFT + int16 LS):  帧间 corr = 0.9964, 有效 SNR = 23.4 dB
Path B (float FFT + float LS):      帧间 corr = 0.9968, 有效 SNR = 21.9 dB
Path C (float FFT → int16 → LS):    帧间 corr = 0.9975, 有效 SNR = 24.8 dB
```

### 结论

三条路径 corr 都是 0.996+。**int16 FFT 在仿真中完全没问题。** 退化发生在 Python 仿真未覆盖的环节——UE→Proxy→gNB 的实际 IPC 传输路径。

**E1 方案取消。此对照实验是整个 debug 的转折点——30 分钟省去 1-2 天无效编码。**

---

## 2.3 阶段 3：STO 诊断——初次错误排除 (11:46 ~ 11:55)

### 行动

对已有 SRS bin 数据做相位 slope 估计和 STO 补偿测试。

### 结果

STO 补偿后 corr 从 0.72 降到 0.29 → 得出"STO 不是根因"的**错误结论**。

### 错误原因

补偿算法有 bug——去除了每帧各自的 phase slope 后，**没有对齐到共同参考帧**。线性相位 (STO 的体现) 恰好是帧间最一致的成分，错误去除后反而暴露了 AWGN 随机性。

### 教训

**这是整个 debug 最危险的一步——差点永久排除正确答案。** 当假说被"否定"时，先审查实验代码再否定假说。

---

## 2.4 阶段 4：根因候选并行排查 (11:55 ~ 12:13)

同时调查三个方向：

### 候选 1：Proxy UL slot 时序未对齐

- 发现 157 次 `[IPC DIAG] UL start_ts not slot-aligned (offset=29917)`
- offset = 30720 - 29917 = 803 ≈ N_TA_offset (800 样本)
- 帧间变化极小：29916/29917/29918，±1 样本
- **初步判断**：±1 样本太小 (per-SC 相位差 0.003 rad)，不足以解释 corr=0.54

**此判断后被证伪**：per-SC 差异虽小，但跨 1200 SC 带宽的累积相位差约 **1.2π**，足以将宽带 correlation 降到 0.5-0.8。

### 候选 2：GT symbol index vs SRS symbol 不匹配

GT 保存 symbol 12，SRS 也用 symbol 12 → **排除**

### 候选 3：OAI TA / FFT 窗动态变化

N_TA_offset=800 固定，不随帧变化；UE 无 TA command (静态 channel rfsim) → **排除**

---

## 2.5 阶段 5：UE int16 量化假说 (12:13 ~ 12:58)

### 假说

UE IFFT 输出 RMS=200，int16 量化后每帧的量化 pattern 不同，导致帧间不一致。

### 反驳

```
量化噪声 SQNR = 200² / (1/12) = 57 dB
实测退化只有 ~12 dB (|α|=0.93 → SNR≈12 dB)
差距 = 45 dB → 量化不可能是根因
```

**结论**：AMP 修改预测无效。量化噪声与实际退化机制**正交**。

---

## 2.6 阶段 6：SNR 标定实验——突破口 (13:04 ~ 13:24)

### 实验设计

4 个 SNR 点 (10/20/30/40 dB)，静态 channel，每点 100 帧 SRS。**按帧间 STO 差 (Δδ) 分组**。

### 关键结果

| SNR 设定 | corr (Δδ=0 组) | corr (全部帧对) | Δδ=0 组帧对数 |
|----------|----------------|----------------|---------------|
| 10 dB | 0.9853 | 0.74 | 182 |
| 20 dB | 0.9949 | 0.78 | 223 |
| 30 dB | 0.9972 | 0.82 | 246 |
| 40 dB | **0.9999** | 0.75 | 200 |

### 分析

**此实验一锤定音**：

1. **同 STO 帧对** (Δδ=0) 的 correlation 随 SNR 单调趋近 1.0 (SNR=40 时 0.9999)
2. 唯一不一致性来源是 AWGN
3. **不存在任何其他系统性退化机制** (排除 int16 精度、filt8 系数、GT 坐标系等所有假说)
4. 跨 STO 帧对的退化 (corr 0.74-0.82) 是 STO 抖动导致的

### 为什么 05-13 的 STO 分组没有得到这个结论

05-13 的 STO 分组用的是连续 STO 值 (float)，分组区间较宽 (0.5 samples)，且当时的注意力在 "int16 低幅度" 假说上。05-14 的改进：

1. 用**帧对之间的 STO 差** (Δδ) 而非绝对 STO
2. 4 个 SNR 点的 parametric sweep 提供了 AWGN 作为唯一残留源的直接证据
3. SNR=40 的 0.9999 corr 是不可辩驳的

---

## 2.7 阶段 7：Fix 1 验证——根因确认 (13:38 ~ 13:45)

### 修复方法

Per-frame weighted phase slope compensation (5 行核心代码)：

```python
for i in range(N_frames):
    phase = np.unwrap(np.angle(H[i, active_sc]))
    w = np.abs(H[i, active_sc])**2
    w /= np.sum(w)
    k_c = k_indices - np.sum(w * k_indices)
    slope = np.sum(w * k_c * (phase - np.sum(w * phase))) / np.sum(w * k_c**2)
    H[i, active_sc] *= np.exp(-1j * slope * k_indices)
```

原理：加权最小二乘拟合频域相位斜率，以 |H|² 为权重（深衰落 SC 权重低），然后去除该线性相位分量。

### 验证结果 (SNR=20 dB, 100 frames)

| 指标 | 修前 | Fix 1 后 | 改善 |
|------|------|----------|------|
| 帧间 corr mean | 0.674 | **0.986** | +0.31 |
| 帧间 corr min | 0.146 | 0.749 | +0.60 |
| 帧平均 SNR | 5.1 dB | **16.5 dB** | **+11.4 dB** |
| NMSE vs 帧平均 p50 | -8.6 dB | **-20.2 dB** | **+11.6 dB** |
| -7 dB floor | 存在 | **消除** | — |

### 因果链闭合

**阶段 6 + 阶段 7 共同闭合因果链**：

- 阶段 6 证明：同 STO 帧无其他退化 (corr→1.0 at high SNR)
- 阶段 7 证明：STO 补偿后全部帧 corr 恢复到 0.986
- 两者共同 → **帧间 STO 抖动是 -7 dB floor 的全部根因**

### 完整因果链

```
OAI/Proxy 时钟域接合 (rfsimulator GPU IPC 路径)
  → UL start_ts 在帧间有 ±1 整样本抖动 (29916/29917/29918)
    → Proxy 信道应用 / OAI gNB FFT 窗起点相对信号有亚样本偏移
      → SRS 信道估计 H(f) 包含帧间变化的线性相位斜率
        → H_obs[k] = H_true[k] × exp(-j2π k δ/N), δ 帧间变化
          → 跨 ~1200 SC 带宽累积相位差约 1.2π (per Δδ=1 sample)
            → 帧间 correlation 降到 0.5-0.8
              → NMSE vs 任何参考 ≈ -7 dB (floor)
```

### 残留观察

- empirical SNR 在 SNR=30/40 被 cap 在 17-21 dB (疑为 OAI int16 pipeline 精度上限)
- Fix 1 后 corr min=0.749 (tail 偏重)，可能来自深衰落 SC 上加权拟合精度不足或偶发大 STO 跳变
- STO 抖动的物理来源 ("为什么会抖") 未追到机制层

---

## 2.8 对前 5 天假说的修正

| 日期 | 假说 | 05-14 修正 |
|------|------|-----------|
| 05-12 | -7 dB = filt8 插值误差 | **错误**：passthru ≈ legacy 已否定，float 仿真确认 |
| 05-12 晚 | -7 dB = GT-SRS 数值坐标系差异 | **部分对**：GT 坐标系有问题 (Doppler)，但修复后 floor 仍在 |
| 05-13 | -7 dB = int16 低幅度 (AMP=512) | **误判**：SQNR=57 dB >> 实测退化；STO 分组数据指向正确方向但未被追到底 |
| 05-13 | STO 是佐证而非根因 | **错误**：STO 本身就是根因 |
| **05-14** | **帧间 STO 抖动 (±0.5 样本)** | **确认**：Δδ=0 corr=0.9999@40dB + Fix 1 corr=0.986@20dB |

---

# 第三部分：2D MMSE Phase 1 — 静态信道验证 (05-14 下午)

Fix 1 消除 floor 后，正式开始 2D MMSE 算法开发。

## 3.1 算法设计

初始设计：频域 3-tap boxcar + 时域 EMA，预算 4-6 ops/SC。

```
H_smooth(t, f) = EMA_time{ boxcar_freq{ H_LS(t, f) } }
```

## 3.2 代码实现

| 文件 | 说明 |
|------|------|
| `G1C/srs_2d_mmse.py` | 滤波器模块：SRS2DFilter (C 风格逐 SC 循环) + SRS2DFilterVec (向量化) |
| `G1C/test_2d_mmse.py` | 测试脚本：split-set GT, outlier 检测, α=0.01~1.0, SNR sweep |
| `G1C/prepare_2d_mmse_data.py` | 数据管线：sweep → STO 补偿 → GT 对齐 → 标准 NPZ |

## 3.3 数据

`data_out/2d_mmse_input/` 下的 NPZ：

- `H_snr{10,20,30,40}dB_static_seed42.npz` — 静态信道 (speed=0), seed=42
- Shape: `(N_frames, 2, 2, 2048)` complex128, `active_sc` 1248 个索引
- 每点 100 帧，经 STO 补偿后

## 3.4 诊断发现

### 发现 1：SNR=40 时钟跳变

最后 4 帧 (96-99) STO 从 ~4 跳到 **~20 样本**，NMSE 飙到 +3.2 dB。

- 根因：采集时 timing sync 异常，非 int16 问题
- 处理：`test_2d_mmse.py` 自动检测并移除 STO > 6 样本的帧
- 去除后 SNR=40 经验 SNR = 17.3 dB，与 SNR=30 (17.2 dB) 一致

这也是对"遗留 2：STO 抖动"的补充——除了常规 ±1 样本抖动，还存在偶发大幅跳变 (~20 样本)。

### 发现 2："int16 ceiling" 是假象

之前误判的 -31 dB 硬 ceiling，实际是 split-set eval 帧数限制：

```
50 帧 eval → 平均增益 10·log₁₀(50) ≈ 17 dB
single-frame ~-14 dB + 17 dB gain ≈ -31 dB ← 恰好对上之前看到的 "ceiling"
```

验证：
- SNR=20 cumulative oracle 在 90 帧时达 **-50.38 dB**，远低于 -31 dB
- SNR=20 理论 100 帧 oracle = -36.8 dB

**结论**：无硬 ceiling。int16 的真实影响在 single-frame 层面 (经验 SNR cap ~17 dB for SNR ≥ 20)，但**多帧平均持续有效且没有上限**。

此发现对遗留 1 ("二级 ceiling") 的影响重大：e-MIMO 管线中的多帧统计聚合 (PDP/协方差) 可以穿透 per-frame ceiling。

### 发现 3：频域 Boxcar 有害

3-tap boxcar 在无噪 GT 上的 NMSE = -28.62 dB（不可消除的 bias）。

原因：信道频率选择性 (roughness=0.016) 使得相邻 SC 平均引入系统误差。

```
所有 SNR 点：EMA+Box 比纯 EMA 差 ~2 dB
```

**结论：去掉 boxcar，算法定型为纯时域 EMA，预算降为 2 ops/SC。**

---

## 3.5 Phase 1 主要结果

### NMSE vs SNR (split-set GT, α=0.1)

| SNR | Raw NMSE | EMA only | EMA+Box | Oracle | EMA 增益 |
|-----|----------|----------|---------|--------|----------|
| 10 dB | -6.52 | -17.01 | -16.78 | -16.71 | **+10.5 dB** |
| 20 dB | -12.30 | -25.28 | -23.44 | -32.55 | **+13.0 dB** |
| 30 dB | -20.66 | -27.65 | -25.06 | -30.93 | **+7.0 dB** |
| 40 dB | -22.73 | -29.79 | -26.53 | -30.51 | **+7.1 dB** |

### α sweep (SNR=20, split-set)

| α | EMA only | EMA+Box |
|---|----------|---------|
| 0.01 | -25.99 | -24.66 |
| 0.02 | -28.66 | -25.98 |
| **0.05** | **-29.07** | -25.68 |
| 0.10 | -25.28 | -23.44 |
| 0.30 | -19.74 | -19.19 |

最优 **α=0.05** (50 帧 eval 窗口下)。α 曲线呈非单调：太小收敛不够，太大平均不充分。

### 关键 implication

EMA 在静态信道下离 Oracle **远** (gap ~21 dB when oracle uses 90 frames)。
之前看到 "EMA 接近 Oracle" 是两者都撞 eval 窗口限制的假象。

**EMA 的真正价值在于动态信道的自动遗忘**——静态数据无法验证这一点。Phase 2 动态数据是完整故事的必要条件。

---

# 第四部分：2D MMSE Phase 2 — 动态信道实验 (05-14 ~ 05-15)

## 4.1 数据生成

```bash
sudo SNR_POINTS="10 20 30" MAX_FRAMES=300 CHANNEL_SEED=42 UE_SPEED=3 \
     bash run_q4_snr_sweep_v8.sh
```

- 3 SNR 点 (10/20/30)，P1B 信道，85-94 配对帧

## 4.2 逐层诊断 (P0 ~ P4)

### P0：Fix 1 (STO 补偿) 在动态信道下验证

**关心的问题**：Fix 1 是否在动态信道下过度补偿？(phase slope 可能混入 Doppler 效应)

**结果**：Fix 1 **有效** (+5.8 dB NMSE 改善，angle std 降低)。
"Fix 1 在动态下过度补偿" 假说 → **排除**。

### P1：GT 帧间相关性异常

**发现**：GT correlation = **0.999960 (所有 lag 下)** → GT 几乎不变。

**初步判断**："speed=3 没生效"。

### P2：Attach-stable 冻结机制

**发现**：handoff 在 proxy log line 14268/14277 (跑完前 9 行才解冻)。
全部数据在 **300s 冻结期内采集**。

```
run_q4_snr_sweep_v8.sh: ATTACH_STABLE_SEC=300 (默认)
实际情况: 冻结期内信道 producer 暂停演化 → 数据全是冻结状态
```

**修法**：设 `ATTACH_STABLE_SEC=0` 重跑。

### P3：关闭冻结后仍然 GT corr=1.0

重跑结果：GT corr 仍然 = 1.0。

**结论**：**不是冻结的问题**，是信道模型本身的行为。

### P4：信道演化机制深挖 (根因定位)

**深入源码**：找到 Doppler 公式在 `channel_coefficients_JIN.py`：

```
H(t) = Σ_l a_l · exp(j·2π/λ · (r̂_l · v) · t)
```

**帧间差分验证**：

1. 信道确实在变 → 帧间有 ~100°/frame 的相位旋转
2. 但去除全局旋转后 NMSE = **-40.8 dB** → **纯 phase rotation，零 shape drift**

**根因**：

```
P1B ray 角度扩展极窄
→ 所有 ray 共享几乎相同的 Doppler shift
→ 信道退化为: 固定 shape × exp(j·ω·t) 全局旋转
→ GT 的 "形状" (除全局相位外) 在 5 秒内完全不变
```

**验证**：多起点平均 de-rotated NMSE 在 5ms ~ 2500ms lag 上全部 -39 ~ -43 dB flat。

---

## 4.3 Phase rotation 速率异常

| Sweep | 实测旋转率 | 理论 3m/s 最大值 |
|-------|-----------|----------------|
| freeze300 | 51 Hz | 35 Hz |
| nofreeze | 56 Hz | 35 Hz |

实测 > 理论最大值。可能原因：
- velocity magnitude 实际 > 3 m/s (v8.py 的 `_speed_mag` 取的是半正态分布的绝对值加 mean)
- lambda_0 参数有误差

---

## 4.4 Phase 2 总结

| 发现 | 含义 |
|------|------|
| Fix 1 在动态下有效 | STO 补偿安全 |
| GT corr=1.0 (P1B) | 信道模型角度扩展太窄 |
| 纯 phase rotation | De-rotation 是必要操作 |
| Shape 5s 内不变 | P1B 不适合验证 adaptive α 的 shape-tracking 能力 |
| Phase rotation > 理论值 | 速度参数或 lambda 需要检查 |

**对教授管线的影响**：PDP、R_H、eigenstructure 全部 phase-invariant → P1B 的纯 phase rotation 对下游**无影响**。

---

# 第五部分：Adaptive α 算法探索 (05-15)

## 5.1 动机

Phase 2 发现纯 phase rotation 后，尝试将 de-rotation 与 adaptive α 结合，使 EMA 滤波器能自适应信道变化速率。

## 5.2 版本迭代

### v3：16-probe innovation + 乘法 α 调节

- **思路**：用 16 个均匀分布的子载波探测 innovation power，乘法更新 α
- **静态结果**：FAIL — α runaway (正反馈导致 α 持续增大)
- **状态**：弃

### v4：直接映射 (无乘法)

- **思路**：innovation → α 的直接函数映射，去掉乘法避免 runaway
- **静态结果**：FAIL — α 仍然偏高
- **P1B 结果**：OK (de-rotation 本身有效)
- **状态**：弃

### v5：p90 baseline + running max

- **思路**：用 innovation 的 p90 分位数作为 baseline，running max 做参考
- **静态结果**：α→min (符合预期)，但 static SNR=10 差
- **P1B 结果**：**很好** (-12 dB，比 fixed α=0.05 好 ~11 dB)
- **状态**：部分采用 (de-rotation 有效，adaptive 部分不稳定)

### v6：3dB de-rotation gate

- **思路**：在 v5 基础上加 de-rotation 触发阈值 (3 dB gate)
- **结果**：同 v5
- **状态**：部分采用

### v7：full-SC innovation + 5dB gate + bidirectional baseline

- **思路**：改用全部 SC 计算 innovation，5 dB gate，双向 baseline 追踪
- **静态结果**：REGRESS — 反馈环 (α ↔ H_smooth ↔ innovation)
- **P1B 结果**：不如 v5
- **状态**：弃

## 5.3 核心困难

**Innovation-based adaptive α 在 bursty SRS noise 下 baseline 估计不稳定**：

1. **正反馈环**：α ↔ H_smooth ↔ innovation
   - α 大 → H_smooth 紧跟噪声 → innovation 小 → α 更大 → 发散
   - α 小 → H_smooth 滞后 → innovation 大 → α 上调 → H_smooth 跟上 → innovation 小 → α 下调 → 振荡

2. **Heavy tail noise**：STO spike 帧 (偶发大 STO 跳变) 导致 innovation 瞬间暴涨
   - Median baseline 被击穿
   - Running max 追踪不及时

3. **De-rotation 在静态低 SNR 下误触发**：
   - 16-probe 估计不够准 → 虚假 Doppler 估计 → 错误 de-rotation
   - Full-SC 估计有反馈问题 → innovation 被 de-rotation 本身污染

## 5.4 当前最佳结果

v5/v6 的 de-rotation 部分有效，adaptive α 部分不稳定：

| 场景 | Fixed α=0.05 | Adaptive (含 de-rotation) | 差值 |
|------|-------------|--------------------------|------|
| Static SNR=30 | -30.2 dB | -29.9 dB | +0.3 dB (OK) |
| P1B sp3 SNR=10 | -1.2 dB | **-12.2 dB** | **-11.0 dB** |
| P1B sp3 SNR=20 | -1.9 dB | **-12.6 dB** | **-10.7 dB** |
| P1B sp3 SNR=30 | -1.6 dB | **-12.9 dB** | **-11.2 dB** |

**De-rotation 本身在有 Doppler 信道上给出 10+ dB 增益**，验证成功。

增益来源分析：
- Fixed α=0.05 在 P1B 下 EMA 无法追踪 ~50 Hz 的全局 phase rotation
- EMA 的 effective bandwidth = α/(2·T_srs) = 0.05/(2×0.08s) = 0.3125 Hz，远低于 50 Hz
- De-rotation 先去除全局旋转 → EMA 只需要追踪残余 (几乎为零) → 大幅改善

---

# 第六部分：架构级发现汇总

## 6.1 SRS 周期实际 = 5ms (不是 80ms)

```
OAI conf: periodicity = 10 (slots) = sl10 = 5 ms
离线数据的 160-slot 间隔是 dump 采样率限制，非 SRS 测量间隔
OAI 内部每 5ms 做一次 SRS 信道估计
```

这意味着：
- 实际 SRS 更新率远高于离线分析可见的
- EMA 在实际系统中有更多帧可用 (每秒 200 帧 vs 离线 12.5 帧)
- Doppler tracking 能力在实际系统中比离线实验更强

## 6.2 P1B 信道特性

```
极窄 angular spread → Doppler 退化为纯 global phase rotation
Shape 在 5s 内完全不变 (-40 dB de-rotated NMSE, flat)
对教授管线下游 (PDP, R_H, eigenstructure) 无影响 (全部 phase-invariant)
```

**含义**：P1B 信道模型不能用来验证 "信道形状变化" 场景下的 adaptive α。需要引入 CDL 等标准信道模型。

## 6.3 int16 精度的真实影响

| 层面 | 影响 | 是否有 ceiling |
|------|------|---------------|
| Per-frame | 经验 SNR cap ~17 dB (for SNR ≥ 20) | 有 (int16 量化) |
| 多帧平均 | Oracle 90帧达 -50.38 dB | **无硬 ceiling** |
| PDP/协方差 | N 帧平均后 SNR 增 10·log₁₀(N) | **无硬 ceiling** |

之前的 -31 dB "ceiling" 是 50 帧 eval 窗口限制的假象。

## 6.4 Sionna 版本

```
sionna==1.0.2, namespace = sionna.phy.*
CDL/TDL class 可用性待验证 (仓库内未使用)
```

## 6.5 OAI int16 链路完整路径

```
Sionna float64 H(f) × X(f)
  → Proxy GPU: round + clip(-32768,32767) → int16    ← 损失 ①
    → OAI int16 DFT (Q15 SIMD, scale=1, 多级 >>1)    ← 损失 ②
      → LS: int32(gen×rx) >> srs_generated_signal_bits → int16  ← 损失 ③
        → filt8: int16 overlap-add (adds_epi16 饱和)    ← 损失 ④
```

每步单独 SNQR ~60 dB (远高于 -7 dB)，但级联 + STO 的宽带累积效应导致了实际 floor。

---

# 第七部分：假说验证总表

下表汇总整个 05-09 ~ 05-15 期间所有被提出和验证的假说：

| # | 假说 | 首次提出 | 关键证据 | 最终状态 |
|---|------|---------|---------|---------|
| 1 | filt8 固定 FIR 插值误差 | 05-12 | passthru ≈ legacy (差 0.15 dB)；float 仿真 filt8=-22 dB | **排除** |
| 2 | filt8 3 点 pilot 平滑 | 05-12 | pilot vs midpoint NMSE 差 0.08-0.25 dB | **排除** |
| 3 | int16 FFT 精度不足 | 05-12 | Python bit-exact 仿真 corr=0.996；SNQR~60 dB | **排除** |
| 4 | UE int16 量化 (RMS=200) | 05-13 | SQNR=57 dB, 比实测退化低 45 dB | **排除** |
| 5 | Proxy AGC 可修复 | 05-14 | 实验恶化 2 dB (FFT overflow) | **排除** |
| 6 | GT-SRS symbol 不匹配 | 05-14 | 两者都用 symbol 12 | **排除** |
| 7 | OAI TA 动态调整 FFT 窗 | 05-14 | N_TA_offset 固定，无 TA command | **排除** |
| 8 | GT Doppler 残余 (speed=0) | 05-12 | 修复后 GT RMS CoV=0.00%；但 floor 仍在 | **修复了但非根因** |
| 9 | GT-SRS 数值坐标系差异 | 05-12 | 帧平均 GT 消除后 self-NMSE 仍 -0.9 dB | **贡献者之一但非主因** |
| 10 | int16 信号幅度过低 (根因) | 05-13 | RMS=200, 0.6% of int16, 7.6 bit 有效 | **重新定性为佐证** |
| 11 | TA 闭环 STO 抖动 (大幅) | 05-12 | TA-OFF: STO std 40x↓, 但 NMSE 仅改善 0.7 dB | **贡献者之一** |
| 12 | **帧间 STO 抖动 (±0.5 样本宽带累积)** | **05-14** | **Δδ=0 corr=0.9999@40dB; Fix 1 后 corr=0.986** | **确认：唯一根因** |
| 13 | Boxcar 频域滤波有害 | 05-14 | 无噪 GT 上 NMSE = -28.62 dB (bias) | **确认** |
| 14 | "int16 ceiling" -31 dB | 05-14 | Oracle 90帧达 -50.38 dB | **排除 (假象)** |
| 15 | P1B 信道 shape drift | 05-15 | De-rotated NMSE = -40.8 dB (flat) | **排除 (纯 phase rotation)** |
| 16 | Attach-stable 冻结导致 GT 不变 | 05-15 | ATTACH_STABLE_SEC=0 后 GT corr 仍=1.0 | **排除** |

---

# 第八部分：遗留问题与决策

## 遗留 1：二级 ceiling (empirical SNR cap ~20 dB)

### 现象

- SNR=30 dB 设定 → Fix 1 后 empirical SNR = 17.2 dB (损失 12.8 dB)
- SNR=40 dB 设定 → Fix 1 后 empirical SNR = 9.7 dB (损失 30.3 dB，但 STO std 异常大)
- NMSE vs 帧平均在 SNR=30/40 dB 收敛到 -21~-22 dB，不再随 SNR 提升

### 05-14 后续实验澄清

之前误判的 -31 dB "ceiling" 实为 split-set 帧数限制的假象：
- 50 帧 eval → 平均增益 ≈ 17 dB → -14 + 17 ≈ -31 (恰好对上)
- 验证：cumulative oracle 90 帧达 -50.38 dB
- **多帧平均没有硬 ceiling**

### 为什么不修

```
e-MIMO 孪生管线:
  SRS → 信道估计 → 2D滤波 → PDP/协方差 → State Sample → AI

遗留 1 位置: "信道估计" per-frame 精度
被吸收位置: "2D滤波"(多帧平均) + "PDP/协方差"(统计聚合)

21 dB per-frame SNR → N=10 帧平均后 31 dB → N=100 帧后 41 dB
完全满足 e-MIMO 特征提取需求
```

### 何时重开

路线切换到 V-RAN (需完整解码 per-frame BLER)，或高 SNR 场景下 AI 特征异常时。

---

## 遗留 2：STO 抖动的物理机制未定位

### 现象

- UL start_ts 在帧间有 ±1 整样本抖动 (29916/29917/29918, std≈0.5 样本)
- 偶发大幅跳变 (~20 样本，SNR=40 最后 4 帧)

### 处理方式

- 常规 ±1 样本：Fix 1 (phase slope 补偿) 完全消除
- 偶发大幅跳变：test_2d_mmse.py 阈值过滤 (STO > 6 样本)

### 可能机制 (未验证)

- rfsimulator GPU IPC 的 futex 唤醒延迟导致 timestamp 量化到不同整数
- OAI 调度器 slot 边界计算的舍入 (`get_samples_slot_timestamp` 累加路径)
- Proxy `proxy_ul_head_combined` 初始化/追赶逻辑中的边界条件

### 为什么不修

- rfsim 仿真中的 STO 机制和真实硬件 timing 行为无关 (IPC 时序 vs RF 链路 propagation)，追查不产生可迁移知识
- Fix 1 已完全消除对下游的影响
- 修复需深入 OAI rfsimulator / GPU IPC 底层 (预估 2-5 天)，收益为零

### 何时重开

Fix 1 在某些场景失效、STO 大幅跳变 >5% 帧、STO 幅度影响 UE attach 稳定性时。

---

## 遗留问题与 e-MIMO 管线的关系

```
教授的管线:
  SRS → 信道估计 → [Fix 1: STO补偿] → 2D滤波 → PDP/协方差 → State Sample → AI

遗留 1 (21dB per-frame ceiling):
  影响: "信道估计" per-frame 精度
  吸收: "2D滤波" + "PDP/协方差"
  → 不传递到 State Sample

遗留 2 (STO 物理机制):
  影响: "SRS → 信道估计" 之间
  消除: [Fix 1]
  → 不传递到 2D滤波及之后
```

**两个遗留都不在 e-MIMO 孪生管线的关键路径上。**

---

# 第九部分：方法论教训

### L1："对照实验先行"比"假说驱动开发"高效 10 倍

- Python int16 FFT 对照实验 (30 分钟) 直接排除 E1，省去 1-2 天
- SNR 标定实验 (15 分钟) 直接锁定 STO，省去 AMP 修改方向
- 回看前 5 天：如果第一天就做"同 STO 帧 vs 不同 STO 帧"分组对比，可能 1 小时就锁定根因

### L2：sinc 模型 / 物理直觉要用对参数

- 首次 STO 分析直觉"±1 样本太小"基于 per-SC 0.003 rad，没考虑宽带累积
- 正确度量：1 样本 STO 在 1200 SC 跨度上累积 2π×1200/2048 ≈ 3.7 rad ≈ **1.2π**
- 按 Δδ 分组实测 |α| 直接绕过了模型依赖——**实验 > 计算**

### L3："量化噪声 SQNR" ≠ "系统有效 SNR"

- SQNR=57 dB 但有效 SNR=5 dB，差距曾让人误以为量化是瓶颈
- 实际退化机制 (STO) 与量化完全正交——两者互相遮蔽

### L4：±1 样本看似微小，但宽带累积效应巨大

- 1 样本 STO 在 2048 点 FFT + 1200 SC 跨度下累积约 1.2π 相位差
- 窄带分段 (300 SC) corr=0.97，但宽带 corr=0.54
- 解释了"分段看一切正常，整体看一塌糊涂"的矛盾

### L5：分组对比是最有力的诊断工具

- 按 Δδ=0/1/2 分组直接给出因果关系，5 行 numpy
- 比回归分析、相位拟合、频域滤波等间接方法可靠得多
- 前提：需要知道"按什么变量分组"——需对系统有足够理解

### L6：实验结果"违反直觉"时优先怀疑脚本

- 阶段 3 的 STO 补偿脚本有 bug (没对齐到参考帧)，导致正确假说被错误排除
- 这是整个 debug 最危险的一步
- 规则：当 STO 补偿让 corr 变差时，优先怀疑脚本而非物理

### L7：先诊断系统 floor 再开发算法

- 如果第一天 (05-09) 就跑静态 channel + SNR=40，能立刻锁定瓶颈
- 而不是花 3 天实现 PDP→R MMSE + EWMA + LSL 再回头诊断

### L8：控制变量实验优先于参数调优

- 静态 channel (speed=0) 是最有力的诊断工具
- 消除对齐、Doppler、GT 变化等所有干扰因素

### L9：Fair comparison 从第一天就要做

- `CHANNEL_SEED=42` 是必须的
- Random seed 曾导致 PDP-R MMSE 的严重误判 (以为大胜实际大败)

### L10：Eval 窗口限制可造成"撞墙假象"

- EMA 和 Oracle 同时撞 50 帧窗口 → 伪装成 ceiling
- Cumulative oracle 在 90 帧达 -50.38 dB → 真实极限远低于表面值

---

# 第十部分：产出文件与复现

## 代码文件

### 2D MMSE 核心

| 文件 | 说明 |
|------|------|
| `G1C/srs_2d_mmse.py` | 滤波器模块: SRS2DFilter (C风格) + SRS2DFilterVec (向量化) + AdaptiveSRS2DFilter (v7) |
| `G1C/test_2d_mmse.py` | 完整测试框架: split-set GT, outlier 检测, α sweep, SNR sweep, --speed 自动区分 |
| `G1C/prepare_2d_mmse_data.py` | 数据管线: sweep → STO 补偿 → GT 对齐 → 标准 NPZ |

### 诊断工具

| 文件 | 说明 |
|------|------|
| `G1C/verify_float_fft_gain.py` | E1 对照实验：int16 vs float FFT 帧间一致性 |
| `G1C/oai_dft_wrapper.py` | bit-exact OAI int16 DFT Python 封装 |
| `G1C/digital_twin_stats.py` | SRS bin 数据加载器 (load_srs_v2) |
| `G1C/test_sinc_interpolation.py` | Float 仿真推翻 filt8 假说 |
| `G1C/diagnose_alpha_floor.py` | Alpha floor 深度诊断 |
| `G1C/diagnose_nmse_floor.py` | NMSE floor 诊断 |
| `G1C/srs_sto_compensate.py` | STO 补偿工具 |
| `G1C/srs_snr_calibration.py` | SNR 标定分析 |

### 运行脚本

| 文件 | 说明 |
|------|------|
| `G1C/run_q4_snr_sweep_v8.sh` | SNR sweep runner |
| `G1C/preflight.sh` | sweep 前清理 zombie/shm/容器 |
| `G1C/launch_all_v8.sh` | 完整启动脚本 |

### OAI 内部

| 文件 | 说明 |
|------|------|
| `nr_ul_channel_estimation.c` | dispatcher 加了 MMSE2D/passthru/oracle case |
| `nr_srs_mmse.c` | PDP-R 实现 + env getter + noise estimator |
| `nr_srs_2d_filter.c/h` | EWMA 实现 |

### 评估工具

| 文件 | 说明 |
|------|------|
| `G1C/eval_pdpR_nmse.py` | 通用 NMSE 评估器 |
| `G1C/lsl_m2_reference.py` | LSL Python reference |
| `G1C/lsl_m2_joint_process.py` | LSL joint-process 验证 |

## 数据文件

| 数据 | 路径 | 说明 |
|------|------|------|
| 静态 4 点 | `G1C/data_out/2d_mmse_input/H_snr{10,20,30,40}dB_static_seed42.npz` | 100 帧/点 |
| 动态 3 点 | `G1C/data_out/2d_mmse_input/H_snr{10,20,30}dB_speed3ms_seed42.npz` | 85-94 帧/点 |
| SNR 标定 | `logs/q4_sweep_20260514_130516/snr_{10,20,30,40}dB/` | Fix 1 验证数据 |
| 实验图 | `G1C/data_out/2d_mmse_results/*.png` | 所有结果图 |

## 复现命令

### Step 0：采集数据 (需要 OAI + Proxy 环境)

```bash
cd /home/dclcom57/OAI_luuuuuu/DevChannelProxyJIN/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy

# 静态
sudo UE_SPEED=0 CHANNEL_SEED=42 SRS_ESTIMATOR=legacy GT_SAVE_EVERY=10 \
     SNR_POINTS="10 20 30 40" MAX_FRAMES=300 ATTACH_STABLE_SEC=300 \
     bash run_q4_snr_sweep_v8.sh

# 动态 (speed=3 m/s)
sudo SNR_POINTS="10 20 30" MAX_FRAMES=300 CHANNEL_SEED=42 UE_SPEED=3 \
     bash run_q4_snr_sweep_v8.sh
```

### Step 1：后处理

```bash
python3 prepare_2d_mmse_data.py \
    --sweep-dir /path/to/q4_sweep_YYYYMMDD_HHMMSS \
    --speed 0 --seed 42
```

### Step 2：分析出图

```bash
python3 test_2d_mmse.py                     # 全部数据
python3 test_2d_mmse.py --speed 3           # 特定 speed
python3 test_2d_mmse.py --speed 0 --snr 20 --alpha 0.05  # 特定参数
```

---

# 第十一部分：下一步方向

## 确定需要做的

| 方向 | 优先级 | 说明 | 预估工作量 |
|------|--------|------|-----------|
| **Plan B (Dual-EMA)** | **高** | 用独立 reference EMA 打破 innovation-α 反馈环 | 1-2 天 |
| **CDL 信道模型接入** | **高** | 通过 3GPP 标准 ray 参数表注入 Rays() 接口，获得真实 shape drift | 2-3 天 |
| **多场景验证** | **中** | CDL-A/C/D × speed × SNR 生成 ~20 数据点 | 1 天/场景 |
| 离线 dump 频率提升 | 低 | 当前 dump 间隔太长，高速数据意义有限 | 0.5 天 |

## Go/No-Go Gate

**Exp 1 (fixed α scan)**：在多场景上找各自最优 α

- 如果最优 α 有 spread (0.01 ~ 0.3) → adaptive α 有研究价值 → 继续 Dual-EMA
- 如果全聚在 0.01-0.05 → SRS dump 间隔太长 → 需要先解决 dump 频率

## 代码当前状态

| 组件 | 状态 |
|------|------|
| `srs_2d_mmse.py` | SRS2DFilter + SRS2DFilterVec 稳定；AdaptiveSRS2DFilter (v7) 有已知反馈环问题 |
| `test_2d_mmse.py` | 完整测试框架，支持 `--speed` 自动区分静态/动态 |
| `prepare_2d_mmse_data.py` | 端到端数据管线完整 |
| 静态数据 | 结果稳定可重现 |
| 动态数据 | 分析管线完整，GT 对齐需 LS alignment |
| Fix 1 (STO 补偿) | Python 层面验证完成，待 C 化移植 |

---

# 附录：关键数值速查

| 指标 | 值 | 来源 |
|------|-----|------|
| Fix 1 NMSE 改善 | +11.6 dB | 阶段 7 |
| Fix 1 corr 改善 | 0.674 → 0.986 | 阶段 7 |
| 同 STO corr @40dB | 0.9999 | 阶段 6 |
| Boxcar bias floor | -28.62 dB | Phase 1 |
| 最优 α (静态 50帧) | 0.05 | Phase 1 |
| EMA 增益 @10dB | +10.5 dB | Phase 1 |
| EMA 增益 @20dB | +13.0 dB | Phase 1 |
| De-rotation 增益 (P1B) | +10~11 dB | Phase 2 / Adaptive |
| P1B phase rotation | ~50-56 Hz | Phase 2 |
| De-rotated NMSE | -40.8 dB | Phase 2 |
| SRS 实际周期 | 5 ms | 架构发现 |
| Per-frame SNR cap | ~17 dB | 遗留 1 |
| UE IFFT RMS | 200 (0.6% int16) | 1.8 |
| 1 样本 STO 宽带相位差 | 1.2π (3.7 rad) | L2/L4 |
