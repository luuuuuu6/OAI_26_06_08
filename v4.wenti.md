# v4.py 潜在问题全面排查

我按严重程度分类列出所有发现的问题。# v4.py 潜在问题完整清单

我按严重程度分类。每条问题包含**位置**、**问题描述**、**风险后果**、以及（如果显然）**修复建议**。

---

## 🔴 严重问题（Critical）

### 1. `carrier_frequency = 3.5` 单位错误 ⚠️
**位置：** 第 194 行  
```python
carrier_frequency = 3.5
```
**问题：** 5G FR1 载频应该是 3.5 GHz = `3.5e9` Hz。Sionna 的 `PanelArray(carrier_frequency=...)` 期望以 **Hz** 为单位。  
**后果：** 信道生成中所有与波长相关的计算（天线间距 λ/2、Doppler、空间相关性）都会差 9 个数量级。整个仿真物理模型失效。  
**修复：** `carrier_frequency = 3.5e9`，并和 `channel_coefficients_JIN.py` 的单位约定核对。

---

### 2. 信道不随时间演变（Doppler 时间相关性丢失）⚠️ 最严重
**位置：** 第 1817-1819 行 + 1875 行  
```python
sample_times_fixed = _tf.cast(
    _tf.range(buffer_symbol_size), gen.rdtype
) / _tf.constant(params['scs'], gen.rdtype)   # 永远从 0 开始
...
while not self.stop_event.is_set():
    h_c128_norm = generate_fn()                # 每次输入完全相同
    ...
    symbol_counter += buffer_symbol_size       # 这个变量算出来后没用！
```
**问题：** `sample_times_fixed` 是**固定**的 `0..buffer_symbol_size-1`，每次 `generate_fn()` 输入完全相同的 sample_times、固定的 topology 和 PDP，因此**生成的每一批 H(f) 序列都完全相同**。`symbol_counter` 累加了但从未喂回 `sample_times`。  
**后果：** 所谓 "时变 Doppler 信道" 实际上是**周期性循环的同一段 channel realization**。任何依赖时间相关性的算法（如时域追踪、Kalman 滤波、长时间 SNR 估计）会得到错误结论。  
**修复：** 
```python
sample_times = _tf.cast(_tf.range(symbol_counter, symbol_counter + buffer_symbol_size),
                        gen.rdtype) / scs_local
# 每次循环重新构造，或用占位符 + feed
```

---

### 3. CUDA Graph 捕获使用默认 Global 模式
**位置：** 第 1023 行  
```python
self.stream.begin_capture()  # 默认 cudaStreamCaptureModeGlobal
```
**问题：** Global 模式下 capture 期间，**任何 thread/stream** 的 CUDA 调用都会被捕获或导致 capture 失败。本工程中：
- N 个 pipeline 各有自己的 stream。
- NoiseProducer 后台线程在 default stream 上运行 `cp.random.randn`。
- UnifiedChannelProducerProcess 在子进程，但同一进程内的多 pipeline 之间会冲突。

**后果：** 
- 多 pipeline warmup 完同时尝试 capture 时，第二个会失败（已被 fallback 到 NORMAL 但永久关闭 graph，丢失加速）。
- NoiseProducer 线程在某个 pipeline capture 时跑 randn → capture 失败或行为未定义。

**修复：** `self.stream.begin_capture(mode=cp.cuda.runtime.streamCaptureModeRelaxed)` 或 `ThreadLocal`。

---

### 4. Producer 进程死亡后系统卡死，无恢复机制
**位置：** 第 2664-2666 行（warmup 阶段仅打印）+ 主循环  
```python
if not self.channel_producers[0].is_alive():
    print(f"[v4 WARN] UnifiedChannelProducerProcess died during warmup")
# ...继续执行
```
**问题：** Producer 死后，每个 slot 调用 `get_batch_view(N_SYM, timeout=30)` 都会等 30 秒后 `TimeoutError`，主循环走 bypass。但 30 秒/slot 等于完全停摆。warmup 阶段检测到死亡也不退出。  
**后果：** Producer 一旦因 OOM、CUDA 错误等挂掉，整个 Proxy 变僵尸，OAI 端会无限等待。  
**修复：** 主循环周期性检查 `proc.is_alive()`，死亡后退出或重启。

---

### 5. Producer 异常处理无限重试
**位置：** 第 1880-1884 行  
```python
except Exception as e:
    print(f"[v4 UnifiedChannelProducerProcess] ERROR ...: {e}")
    traceback.print_exc()
    time.sleep(1.0)   # 然后 while 循环继续
```
**问题：** 任何非 OOM 异常都 sleep 1 秒重试。如果是确定性错误（如 shape 不匹配、KeyError），会**每秒打印同样的 traceback 直到天荒地老**。  
**后果：** 日志爆炸、CPU 浪费、问题被掩盖。  
**修复：** 计数器，连续失败 N 次后 break。

---

### 6. Socket mode 实际上不支持 multi-UE
**位置：** 第 2067-2071, 2151, 2222-2226, 2246, 2250 行  
```python
self.pipeline_dl = self.pipelines_dl[0]              # 别名
self.gpu_slot_pipeline = self.pipeline_dl
self.channel_buffer = self.channel_buffers[0]        # 只用 UE0 的 channel
...
for u in list(self.ues.values()):
    u.send(hdr_raw, processed)                       # 同样 processed 广播
self._e2e_dl_per_ue[0] += 1                          # 硬编码 UE0
```
**问题：** Socket 模式下，所有 UE 共享 UE0 的 channel buffer 和 pipeline，且广播相同的 `processed` 数据；同时 e2e 计数硬编码 `[0]`。这与 v4 的 multi-UE 设计完全不符，但代码不报错也不警告。  
**后果：** 用户用 socket mode + `--num-ues 4` 跑会得到错误结果（所有 UE 看到相同信号），还以为系统正常。  
**修复：** 在 socket mode 下若 `num_ues > 1` 直接报错，或正确实现 per-UE socket。

---

### 7. `bypass_copy` 不对称分支每次分配 GPU 内存
**位置：** 第 476 行  
```python
dst_2d = cp.zeros((nsamps, dst_nbAnt * 2), dtype=cp.int16)
```
**问题：** `bypass_copy` 在天线数不对称时每次调用都 `cp.zeros(...)` 新分配。这是主循环热路径 —— 每个 slot 都要走。  
**后果：** CUDA allocator 压力，频繁的 cudaMalloc/cudaFree（即便有内存池）会拖慢吞吐。  
**修复：** 预分配 `self._bypass_dst_2d` 并 `[:] = 0` 复用。

---

### 8. `_e2e_dl_per_ue[0]` 在 Socket mode 下硬编码
**位置：** 第 2246, 2250 行  
即使 `num_ues > 1`，e2e 统计永远只更新 UE0。配合上面 #6 的问题，导致统计完全失真。

---

### 9. `gnb_ant` 与 `gnb_nx*gnb_ny` 一致性无校验
**位置：** main 中  
**问题：** 用户可以指定 `--gnb-ant=1 --gnb-nx=2 --gnb-ny=2`，导致：
- Pipeline 按 1 天线分配。
- Sionna PanelArray 按 2×2=4 天线生成 channel。
- channel.shape[1:3] = (n_rx, n_tx) 与 pipeline.gpu_H 形状不匹配 → `gpu_H[:, :n_r, :n_t] = ...` slice 不对应。

**后果：** 静默错误的天线映射，难以察觉。  
**修复：** main 入口 `assert gnb_ant == gnb_nx * gnb_ny`，UE 同。

---

### 10. `Endpoint` dataclass 使用 PEP 604 union 语法
**位置：** 第 1373 行  
```python
hdr_vals: Tuple[int,int,int,int,int]|None = None
```
**问题：** `X | None` 写法需要 Python ≥ 3.10。  
**后果：** 在 3.9 及以下（OAI 仿真环境常见 Ubuntu 20.04 默认 Python）直接 SyntaxError，根本起不来。  
**修复：** `Optional[Tuple[...]]` 或加 `from __future__ import annotations`。

---

## 🟠 较严重问题（High）

### 11. IPCRingBuffer view 在 wrap-around 时返回 copy 而非 view，行为不一致
**位置：** 第 1626-1632 行  
```python
if end <= s.maxlen:
    batch = self.buffer[s.read_idx.value:end]   # view (slice)
else:
    batch = cp.concatenate([...])               # copy (new array)
```
**问题：** 非 wrap → view，wrap → copy。`release_batch` 行为对两者意义不同。如果未来代码修改 view 内容（写入），wrap 与非 wrap 行为会发散。  
**当前后果：** 仅读取下不会出错，但是隐患。

---

### 12. Channel 生产 / 消费速率不匹配 时无反压
**位置：** 第 1864-1873 行  
```python
inserted = ring_buffers[ue_k].try_put_batch(h_ue_k)
dropped = h_ue_k.shape[0] - inserted
if dropped > 0:
    drop_count[ue_k] += dropped
    # 只 print 不处理
```
**问题：** 当 consumer 慢于 producer，buffer 满后新 channel 数据被 drop，consumer 拿到的是**旧 channel**。drop 仅日志，不反馈给主进程。  
**后果：** 信道在时间上不连续——consumer 反复消费同一段旧信道，**Doppler 相关性被进一步破坏**（叠加问题 #2 后更严重）。

---

### 13. `partial slot` bypass 时不应用 channel
**位置：** 第 2461-2466 行  
```python
if remaining > 0:
    self.ipc_gnb.bypass_copy(...)   # 直接 bypass，不过 channel
```
**问题：** 当 `delta` 不是 `slot_samples` 的整数倍时，剩余部分**直接拷贝**，不施加信道。`apply_ch=True` 配置下也走 bypass。  
**后果：** 每次有 partial slot，信号短暂"变干净"，引入信号不连续性。配合诊断日志计数（`_dl_remainder_count`），如果 remainder 频繁（OAI 端 buffer flush 不对齐），就会有大量 bypass 段。  
**修复：** partial 部分也应用零填充到 slot 边界后过 channel，再切回所需长度。

---

### 14. `_ul_dummy_out` 浪费 GPU 写带宽
**位置：** 第 2572-2577, 2721-2722 行  
```python
self._ul_dummy_out = cp.zeros(self.pipelines_ul[0].total_int16_out, dtype=cp.int16)
...
self.pipelines_ul[k].process_slot_ipc(arr_in, ..., self._ul_dummy_out, ...)
self._ul_accum += self.pipelines_ul[k].gpu_out
```
**问题：** UL superposition 中只需要 `gpu_out`（complex128 累加用），但 `process_slot_ipc` 末尾仍然把结果 `clip+cast` 写到 `_ul_dummy_out`（int16 量化），随后再被 `_fused_clip_cast_kernel` 重新做一遍量化。  
**后果：** 每个 UE 每个 slot 多一次 `clip+cast+write` 的 GPU 操作，浪费。N_UE=4 时浪费 ~30% UL 路径。  
**修复：** `process_slot_ipc` 增加 `skip_quant=True` 参数，跳过最后步骤直接返回 `gpu_out`。

---

### 15. `_stalled_ue_logged` 永不清除
**位置：** 第 2778 行  
```python
self._stalled_ue_logged.add(sk)
```
**问题：** UE 一旦被标记 stalled 就永远在集合里。即使 UE 后来恢复（active_set 重新包含它），也不会重新发 log。如果 UE 再次 stall，也不会再 log。  
**后果：** 监控失效。  
**修复：** 在 active_set 中检测到 UE 时从 `_stalled_ue_logged` 移除。

---

### 16. NoiseProducer + 多 UE 的 VRAM 占用爆炸
**位置：** 第 2048, 2051 行  
```python
nb_dl = RingBuffer(shape=(2, noise_len_dl), dtype=cp.float64, maxlen=256)
```
**问题：** `noise_len = 30720 * gnb_ant`，single buffer = `256 × 2 × noise_len × 8B`。
- gnb_ant=4 → 单 buffer ≈ 480 MB。
- 每 UE 两个（DL+UL）≈ 1 GB / UE。
- N_UE=4 → ~4 GB **仅噪声缓冲**。

**后果：** GPU OOM，尤其在 16/24 GB 显存卡上。  
**修复：** maxlen=64 已足够（预生产 64 batch 远多于消费速率）；或用 float32（噪声本身不需要 float64 精度）。

---

### 17. `set_last_ul_rx_ts` 在 per-UE bypass 路径中被多次调用，触发多次 futex_wake
**位置：** 第 2821 行  
```python
for k in range(N):
    ...
    self.ipc_gnb.set_last_ul_rx_ts(int(ue_head - 1))   # 每个 UE 调一次
```
**问题：** N 个 UE 各 wake 一次，OAI 端每次 wake 后 spurious 检查。  
**修复：** 收集后只在循环外 wake 一次（last write wins）。

---

### 18. GTBatchSaver._flush 同步阻塞主循环
**位置：** 第 800-824 行  
```python
np.savez_compressed(fname, ...)   # 同步写盘 + 压缩
```
**问题：** 每 100 slot（BATCH_SIZE）阻塞主循环数十至数百 ms。  
**后果：** UL 链路抖动（jitter），p99 拉高。  
**修复：** 后台线程异步写盘，或写未压缩 npz（np.savez 而非 np.savez_compressed）。

---

### 19. CUDA Graph capture 失败永久禁用，但 NoiseProducer 仍在跑
**位置：** 第 1047-1049 行  
```python
print(f"[CUDA Graph] Capture failed - fallback to normal: {e}")
self.graph_captured = False
self.use_cuda_graph = False    # 永久禁用
```
**问题：** capture 失败的常见原因之一就是 NoiseProducer 在 default stream 跑 randn 干扰（见 #3）。一旦失败，性能永久退化到 NORMAL 模式。  
**后果：** 一次冷启动失败导致整个会话性能折半。  
**修复：** 关联到 #3 修复（用 Relaxed/ThreadLocal 模式）。

---

## 🟡 中等问题（Medium）

### 20. `pl_linear != 1.0` 在 graph 内是 Python 分支
**位置：** 第 996 行  
```python
if pl_linear != 1.0:
    self.gpu_out *= cp.float64(pl_linear)
```
**问题：** Python 标量比较，graph 捕获时基于 capture 时刻的 pl_linear 值固化。运行时改 pl_linear 不生效（除非 _need_recapture 触发）。  
**当前实现：** _need_recapture 检查 pl_linear 变化，OK。但写法上有歧义。  
**修复：** 直接 `self.gpu_out *= cp.float64(pl_linear)` 不带 if，让 GPU 处理 ×1.0 的情况（性能差异微乎其微）。

---

### 21. `load_p1b_per_ue` 每次读完整 npz
**位置：** 第 142-158 行  
```python
def load_p1b_per_ue(npz_path, rx_index):
    data = np.load(npz_path, allow_pickle=True)
    ...
    data.close()
    return result
```
**问题：** `load_p1b_stacked` 调用 N 次（第 164 行），每次都重新 mmap/load 整个 npz 文件。  
**后果：** 启动慢（如果 npz 几百 MB）。  
**修复：** 一次 `np.load` 后传入 lazy slice。

---

### 22. `try_put_batch` 持锁期间做 `cp.cuda.Device(0).synchronize()`
**位置：** 第 1593 行  
```python
with s.not_full:
    for i in range(n):
        ...
    if inserted > 0:
        _cp.cuda.Device(0).synchronize()    # 持锁同步整个设备
        s.not_empty.notify_all()
```
**问题：** 持有跨进程锁的同时执行 device-wide sync，所有等待 `not_full` / `not_empty` 的进程都被阻塞数毫秒。  
**修复：** sync 移出锁外（先 sync，再获取锁更新 count + notify）；或改用 stream sync。

---

### 23. `tf.experimental.dlpack.to_dlpack` 已废弃
**位置：** 第 1856 行  
```python
h_cp = _cp.from_dlpack(_tf.experimental.dlpack.to_dlpack(h_c128_norm)).copy()
```
**问题：** TF 2.13+ 标记为 deprecated；TF 2.16+ 可能移除。fallback 路径 `_cp.asarray(h_c128_norm.numpy())` 触发 GPU→CPU→GPU 拷贝（极慢）。  
**修复：** 用 `tf.experimental.dlpack.to_dlpack` 替换为 `__dlpack__` 协议或直接通过 `tf.config.experimental.get_memory_info` 协调。

---

### 24. `fft_lib` 参数被忽略
**位置：** main 第 2919 行 + Proxy 构造器  
```python
ap.add_argument("--fft-lib", type=str, default="np", choices=["np", "tf"])
```
**问题：** 参数收了但 GPUSlotPipeline 内部全用 `cp.fft.fft`，`fft_lib` 实际未使用 — dead arg。  
**修复：** 移除或实现。

---

### 25. handle_queue 等 N 次但只有一个 producer，超时后没 join
**位置：** 第 2130-2148 行  
```python
for k in range(N):
    try:
        ipc_handle = handle_q.get(timeout=120)
    except Exception as e:
        ...
        if proc.is_alive():
            proc.terminate()
        raise ...
```
**问题：** 第一次 get 成功但第二次失败时，前面已创建的 `IPCRingBufferConsumer` 没 cleanup（持有 CUDA IPC handle 泄漏）。  
**修复：** 在 except 分支里 cleanup 已创建的 consumer。

---

### 26. `120 秒` 超时对 XLA 启动可能不够
**位置：** 第 2132 行  
**问题：** XLA 编译 + Sionna 初始化在第一次启动可能超 2 分钟（特别是 cuDNN 缓存冷启动）。即使没启用 XLA，line 1840-1843 的 `_ = generate_fn()` 也是 placement 在 handle put **之后**。但 XLA 编译消耗在 handle put 之前不会发生。  
**风险点：** Sionna `_H_PDP_FIX` 编译慢；cuBLAS lazy load。  
**修复：** 提到 240 或 300 秒，或用心跳机制。

---

### 27. `_ul_clip_3d` 似为 dead code
**位置：** 第 2720 行  
```python
self._ul_clip_3d = cp.zeros((total_cpx, self.gnb_ant, 2), dtype=cp.float64)
```
**问题：** 分配后没看到任何使用。被 `_fused_clip_cast_kernel` 取代后忘删。  
**后果：** 浪费 ~480KB / N_UE GPU 内存（不大但是死代码）。

---

### 28. `_ipc_apply_channel_for_ue` 中 timeout 时 bypass 但**仍 release**？
**位置：** 第 2398-2404 行  
```python
try:
    channels, n_held = self.channel_buffers[ue_idx].get_batch_view(N_SYM)
except TimeoutError:
    print(f"[v3 WARN] UE[{ue_idx}] channel buffer timeout — bypass copy")
    src_ipc.bypass_copy(...)
    return  # 没调用 release_batch (因为 get_batch_view 抛出，没获得 view)
```
等等仔细看：`get_batch_view` 如果超时是 raise TimeoutError，`channels, n_held` 都没被赋值。所以 release 不需要调用。**这条没问题**，但代码读起来需要确认 `get_batch_view` 在抛异常前**没**修改 read_idx。看实现（第 1619-1625 行）抛异常发生在 read_idx 修改之前。**OK**。

但 `_ipc_ul_superposition_slot` 第 2554-2559 行 timeout 后用 `continue`，跳到下一个 UE。这次也没 release。**仍然 OK**。

**虽然 OK，但建议加注释**说明 timeout 路径不需要 release。

---

### 29. `mean_xpr` / `stddev_xpr` 硬编码 UMa-NLOS
**位置：** 第 2113-2114 行  
```python
'mean_xpr': mean_xpr_list["UMa-NLOS"],
'stddev_xpr': stddev_xpr_list["UMa-NLOS"],
```
**问题：** P1B 数据可能来自 UMi 或其他场景，这里强制用 UMa-NLOS 的 XPR 参数。  
**修复：** 暴露 `--scenario` 参数，或从 P1B npz 读取场景元数据。

---

### 30. `set_BS` 函数有 `tx_antennas` 计算但 UnifiedChannelProducerProcess 内部不用
**位置：** 第 221-235 行  
死代码，UnifiedChannelProducerProcess 用自己的 BSexample dict，不调用 `set_BS`。

---

### 31. `pad[:, j, j, :] = 1.0` 假设方阵单位
**位置：** 第 2326-2327, 2411-2412, 2565-2566 行  
```python
for j in range(min(n_r, n_t)):
    pad[:, j, j, :] = 1.0
```
**问题：** 当 channel batch 不足 N_SYM 时填充单位"channel"。`min(n_r, n_t)` 仅给方阵对角填 1，非方阵的额外天线全 0。  
**后果：** 在不对称 MIMO（n_r != n_t）下，"补齐"的 symbol 是部分能量的，引入信号能量跳变。  
**修复：** 对于 padding，统一使用全 0 channel 或保持上一帧 channel；记录这是个不应该频繁发生的边界情况。

---

### 32. `Tuple[...]` 在 dataclass 中做注解用 PEP 484 风格
和 #10 同源。

---

### 33. `_buf_iq_out_3d` 用 `cp.float64` 但目标 int16
**位置：** 第 924, 1017-1019 行  
```python
self._buf_iq_out_3d = cp.zeros((self.total_cpx, n_rx_out, 2), dtype=cp.float64)
...
self._buf_iq_out_3d[:, :, 0] = cp.clip(cp.around(...), -32768, 32767)
self.gpu_iq_out[:] = self._buf_iq_out_3d.ravel().astype(cp.int16)
```
**问题：** 中间 buffer 用 float64 存量化后的整数值，最后才 cast。浪费 4× 内存带宽。  
**修复：** `_buf_iq_out_3d` 直接用 `cp.int16`，去掉最后的 `astype`。

---

### 34. 全局 `noise_std_abs` 既是 cp.float64 又是 None
**位置：** 第 3050-3055, 1000-1002 行  
```python
noise_std_abs = cp.float64(_noise_rms / _math.sqrt(2.0)) if GPU_AVAILABLE else None
...
if noise_std_abs is not None:
    self._tmp_noise = noise_std_abs * (...)
```
**问题：** GPU 不可用时 `noise_std_abs=None` 走 SNR 模式，但 `noise_dBFS` 模式逻辑上不应回退到 SNR 模式。绝对噪声 floor 配置在 CPU 模式下被静默忽略。  
**修复：** CPU 模式分支单独处理 dBFS。

---

## 🟢 轻微问题（Low）

### 35. `LOG_LINES` 用列表 + 长度检查不释放
第 91-92, 173-176 行：固定 300 上限的内存。每次 append 检查，O(1) 但永不释放。生命周期长会占常量内存。

### 36. `print(f"...")` 大量分散，无 logging 等级
全文混用 `print` 与 `logging`，但 `logging` 实际未配置使用。

### 37. `_libc.syscall` 直接调用 futex 无错误处理
第 680-683 行：syscall 返回值未检查。futex_wake 失败（如 EINVAL）静默。

### 38. `get_ofdm_symbol_indices` 可缓存
第 237-245 行：基于固定 `SYMBOL_SIZES` 计算，可全局缓存常量。

### 39. `_random.sample(all_indices, num_ues)` 可能 IndexError
第 136 行：如果 `len(all_indices) < num_ues`，`random.sample` 抛 ValueError。无前置检查。

### 40. `validate_rx_indices` 使用 `sys.exit(1)` 而非异常
第 128 行：在库代码中直接 exit 不友好（无法被上层 try/except 捕获）。

### 41. `signal.signal(signal.SIGTERM, _sigterm_handler)` 在 spawn 子进程中可能不被继承
第 3158 行：仅在主进程注册。子进程（UnifiedChannelProducerProcess）的 SIGTERM 处理是默认（直接终止），可能跳过 cleanup（IPC handle 泄漏 GPU 内存）。

### 42. `GTBatchSaver` 部分字段无 `complex64` 量化误差说明
GT 矩阵从 complex128 cast 到 complex64 (line 791, 793)，没记录这个精度损失。在做精度对比验证时可能误判为算法误差。

### 43. `Tuple[int,int,int,int,int]|None` 与 `Optional[...]` 混用风格不一致

### 44. `bypass_copy` 内调用 `cp.cuda.Stream.null.synchronize()`
第 452 行：default stream 同步而非 pipeline stream，混用 stream 增加同步成本。

### 45. `signal_handlers` 里 `KeyboardInterrupt` 抛出方式有竞态
SIGTERM → `raise KeyboardInterrupt` 不是 signal-safe（Python 3 的 signal handler 在主线程执行 OK，但子线程不保证）。

---

## 📋 总结：优先修复路线

| 优先级 | 问题 | 影响范围 |
|--------|------|---------|
| **P0** | #1 carrier_frequency 单位 | 整个物理仿真 |
| **P0** | #2 sample_times 不更新 | Doppler 时变性 |
| **P0** | #10 Python 3.10 语法 | 部署兼容性 |
| **P1** | #3 CUDA Graph mode | 多 pipeline 性能 |
| **P1** | #4 #5 Producer 鲁棒性 | 可用性 |
| **P1** | #6 #8 Socket multi-UE | 误用风险 |
| **P1** | #14 #16 #18 内存 / 性能 | 吞吐和延迟 |
| **P2** | 其他 | 优化和清理 |

**最值得首先验证的是 #2**：跑两遍 generate_fn() 检查 H 是否完全相同。如果是（很可能），所有声称的 Doppler 信道结果都需要重新评估。

如果你想我深入某个问题（例如写出 #2 的最小复现脚本，或具体设计 #14 的 `skip_quant` API），告诉我。