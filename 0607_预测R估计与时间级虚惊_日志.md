# SRS 预测 R 估计对比 + 时间级"虚惊"诊断 — 日志

| 字段 | 值 |
|------|----|
| 日期 | 2026-06-07 |
| 主题 | ① §8.1 参数化/PSD/多态多普勒 R 估计离线对比;② 真实 capture 上 Wiener 预测 A/B;③ "时间级 Kalman 高动态发散"副发现的端到端排查 → 确认为 replay 工具假象;④ 厘清 C 端离线验证代码体系 |
| 测试床 | native rfsimulator + channelmod;离线 `test_srs_2d_offline.py` 合成 sweep + `replay_true2d.py` 真实 capture |
| 关联 | `0605_SRS信道预测_方向A_日志.md`(§8 路线)、`0531_True2D_端到端诊断与修复日志.md`(STO/相位模式) |
| 状态 | 经典预测确认到顶(EMA-Wiener≈genie);时间级生产健康(虚惊已澄清);下一步空间在中长地平线 AI |

---

## 1. §8.1 预测自相关 R 估计:四方离线对比(合成 CDL-C/E)

在 `test_srs_2d_offline.py` 预测 sweep 接入时间维 Wiener 四方对比(`--predict-wiener`),四者**从同一含噪 Y 预测,只在 r(d) 来源不同**,干净隔离"R 估计方差":

- **EMA**:因果 EMA 估 r(d)(C 端现用)
- **param(单多普勒)**:`_param_R`,单主多普勒 + 指数衰减
- **PSD 投影**:`_psd_R`,功率谱裁非负 → 还原合法正定 Toeplitz(Wiener-Khinchin)
- **K-line 多态多普勒**:`_kline_R`,功率谱取最强 K 个多普勒分量
- **genie**:真信道算 r(d),零估计方差(下限)

### 合成结果(trials=32,SNR20,4 速度 × 2 周期)

崩溃(NMSE>0dB)次数:**EMA 26/64、param 37/64、PSD 0/64、genie 0/64**。

| 地平线 | PSD vs EMA | PSD vs param | PSD 距 genie 残差 |
|---|---|---|---|
| k=1 | −4.80 dB | −2.07 dB | **+1.19 dB** |
| k=2 | −4.45 | −2.07 | +1.00 |
| k=4 | −3.23 | −1.85 | +0.79 |
| k=8 | −1.26 | −1.48 | +0.81 |

**合成结论:** PSD 投影 0 崩溃(复刻 genie 的正定性)、距 genie 仅 ~1dB,无形状偏差,远胜单多普勒 param。

---

## 2. 真实 capture A/B:合成结论被推翻

在真实 native capture 上跑同一框架(`replay_true2d.py --wiener-compare-r`),结果**与合成相反**:

| 预测器 | 中速 capture | 动态 capture(decorr 0.355) |
|---|---|---|
| W-emaR(裸EMA) | **+2.76** | **+3.39** |
| W-param1D | +2.27 | +1.63 |
| W-kline2 / kline3(多态多普勒) | — | +2.18 / +2.15(K=2 即饱和) |
| W-psdR | +1.26 ❌ | +3.27 |
| W-genie(上限) | +3.09 | +3.55 |

(单位 dB,k≤5 平均 gain vs ZOH)

**真实结论:**
1. **裸 EMA 在真实数据上一直贴近 genie**(差 0.16~0.3dB);合成里的"暴走"在真实数据**不出现**(预测输入是频域 Toeplitz 滤波后的 `est_t2d`,r(d) 本就干净)。
2. **PSD/param/多态多普勒在真实数据上没有增益**:PSD 中性偏负、param 与 K-line 始终输 EMA ~1dB。任何"降维参数化"都≤持平。
3. **多态多普勒(K=2/3)比单多普勒好 +0.5dB,但 K=2 饱和,仍输裸 EMA ~1.2dB** → 真实 CDL 多普勒谱连续,几条离散谱线拟合丢信息。
4. **C 端现用的 EMA-R Wiener 已是经典线性预测的实际最优,无需移植 PSD/param。**

→ 经典预测到顶:genie 也只有 +3.5dB@k=1 且 k≥4 快速衰减。AI 唯一空间在**中长地平线**(乐观 +1~3dB + knee 后移),须实测量化,不能纸上承诺。

---

## 3. 副发现排查:"时间级 Kalman 高动态发散" = replay 工具假象(虚惊)

### 现象
replay 的 `true2d-freq+PKF(time)` 在 12 条 06-06 capture 上 **11/12 严重退化**(freq −20~−46dB → +PKF −2~−15dB),且**与动态无关**(越干净越糟),仅 `134257`(近静态)正常。

### 排查
1. **不是相位模式问题**:把 replay 时间级从 scalar(mode0)换成 mode2(STO-ramp 去旋转)重测,**两者都崩,mode2 没救**(否定"STO ramp 致 Kalman 发散"假设)。
2. **生产 C 实际健康**(`eval_nmse_sto.py` 读 C 真输出 vs GT):
   - 214506:**−24.93 dB**(replay +PKF 仅 −14.88)
   - 143415:**−45.02 dB**(replay +PKF 仅 −2.41)
   - 223507(动态):**−16.02 dB**(replay +PKF 仅 −2.06)
3. **根因:replay 喂错输入**。true2d capture 落盘的 srs_matrix **已是 C 跑完 freq+PKF+gate 的成品**(replay 报的 `raw≈−44dB` ≈ C eval `−45dB`)。replay 把成品当原料,**又套一遍自己的 freq+离线 PKF → 二次处理搞坏**。生产 C 另有高 SNR legacy-gate 混合,离线 ladder 也没有。

### 处置
- **删除** `replay_true2d.py` 的 `true2d-freq+PKF(time)` ladder(留注释说明)。
- replay 只用于 **passthru** capture 的 freq 级离线探查;**判定生产质量认 `eval_nmse_sto.py`**。
- 更正认知:`time_pkf_riccati` 与 C `nr_srs_pkf_update` **位级一致(≤1 LSB)**,不是劣质镜像;崩纯属输入错误。

---

## 4. C 端离线验证代码体系(厘清,防再踩坑)

**两层验证,都在仓库:**

| 层 | 位置 | 作用 |
|---|---|---|
| 算法原型 | `test_srs_2d_offline.py`(`time_pkf_riccati` / `--cascade` / `--true2d`) | 进 C 前证明算法在 CDL 上成立 |
| 位级移植 | `openairinterface5g_whan/.../NR_ESTIMATION/c_unit_tests/` | 证明 C == 离线 golden |

`c_unit_tests/run_all.sh` 本日重跑 **全 PASS(worst ≤1 LSB)**:
- 时间级 `nr_srs_pkf_update` ↔ `golden_pkf.py`
- freq `nr_srs_mmse_freq_filter` ↔ `golden_freq.py`
- 预测 `nr_srs_pkf_predict` ↔ `golden_pkf_predict.py`
- Wiener 预测 `nr_srs_wiener_predict` ↔ `golden_wiener_predict.py`

→ 整链路(freq + PKF 时间级 + 预测 + Wiener)C 实现均经离线验证且位级一致,**生产健康**。

---

## 5. 代码改动(本日)

- `test_srs_2d_offline.py`:新增 `_psd_R`、`_kline_R`;`time_wiener_predict` 加 `r_psd` / `r_kline` / `r_truth`(genie 隔离)参数;预测 sweep 加 `--predict-wiener` 四方对比 + CSV 列 + NaN 感知打分。
- `replay_true2d.py`:Wiener 对比加 `W-psdR` / `W-kline2/3` 变体;**移除** `+PKF(time)` 误导 ladder。

## 6. 下一步(建议优先级)
1. 收拢成果写报告/组会:经典预测到顶 + 时间级生产健康(虚惊已结案)。
2. 若继续:中长地平线(k=4~16)AI 预测器离线 baseline,量化相对 genie 的真实头顶。
3. 不再投入:PSD/param/多态多普勒 R(真实数据上无增益)。
