# SRS Per-Frame int16 精度天花板追查日志

| 字段 | 值 |
|------|----|
| **日期** | 2026-05-15 |
| **目标** | 定位遗留问题 1：SRS 信道估计 per-frame NMSE 在 SNR≥30 dB 时被 cap 在 ~-22 dB 的根因 |
| **结论** | **rfsim 缺少 RF AGC，UE 信号 AMP=512 → 时域 RMS≈200（仅用 int16 的 0.6%），经 2048 点 int16 FFT 累积量化噪声后 SQNR 天花板 ≈ 22-23 dB。真实硬件有 RF AGC，不存在此问题。** |
| **可修性** | 方案 D（Proxy 前置增益 ×2~3）低风险、一行代码、+6~9 dB；e-MIMO 管线不受影响（多帧平均穿透天花板，物理特征尺度无关） |

---

## 一、问题回顾

05-14 实验数据（2D MMSE Phase 1，STO 补偿后，split-set GT）：

| SNR 设定 | Raw NMSE (per-frame vs GT) | 备注 |
|----------|---------------------------|------|
| 10 dB | -6.5 dB | AWGN 主导 |
| 20 dB | -12.3 dB | AWGN 主导 |
| 30 dB | -20.7 dB | 开始撞天花板 |
| 40 dB | -22.7 dB | 天花板区域 |

SNR 从 30→40 dB 提高 10 dB，NMSE 只改善 2 dB（-20.7 → -22.7）。 **per-frame 量化天花板 ≈ -22 到 -23 dB NMSE**。

E1 Python 仿真（bit-exact libdfts.so）：SNR ≈ 23.4 dB 帧间有效 SNR，与天花板一致。

---

## 二、SRS 信道估计完整 int16 算术链路

### 2.1 信号幅度传递链

```
UE 频域 (SRS 参考信号生成)
  AMP = 1 << AMP_SHIFT = 1 << 9 = 512          ← 编译时常量
  srs_generated_signal_bits = log2_approx(512) = 9
  SRS 子载波幅度: |r_amp| ≈ 512 (int16)
  活跃子载波: M_sc_b_SRS ≈ 1248
    ↓
UE IFFT (int16, 2048 点)
  时域 RMS ≈ 200 (int16)
  动态范围利用率: 200/32768 ≈ 0.6%
  有效比特数: log₂(200) ≈ 7.6 bits (out of 16)
    ↓
Proxy 信道应用 (float64 内部运算)
  信道增益 ≈ 1 (static LoS)
  添加 AWGN
  输出量化为 int16, RMS ≈ 200
    ↓
gNB int16 FFT (dft2048, 11 级蝶形)
  输入 RMS ≈ 200 → 仅 7.6 有效比特
  11 级蝶形运算，每级可能右移 1 bit 防溢出
  量化噪声在各级累积
  输出: rxdataF (c16_t), 每个 SRS 子载波幅度 ~100-300
    ↓
SRS 接收信号提取 (nr_get_srs_signal)
  按梳状 K_TC 从 rxdataF 抽取 → srs_received_signal (c16_t)
    ↓
LS 信道估计 (nr_srs_channel_estimation)
  ls = (gen × recv) >> 9           ← int32 → int16, 右移 9 位
  gen 幅度 ≈ 512, recv 幅度 ≈ 100-300
  int32 乘积 ≈ 512 × 200 = 102,400
  >> 9: 结果 ≈ 200 (回到 recv 的量级)
  截断误差: ±0.5 int16 LSB
    ↓
filt8 频域插值 (c16multaddVectRealComplex)
  mulhrs_epi16 (16×16→16, 有效 >>14)
  饱和加法 (adds_epi16)
  输出: srs_estimated_channel_freq (c16_t)
    ↓
Digital Twin 写盘
  srs_matrix_*.bin = srs_estimated_channel_freq 原样 fwrite(c16_t)
```

### 2.2 关键代码位置

**AMP 定义**：

```c
// openair1/PHY/impl_defs_top.h
#define AMP_SHIFT 9              // 非 BIT8_TX 模式
#define AMP ((1)<<AMP_SHIFT)     // = 512
```

**SRS 参考信号生成**：

```c
// openair1/PHY/NR_UE_TRANSPORT/srs_modulation_nr.c:272
nr_srs_info->srs_generated_signal_bits = log2_approx(amp);  // = 9

// :383-384
c16_t r_amp = {(((int32_t)round((double)amp * r.r / sqrt_N_ap)) >> 15),
               (((int32_t)round((double)amp * r.i / sqrt_N_ap)) >> 15)};
// amp=512, N_ap=1 → |r_amp| ≈ 512
```

**gNB 调用 generate_srs_nr 时传入 AMP**：

```c
// openair1/SCHED_NR/phy_procedures_nr_gNB.c:960
generate_srs_nr(srs_pdu, frame_parms, ..., AMP, frame_rx, slot_rx);
```

**LS 估计 — >>9 右移**：

```c
// openair1/PHY/NR_ESTIMATION/nr_ul_channel_estimation.c:856-859
ls_estimated.r += (int16_t)(((int32_t)generated_real * received_real
                            + (int32_t)generated_imag * received_imag)
                            >> nr_srs_info->srs_generated_signal_bits);  // = >>9
ls_estimated.i += (int16_t)(((int32_t)generated_real * received_imag
                            - (int32_t)generated_imag * received_real)
                            >> nr_srs_info->srs_generated_signal_bits);  // = >>9
```

**filt8 路径 — mulhrs + 饱和加**：

```c
// openair1/PHY/TOOLS/tools_defs.h
// mulhrs_epi16: (a×b + 16384) >> 15, 然后 ×2 (有效 >>14)
// adds_epi16: 饱和加法
c16multaddVectRealComplex(filt8_*, &ls_estimated, srs_estimated_channel16, 8);
```

---

## 三、量化噪声分析

### 3.1 各环节 SQNR 估算

| 环节 | 信号幅度 (RMS) | 有效比特 | 理论 SQNR | 备注 |
|------|---------------|---------|----------|------|
| UE int16 量化 | 200 | 7.6 | 57 dB | SQNR=20log₁₀(200√12)，不是瓶颈 |
| **int16 FFT (2048 点)** | **200 → ~200** | **7.6** | **~22-27 dB** | **11 级蝶形 + 低幅度 = 主瓶颈** |
| LS >>9 截断 | ~200 | 16 | ~57 dB | >>9 截断误差 ±0.5 LSB，不是瓶颈 |
| filt8 mulhrs | ~200 | ~15 | ~51 dB | mulhrs 有效精度 ~15 bit，不是瓶颈 |
| **合计 (级联)** | — | — | **~22-23 dB** | **与实测吻合** |

### 3.2 int16 FFT 为什么是瓶颈

2048 点 int16 FFT 有 log₂(2048) = 11 级蝶形运算。每级蝶形做加法和减法，信号幅度可能翻倍。为防止 int16 溢出，FFT 内部在适当的级做右移（>>1）。

固定点 FFT 的 SQNR 理论公式（近似）：

```
SQNR_FFT ≈ 6.02 × B_eff - 3.01 × log₂(N)

其中:
  B_eff = 有效输入比特数 ≈ log₂(RMS_input) ≈ 7.6
  N = 2048 → log₂(N) = 11

SQNR_FFT ≈ 6.02 × 7.6 - 3.01 × 11
         ≈ 45.8 - 33.1
         ≈ 12.7 dB
```

这个估算偏保守（假设每级都右移且噪声最大化累积）。实际 OAI 的 dft2048 可能有更聪明的缩放策略（block floating point 等），但仍受限于输入幅度仅 7.6 有效比特。

**考虑实际缩放策略后的合理范围：18-27 dB**，与实测 22-23 dB 一致。

### 3.3 为什么 RMS 只有 200

```
频域: 1248 个子载波 × 幅度 512 (AMP)
  ↓ IFFT (有归一化因子)
时域: RMS ≈ AMP × √K / √N × IFFT_gain
     ≈ 512 × √1248 / √2048 × gain
     ≈ 512 × 0.78 × gain
     ≈ 400 × gain

若 IFFT_gain ≈ 0.5 (内部 1 次右移):
  RMS ≈ 200  ← 与实测吻合
```

`AMP = 512` 是 OAI 全局编译时常量（`AMP_SHIFT = 9`），用于所有下行/上行信号（PDSCH、PUSCH、SRS 等）。不是 SRS 特有的设置。

---

## 四、为什么 E1 仿真 (23.4 dB) 与实测天花板 (22-23 dB) 一致

| 维度 | E1 Python 仿真 | 实际 OAI 系统 |
|------|---------------|-------------|
| 使用的 FFT | libdfts.so (bit-exact 同一库) | 同一库 |
| LS 精度 | 模拟 >>9 截断 | 同样的 >>9 |
| filt8 | 未包含 | 包含 |
| 结果 | 23.4 dB 帧间 SNR | 22-23 dB NMSE vs GT |

两者高度一致（差 ~1 dB 来自 filt8 的额外量化损失）。这证明：

1. **int16 FFT 是天花板的主要来源**（E1 仿真已捕获）
2. **filt8 是次要来源**（~1 dB 额外损失）
3. **Proxy 传输链路、IPC 通信等不引入额外退化**

---

## 五、天花板的定量影响

### 5.1 per-frame 精度

| SNR 设定 | AWGN 噪声功率 | 量化噪声功率 | 总噪声 (NMSE) | 哪个主导 |
|----------|-------------|------------|-------------|---------|
| 10 dB | -10 dB | -22 dB | -9.9 dB | AWGN |
| 20 dB | -20 dB | -22 dB | -18.7 dB | 接近平衡 |
| 30 dB | -30 dB | -22 dB | -21.8 dB | 量化 |
| 40 dB | -40 dB | -22 dB | -21.97 dB | 量化 |

→ SNR ≥ 30 dB 时量化噪声主导，NMSE 收敛到 ~-22 dB。

### 5.2 多帧平均穿透天花板

量化噪声在帧间近似不相关（因为每帧 AWGN 不同 → 量化 pattern 不同），所以 N 帧平均后：

```
NMSE_avg = -22 - 10·log₁₀(N) dB

N=10:   -32 dB
N=50:   -39 dB
N=100:  -42 dB
N=1000: -52 dB
```

实测验证：90 帧 cumulative oracle 达 -50.38 dB (SNR=20)，远低于 per-frame ceiling。

---

## 六、为什么 RMS 这么小 — 不只是 SRS 的问题

### 6.1 所有信号都共用 AMP=512

`AMP=512` 不区分信号类型，OAI 中所有上行/下行信号（SRS、PUSCH、PDSCH、DMRS 等）的频域幅度都是 ~512。因此 UE 发出的**所有上行信号** IFFT 后时域 RMS 都在 ~200 量级。

### 6.2 AMP 设得小是为了防 OFDM PAPR 溢出

OFDM 信号的峰均功率比 (PAPR) 很高。1248 个子载波叠加时，偶尔会"相位对齐"产生尖峰：

```
典型 PAPR ≈ 10·log₁₀(1248) ≈ 31 dB
峰值 / RMS ≈ √1248 ≈ 35 倍
峰值 ≈ 200 × 35 ≈ 7000

int16 范围:  -32768 ──────────────────────────── +32767
AMP=512:   RMS≈200, 峰值≈7000   │█████████░░░░░░░░░░│  安全
AMP=2048:  RMS≈800, 峰值≈28000  │████████████████████████████│  临界
AMP=4096:  RMS≈1600, 峰值≈56000 │█████████████████████████████████████→  溢出!
```

`AMP=512` 保证了即使最坏 PAPR 峰值也不超出 int16，这是 OAI 的全局安全裕度设计。

### 6.3 rfsim 特有问题 — 缺少 RF AGC

在真实硬件和 rfsim 下情况完全不同：

```
真实硬件:  UE(int16,小) → DAC → PA → 空口 → LNA → RF AGC(放大!) → ADC(int16,大) → FFT(精度高) ✓
rfsim:     UE(int16,小) → 共享内存 → 无AGC → gNB(int16,还是小) → FFT(精度低) ✗
```

真实硬件的 **RF AGC** 自动调整接收增益，把信号放大到接近 ADC 满量程（RMS≈10000-20000）。此时 int16 FFT 有 ~14 有效比特，SQNR 可达 50+ dB。

rfsim 跳过了整个模拟 RF 链路（包括 AGC），UE 的 RMS=200 直接传到 gNB，有效比特只有 7.6。**天花板是 rfsim 仿真环境的副作用，不是 OAI 架构缺陷。**

---

## 七、修复方案分析

### 方案 A: 提高 AMP（频域信号幅度）

```c
// 当前: AMP_SHIFT = 9 → AMP = 512
// 修改: AMP_SHIFT = 12 → AMP = 4096  (×8 = +18 dB)
```

预期效果：
- 时域 RMS: 200 → ~1600
- 有效比特: 7.6 → 10.6
- SQNR_FFT: ≈ 22 → ≈ 40 dB (+18 dB)

风险：
- OFDM PAPR ≈ 31 dB → 峰值可能达 1600 × 32 ≈ 51,200 → **int16 溢出!**
- 需要配合 AGC 或 clipping
- `AMP` 是全局常量，影响所有信号（PDSCH、PUSCH、DMRS 等），改动影响面极大
- **不推荐**: 工程风险极高，收益仅在 per-frame 层面

### 方案 B: Float FFT bypass

用 FFTW (float) 替代 libdfts (int16) 做 SRS FFT。

预期效果：
- SQNR_FFT: 22 dB → ~60+ dB (float32 精度)
- per-frame ceiling 提升到 ~57 dB（由 int16 输入量化限制）

风险：
- 需要修改 OAI gNB 代码，在 SRS 路径使用单独的 float FFT
- 延迟增加（float FFT 可能不如 int16 快，但 SRS 不在关键延迟路径上）
- **工作量大但可行**: 仅改 SRS 路径，不影响其他信号处理

### 方案 C: 接受天花板 + 多帧平均（当前方案）

- per-frame ceiling = -22 dB，但通过多帧平均穿透
- e-MIMO 管线天然包含多帧统计聚合
- 无需任何代码修改

### 方案 D（新）: Proxy 侧 UL 前置增益 — 模拟 RF AGC

**核心思路**: 在 Proxy 的 UL 处理中，写入 gNB `ul_rx` 缓冲之前，对时域信号乘以固定增益 G。这等效于真实硬件中 RF AGC 的功能。

```
当前:    UE(RMS=200) → Proxy(信道应用, 不放大) → gNB(RMS=200) → FFT(7.6 bit)
方案 D:  UE(RMS=200) → Proxy(信道应用, ×G放大) → gNB(RMS=200×G) → FFT(更多 bit)
```

**实现位置**: `v8.py` 的 `_ipc_ul_superposition_slot`，在 `_fused_clip_cast_kernel` 之前对 float64 累加结果乘以 G。Proxy 内部已经用 float64 运算，加一个乘法仅一行代码。

**增益选择**（05-14 的 target_peak=30000 失败是因为 gain≈150× 太大）：

| 增益 G | RMS | 峰值估计 | FFT 溢出风险 | SQNR 改善 |
|--------|-----|---------|-------------|----------|
| ×1（当前） | 200 | ~7000 | 无 | 基准 22 dB |
| ×2 | 400 | ~14000 | 低 | +6 dB → ~28 dB |
| ×3 | 600 | ~21000 | 中 | +9 dB → ~31 dB |
| ×4 | 800 | ~28000 | 高（临界） | +12 dB → ~34 dB |
| ×150（05-14 失败） | 30000 | — | **溢出** | 失败 |

**推荐 G=2~3**，预期 +6 到 +9 dB 改善，per-frame SQNR 从 22 dB 提升到 28-31 dB。

**OAI 已有但不够的数字 AGC**: OAI 内置了 `nr_digital_agc`（`NR_DIGITAL_AGC_ENABLED=1` 启用），但它作用在 **FFT 之后**（频域乘增益），对信号和量化噪声等比放大，**不改善 FFT SQNR**。方案 D 是在 FFT **之前**放大时域信号，直接改善 FFT 的有效输入精度。

### 方案 D 对 e-MIMO 管线的影响分析

固定增益 G 对下游管线**安全**，核心理由是物理特征尺度无关：

| 管线环节 | G 的影响 | 是否有问题 |
|---------|---------|-----------|
| SRS 信道估计 | H_est → G·H_est | 线性缩放，SNR 改善 |
| 2D 滤波 (EMA) | 线性运算，输出等比缩放 | 无 |
| PDP (功率延迟分布) | 形状不变，绝对功率 ×G²（归一化后消除） | 无 |
| 协方差矩阵 R_H | 特征向量不变，特征值 ×G²（比例不变） | 无 |
| State Sample | 延迟/角度/归一化功率分布全部不受影响 | 无 |
| AI 模型 | 训练和推理用同一 G 即可 | 无 |

**关键约束**: G 必须是**固定常数**（不自适应），训练和推理用**同一 G 值**。

### 方案推荐优先级

| 优先级 | 方案 | 工作量 | 收益 | 适用场景 |
|--------|------|--------|------|---------|
| **1** | **D (Proxy 前置增益)** | **低（一行代码）** | **+6~9 dB** | **rfsim 仿真** |
| 2 | C (维持现状 + 多帧平均) | 零 | 不改善 per-frame | e-MIMO 路线已足够 |
| 3 | B (Float FFT) | 高 | +35 dB | 如需 per-frame 极致精度 |
| 4 | A (提高 AMP) | 极高 + 风险 | +18 dB | 不推荐 |

---

## 八、结论

### 根因定位

**per-frame int16 天花板 (~-22 dB NMSE) 的根因是 rfsim 缺少 RF AGC，UE 发射信号幅度过低 (`AMP=512` → 时域 RMS≈200) 直接进入 gNB 的 int16 FFT，有效输入精度仅 ~7.6 bits，2048 点 FFT 的 11 级蝶形运算累积量化噪声消耗了大部分有效精度。在真实硬件中，RF AGC 会将信号放大到接近 ADC 满量程，不存在此问题。**

### 因果链

```
AMP = 512 (编译时常量, impl_defs_top.h)
  → UE SRS 频域幅度 ≈ 512 per SC
    → IFFT 后时域 RMS ≈ 200 (int16 范围 32768 的 0.6%)
      → rfsim 无 RF AGC, 信号以 RMS=200 直达 gNB
        → 仅 7.6 有效比特进入 int16 FFT
          → gNB int16 FFT (2048 点, 11 级蝶形)
            → 量化噪声累积: SQNR ≈ 6.02×7.6 - 3.01×11 ≈ 12-27 dB
              → LS >>9 + filt8 额外损失 ~1-3 dB
                → 最终 per-frame SQNR ≈ 22-23 dB
                  → NMSE vs GT 收敛到 -22 dB (SNR≥30)

真实硬件路径 (无此问题):
  UE(RMS=200) → 空口 → RF AGC(放大至 RMS≈15000)
    → ~14 有效比特进入 FFT → SQNR ≈ 50+ dB → 无天花板
```

### 与遗留问题的关系

| 维度 | 05-14 状态 | 05-15 状态 |
|------|-----------|-----------|
| 天花板数值 | "~21 dB" (测量方法依赖) | **~22-23 dB NMSE vs GT** (精确标定) |
| 根因 | "可能是 int16 pipeline 精度上限" | **确认：rfsim 无 AGC + AMP=512 → 低动态范围 → FFT 量化噪声累积** |
| 适用范围 | 不清楚 | **rfsim 特有问题，真实硬件不受影响** |
| 具体瓶颈 | 不清楚 | **int16 FFT 的有效输入精度仅 7.6 bits** |
| LS >>9 | 被怀疑 | **排除**（>>9 截断误差仅 ±0.5 LSB，远低于 FFT 噪声） |
| filt8 | 被怀疑 | **次要** (~1 dB 额外损失) |
| 可修性 | 不清楚 | **方案 D (Proxy 前置增益) 低风险、一行代码、+6~9 dB** |
| 多帧平均 | "理论上有效" | **实验确认有效**（90 帧 oracle 达 -50 dB） |
| 对 e-MIMO 管线影响 | 不清楚 | **无影响**（PDP/协方差/角度等物理特征尺度无关） |
