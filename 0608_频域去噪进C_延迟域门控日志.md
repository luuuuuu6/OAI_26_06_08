# SRS 频域去噪进 C —— 延迟域支撑门控(nr_srs_delay_gate)

| 字段 | 值 |
|------|----|
| 日期 | 2026-06-08 |
| 主题 | 把 `0607_MIMO增强` §2 验证的"频域(延迟域)去噪"从离线落到 C(true2d 估计路径),env 门控、默认关 bit-exact、native 实测增益+时延 |
| 一句话结论 | **延迟域峰值相对门控(`SRS_DENOISE_PEAK_DB=30`)**:对 PKF+legacy-blend 后的最终估计 `IDFT→保留峰值30dB内抽头→DFT`,native 实测 **2×2 +4.0dB / 4×4 +1.7~3dB**,延迟 **~17µs/链路**,默认关时逐位等价。 |
| 关联 | `0607_MIMO增强_多符号与频域去噪分析日志.md` §2(动机/离线证据);`0607_降低时延_freq_wiener实时化优化日志.md`(时延基建/Option B) |
| 状态 | **达成**:离线/golden/harness/native 四层验证通过;过程中定位并修复 2 个 native-only bug(频域支撑污染、legacy-blend 覆盖顺序)。 |

---

## 0. 背景与动机(承 `0607_MIMO增强` §2)

`0607` 把 MIMO 与 1×1 的 NMSE 差距拆成三块:**物理功率分摊(−3/−6dB,硬地板)+ 频域可恢复去噪(~2-3dB)+ 时间方向(高速无余量)**。其中**频域可恢复去噪**是唯一"免费、纯处理"能拿的:

- 关键观察(`0607` §2.2):把估计 IDFT 到延迟域,**真信道只占延迟范围 ~4%**(1×1 \|n\|≤39 / 2×2 ≤37 / 4×4 ≤40),其余 ~96% 的延迟抽头是纯噪声/泄漏。
- 现有 freq-Wiener Stage1 是**局部 16 导频 Toeplitz LMMSE**,它做"频域局部平滑",**够不着整段延迟谱里那 96% 的全局噪声底**;低 SNR(多端口)时空区噪声多 → 这就是离线硬窗扫描能多榨 +2.9/+2.7dB 的余量(`0607` §2.3)。
- `0607` §2.5 的结论:安全有效版**必须做进 freq-Wiener、复用跨帧 EMA PDP**,不能用逐帧或独立后处理。

本日志即把这件事落到 C,并在过程中把"做进 Stage1 内部"修正为"做在 PKF+blend 之后对最终输出"(理由见 §3.2)。

---

## 1. 算法与实现(结合代码)

### 1.1 门控函数 `nr_srs_delay_gate()`(`nr_srs_mmse.c:1538`)

整条函数对**每条 (rx,tx) 链路、每个 SRS 时机**的最终估计 `est[]`(长度 = ofdm_symbol_size = 2048)就地去噪。

**(0) 门控 + 守卫**(`:1538`起):
```c
void nr_srs_delay_gate(c16_t *est, int ofdm_size, int ant, int port)
{
  const double peak_db = nr_srs_get_denoise_peak_db();
  if (peak_db <= 0.0) return;                 // 默认关 -> 立即返回, 路径 bit-exact
  if (est == NULL || ant < 0 || port < 0 || port >= NR_SRS_TRUE2D_MAX_TX) return;
  if (ofdm_size != SRS_MMSE_PDP_IFFT_SIZE) return;  // gate 按 2048 点 IDFT 设计
  const int N = SRS_MMSE_PDP_IFFT_SIZE;       // 2048
```

**(1) 记录原始频域支撑 `fsupp`**(`:1555`,关键,见 §3.1 bug):
```c
uint8_t fsupp[SRS_MMSE_PDP_IFFT_SIZE];
for (int s = 0; s < N; s++)
  fsupp[s] = (est[s].r != 0 || est[s].i != 0) ? 1 : 0;   // SRS 只占激活带, 其余为 0
```
SRS 估计只在激活带(~1248/2048)非零,保护带为 0。`dft` 回写会铺满全带,必须在最后恢复这些零。

**(2) 懒分配跨帧 EMA 状态**(`:1560`,镜像 `nr_srs_mmse_ensure_capacity` 的节奏,单线程):
```c
// g_denoise_ema[ant][port][n] (double), g_denoise_init[ant][port] (uint8)
// 与 freq-Wiener 的 g_true2d_pdp_ema 分开, 但同维度(RX 动态, TX 固定 4)
```

**(3) IDFT 到延迟域**(`:1582`,复用 OAI 定点 idft,scale=1):
```c
c16_t h_time[SRS_MMSE_PDP_IFFT_SIZE] __attribute__((aligned(32)));
idft(get_idft(N), (int16_t *)est, (int16_t *)h_time, 1);
```

**(4) 跨帧 EMA 延迟功率谱**(`:1585`,α=0.95 复用 `g_true2d_pdp_alpha`):
```c
double *ema = g_denoise_ema[ant][port];
if (!g_denoise_init[ant][port]) {
  for (int n = 0; n < N; n++) ema[n] = (double)c16_power(h_time[n]);  // 首帧初始化
  g_denoise_init[ant][port] = 1;
} else {
  const double a = (double)g_true2d_pdp_alpha;                        // 0.95
  for (int n = 0; n < N; n++) ema[n] = a*ema[n] + (1.0-a)*(double)c16_power(h_time[n]);
}
```
跨帧平滑是稳健找支撑的前提(`0607` §2.5 已证:逐帧 PDP 太吵,跨帧 EMA 才稳)。

**(5) 噪底 + 峰值相对阈值**(`:1597`):
```c
// 噪底 = 中段 [N/2, 3N/4) 均值: 纯噪声区(避开 n~0 信道 与 n~N 负延迟 wrap),
// O(N) 无 qsort -> 刻意不碰 freq-Wiener 那 ~100µs 鲁棒噪底排序瓶颈
double nf_acc = 0.0;
for (int n = N/2; n < (N*3)/4; n++) nf_acc += ema[n];
const double nf = fmax(1.0, nf_acc / (double)((N*3)/4 - N/2));
double pmax = 0.0;
for (int n = 0; n < N; n++) if (ema[n] > pmax) pmax = ema[n];
const double thr_peak = pmax * pow(10.0, -peak_db/10.0);   // 峰值 - peak_db dB
const double thr = (thr_peak > 2.0*nf) ? thr_peak : 2.0*nf; // floor guard
```

**(6) 掩码 + guard 膨胀 + 应用**(`:1616`):
```c
uint8_t keep[N];
for (int n=0;n<N;n++) keep[n] = (ema[n] >= thr) ? 1 : 0;
// guard 膨胀: 任一 ±guard 内有保留位则保留(留信道边缘余量)
... dilate by SRS_DENOISE_GUARD ...
for (int n=0;n<N;n++) if (!keep[n]) { h_time[n].r=0; h_time[n].i=0; }  // 支撑外置零
```

**(7) DFT 回频域 + 恢复支撑**(`:1645`):
```c
dft(get_dft(N), (int16_t *)h_time, (int16_t *)est, 1);   // 对称 scale -> 往返保幅
for (int s=0;s<N;s++) if (!fsupp[s]) { est[s].r=0; est[s].i=0; }  // 还原保护带零(§3.1)
```

> **定标说明**:OAI `idft`/`dft` 用 scale=1 对称归一(各 ~1/√N),往返近似恒等;且 NMSE 评估用逐帧最优复标量对齐(`a=<g,s>/<g,g>`)**会吸收任何往返常数因子**,所以即使定标有常数偏差也不影响增益。已实测 int16 往返不损失增益(§4.1)。

### 1.2 开关(env,默认全关 → 路径逐位不变)

| env | 默认 | 作用 |
|---|---|---|
| `SRS_DENOISE_PEAK_DB`(`:1511` getter) | 0(关) | >0 开启;保留"低于最强径该 dB 内"的延迟抽头。30 为验证甜点 |
| `SRS_DENOISE_GUARD`(`:1526` getter) | 2 | 支撑掩码向两侧膨胀的延迟 bin 数 |

getter 首次调用缓存(`static`),零稳态开销。

### 1.3 调用点(`nr_ul_channel_estimation.c`,true2d 分支)

顺序必须是 `PKF 更新 → legacy-blend → nr_srs_delay_gate`(**gate 是 dump 前最后一步**,见 §3.2):
```c
// nr_ul_channel_estimation.c ~:1098-1145 (true2d 分支)
srs_tmg_add(SRS_TMG_PKF, ...);                 // PKF 写 srs_estimated_channel_freq
... legacy-blend: 高 SNR 时按 beta 混向 legacy ...   // 见 §3.2
double _t_dn = srs_now_ns();
nr_srs_delay_gate(srs_estimated_channel_freq[ant][p_index],
                  frame_parms->ofdm_symbol_size, ant, p_index);
srs_tmg_add(SRS_TMG_DENOISE, srs_now_ns() - _t_dn);   // 新增计时 bucket denoise_gate
```
仅 `SRS_ESTIMATOR=true2d` 走此分支。新增枚举 `SRS_TMG_DENOISE` + 名字 `denoise_gate`(`SRS_TIMING=1` 时打印)。

---

## 2. 阈值为什么用"峰值相对 30dB"(三法对照,关键设计决策)

延迟域的"底"**不是平坦白噪声**,而是带限信号特有的结构:dump 是带限的(1248/2048 激活,保护带补零),IDFT 后每个抽头拖一条 **sinc 泄漏裙边**,估计噪声也被带限(在延迟域相关、非白)。在真实 dump 上(int16 量化,模拟定点)实测三种阈值:

| 阈值法 | 1×1 | 2×2 | 4×4 | 结论 |
|---|---|---|---|---|
| **peak_db=30(选用)** | **+0.57** | **+3.13** | **+2.94** | 最优,三者全正、1×1 安全 |
| CFAR(γ×噪底,γ=4~20) | ~0(伤) | ~0 | ~0 | **失效**:支撑 500~1200/2048,几乎不切 |
| 能量占比 0.99 | +0.81 | +1.28 | +0.84 | 远不如;0.95 伤 1×1,无单一安全值 |

**为什么 CFAR(教科书"自适应")在这里失效**:CFAR 假设延迟域底是**平坦白噪声**(|h|²~指数分布,thr=γσ² 给定虚警率)。但本信号底是 **sinc 泄漏裙边 + 带限相关噪声**,远非平坦 → γ×(中段均值)留下整条裙边(支撑 500~1200)→ 不去噪。**生产 C 也是对这个带限 dump 做 idft,泄漏裙边同样存在**,所以这不是离线伪影,CFAR 在真 C 里也会同样失效。

**为什么 peak_db 赢**:
1. 它**不假设平坦噪底**,只砍"比最强径低 30dB"以下 —— 正好对付缓慢衰减的泄漏裙边。
2. **峰值相对天然 SNR 自适应**:支撑宽度随 SNR 变(1×1 高 SNR ~70 抽头;4×4 低 SNR 自动放宽),一个常数对 1×1/2×2/4×4 通用。
3. 30dB 是**检测工作点**,与码里 `SRS_MMSE_WINDOW=16`、`g_true2d_pdp_alpha=0.95` 同类——经验证的稳健常数,非凭空。

**peak_db 扫描**(离线,int16):25→伤 1×1(−0.84)/2×2 +2.52/4×4 +3.26;**30→甜点**(+0.57/+3.13/+2.94);40→切太少增益缩水。

> 注:`offline_freq_denoise.py` 保留了 `--gamma`(CFAR)、`--energy-frac`(能量占比)两个对照模式,作为"试过、更差、故选 peak_db"的可复现证据。

---

## 3. 过程中定位并修复的两个 native-only bug(代码级)

这两个 bug 离线/harness 都抓不到(它们对的是"对最终 dump 做去噪"这一操作),只有 native 全链路才暴露。

### 3.1 频域支撑污染(DFT 回写写满全带)
**现象**:开去噪后 native 全带 NMSE 崩到 −2dB;但用真实 SRS 带(1248)单独评分却是 −22.5dB(带内正常)。诊断发现 **dump 的 active 集从 1248 涨到 2048**。
**根因**:`dft` 把(掩码后的)延迟谱铺满**全部 2048 子载波**,污染原本为零的保护带;`eval_nmse_sto` 从 dump 自身算 active 掩码 → 把带外污染也算进 NMSE。
**修复**(`:1555` 记录 + `:1648` 恢复):idft 前存 `fsupp`(输入非零位),dft 后把非 `fsupp` 子载波重新置零,对齐离线 `from_delay` 的 `out[:,aidx]=fr[aidx]`。验证:输入 1247 非零 → 输出 1247。

### 3.2 legacy-blend 覆盖顺序(4×4 去噪被冲掉)—— 最隐蔽
**现象**:修了 3.1 后,2×2 去噪生效(−16→−20),但 **4×4 完全无增益**(−15.3 不变);`denoise_gate` 计时 16µs(确实跑了)。
**调查**:加一次性 debug 打印(peak/nf/thr/kept),native 4×4 显示 **`kept=33/2048`**(掩码确实在切!)但输出没变 → "切了又被改回去"。
**根因**:gate 原放在 PKF 后、**legacy-blend 之前**。legacy-blend 按**每链路导频 SNR**把 true2d 估计混向**未去噪的 legacy**(`srs_est[mem_offset]`):
```c
// nr_ul_channel_estimation.c, legacy-blend 核心
double beta = (snr_db - (gate_db - 0.5*width)) / width;   // gate_db=26, width=4
beta = clamp(beta, 0, 1);                                 // snr>28dB -> beta=1
if (beta > 0) t2[sc] = (1-beta)*t2[sc] + beta*leg[sc];    // beta=1 => 输出=legacy
```
- -nd -6 下**导频 SNR ~40dB → beta=1.0** → 输出 100% 换成 legacy → gate 的去噪被整段覆盖。
- 2×2、4×4 导频 SNR 都 ~40dB(beta 中位=1),逐帧 beta 有波动;**4×4 几乎全 beta=1 → 彻底覆盖;2×2 有低 beta 帧 → 部分存活**(这也解释了为何 2×2 一度"看着像生效"——其实被 beta + seed 混淆)。

**修复**:把 gate 移到 **legacy-blend 之后**(dump 前最后一步)。即便 beta=1(输出=legacy),gate 也对最终估计做延迟门控,普适受益(legacy filt8/16 在低 SNR 下同样有带外噪声可去)。修复后 4×4 harness 再去噪从 +2.72 → −0.55(确认已生效);native 4×4 −15.3→−18.3。

> 附带收益:此修复也让"高 SNR 被 blend 成纯 legacy"的 4×4 链路也能享受延迟去噪。

---

## 4. 验证(四层)

### 4.1 离线(真实 dump,`offline_freq_denoise.py`)
- 硬窗扫描复现 `0607` §2.3:1×1 +0.85(L=64)/2×2 +2.94(L=48)/4×4 +2.69(L=48)。
- EMA + peak_db=30:**float64** +0.57/+3.13/+2.94;**int16 量化(`--quantize`,模拟 OAI c16 idft)** +0.55/+3.11/+2.91 → **定点往返不损失增益**(被掩码丢弃的是小噪声抽头,保留的大支撑抽头量化误差 ~1-2%)。

### 4.2 C 正确性(`c_unit_tests/`,bit-exact 守门)
- `golden_freq.py`:默认关时 C↔float64 镜像逐位等价(worst 1.0 LSB),**全套 golden 无回归**。
- `test_denoise` 驱动(`test_denoise_main.c` + LUT 快版 idft/dft shim `srs_test_dft_fast.c`)+ `eval_denoise_real.py`:把**真实 dump** 喂进**真实 `nr_srs_delay_gate`**,NMSE vs GT 复现离线增益(1×1 +0.55 / 2×2 +3.11 / 4×4 +2.91);并用"**ON dump 再去噪 ≈0**"判据确认生效(2×2 ON −0.06 / 4×4 修复后 −0.55)。

### 4.3 native(rfsim,真 MIMO CIR,同 seed=4242,CDL-C 60km/h,-nd -6)

| 配置 | OFF NMSE | ON NMSE | 增益 | denoise_gate/链路 | total_wall OFF→ON | coh |
|---|---|---|---|---|---|---|
| **2×2** | −16.44 | **−20.45** | **+4.01dB** | 16.98µs | 1005→1063µs | 0.985→0.994 |
| **4×4** | −12.78 | **−14.47** | **+1.68dB** | 16.48µs | 4021→4232µs | 0.967→0.977 |

- 真 MIMO CIR:`cir_cdl_c_s60_2x2.bin`(2tx×2rx)、`cir_cdl_c_s60_4x4.bin`(4tx×4rx);**`NB_RX=N UE_TX=N` 必须同设**(否则维度不匹配,详见 §6 坑)。
- 4×4 单发(`234827`)曾测到 −18.29(≈+3);本 A/B +1.68 偏低主因:**仅 127 帧**(4×4 慢、SRS dump 少)NMSE 方差大 + 两发 decorr 0.27/0.55(同 seed 但 handoff 窗不同)。4×4 增益区间 **+1.7~3dB**,需拉长采集取稳值。

### 4.4 证据链小结
离线证"算法可恢复 ~3dB(含 int16)" → golden 证"C==算法且默认关 bit-exact" → harness 证"真 C 在真实 dump 上复现增益" → native 证"全链路端到端增益 + 实测时延"。

---

## 5. 时延(实测,`SRS_TIMING=1`)

### 5.1 去噪本身的开销
- **`denoise_gate` ~16-17µs/链路** = 1 IDFT + 1 DFT(各 ~3µs 定点)+ O(N) EMA/掩码/中段均值。
- **关键:不触发那 ~100µs 鲁棒噪底 qsort**(`0607_降时延` 的真瓶颈)——gate 噪底用中段 [N/2,3N/4) 均值(O(N) 无排序)。
- 2×2(4 链路):`total_wall` +58µs;4×4(16 链路):+~210µs。占 `freq_wiener`(~190µs)的 ~9%,占 `total_wall` 个位数百分比。

### 5.2 去噪 + Option B(`SRS_SHARE_R=1`)叠加 —— 4×4 实时余量(native 实测)

| 指标 | 去噪 ON,共享R 关 (000821) | **去噪 ON,共享R 开 (002500)** | 变化 |
|---|---|---|---|
| freq_wiener | 188µs | **91µs** | −52% |
| denoise_gate | 16.5µs | 16.6µs | 不变(去噪不碰 R/噪底) |
| **total_wall** | 4232µs (max 4946) | **2612µs (max 3099)** | **−38%,远离 5ms** |
| NMSE | −14.47 | −18.42(coh 0.990) | 去噪生效 |

- **不开 Option B 时 4×4 余量紧**:`total_wall` 4.0-4.2ms(max 4946µs)≈ 贴 5ms SRS 周期;大头是 `freq_wiener×16 ≈ 3ms`,去噪只占 ~5%。
- 核心降时延(`0607_降时延` 的 26.5×:求逆一次+twiddle表+权重缓存)**始终启用**,`freq_wiener ~190µs` 即其结果。
- **去噪与 Option B 正交可叠加**:`SRS_DENOISE_PEAK_DB=30 SRS_SHARE_R=1`;Option B 把同 UE 各 rx 共用一份 R(sibling 跳过最贵的 ~100µs 噪底+R-build)→ freq_wiener 减半、total_wall 4.2→2.6ms;去噪开销不变。**4×4 实时从容(2.6ms ≪ 5ms)且去噪生效。**

---

## 6. 复现 + 关键坑

```bash
# 重编(build 树 root-owned, 要 sudo; launch_native.sh 不自动重编)
cd .../openairinterface5g_whan/cmake_targets/ran_build/build && sudo ninja nr-softmodem
cd .../vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy

# 2×2 同 seed A/B(OFF / ON)
sudo SRS_TIMING=1                        NB_RX=2 UE_TX=2 bash launch_native.sh -e true2d -cir cir/cir_cdl_c_s60_2x2.bin -nd -6 -seed 4242 -d 120
sudo SRS_TIMING=1 SRS_DENOISE_PEAK_DB=30 NB_RX=2 UE_TX=2 bash launch_native.sh -e true2d -cir cir/cir_cdl_c_s60_2x2.bin -nd -6 -seed 4242 -d 120
# 4×4: NB_RX=4 UE_TX=4 -cir cir/cir_cdl_c_s60_4x4.bin (实时紧时加 SRS_SHARE_R=1)
python3 eval_nmse_sto.py --ab <OFF> <ON>
grep "SRS timing" <ON>/gnb.log         # denoise_gate / freq_wiener / total_wall

# 离线 + 阈值对照 + C 正确性
python3 offline_freq_denoise.py --run-dir <run> --mode ema --peak-db 30 --quantize
python3 offline_freq_denoise.py --run-dir <run> --mode ema --gamma 6          # CFAR(对照, 差)
python3 offline_freq_denoise.py --run-dir <run> --mode ema --energy-frac 0.99 # 能量占比(对照, 差)
cd .../NR_ESTIMATION/c_unit_tests && make && python3 golden_freq.py            # bit-exact 守门
python3 eval_denoise_real.py --run-dir <run> --peak-db 30                      # C gate 跑真实 dump
```

**坑**:
1. **重编**:`launch_native.sh` 直接跑预编译 `nr-softmodem`,**改 C 后必须 `sudo ninja nr-softmodem`**,否则跑的是旧二进制(查二进制 mtime vs 源码 mtime;或看 `denoise_gate` bucket 是否出现)。
2. **天线匹配**:CIR 文件维度必须 = 运行天线数。`NB_RX=N` 必须**同时** `UE_TX=N`,且 `-cir` 用 `..._NxN.bin`;否则 rfsim 把多余天线对的信道**置零**(gnb.log 报 `CIR replay dim mismatch ... extra pairs zeroed`)→ 第二天线 GT 全零、NMSE 出现 +332dB 垃圾(`0607` 那批 1T×2R + SISO CIR 即踩此坑)。
3. **GT 可信度**:跑完看 `[native-gt] conjugation coh(plain/conj)`,要 >0.9 才可信;快速信道偶发塌到 0.09(GT 配对失手),那种 run 直接丢弃重跑。
4. **同 seed 才是公平 A/B**:OFF/ON 用同 `-seed` 才同信道,ΔNMSE 才是纯去噪增益。

---

## 7. 文件索引(行号对当前源码)

| 文件 | 内容 |
|---|---|
| `nr_srs_mmse.c` | `nr_srs_delay_gate()`(:1538)、`g_denoise_ema/init` 状态(:732)、env getter(:1511 peak_db / :1526 guard)、fsupp 支撑记录(:1555)/恢复(:1648)、IDFT(:1583)、EMA(:1585)、中段均值噪底+阈值(:1597)、掩码+guard(:1616)、DFT(:1646) |
| `nr_srs_mmse.h` | `nr_srs_delay_gate()` 声明 |
| `nr_ul_channel_estimation.c` | blend 后调用 gate(~:1142)、`SRS_TMG_DENOISE` 枚举/名字/打印 |
| `c_unit_tests/test_denoise_main.c` `srs_test_dft_fast.c` `Makefile` | 真实数据 C 验证驱动 + LUT 正/反变换快版 shim;`srs_test_idft.c` 加正向 dft 使 test_freq 链接 |
| `vRAN_Socket/.../offline_freq_denoise.py` | 离线去噪:hardwin/ema 模式 + 三阈值法(peak_db/gamma/energy-frac)+ `--quantize` int16 检验 |
| `vRAN_Socket/.../eval_denoise_real.py` | 把真实 dump 逐链路喂进 C `test_denoise`,NMSE vs GT |

---

## 8. 待办 / 后续

- [x] 去掉 `nr_srs_delay_gate` 里的临时诊断日志(已删,2026-06-08;golden 仍 PASS)。
- [x] 4×4 配 `SRS_SHARE_R=1` 跑一发,确认去噪 + 共享R 叠加(§5.2:total_wall 4.2→2.6ms,去噪生效)。
- [ ] 4×4 拉长采集(更多帧)拿更稳的增益数(当前 127 帧方差大,+1.7~3dB)。
- [ ] (可选)把 `SRS_DENOISE_PEAK_DB`/`GUARD` 接成 `gnb.conf` 配置项而非仅 env。
- [ ] (可选)低速场景(30km/h)复测:SNR/时延余量不同,验证 peak_db=30 仍普适。
