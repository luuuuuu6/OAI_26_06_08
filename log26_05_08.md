# 26-05-08 — v7 Noise Injection 诊断 / OAI Attach 失败溯源

> 这个 log 记录 5/8 一次完整的诊断过程：从"PRACH 误检 → DL PDSCH bug → UL 时序 bug → zero-padding bug"四次错误假设，到最后用 dump 数据钉死真相 = **OFDM FFT 处理增益 + DMRS 估计增益**（物理预期，不是 bug）。

---

## TL;DR (先看结论)

1. **v7 channel proxy 的 noise 注入代码 (`v7.py:1144-1145`) 没有 bug**。`mean(|gpu_out|²)` 算的就是完整时域信号能量平均，OFDM 时域是连续的，没有零填充区间。
2. **设的 `--snr-dB -5` 跟 gNB 报的 `ulsch SNR 16 dB` 之间的 21 dB 差距是物理预期**：`SNR_demod = SNR_set + FFT_gain + DMRS_gain`，PUSCH 5 PRB 配置下 FFT_gain = +15.3 dB，DMRS_gain = +5.7 dB（实测），合计 +21 dB。
3. **5/7 SNR=-5 attach ABORTED 的真因是 PDCCH polar code 在临界 SNR 解不出 UL grant**：UL grant DCI 用 AL=4 → FFT_gain +8.5 dB → demod SNR ≈ +9.5 dB → polar BLER 50-80% → 5 次只过 1 次。修法：调高 PDCCH aggregation level (AL=4 → AL=8 或 16)。
4. **历史所有 SNR sweep 数据无需重跑**，只需把横轴按 offset 表平移：SRS 4 PRB 测量加 `+21.0 ± 0.5 dB`。

---

## 1. 起点：v7/v8 同口径 sweep 表现等价

5/7 晚上 - 5/8 凌晨连跑了 v7 / v8 的多组对比：

| sweep | proxy | trigger | gt | srs | dur | 结论 |
|---|---|---|---:|---:|---:|---|
| `q4_sweep_20260507_235502` | v7 | srs | 26 | 2 | 218s | ✅ OK 基线 |
| `q4_sweep_20260508_000135` | v8 | srs | 27 | 1 | 220s | ✅ v8 等价 |
| `q4_sweep_20260508_000753` | v7 (GT_SAVE_EVERY=1 误用 10) | srs | 53 | 0 | 432s | ❌ PARTIAL，attach 偶发失败 |
| `q4_sweep_20260508_105546` | v7 | srs | 31 | 1 | 260s | ✅ OK，恢复正常 |

**v7 在 SNR=10 历史命中率 ≈ 78% (8 OK / 2 PARTIAL out of 10 runs)。** 偶发性来自 OAI PRACH preamble 随机选择 + UE 频偏 + GPU 噪声序列 random，跟 v7/v8 改动无关。

## 2. 79 次 RA failed at WAIT_Msg3 的真相

5/8 OK 那次 gnb.log 数据：
- 79 次 `RA failed at WAIT_Msg3`，**79 个不同 RNTI** (大部分是 gNB 噪声误检的伪 PRACH)
- UE 端实际只发了 33 次 PRACH (差 46 次是伪检)
- 关键 OAI 源码 `nr_prach.c:740-755`：**PRACH 检测器只取最大 peak，没做任何 SNR 门限**

这部分**不是 bug**，是 OAI 上游设计权衡（"宁可错抓 100 个，不漏掉真 UE"）。伪 RNTI 不消耗有效 PUSCH 带宽（max HARQ rounds 后自动释放），日志噪声但功能正确。

可调旋钮（不修源码）：`preambleReceivedTargetPower=-90` (UE Tx +6 dB), `prach_ConfigurationIndex=16` (PRACH 时机变稀疏), `ra_ResponseWindow=6` (给 gNB 多 6ms 解 LDPC)。但实测 SNR=10 这些不是瓶颈，没动。

## 3. 错误诊断的四次反转

时间序：

### 3.1 ❌ "DL PDSCH (RRC Setup) LDPC 失败"（错）
看到 SNR=-5 dB attach 后 `RRCSetupComplete = 0`，第一反应是 UE 没收到 RRC Setup。但 UE log line 131 明明白白：

```
131: [NR_RRC] Received NR_RRCSetup
133: State = NR_RRC_CONNECTED
136: Generating RRCSetupComplete (bytes 33)
```

UE 收到了 RRC Setup，也生成了 33 字节 RRCSetupComplete。我跳过了"看 UE log"这一步直接结论 PDSCH LDPC 失败，被打脸。

### 3.2 ❌ "UL PUSCH (RRCSetupComplete) LDPC 失败 / DTX"（部分错）
gNB 端只收到 12 字节 MAC PDU，SRB1 收到 0 字节，配上 `ulsch_rounds 5/1/0/0` 看似 UL PUSCH 解码不稳。但忽略了：5 次 ulsch 包含 1 次 DTX (UE 完全没在期望的资源上发)，DTX 通常意味着 UE 没收到 UL grant，**这是 PDCCH 问题，不是 PUSCH 问题**。

### 3.3 ❌ "v7 noise 注入有 zero-padding bug"（被 dump 数据证伪）
看到 `gNB ulsch SNR=16 dB` vs `--snr-dB=-5`，差 21 dB，假设 ChannelProducer 把 `-5 dB` 算到了"零填充区域被平均进去"的 signal_power 上，导致实际 sigma 比设的低 21 dB。

围绕这个假设：
- 写了 `nr_prach.c` PRACH 检测器无门限的物理推算（这部分是对的，但跟主 bug 无关）
- 算了 PUSCH 5 PRB Parseval 占比 (60/1024) → 估"+12.3 dB FFT 增益"
- 但当时把这 12.3 dB **错位归因为"signal_power 测量偏差"，不是物理过程**
- 在 `v7.py` 加了 noise diagnostic dump patch + 写离线分析脚本 + mock test

dump 跑出来 120 个 slot：
- 118 个全是 0 (attach 早期静默 slot 或 TDD 切换)
- 仅 2 个有真实数据 (slot 54, 84)
- 这 2 个的 `bug_sig ≈ real_sig` (差 0.001-0.02 dB)，**SNR_bias = 0.00 dB**

→ **完全证伪 zero-padding 假设**。`active_pct = 93.33% = 14/15` 是 OFDM 数据/(数据+CP) 结构常数，**OFDM 时域 IFFT 输出在整个 slot 上都是连续非零的**，没有"symbol 之间的空隙是 0"这种东西。频域稀疏 (60/1024 SC) 在时域不表现为零填充，而是表现为"所有时域样本都非零，但每样本能量按 Parseval 守恒降低"。

### 3.4 ✅ 真相：FFT 处理增益 + DMRS 估计增益（物理学，不是 bug）

```
SNR_demod = SNR_set + G_FFT + G_DMRS
G_FFT  = 10·log10(N_FFT / N_used_SC)    ← OFDM "spread" 处理增益，确定性
G_DMRS = channel-estimation processing gain   ← 经验值，约 5-10 dB
```

PUSCH 5 PRB (60 SC) 配置下：
- G_FFT = 10·log10(2048/60) = +15.3 dB
- G_DMRS = +5.7 dB (gNB 实测反算: 16 - (-5) - 15.3 = 5.7)
- 合计 offset = **+21.0 dB** → 设 -5 dB 的 demod SNR 实际是 +16 dB ✓

## 4. 修正后的 offset 表（关键 — 包含 reviewer 反馈修正）

| 信号 / 分配 | N_used SC | G_FFT (dB) | G_DMRS (dB) | **总 offset** |
|---|---:|---:|---:|---:|
| **PRACH B4 (long ZC L_RA=839)** | 839 | +3.9 | 0 | +3.9 |
| **PRACH A2 (short ZC L_RA=139)** ⚠️ | 139 | **+11.7** | 0 | **+11.7** |
| Msg3 PUSCH (5 PRB, 60 SC) | 60 | +15.3 | +5.7 | +21.0 |
| Msg3 PUSCH (10 PRB, 120 SC) | 120 | +12.3 | +5.7 | +18.0 |
| **SRS partial (4 PRB, 48 SC)** ⭐ | 48 | +16.3 | +5 (SRS) | **+21.3** |
| SRS full BW (1272 SC) | 1272 | +2.1 | +5 | +7.1 |
| Msg4 PDSCH (12 PRB, 144 SC) | 144 | +11.5 | +5.7 | +17.2 |
| SIB1 PDSCH (24 PRB, 288 SC) | 288 | +8.5 | +5.7 | +14.2 |
| **PDCCH UL grant (AL=4, 4 CCE = 288 SC)** ⚠️ | **288** | **+8.5** | +6 (DCI DMRS) | **+14.5** |
| PDCCH (AL=8, 8 CCE = 576 SC) | 576 | +5.5 | +6 | +11.5 |
| PDCCH (AL=16, 16 CCE = 1152 SC) | 1152 | +2.5 | +6 | +8.5 |
| 持续 PDSCH wideband (1272 SC) | 1272 | +2.1 | +5.7 | +7.8 |

⚠️ 与第一版 offset 表的关键修正：
- **PRACH A2**: 第一版误算 12 SC → +22.3 dB；正确是 ZC seq L_RA=139，FFT_gain = +11.7 dB
- **PDCCH AL=4**: 第一版误算 24 RE → +19.3 dB；正确是 1 CCE = 6 REG = 6 PRB × 1 sym = 72 RE，4 CCE = 288 SC，FFT_gain = +8.5 dB
- **G_DMRS**: 第一版猜 +8 dB；用 gNB 实测反算 = +5.7 dB

## 5. 修正后的 5/7 SNR=-5 ABORTED 真因 ✅

| 信号 | 修正后 demod SNR | 状态 | 解释 |
|---|---:|---|---|
| SSB / PBCH | +5+ (窄带累积) | ✅ | UE Initial sync ✅ |
| SIB1 PDSCH | -5+8.5+5.7 = +9.2 dB | ✅ | MCS 0 LDPC OK |
| Msg2 RAR | -5+8.5+0 = +3.5 dB | 边界 | UE 端 1 次 LDPC fail / 2 次解出 (吻合) |
| Msg3 PUSCH (5 PRB) | -5+15.3+5.7 = +16 dB | ✅ | gNB 实测 16 dB (吻合) |
| Msg4 PDSCH (12 PRB) | -5+11.5+5.7 = +12.2 dB | ✅ | HARQ 重传搞定 (gnb.log: "Received Nack...preparing retransmission" → 第二次 ACK) |
| **UL grant DCI (PDCCH AL=4)** | **-5+8.5+6 = +9.5 dB** | **⚠️ 真凶** | **polar code BLER 50-80%, 5 次只过 1 次** |

→ ulsch_rounds 5/1/0/0 + DTX 1 = **UE 大部分时候解不到 UL grant DCI** → DTX → gNB 等不到 RRCSetupComplete → 超时 ABORTED。

**全程物理学，无需 rxgain 饱和 / max_active_UE / LDPC 边界等臆测**。

## 6. 修法（如果需要在 SNR=-5 也稳定 attach）

**调 PDCCH aggregation level**：OAI conf 里 `searchSpaceZero` / `coreset` 配置。AL=4 → AL=8 让 PDCCH FFT_gain 多 3 dB，配合 polar 编码增益（编码率从 1/N 变成 1/(2N)），demod 阈值能下移约 3-5 dB → SNR=-5 dB 也能稳定解 DCI。

注意：AL 调高占用更多 control region 资源，影响多 UE 容量，单 UE 实验场景下无副作用。

## 7. 这次 dump 工具的产出（保留以备后用）

虽然结论是"v7 没 bug"，但工具本身不浪费：

- **`v7.py` 的 noise diagnostic dump patch**（env-gated, zero overhead when off）
  - `V7_NOISE_DIAG_DIR` 启用 dump
  - `V7_NOISE_DIAG_DENSE_UNTIL` / `DENSE_EVERY` / `EVERY` / `MAX` 控制采样
  - `cp.copyto(self._diag_pre_noise_buf, self.gpu_out)` 在 `_gpu_compute_core` 内部 graph-capturable
  - dump pre-noise + post-noise + sigma²_v7 三件套
- **`analyze_noise_diag.py` 离线分析脚本**
  - active_ratio bucketing (PRACH/Msg3/PUSCH/Continuous)
  - 三方 sanity check (real_sig / bug_sig / sigma²_v7)
  - 理论曲线 R² fit
  - 散点图 CSV 导出
- **`launch_all_v7.sh` 的 env forwarding 改动**：让 docker exec 自动透传 `V7_NOISE_DIAG_*`

以后换 v6/v8 验证 noise 注入正确性、或者验证别的 ChannelProducer 实现，patch 直接可复用。

## 8. 历史 sweep 数据复活方案（无需重跑）

之前画过的所有 `SNR_set ∈ {-5, 0, +5, +10, +15, +20, +25}` sweep 曲线，按"测量目标信号"做横轴平移：

| 测量目标 | offset (dB) | 历史 SNR_set=-5 实际是 |
|---|---:|---:|
| **SRS 4 PRB** ⭐ | +21.3 | demod +16.3 dB |
| SRS full BW | +7.1 | demod +2.1 dB |
| PUSCH 5 PRB | +21.0 | demod +16.0 dB |
| PDCCH AL=4 (UL grant) | +14.5 | demod +9.5 dB |

`5/2 SNR=10 OK` 那批数据 → SRS demod ≈ **+31.3 dB**，难怪稳定。

## 9. 下一步操作清单（按优先级）

### Phase 1 (纸面工作, ~30 min) ✅ 本日志已完成
1. ✅ 改 offset 表 (PRACH +11.7 / PDCCH +8.5 / DMRS +5.7)
2. ✅ 用 PDCCH polar 临界这个新解释重写 5/7 ABORTED 诊断段

### Phase 2 (一晚上的实验, 验证 offset 框架)
3. 📋 跑 SNR sweep + BLER 曲线对比 NR 标准曲线
   - SNR_set ∈ {-10, -5, 0, +5, +10}
   - PRB 分配固定 5 PRB, MCS 0
   - 取 gNB stats 输出 ulsch BLER
   - 画 BLER vs (SNR_set + 21 dB) 曲线
   - 跟 3GPP / Sionna 标准 PUSCH MCS 0 BLER 曲线对比
   - 落在 ±1 dB 内 → 物理 + offset 完全验证

### Phase 3 (如果 phase 2 不够稳)
4. 📋 调 OAI PDCCH AL=4 → AL=8 让 SNR=-5 也能稳定 attach
   - `searchSpaceZero` / `coreset` 配置
   - 不影响单 UE 实验场景

### Phase 4 (如果想 paper 化)
5. 📋 写 setup section 的 SNR 定义说明：
   > "Our channel proxy injects AWGN at the time domain after IFFT, with noise variance scaled to the per-sample signal power averaged over the entire slot. The configured SNR_set corresponds to a per-time-sample SNR; the equivalent per-subcarrier demodulation SNR is given by:
   > 
   > SNR_demod = SNR_set + G_FFT + G_DMRS
   > 
   > where G_FFT = 10·log10(N_FFT/N_used_SC) is deterministic from the allocation, and G_DMRS is the channel-estimation processing gain calibrated empirically to **5.7 ± 0.5 dB** for our static channel and single-symbol type-A DMRS configuration."

## 10. 留下的开放问题

1. ❓ DMRS gain 在动态信道下（attach-stable trigger 后）是多少？这次实测 +5.7 dB 是 attach-stable 期间的静态信道值，动态后可能更低（典型 +3~5 dB）。
2. ❓ SRS 跟 PUSCH DMRS 的 G_proc 不一定相同（SRS ZC 序列短、单 symbol、无 LDPC 帮助），需要单独标定 — 现在表里写的 +5 dB 是估的。
3. ❓ PDCCH polar 在 demod SNR ≈ +10 dB 的精确 BLER 多少？OAI 用的是 SCL decoder + CRC-aided list size 8，需要查 3GPP 38.212 Annex A 或者 Sionna polar 实验数据对比。

## 11. 关键代码位置（以后参考）

- v7 noise injection: `v7.py:1144-1145` (`mean(|gpu_out|²) → sigma²`)
- v7 dump hook: `v7.py:1138-1141` (`cp.copyto pre-noise`), `v7.py:1491-1525` (dump in `process_slot_ipc`)
- v7 init dump state: `v7.py:1090-1108` (env vars + `_diag_pre_noise_buf` allocation)
- OAI PRACH detector (no SNR gate): `nr_prach.c:740-755`
- OAI gnb conf PRACH params: `gnb.sa.band78.fr1.106PRB.usrpb210.conf:80-122`
- launch env forwarding: `launch_all_v7.sh:515-522`
- launch v8 GT_SAVE_EVERY override: `launch_all_v8.sh:73`

---

## 12. 下午的尝试：SRS SNR 标定（失败 / 主动叫停）

### 12.1 动机和方法
诊断完 §3-§5 之后，提议做一组 SNR sweep + 线性回归，标定 OAI `[SRS Twin]` log 报的 SNR (`SNR_meas`) vs ChannelProducer 设置的 `SNR_set`，反算出 `G_proc_SRS`。准备工作：
- 修正 `G_FFT_SRS = +5.16 dB`（基于源码事实：`m_SRS=104 PRB`, `K_TC=2`, `M_sc_b_SRS=624 SC`）
- 写好 `srs_snr_calibration.py`（线性回归 + slope 诊断 + 斜率/截距/R²）
- mock test 通过

### 12.2 实跑结果（颠覆假设）

跑了 `for SNR in 20 15 10 5; do ... done`，4 个独立 sweep：

| sweep | SNR_set | status | n_srs | `[SRS Twin]` SNR_meas |
|---|---:|---|---:|---:|
| q4_sweep_20260508_143946 | **20** | **ABORTED** | **0** | 17 dB |
| q4_sweep_20260508_144628 | **15** | **OK** | 3 | 19 dB |
| q4_sweep_20260508_145222 | **10** | **OK** | 3 | 17 dB |
| q4_sweep_20260508_145845 | **5** | **OK** | 1+ | 17 dB |

**SNR_meas 几乎是常数 17-19 dB**，跟 SNR_set 设的 5/10/15/20 dB 没有线性关系。

**slope ≈ 0**（不是预期的 1.0）→ 所有点都在"OAI SRS estimator 饱和区"。

### 12.3 结论
**OAI 的 `dB_fixed(signal_power) - dB_fixed(noise_power)` 不是 raw per-SC demod SNR**：
- 它是 SRS estimator 的"signal vs estimation residual"比率
- 在 raw SNR ≥ 5 dB 时 estimator 本身已经能压低 residual 到底噪 → ratio 锁在 17-19 dB
- **不能用 `[SRS Twin]` SNR 反推 ChannelProducer SNR**

中午用 SNR=10 单点反算的 `G_proc_SRS=+1.84 dB` 是**单点凑巧**，不是真实物理量。

`srs_snr_calibration.py` 标记为 deprecated（保留但不再用）。

### 12.4 SNR=20 ABORTED 的 sub-finding
RA failed 仅 9 次（vs SNR=10 时的 79 次），attach 完成（RRCSetupComplete=1, RRCReconfigurationComplete=1），但 SRS Dump summary 是空的。
**说明 attach 完成后 SRS 调度还没在 420s ceiling 内启动起来**——OAI 调度时序问题，跟 SNR 无关。

---

## 13. 下午第二次尝试：用已有 .bin 跑 NMSE（更狠的发现）

主动放弃 SNR 标定后，至少跑一遍 NMSE 看 4 个 sweep 的 .bin 文件能给出什么。

### 13.1 数据

```
=== snr_15dB ===  (n_srs=3 → 300 SRS frames)
  Align: SLOT  paired 202 (median gap 3 slots)
  Per-frame NMSE: mean=+11.93 dB, range=[-5.8, +28.4]
  NMSE vs N: N=1 → 8.75 dB, N=202 → 15.19 dB, monotone=NO

=== snr_10dB ===  (n_srs=3)
  Align: SLOT  paired 201 (median gap 2 slots)
  Per-frame NMSE: mean=+10.48 dB, range=[-9.7, +39.6]
  NMSE vs N: N=1 → 25.38 dB, N=201 → 20.03 dB, monotone=NO

=== snr_5dB ===  (n_srs=1+)
  Align: SLOT  paired 141 (median gap 2 slots)
  Per-frame NMSE: mean=+6.24 dB, range=[-7.3, +43.6]
  NMSE vs N: N=1 → 15.68 dB, N=141 → 15.19 dB, monotone=NO
```

### 13.2 三个红灯

1. **NMSE 全为正值**：`NMSE = ‖H_srs - H_gt‖² / ‖H_gt‖²` 正 dB → 误差能量比信号能量还大 → SRS 估计跟 GT **根本对不上**。健康 NMSE 应该是 -10 ~ -25 dB。

2. **NMSE 反 SNR 相关**：5 dB → 6.24 < 10 dB → 10.48 < 15 dB → 11.93。**完全反物理**。
   - 真实物理：SNR ↑ → NMSE ↓
   - 实测：SNR ↑ → NMSE ↑
   - **意味着 NMSE 不是被 SNR 决定的，是被别的因素主导**

3. **NMSE vs N 不单调**：例如 SNR=10 行：N=1 → 25.38 → ... → N=201 → 20.03（中间 N=1 比 N=201 还差）。
   - 说明 paired frames 之间**不同质**——里面混了"对得上"和"对不上"两类
   - batch 平均时互相污染

### 13.3 这跟 group meeting §9 完全印证

```markdown
§9 (groupmeeting_0507_srs_mmse_notes.md):
最近暴露的 SRS--GT 对应问题不是新增 bug，而是过去在准静态信道下被掩盖的潜在缺陷。
我用 paired_prefix Signature Gate、REQUIRE_N50、bad_frames_<case>.csv 把它稳定检出。
所以短期工作优先级前移：
  先固化 pairing 闸门 → 再定位 bad-frame → 再调 mmse1d 参数 → 再用干净样本做最小回归。
否则 legacy 与 mmse1d 的 NMSE 差异会混入 pairing 噪声。
```

之前在 SNR=10 单点 + 5/8 上午 sweep 上 NMSE 看起来还能用，是因为**单一 SNR + 单一 sweep 下 paining 噪声"刚好"低**。多 SNR 一起跑，pairing 噪声立刻浮出水面。

**今天下午所有 NMSE 数据都不可信**——直到 pairing gate 固化前不能做任何 NMSE 结论。

---

## 14. 今天的真正发现 vs 跑偏（全景总结）

### 真正发现（有价值，写进研究记录）
- v7 noise injection 没有 zero-padding bug（`mean(|gpu_out|²)` 对 OFDM 时域信号是正确的）
- "21 dB SNR 偏差"是 OFDM FFT + DMRS 物理处理增益（不是代码 bug）
- PRACH/PDCCH/PUSCH/SRS 各自的 G_FFT 已用 OAI 源码事实算清楚
- SNR=-5 ABORTED 真凶是 PDCCH polar AL=4 在 demod ≈ +9.5 dB 临界（不是 PUSCH 也不是 rxgain）
- OAI nr_prach.c 检测器无 SNR 门限，伪 PRACH 检测是上游设计，不是 bug
- OAI conf 里 `freq_hopping_c_srs=0` 实际**被 nr_radio_config.c:930 忽略**（hardcoded c_SRS=25 → m_SRS=104 PRB）
- OAI `[SRS Twin]` SNR 在 raw SNR ≥ 5 dB 几乎饱和在 17-19 dB → **不是 demod SNR 的代理**

### 跑偏（要诚实承认）
- 中途想用 OAI `[SRS Twin]` log 反向标定 ChannelProducer
- 标定假设 `SNR_meas = SNR_set + offset` 完全不成立（实测 slope ≈ 0）
- 4 个 sweep 的 NMSE 全为正值 + 反 SNR 相关 → 实际是 pairing/scale 主导
- 这印证了 group meeting §9 早已识别的 SRS-GT pairing 问题
- **整个下午的 SRS SNR 标定 + NMSE 跑分都不能用**，因为 pairing 没修

### 教训（操作层面）
1. **"再多验证一步就稳了" 是 sunk cost 信号**——本来 §3-5 物理诊断已经闭环，不该继续延伸到 SRS 标定
2. **跑实验前先问"这个数据是否在你下一次决策的关键路径上"**——SRS SNR 标定不在 mmse1d 主线上
3. **数据反 SNR 相关 = 物理直觉立刻应该亮红灯**——不是模型不准，是上游 (pairing) 出了问题
4. **group meeting §9 已写明短期优先级**——任何偏离这个优先级的实验都要先质问

---

## 15. 决定 / 下一步

**关闭今天所有"诊断扩展线"**：
- ❌ SRS SNR 标定（slope=0，方法无效）
- ❌ 用今天 NMSE 数据做任何结论（pairing 污染）
- ❌ Phase 2 PUSCH BLER 验证（不在主线）
- ❌ Phase 3 调 PDCCH AL=8（仅在需要 SNR=-5 attach 时才做）

**回到 group meeting §9 短期最优先**：
1. 看 `bad_frames_<case>.csv` / `paired_prefix` Signature Gate 的当前状态
2. 定位 pairing 失败的根因（slot misalignment / scale / phase wrap / 时延补偿）
3. 修复后用干净 paired 样本做 legacy vs mmse1d 最小回归
4. 同步：OAI `start_meas/stop_meas` in-place 测时 + `R_pp` 复用工程优化

**保留备用工具**（以后真要写 paper / 复现实验时可能用）：
- `analyze_noise_diag.py` + `srs_snr_calibration.py` + v7 noise diag patch — 作为 ChannelProducer 物理验证工具，不再用于 SRS 标定

---

## 反思 / 教训（诊断阶段）

1. **不要在没看 UE log 的情况下推断"UE 没收到"** — 第 3.1 错的根因
2. **不要把 OFDM 频域稀疏当成时域零填充** — 第 3.3 错的根因。Parseval 是基本物理量守恒，这个直觉本来该秒过的。
3. **OAI 报的 SNR 不是 raw channel SNR** — 接收端报的所有 SNR 都已经经过了 FFT + DMRS 估计的处理增益累加，跟 ChannelProducer 注入的"时域 SNR"不在一个量纲上
4. **PDCCH 在低 SNR 总是先死** — 因为它用 polar code（无 HARQ），且 aggregation level 直接决定能用多少 RE。当 PDSCH 能解、PUSCH 能发但 ulsch 频繁 DTX 时，**第一嫌疑应该是 PDCCH 不是 PUSCH**
5. **dump 数据比理论推算重要** — 这次靠 dump 把"21 dB bug"假设钉死，但更重要的是承认推算容易错（PDCCH RE 数算成 24 vs 288 差了 12 倍），dump 是最后一道防线

## 反思 / 教训（标定阶段，下午追加）

6. **`[SRS Twin]` SNR ≠ link SNR** — 任何 receiver 报告的 SNR 都需要先理解它的定义（raw / post-FFT / post-DMRS / post-equalization / signal-vs-residual ratio），不能直接当 SNR_set 的代理
7. **NMSE 反 SNR 相关 → 立即停止 SNR 实验，去查 pairing/scale** — 这种"反物理"信号比"SNR 标定有偏"严重得多，必须先解决
8. **写日志 ≠ 写 paper** — 我们今天没写 paper 也没要 reviewer 看，那就不需要"标定 G_proc_SRS 给审稿人交代"。早上目标设错了
9. **group meeting notes 是研究优先级的真理之源** — 任何跟 §10 列出的优先级冲突的实验，第一反应是"为什么这件事比 §10 重要"，而不是"再跑一下看看"
10. **"victory lap 跑得太快"是个常见警告** — 当我刚承认错误就立刻想做下一个验证，那个下一个验证大概率也是错方向。先停下来问"我现在真正知道什么 / 不知道什么"
