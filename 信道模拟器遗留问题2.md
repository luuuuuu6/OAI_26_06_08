# STO 抖动物理机制追查日志

| 字段 | 值 |
|------|----|
| **日期** | 2026-05-15 |
| **目标** | 定位遗留问题 2：STO 帧间 ±1 样本抖动的物理机制（"为什么在抖"） |
| **结论** | **OAI UE 的 PI 时序同步控制器 `nr_adjust_synch_ue` 在 rfsim 下产生不必要的 ±1 样本帧边界修正** |
| **前序工作** | 05-14 已定位到"什么在抖"（`start_ts % 30720 = {29916,29917,29918}`），Fix 1 已消除下游影响 |
| **修复方案** | UE 启动参数加 `--ue-timing-correction-disable`，零代码修改 |

---

## 一、问题回顾

05-14 DEBUG_LOG 中确认：

- UL `start_ts` 在帧间有 ±1 整样本抖动（29916/29917/29918，std≈0.5 样本）
- 这是 -7 dB NMSE floor 的全部根因
- Fix 1 (per-frame phase slope 补偿) 已在信号处理层消除影响
- **但 STO 抖动的物理来源（"为什么会抖"）未追到机制层**

三个候选假说：

| 假说 | 来源 |
|------|------|
| A: rfsimulator GPU IPC futex 唤醒延迟 → timestamp 量化 | 05-14 猜测 |
| B: OAI 调度器 slot 边界计算舍入 (`get_samples_slot_timestamp`) | 05-14 猜测 |
| C: Proxy `proxy_ul_head_combined` 初始化/追赶逻辑边界条件 | 05-14 猜测 |

**本次追查结果：三个候选全部排除。真正根因是第四个——UE 侧 PI 时序控制器。**

---

## 二、追查过程

### 2.1 信号路径梳理

UL timing 的完整传递链（GPU IPC 模式）：

```
OAI UE 主循环 (nr-ue.c)
  ├─ trx_read_func() → rx_timestamp (从 rfsim 的 nextRxTstamp)
  ├─ writeTimestamp = rx_timestamp + D - firstSymSamp - N_TA_offset - timing_advance
  ├─ trx_write_func(writeTimestamp, samples, writeBlockSize)
  │    └─ rfsimulator_write()
  │         ├─ timestamp -= command_line_sample_advance
  │         └─ gpu_ipc_v7_ul_write(ctx, samples, nsamps, nbAnt, timestamp)
  │              └─ v7_circ_write()
  │                   ├─ CUDA memcpy → GPU 环形缓冲 ul_tx
  │                   ├─ SHM[328] = timestamp        (last_ul_tx_ts, uint64)
  │                   ├─ SHM[336] = nsamps            (last_ul_tx_nsamps, uint32)
  │                   └─ futex_wake(UL_TX_SEQ)
  │
Proxy (v8.py)
  ├─ cur_ul = ipc_ues[k].get_last_ul_tx_ts()     → read SHM[328]
  ├─ ul_ns  = ipc_ues[k].get_last_ul_tx_nsamps() → read SHM[336]
  ├─ ue_head = cur_ul + ul_ns
  ├─ proxy_ul_head_combined 追踪 min(ue_heads)
  ├─ _ipc_ul_combine(start_ts=proxy_ul_head_combined, delta)
  │    ├─ [IPC DIAG] UL start_ts=... not slot-aligned (offset=start_ts % 30720)
  │    └─ 按 slot 切分 → _ipc_ul_superposition_slot() → 写 gNB ul_rx
  └─ set_last_ul_rx_ts() + futex_wake → 通知 gNB
```

### 2.2 候选假说排除

#### 假说 A 排除：rfsimulator timestamp 量化

`rfsimulator_write` 中：

```c
// radio/rfsimulator/simulator.c:1287-1290
timestamp -= device->openair0_cfg->command_line_sample_advance;
return rfsimulator_write_internal(device->priv, timestamp, ...);
```

`command_line_sample_advance` 是固定常数（通常为 0）。`timestamp` 直接传给 `gpu_ipc_v7_ul_write`，无任何量化、取模或舍入操作。**排除。**

#### 假说 B 排除：slot 边界计算舍入

`get_samples_per_slot` 对 mu=1：

```c
// openair1/PHY/INIT/nr_parms.c:250-260
samp_count = (slot % (fp->slots_per_subframe / 2)) ? fp->samples_per_slotN0 : fp->samples_per_slot0;
```

mu=1 时 `slots_per_subframe = 2`，`slots_per_subframe / 2 = 1`，`slot % 1 = 0` 恒成立。所有 slot 返回 `samples_per_slot0 = 30720`，无任何 slot 间差异。`get_samples_slot_timestamp` 纯整数累加，无舍入。**排除。**

#### 假说 C 排除：Proxy `proxy_ul_head_combined` 边界条件

Proxy 的 `proxy_ul_head_combined` 完全跟随 UE 的 `ue_head = last_ul_tx_ts + last_ul_tx_nsamps`。Proxy 不做任何 timestamp 修正、对齐或舍入。IPC DIAG 中报告的 `offset = start_ts % 30720 = 29917` 正是 UE 写入 SHM 的值的直接反映。**Proxy 是忠实传递者，不是抖动来源。排除。**

### 2.3 真正根因定位

#### 线索：帧边界的 readBlockSize 修正

`nr-ue.c` 主循环中，每帧最后一个 slot 有特殊处理：

```c
// executables/nr-ue.c:1176-1186
int iq_shift_to_apply = 0;
if (slot_nr == nb_slot_frame - 1) {
    iq_shift_to_apply = shiftForNextFrame;
    // ...
    shiftForNextFrame = -round(UE->max_pos_acc * get_nrUE_params()->time_sync_I);
}
const int readBlockSize = get_readBlockSize(slot_nr, fp) - iq_shift_to_apply;
```

`shiftForNextFrame` 非零时，帧末 slot 的 `readBlockSize` 和 `writeBlockSize` 都被修改：

```c
// executables/nr-ue.c:1224
int writeBlockSize = fp->get_samples_per_slot(...) - iq_shift_to_apply;
```

这意味着：
- `readBlockSize = 30720 - F ∓ shift`（F = firstSymSamp，最后 slot 无下一 slot 的 firstSym）
- `writeBlockSize = 30720 ∓ shift`
- `rx_timestamp` 在 rfsim 中按 `nextRxTstamp += nsamps` 递增，所以每帧漂移 `∓shift` 样本
- `writeTimestamp = rx_timestamp + 常数` 同样漂移

#### `shiftForNextFrame` 的来源

两条路径设置 `shiftForNextFrame`：

**路径 1（帧边界）**：

```c
// executables/nr-ue.c:1183
shiftForNextFrame = -round(UE->max_pos_acc * get_nrUE_params()->time_sync_I);
```

默认 `time_sync_I = 0.0`，所以此路径始终返回 0。

**路径 2（PBCH 解码后 — 这是实际触发路径）**：

```c
// executables/nr-ue.c:1244-1246
int ret = UE_dl_preprocessing(UE, &curMsgRx->proc, ...);
if (ret != INT_MAX)
    shiftForNextFrame = ret;
```

`UE_dl_preprocessing` → `pbch_pdcch_processing` → `nr_adjust_synch_ue`：

```c
// openair1/SCHED_NR_UE/phy_procedures_nr_ue.c:966-969
if (ue->no_timing_correction==0 && pbchSuccess == 0) {
    sampleShift = nr_adjust_synch_ue(fp, ue, gNB_id, ...);
}
```

#### `nr_adjust_synch_ue` — 抖动的发源地

完整函数（`openair1/PHY/NR_UE_ESTIMATION/nr_adjust_synch_ue.c`）：

```c
int nr_adjust_synch_ue(...) {
    int max_val = 0, max_pos = 0;

    // [Step 1] CIR 峰值搜索 — 整数位置量化
    for (int i = -fp->nb_prefix_samples; i < fp->nb_prefix_samples; i++) {
        int temp = 0;
        int j = (i < 0) ? (i + fp->ofdm_symbol_size) : i;
        for (int aa = 0; aa < fp->nb_antennas_rx; aa++) {
            int Re = dl_ch_estimates_time[aa][j].r;
            int Im = dl_ch_estimates_time[aa][j].i;
            temp += (Re*Re/2) + (Im*Im/2);
        }
        if (temp > max_val) {
            max_pos = i;       // ← 整数, rfsim 下可能为 0 或 ±1
            max_val = temp;
        }
    }

    // [Step 2] IIR 滤波降噪
    ue->max_pos_iir = ((ue->max_pos_iir * coef) >> 15) + (max_pos * ncoef);
    const int diff = (ue->max_pos_iir + 16384) >> 15;
    int sampleShift = diff;

    // [Step 3] PI 控制器输出
    const double PID_P = get_nrUE_params()->time_sync_P;  // 默认 0.5
    const double PID_I = get_nrUE_params()->time_sync_I;  // 默认 0.0
    int sample_shift = -round(sampleShift * PID_P + ue->max_pos_acc * PID_I);
    //   sampleShift=1 → -round(0.5) = -1  (C round 0.5 away from zero)
    //   sampleShift=0 → -round(0.0) =  0

    // [Step 4] 状态更新
    ue->max_pos_iir += -round(sampleShift * PID_P) * 32768;  // P-term 反馈
    ue->max_pos_acc += max_pos;                                // I-term 累积

    return sample_shift;  // → 成为 shiftForNextFrame
}
```

---

## 三、根因完整因果链

```
[Step 1] nr_adjust_synch_ue: CIR 峰值搜索 (整数精度)
    在 rfsim 静态信道下, 初始同步有 0-3 样本量化偏差
    (实测: offset ≈ 803 vs N_TA_offset = 800, 差 3 样本)
    max_pos = {0, 1, -1}  (取决于噪声和量化)

[Step 2] IIR 滤波 + PI 控制器
    PID_P = 0.5, PID_I = 0.0 (默认参数)
    sampleShift = IIR_filter(max_pos)
    sample_shift = -round(sampleShift × 0.5)
      当 sampleShift = 1 → sample_shift = -1
      当 sampleShift = 0 → sample_shift =  0
    → 在 0 和 ±1 之间振荡

[Step 3] 帧边界应用 (nr-ue.c, 每帧最后一个 slot)
    iq_shift_to_apply = shiftForNextFrame = sample_shift
    readBlockSize  = get_readBlockSize(slot_19, fp) - iq_shift_to_apply
    writeBlockSize = 30720 - iq_shift_to_apply

[Step 4] rx_timestamp 漂移
    rfsimulator: nextRxTstamp += readBlockSize
    正常帧: nextRxTstamp += 30720 (net per slot)
    修正帧: nextRxTstamp += 30720 ∓ 1
    → rx_timestamp 每次修正漂移 ∓1

[Step 5] writeTimestamp 漂移
    writeTimestamp = rx_timestamp + D - firstSymSamp - N_TA_offset - timing_advance
    D, firstSymSamp, N_TA_offset, timing_advance 均为常数 (rfsim 静态信道)
    → writeTimestamp 继承 rx_timestamp 的 ±1 漂移

[Step 6] GPU IPC SHM 写入
    gpu_ipc_v7_ul_write → v7_circ_write:
      SHM[328] (last_ul_tx_ts) = writeTimestamp - command_line_sample_advance
      SHM[336] (last_ul_tx_nsamps) = writeBlockSize
    → 两者都反映 ±1 修正

[Step 7] Proxy 读取
    proxy_ul_head_combined = last_ul_tx_ts + last_ul_tx_nsamps
    start_ts = proxy_ul_head_combined
    start_ts % 30720 = {29916, 29917, 29918}  (帧间变化)

[Step 8] SRS 信道估计受影响
    H_obs[k] = H_true[k] × exp(-j2π k δ/N), δ 帧间变化 ±1
    跨 1200 SC 累积相位差 ≈ 2π × 1200/30720 × 1 ≈ 0.245 rad (per Δδ=1)
    帧间 correlation 降到 0.5-0.8 → NMSE floor ≈ -7 dB
```

---

## 四、控制器行为分析

### 为什么 rfsim 下 `max_pos` 不恒为 0

1. **初始同步精度限制**: 初始 PSS/SSS 同步的定位精度为整数样本。如果真实 CIR 峰值在 0.4 样本位置，初始同步会锁定到 0 或 1，引入 0-1 样本的系统偏差。

2. **N_TA_offset 量化**: `determine_N_TA_offset` 使用整数除法 `(N_TA_offset × samples_per_subframe) / (4096 × 480)`，可能引入 1-3 样本的截断误差。实测 offset ≈ 803 vs 预期 800，差 3 样本正是这种截断。

3. **AWGN 扰动**: 即使 SNR 很高，CIR 的能量分布在峰值附近的两个整数位置上可能非常接近，AWGN 的微小变化就能让 `max_pos` 在两个值之间翻转。

### 控制器振荡机制

```
Frame N:   max_pos = 1 → sample_shift = -1 → timing 前移 1 样本
Frame N+1: max_pos = 0 (峰值在新窗口里回到了 0) → sample_shift = 0
Frame N+2: max_pos = 1 (没有修正, 偏差又回来了) → sample_shift = -1
...
→ 在 0 和 -1 之间周期振荡, 频率 = SSB 周期 / 2
```

IIR 滤波器 (coef=16384) 和 P-term 反馈 (`max_pos_iir += -round(sampleShift × 0.5) × 32768`) 会抑制振荡幅度，但无法消除因为：
- 控制器输出经过 `round()` 量化到整数
- ±0.5 边界上 `round()` 的不连续性导致极限环 (limit cycle)
- P-only 控制器（无 I 项）对恒定偏差无法完全消除稳态误差

### 偶发大幅跳变 (~20 样本) 的解释

如果某帧 PBCH 信道估计异常（例如由于 proxy 信道应用的 timing 同步问题或噪声尖峰），`max_pos` 可能跳到较大值（如 10-20 样本）。虽然 IIR 滤波器会平滑，但如果 `coef` 不够大、或者连续多帧异常，`sampleShift` 会跟随跳变，产生大的 `sample_shift`。

这解释了 SNR=40 最后 4 帧 STO 从 ~4 跳到 ~20 样本的现象。

---

## 五、当前 UE 启动参数（缺少 timing correction 禁用）

`launch_all_v8.sh` 第 692-698 行:

```bash
(cd "$LOG_DIR" && eval "$UE_GPU_ENV $UE_AGC_ENV" \
"$BUILD_DIR/nr-uesoftmodem" \
    -r 106 --numerology 1 --band 78 -C 3619200000 \
    --uicc0.imsi "$imsi" \
    --rfsim \
    $UE_ANT_ARGS \
    > "ue${ue_idx}.log" 2>&1) &
```

默认参数状态:

| 参数 | 默认值 | 影响 |
|------|--------|------|
| `--ue-timing-correction-disable` | 未设置 (`no_timing_correction=0`) | timing 修正**已启用** |
| `--time-sync-P` | 0.5 | P 控制器增益 |
| `--time-sync-I` | 0.0 | I 控制器增益 (无积分项) |

→ UE 每次 PBCH 解码成功后都会调用 `nr_adjust_synch_ue`，产生 ±1 修正。

---

## 六、修复方案

### 方案 A（推荐）: 运行时参数禁用 — 零代码修改

在 `launch_all_v8.sh` 的 UE 启动命令中加 `--ue-timing-correction-disable`:

```bash
"$BUILD_DIR/nr-uesoftmodem" \
    -r 106 --numerology 1 --band 78 -C 3619200000 \
    --uicc0.imsi "$imsi" \
    --rfsim \
    --ue-timing-correction-disable \    # ← 加这一行
    $UE_ANT_ARGS \
    > "ue${ue_idx}.log" 2>&1
```

效果:
- `no_timing_correction = 1`
- `nr_adjust_synch_ue` 永远不被调用
- `shiftForNextFrame` 永远为 0
- `readBlockSize` / `writeBlockSize` 永远为标准值
- `writeTimestamp` 帧间完全一致
- STO 抖动从源头消除

安全性:
- rfsim 中 UE 和 gNB 共享同一仿真时钟，**没有真实时钟漂移**
- 该控制器在 rfsim 下是多余的，禁用无副作用
- 不影响初始同步（PSS/SSS 同步是独立路径）
- **注意**: 真实硬件或 NTN 场景下不应禁用

### 方案 B: 在 Proxy 层对齐（已有 — Fix 1）

Fix 1 (per-frame phase slope 补偿) 在 SRS 信道估计后消除 STO 引起的线性相位。这是信号处理层的"后处理"方案，已验证有效 (corr 0.54→0.986)。但它不消除抖动本身，只消除其对下游的影响。

### 方案 C: 修改 OAI 代码（不推荐）

在 `nr-ue.c` 中加 `IS_SOFTMODEM_RFSIM` 条件判断，rfsim 模式下强制 `shiftForNextFrame = 0`。需要修改并重新编译 OAI，工作量大，且方案 A 已完全解决。

### 推荐策略

```
方案 A (--ue-timing-correction-disable)  →  消除抖动源头  [推荐]
  +
方案 B (Fix 1 phase slope 补偿)          →  防御残余/异常  [保留]
```

双重保护: 方案 A 消除 99%+ 的 STO 抖动，Fix 1 作为安全网处理可能的残余或其他来源的 timing 偏差。

---

## 七、假说排除总结

| 假说 | 证据 | 状态 |
|------|------|------|
| A: rfsimulator GPU IPC futex 唤醒延迟 | timestamp 直传无量化 (`v7_circ_write` 直接写入参数值) | **排除** |
| B: OAI 调度器 slot 边界舍入 | mu=1 所有 slot 大小相同 (30720), 纯整数累加无舍入 | **排除** |
| C: Proxy `proxy_ul_head_combined` 边界条件 | Proxy 忠实读取 SHM, 不做任何 timestamp 修正 | **排除** |
| **D: UE PI 时序控制器 `nr_adjust_synch_ue`** | **CIR 峰值量化 → P 控制器 → ±1 round → shiftForNextFrame → readBlockSize/writeBlockSize 修正 → timestamp 漂移** | **确认** |

---

## 八、关键代码文件索引

| 文件 | 作用 | 关键行 |
|------|------|--------|
| `executables/nr-ue.c` | UE 主循环, 帧边界 shift 应用 | 1176-1186, 1214-1216, 1224, 1244-1246 |
| `openair1/PHY/NR_UE_ESTIMATION/nr_adjust_synch_ue.c` | PI 时序控制器 (抖动发源地) | 50-64 (峰值搜索), 77-80 (PI 输出), 95-96 (状态更新) |
| `openair1/SCHED_NR_UE/phy_procedures_nr_ue.c` | PBCH 解码后调用 timing 调整 | 966-969 (`no_timing_correction` 门控) |
| `radio/rfsimulator/gpu_ipc_v7.c` | GPU IPC SHM 写入 | 437-446 (`gpu_ipc_v7_ul_write`), 254-331 (`v7_circ_write`) |
| `radio/rfsimulator/gpu_ipc_v7.h` | SHM 布局定义 | 77-125 (偏移 328/336 定义) |
| `radio/rfsimulator/simulator.c` | rfsimulator write 入口 | 1287-1290 (`command_line_sample_advance` 扣减) |
| `openair1/PHY/INIT/nr_parms.c` | slot 大小计算 | 250-287 |
| `executables/nr-uesoftmodem.h` | 命令行参数定义 | 70 (`--ue-timing-correction-disable`), 73-74 (`time-sync-P/I`) |

---

## 九、与 05-14 DEBUG_LOG 的关系

本日志是 `DEBUG_LOG_20260514_STO_ROOT_CAUSE.md` 遗留问题 2 的完整解答:

| 项目 | 05-14 状态 | 05-15 状态 |
|------|-----------|-----------|
| "什么在抖" | ✅ 已定位 (start_ts ±1 样本) | ✅ 不变 |
| "为什么在抖" | ❌ 未追查 | ✅ **已定位** (nr_adjust_synch_ue PI 控制器) |
| 修复状态 | Fix 1 消除下游影响 | Fix 1 + `--ue-timing-correction-disable` 双重保护 |
| 偶发大跳变机制 | ❌ 不清楚 | ✅ **已解释** (PBCH 估计异常 → max_pos 跳变) |

本日志将 STO 问题从"现象层定位"推进到"机制层定位 + 修复方案确认"。
