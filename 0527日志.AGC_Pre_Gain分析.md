# AGC Pre-Gain 方案分析日志

| 字段 | 值 |
|------|----|
| **日期** | 2026-05-27 |
| **关联** | 遗留问题1（信道模拟器遗留问题1.md） |
| **目标** | 确认遗留问题1根因与 AGC 的关系，评估并落地 Proxy 侧 UL pre-gain，接入 v9 启动/扫参脚本 |
| **状态** | 已实现：`v8.py` 支持 `--ul-pre-gain`；`launch_all_v9.sh` / `run_q4_snr_sweep_v9.sh` 已接入参数链路 |

---

## 一、遗留问题1 根因确认：与 AGC 直接相关

### 问题现象

SRS 信道估计 per-frame NMSE 在 SNR≥30 dB 时被 cap 在 ~-22 dB。

### 根因链

```
AMP = 512 (OAI 编译时常量)
  → UE SRS 频域幅度 ≈ 512
    → IFFT 后时域 RMS ≈ 200 (int16 满量程的 0.6%)
      → rfsim 无 RF AGC, RMS=200 直达 gNB
        → 仅 7.6 有效比特进入 int16 FFT (2048 点, 11 级蝶形)
          → 量化噪声累积 → SQNR 天花板 ≈ 22-23 dB
```

### 真实硬件 vs rfsim 对比

| 路径 | 信号流 | FFT 输入 RMS | 有效比特 | SQNR |
|------|--------|-------------|---------|------|
| **真实硬件** | UE → 空口 → **RF AGC 放大至 RMS≈15000** → ADC → FFT | ~15000 | ~14 | 50+ dB |
| **rfsim** | UE → 共享内存 → **无 AGC** → gNB → FFT | ~200 | ~7.6 | ~22 dB |

**结论：天花板是 rfsim 缺少 RF AGC 的副作用，不是 OAI 架构缺陷。**

### OAI 内置 nr_digital_agc 为什么没用

OAI 有 `nr_digital_agc`（`NR_DIGITAL_AGC_ENABLED=1` 启用），但它作用在 **FFT 之后**（频域），对信号和量化噪声等比放大，**不改善 FFT SQNR**。需要的是 FFT **之前**的时域增益。

---

## 二、增益选择分析：×2 不够，推荐 ×4

### 文档中的保守估算 vs 实际统计

文档中 `Peak ≈ RMS × √N_sc ≈ 200 × 35 ≈ 7000` 是绝对理论最大值（所有子载波完美同相），实际发生概率极低。

| CCDF 百分位 | PAPR (dB) | Peak/RMS | G=4 时实际峰值 | G=8 时实际峰值 |
|------------|-----------|----------|-------------|-------------|
| 理论最大 | 31 dB | 35× | 28,000 | 56,000 |
| 99.99% | ~12 dB | ~4× | 3,200 | 6,400 |
| 99.9% | ~10 dB | ~3.2× | 2,560 | 5,120 |
| 99% | ~8 dB | ~2.5× | 2,000 | 4,000 |

`_fused_clip_cast_kernel` 已内置 clip 到 ±32767 的保护，偶发极端峰值被裁剪只影响 <0.01% 采样点。

### 各增益方案对比

| 增益 G | FFT 输入 RMS | 有效比特 | 预估 SQNR | 改善 | 溢出风险 | 评价 |
|--------|------------|---------|----------|------|---------|------|
| ×1（当前） | 200 | 7.6 | ~22 dB | 基准 | 无 | — |
| ×2 | 400 | 8.6 | ~28 dB | +6 dB | 极低 | **改善有限** |
| **×4** | **800** | **9.6** | **~34 dB** | **+12 dB** | **极低** | **推荐** |
| ×6 | 1200 | 10.2 | ~37 dB | +15 dB | 低 | 可以 |
| ×8 | 1600 | 10.6 | ~40 dB | +18 dB | 中 | 激进 |

**推荐 G=4**：+12 dB 实质性改善，理论最大峰值 28000 仍在 int16 范围内。

---

## 三、实现方案：改 Proxy，不改 OAI

### 为什么改 Proxy (v8.py) 而不改 OAI

| 维度 | 改 Proxy (v8.py) | 改 OAI |
|------|-----------------|--------|
| 修改量 | 小范围参数 + 量化前乘法 | 全局常量/rfsim C 代码 |
| 影响范围 | 仅 Channel Proxy | 所有信号 (PDSCH/PUSCH/DMRS/SRS) |
| 风险 | 零（float64 内部运算） | 高（PAPR 溢出） |
| 需要重编译 OAI | 否 | 是 |
| 逻辑等效 | RF AGC（FFT 前放大） | — |

### OAI 侧为什么不是主修改点

当前 OAI 里确实已有 `nr_digital_agc`，但它的作用位置是：

```
rfsim read → ru->common.rxdata (time-domain int16)
  → nr_slot_fep_ul()
    → dft(...)
    → nr_digital_agc_update_and_apply(rxdataF)   ← FFT 之后
```

也就是说，OAI 的 digital AGC 只能放大 FFT 后的 `rxdataF`。遗留问题1的瓶颈发生在 **进入 int16 FFT 之前**：时域 RMS≈200，只有 ~7.6 有效比特。FFT 后再放大，会把信号和 FFT 量化噪声一起放大，无法改善 FFT SQNR。

因此本次方案把增益放在 Channel Proxy 写入 gNB `ul_rx` 之前：

```
Proxy float64 信道输出 → UL 多 UE 叠加 → pre-gain → int16 clip/cast → gNB ul_rx → OAI FFT
```

### 数据流与修改点

```
UE (OAI, int16, RMS≈200)
  → gpu_ul_tx 共享内存
    → Proxy v8.py _ipc_ul_superposition_slot
      → 信道应用 (float64)
      → 累加 _ul_accum (float64)
      → ★ fixed pre-gain G ★            ← 改这里
      → _fused_clip_cast_kernel → int16
    → gpu_ul_rx 共享内存
  → gNB (OAI, int16, RMS≈200×G)
    → FFT (有效比特更多) → SQNR 更高
```

### 实际修改文件

1. `DevChannelProxyJIN/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy/v8.py`
2. `DevChannelProxyJIN/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy/launch_all_v9.sh`
3. `DevChannelProxyJIN/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy/run_q4_snr_sweep_v9.sh`

---

## 四、`v8.py` 实际实现

### 改动 1: CLI 参数

```python
ap.add_argument("--ul-pre-gain", type=float, default=1.0,
                help="Fixed UL time-domain pre-gain before int16 quantization "
                     "to emulate RF AGC in rfsim/Channel Proxy (default: 1.0)")
```

默认值 `1.0`，保持原行为完全不变。

### 改动 2: 参数校验

```python
if args.ul_pre_gain <= 0.0:
    print("[ERROR] --ul-pre-gain must be > 0")
    sys.exit(1)
```

### 改动 3: 存储增益值 (`Proxy.__init__`)

```python
self.ul_pre_gain = float(ul_pre_gain)
```

### 改动 4: 核心 — 应用增益

位置：`_ipc_ul_superposition_slot()` 中，多 UE `_ul_accum` 完成后，`_fused_clip_cast_kernel` 前。

```python
if self.ul_pre_gain != 1.0:
    if not hasattr(self, '_ul_gain_work') or self._ul_gain_work.shape != self._ul_accum.shape:
        self._ul_gain_work = cp.empty_like(self._ul_accum)
    cp.multiply(self._ul_accum, cp.float64(self.ul_pre_gain), out=self._ul_gain_work)
    accum_f64 = self._ul_gain_work.view(cp.float64)
else:
    accum_f64 = self._ul_accum.view(cp.float64)
```

说明：

- 没有使用 `self._ul_accum *= G` 原地修改，避免污染原始叠加结果。
- 使用懒分配的 `_ul_gain_work` 工作缓冲，避免每 slot 反复分配。
- 乘法发生在 `complex128/float64` 域，之后由已有 `_fused_clip_cast_kernel` 做 round + clip 到 int16。

### 启动日志

`v8.py` 启动时会打印：

```text
UL Pre-Gain: x4 (FFT-pre AGC emulation)
```

若为默认值：

```text
UL Pre-Gain: x1 (disabled)
```

---

## 五、v9 脚本关系：名字是 v9，Proxy 仍是 v8.py

### 关键事实

当前工作区没有 `v9.py`。`launch_all_v9.sh` 和 `run_q4_snr_sweep_v9.sh` 是 v9 版本的启动/扫参脚本，但默认 Proxy 入口仍是：

```bash
PROXY_VER="${PROXY_VER:-v8}"
PROXY_SCRIPT="${PROXY_VER}.py"
```

因此默认实际启动的是：

```bash
/workspace/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy/v8.py
```

这意味着 pre-gain 必须接入这两个 v9 脚本，否则直接运行 v9 脚本时不会自动带上 `--ul-pre-gain`。

### `launch_all_v9.sh` 已接入

新增环境变量/默认值：

```bash
UL_PRE_GAIN="${UL_PRE_GAIN:-1.0}"
```

新增命令行参数：

```bash
-ulg G
```

参数校验：

```bash
ERROR: -ulg/UL_PRE_GAIN must be a positive number
```

传给 Proxy：

```bash
PROXY_EXTRA_ARGS="$PROXY_EXTRA_ARGS --ul-pre-gain $UL_PRE_GAIN"
```

日志目录标记：

```bash
..._ulg4.0
```

启动摘要：

```text
UL Gain: pre-gain x4.0 before gNB int16 RX
```

### `run_q4_snr_sweep_v9.sh` 已接入

新增环境变量：

```bash
UL_PRE_GAIN="${UL_PRE_GAIN:-1.0}"
```

写入 sweep manifest：

```bash
# ul_pre_gain: 4.0
```

每个 SNR 点调用 launcher 时传入：

```bash
-ulg $UL_PRE_GAIN
```

完整链路：

```
run_q4_snr_sweep_v9.sh
  UL_PRE_GAIN=4.0
    → launch_all_v9.sh -ulg 4.0
      → v8.py --ul-pre-gain 4.0
        → Proxy._ipc_ul_superposition_slot()
          → gNB ul_rx 写入前放大 ×4
```

---

## 六、使用方式

### 单次启动

默认不启用 pre-gain：

```bash
sudo bash launch_all_v9.sh
```

推荐 G=4：

```bash
sudo bash launch_all_v9.sh -ulg 4.0
```

也可用环境变量：

```bash
sudo UL_PRE_GAIN=4.0 bash launch_all_v9.sh
```

### SNR Sweep

默认不启用 pre-gain：

```bash
sudo bash run_q4_snr_sweep_v9.sh
```

推荐 G=4：

```bash
sudo UL_PRE_GAIN=4.0 bash run_q4_snr_sweep_v9.sh
```

建议 A/B 对比：

```bash
# baseline
sudo UL_PRE_GAIN=1.0 ONLY_SNR="30 40" bash run_q4_snr_sweep_v9.sh

# pre-gain
sudo UL_PRE_GAIN=4.0 ONLY_SNR="30 40" bash run_q4_snr_sweep_v9.sh
```

预期：SNR≥30 dB 时，per-frame NMSE 天花板从约 -22 dB 下探到约 -30~-34 dB 区间。

---

## 七、对 e-MIMO 管线的影响

固定增益 G 对下游管线**安全**：

| 管线环节 | G 的影响 | 问题 |
|---------|---------|------|
| SRS 信道估计 | H_est → G·H_est（线性缩放） | 无 |
| 2D 滤波 (EMA) | 输出等比缩放 | 无 |
| PDP (功率延迟分布) | 归一化后消除 | 无 |
| 协方差矩阵 R_H | 特征向量不变，特征值 ×G² | 无 |
| AI 模型 | 训练和推理用同一 G | 无 |

**关键约束**：G 必须是固定常数（非自适应），训练和推理用同一 G 值。

---

## 八、验证记录

已完成静态检查：

```bash
python3 -m py_compile DevChannelProxyJIN/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy/v8.py
bash -n DevChannelProxyJIN/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy/launch_all_v9.sh
bash -n DevChannelProxyJIN/vRAN_Socket/G1C_MultiUE_MIMO_Channel_Proxy/run_q4_snr_sweep_v9.sh
```

结果：均通过。

IDE lint：`v8.py` 无新增 lint 错误。

尚未完成：实际 OAI + Proxy 端到端实验，需要运行 `UL_PRE_GAIN=4.0` 的 SNR sweep 后比较 SRS per-frame NMSE。

---

## 九、待办

- [x] 将 pre-gain 改动写入 `v8.py`
- [x] 将 pre-gain 参数接入 `launch_all_v9.sh`
- [x] 将 pre-gain 参数接入 `run_q4_snr_sweep_v9.sh`
- [x] 完成语法/静态检查
- [ ] 实验验证 G=4 时 per-frame NMSE 天花板是否从 -22 dB 降至 ~-34 dB
- [ ] 对比 G=1/2/4/6 的 clipping 与 NMSE 曲线
- [ ] 确认多 UE 场景下 G=4 是否需要调整（保守建议 G≈4/√N_ue，单 UE 先用 G=4）
