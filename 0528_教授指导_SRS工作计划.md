# SRS 侧工作计划（基于 0528 教授指导）

| 字段 | 值 |
|------|----|
| **日期** | 2026-05-28 |
| **负责人** | 刘（LIULU） |
| **指导来源** | 0528 录音（教授 5分07秒指导） |
| **核心关键词** | SRS Structure 提取 · 持续更新环路 · 上下行统合思考 · RAN Twin 接口 |

---

## 〇、教授原话核心摘要（仅涉及刘的部分）

### 教授 01:53
> "刘，在你做的信道估计基础上，把**编码（Encoding）部分**加进去，把**Structure 提取部分**类似地加进去，然后看看**以环路（loop）形式一起运行**时效果如何。"

### 教授 02:11
> "先从**上行 SRS 在基站端做估计**开始。做的时候，使用和终端通过 PMI 做 Structure 估计时**相同概念的模块**，做一个**持续更新**的模块。然后思考下行接收环路和上行接收环路**如何结合**。"
>
> "第二部分：通过 SRS 自然产生 Structure 的模型，连接到 **RAN Twin**，从 **Processing Information** 角度把模型中间过程提供给 RAN Twin。但这部分 RAN Twin 需要什么还不明确，先知道有这个方向就行。"

### 教授 03:40
> "先从**收集数据、获取硕士成果**开始做。"

---

## 一、当前已有基础（你已完成的）

| 模块 | 状态 | 成果 |
|------|------|------|
| OAI-Channel 统合模拟器 | ✅ | v8/v9 全链路运行，多 UE MIMO |
| SRS 信道估计 — Legacy (filt8/16) | ✅ | NMSE -10.87 dB baseline |
| SRS 信道估计 — 时域 Kalman+IAE | ✅ | NMSE -11.16 dB |
| SRS 信道估计 — **FreqSmooth 自适应频域去噪** | ✅ **今日完成** | NMSE **-17.39 dB** (+6.52 dB) |
| GT-SRS 对齐 + NMSE 评估管线 | ✅ | eval_nmse_clean.py + eval_freq_smooth.py |
| SNR Sweep 自动化 | ✅ | run_q4_snr_sweep_v9.sh |
| StructuredChannelOutput (Python prototype) | 🔶 85% | 有框架，字段定义完成，但未实际填充全部 |
| C 端结构化输出 | ❌ 0% | 尚未实现 |

---

## 二、教授要求 → 你需要做的事（全景）

```
教授要求的完整图景（刘的 SRS 视角）：

                    你已做完的部分
                  ┌─────────────────┐
     SRS 导频 ──→│  LS 估计         │
                  │  filt8/16 插值   │
                  │  FreqSmooth 去噪 │
                  │  Kalman 时域滤波  │
                  └────────┬────────┘
                           │ H_srs[t] (全带信道估计)
                           ▼
              ┌────────────────────────┐
              │  ★ 教授要求新增的部分 ★  │
              │                        │
              │  ① Structure 提取       │
              │    · PDP (p)            │
              │    · 空间协方差 (R_rx)    │
              │    · Doppler (d)        │
              │    · 主要奇异值 (sv)     │
              │                        │
              │  ② 编码/压缩            │
              │    · 延迟域截断 (h_taps) │
              │    · 或 DFT 压缩 (H_c)  │
              │                        │
              │  ③ 持续更新环路          │
              │    · EMA 状态平滑        │
              │    · inst / state 双通道 │
              │    · confidence 置信度   │
              └────────────┬───────────┘
                           │ (S_ul, H_ul)
                           ▼
              ┌────────────────────────┐
              │  ④ 上下行统合思考        │
              │  · SRS only 场景        │
              │  · CSI-RS only 场景     │  ← 准秀的 S_dl, H_dl
              │  · 双信号混合场景        │
              │  · 不同周期配置          │
              └────────────┬───────────┘
                           │
                           ▼
              ┌────────────────────────┐
              │  ⑤ RAN Twin 接口        │ ← 暂不急，了解即可
              │  · Processing Info 输出  │
              │  · 模型中间过程 → Twin   │
              └────────────────────────┘
```

---

## 三、分阶段实施计划

### Phase A：信道估计增强收尾 + 多场景验证（1~2 周）

> **目标**：把今天的 FreqSmooth 成果固化，扩展到多场景验证，形成可答辩的信道估计核心成果。

| # | 任务 | 具体内容 | 交付物 |
|---|------|---------|--------|
| A1 | FreqSmooth 代码固化 | 清理 debug 输出，整理注释，确保环境变量向后兼容 | 干净的 `nr_srs_freq_smooth.c` |
| A2 | 频选信道验证 | CDL-A (RMS DS=129ns) + CDL-C (RMS DS=209ns) 场景下运行，验证自适应窗口能自动缩小 | NMSE 对比表 (flat vs 频选) |
| A3 | 高速场景验证 | UE speed=30 km/h + 120 km/h，验证 Kalman 在高速下的增量价值 | Kalman 增量 vs 速度曲线 |
| A4 | UL Pre-Gain g=4 联合验证 | 解决 g=2 的 int16 削波问题（或直接用 g=4），验证 SQNR 提升对 FreqSmooth 的叠加效果 | g=1 vs g=4 NMSE 对比 |
| A5 | 全量 SNR Sweep | 5/10/15/20/25/30 dB × {Legacy, FreqSmooth, FreqSmooth+Kalman} | 综合 SNR vs NMSE 曲线图 |

**Phase A 的意义**：这是教授说的"先收集数据、获取硕士成果"的基础层。

---

### Phase B：Structure 提取模块实现（2~3 周）

> **目标**：教授核心要求——在 SRS 信道估计输出上加入 Structure 提取，使用和 DL 侧 Structure head **相同概念**的模块。

#### B1. 理解"相同概念"的含义

DL 侧（准秀）的 Structure head 提取的是：

| Structure 字段 | DL 侧来源 | UL SRS 侧对应做法 |
|---------------|----------|-----------------|
| PDP (p) — 功率延迟分布 | CsiNet 内部 latent → Structure head | **IFFT(H_srs) → \|h(τ)\|² → 截断有效 taps** |
| 空间协方差 (R) | CsiNet → R_tx_state | **R_rx = E[H·H^H]**，跨天线的瞬时+平滑协方差 |
| Doppler (d) | CsiNet → d_state | **跨帧 H(t) 做 FFT → Doppler 频谱** |
| Singular Values (sv) | — | **SVD(H_srs) → 前 k 个奇异值** |
| confidence | state 收敛度 | **基于 Kalman P 或 innovation 序列** |

#### B2. 具体实现任务

| # | 任务 | 实现位置 | 说明 |
|---|------|---------|------|
| B2.1 | PDP 提取 | Python 离线 → 后续嵌入 C | `H_srs[k]` → IFFT → `\|h[l]\|²`，估计 RMS delay spread + 有效 taps 数 |
| B2.2 | 空间协方差 R_rx | Python 离线 → 后续嵌入 C | `R_rx_inst = H·H^H` (per-frame)，`R_rx_state = EMA(R_rx_inst)` |
| B2.3 | Doppler 估计 | Python 离线 | 跨帧 `H(t, k)` 沿 t 轴做 FFT → Doppler spread → `speed_ms` |
| B2.4 | SVD 分解 | Python 离线 | `svd(H_srs)` → 前 k 个奇异值 → 信道秩/条件数 |
| B2.5 | 置信度 (confidence) | Python 离线 | Kalman 的 P 矩阵 / innovation 方差 / roughness-noise ratio |
| B2.6 | EMA 状态平滑 | Python 离线 | 对 p/R/d 各维度做 3 通道 EMA（fast/medium/slow），和准秀侧 SharedStateCell 概念一致 |

#### B3. 交付物

- `srs_structure_extractor.py`：离线从 SRS bin 提取全部 Structure 字段
- 可视化工具：PDP 热力图 / R_rx 矩阵图 / Doppler 谱图 / sv 分布
- 验证：提取结果与 Sionna GT 真值对比，每个 Structure 字段的估计精度

---

### Phase C：信道压缩/编码模块（1~2 周）

> **目标**：教授说的"编码（Encoding）部分"——把 SRS 全带信道压缩为紧凑表示。

| # | 任务 | 说明 |
|---|------|------|
| C1 | 延迟域截断 (h_taps) | IFFT(H_srs) → 保留前 L_eff 个 taps（L_eff 由 PDP 中的 RMS delay spread 决定） |
| C2 | DFT 重建验证 | `H_c = DFT(h_taps, N_sc)` → 与原始 H_srs 对比 → 压缩损失分析 |
| C3 | 压缩率 vs 精度曲线 | L_eff = {4, 8, 16, 32, 64} 下的重建 NMSE |
| C4 | 格式对齐 | 输出 `(S_ul, H_ul)` 格式与准秀的 `(S_dl, H_dl)` 保持**统一数据层**一致 |

---

### Phase D：持续更新环路（Loop）实现（2~3 周）

> **目标**：教授核心要求——"做一个持续更新的模块"，以 loop 形式运行。

#### D1. 环路架构设计

```
每个 SRS 周期（5 ms）触发一次：

  H_srs_new[t] ─────────────────────────┐
        │                                │
        ▼                                ▼
  ┌──────────┐    ┌──────────────┐  ┌──────────┐
  │Structure │    │ 信道压缩      │  │ State    │
  │提取      │    │ h_taps → H_c │  │ 更新     │
  │p,R,d,sv  │    └──────┬───────┘  │(3ch EMA) │
  └────┬─────┘           │          └────┬─────┘
       │                 │               │
       ▼                 ▼               ▼
  ┌──────────────────────────────────────────┐
  │        S_ul[t], H_ul[t] — 统一输出       │
  │  p_inst/p_state, R_inst/R_state,         │
  │  d_inst/d_state, sv, confidence,         │
  │  h_taps, H_c, rsrp, snr_db              │
  └──────────────────────────────────────────┘
       │
       ▼  保存 / 送往统一数据层
```

#### D2. 具体任务

| # | 任务 | 说明 |
|---|------|------|
| D1 | 离线 Loop 验证 | 读取一次实验的全部 SRS bin，逐帧模拟 Structure 提取 + State 更新 + 编码，输出 `(S_ul, H_ul)` 时间序列 |
| D2 | 跟踪性能评估 | 在信道突变/速度变化/SNR 变化场景下，评估 Structure state 的跟踪延迟和精度 |
| D3 | C 端移植 | 将 Structure 提取核心逻辑嵌入 OAI C 代码（`nr_ul_channel_estimation.c` 下游），实现在线 loop |
| D4 | 输出接口 | 实现 `StructuredChannelOutput` 的 C 结构体 → 二进制 dump → Python 读取的完整链路 |

---

### Phase E：上下行统合思考（思考 + 设计文档，1~2 周）

> **目标**：教授说的"思考上下行环路如何结合"——你不需要实现准秀的部分，但需要思考**接口和统合方案**。

| # | 任务 | 说明 |
|---|------|------|
| E1 | 场景矩阵定义 | 定义 SRS-only / CSI-RS-only / 双信号 × 不同周期的场景组合 |
| E2 | 信息互补分析 | 分析 UL SRS 能提供而 DL CSI-RS 不能提供的信息（反之亦然）。例如：SRS 直接给 gNB 完整 H，而 CSI-RS 只通过 PMI 给有限反馈 |
| E3 | 统合更新策略设计 | 当 gNB 同时拥有 SRS 估计的 H_ul 和 PMI 反馈的 partial H_dl 时，如何融合？Kalman 双观测源？加权融合？ |
| E4 | 设计文档 | 写一份"UL-DL 联合信道获取统合方案"设计文档，包含架构图 + 信息流 + 接口定义 |

**注意**：这一步教授说"先以下行为中心推进"，所以你的重心是 SRS 侧做好，同时从架构层面思考统合。实际联调需要等准秀那边 DL 侧就绪。

---

### Phase F：RAN Twin 接口预研（了解 + 初步设计，1 周）

> **目标**：教授说的"第二部分"——连接 RAN Twin，但"需要什么还不明确，先知道有这个方向"。

| # | 任务 | 说明 |
|---|------|------|
| F1 | 了解 RAN Twin 需求 | 和 Minji 沟通，了解 Twin 侧需要什么格式的 Processing Information |
| F2 | 定义中间表示 | 列出 SRS 处理流程中可以输出给 Twin 的中间结果（LS 原始估计、Kalman 状态、FreqSmooth 后的平滑估计、Structure 各字段） |
| F3 | 接口草案 | 写一份初步的 SRS→Twin 数据接口文档 |

---

## 四、时间线总览

```
2026-05-28                                                    硕士成果
    │                                                           ▼
    ▼
 Week 1-2    Phase A: FreqSmooth 固化 + 多场景验证 ←── 当前最紧急
    │
 Week 3-4    Phase B: Structure 提取模块 (Python 离线)
    │
 Week 5      Phase C: 信道压缩/编码
    │
 Week 6-7    Phase D: 持续更新环路 (Loop) + C 端移植
    │
 Week 8-9    Phase E: 上下行统合设计文档
    │
 Week 10     Phase F: RAN Twin 接口预研
    │
    ▼
 ~2026-08    可答辩的完整成果链
```

---

## 五、硕士论文成果链（教授期望的故事线）

教授最后说"先从收集数据、获取硕士成果开始"。你的论文故事线应该是：

```
第一章：问题定义
  · 5G NR SRS 上行信道估计的挑战（int16 量化、低 SQNR、实时性约束）

第二章：OAI-Channel 统合模拟器
  · 你搭建的完整 OAI + Channel Proxy + GT 评估管线
  · 这是实验平台，也是贡献之一

第三章：SRS 信道估计增强
  · Stage 1: 频域自适应去噪 (FreqSmooth) — 今天完成的核心成果
  · Stage 2: 时域 Kalman 滤波
  · 级联架构的效果：-10.87 → -17.39 dB (+6.52 dB)
  · 多场景泛化性验证 ← Phase A 要做的

第四章：SRS Structure 提取与持续更新环路
  · 从 SRS 估计中提取 Structure (PDP, R_rx, Doppler, sv)
  · 持续更新的 loop 架构
  · 与 DL CSI Feedback 的概念一致性 ← Phase B/C/D

第五章：上下行联合信道获取展望
  · UL SRS + DL CSI-RS 统合方案设计
  · RAN Twin 接口思考 ← Phase E/F

结论
```

---

## 六、每周汇报点（建议向教授展示的节点）

| 周次 | 展示内容 |
|------|---------|
| Week 2 | FreqSmooth 多场景结果表（flat / CDL-A / CDL-C × SNR × 速度） |
| Week 4 | Structure 提取 demo：PDP/R_rx/Doppler 可视化 vs GT |
| Week 5 | 信道压缩率 vs 精度曲线 |
| Week 7 | 完整 Loop 运行 demo：逐帧输出 (S_ul, H_ul) + 跟踪性能 |
| Week 9 | 上下行统合设计文档 review |
